# Surrenderless — project state (source of truth)

This document aligns Cursor, ChatGPT, and humans on **product intent**, **current reality**, and **where to invest**. It describes the repo as of its last update; verify against code when in doubt.

---

## 1. True product goal

**Surrenderless is a chat-first action assistant.** The user should eventually **only chat** with Surrenderless — not manually hop across forms, tabs, and external sites as the primary experience. The user **chats**; the app:

- Gathers facts **conversationally**
- **Structures** the case
- **Collects and organizes** proof (screenshots, receipts, emails, and similar — tied to the case in-app)
- **Prepares** submissions, messages, and complaints for review (in-app previews and drafts today; more automation later where feasible)
- Gets **approval**
- **Submits or queues** actions when technically (and legally) possible
- **Tracks** confirmations, filings, and follow-ups
- **Carries** the issue from beginning to end

Existing **`/justice/*`** pages (intake form, evidence page, prep surfaces, etc.) are **interim scaffolding** toward that single conversational shell — not the final product shape.

Anything that permanently treats “open another tab and paste into a government form” as the final experience is **out of scope for the long-term product**, even if it is useful as an interim step.

---

## 2. Current reality

The shipped experience is **still a multi-page Consumer Justice case-management scaffold** (many prep surfaces and manual filing paths), but **navigation across hub, preview, plan, and prep surfaces now promotes AI conversational chat intake first** toward the long-term **chat-first action** goal (§1): describe the issue once in chat, structure the case, review a submission preview, approve, then follow evidence, filings, tasks, and plan guidance inside the app (filing/queueing only where feasible today; much remains manual prep and external filing).


- **Stack:** Next.js (App Router), TypeScript, **Clerk** (auth), **Supabase** (persistence via service-role API routes), **REST API routes**, **session/local storage** for active case + timeline mirror.
- **UX:** Multi-page **justice** routes (`/justice/*`): **`/justice`** workspace hub (thin landing); **primary chat intake** — **AI conversational** intake at **`/justice/chat-ai`** (§2; hub/header **Chat intake** → **`/justice/chat-ai`**; preview empty-state/footer **Chat intake** → **`/justice/chat-ai`**; §2 **Justice-wide navigation alignment**); **step-by-step scripted** intake at **`/justice/chat`** (labeled **Step-by-step chat** where linked; **unchanged** scripted Q&A); **structured form** intake at **`/justice/intake`** (labeled **Structured form** / **Edit structured form** where linked; loads saved answers for direct field edit); action plan; **`/justice/preview`** (in-app **submission draft preview**: deterministic text from **`buildSubmissionDraftPreview.ts`** remains **source of truth / fallback**; optional **AI-assisted** draft via signed-in **`POST /api/justice/preview-draft`** (Clerk **`getUserOr401`**, **`rateLimit`**, server-only **`OPENAI_API_KEY`**, prompts from **`buildSubmissionDraftAiPrompt.ts`**); UI copy states case details are sent to **OpenAI**; AI output is **review-only**, nothing filed; **no** DB writes, schema migrations, embedded browser, or filing automation for AI; destination selector from current justice destinations; **“I reviewed this draft”** before **Continue to action plan** → **`/justice/plan`**), per-destination prep pages, dedicated evidence page, saved cases, **`/justice/handling`** **Handling workbench** (§2 **Handling workbench — internal triage surface**), archived cases, packet (aggregate/export). **`/justice/plan`** links to **`/justice/preview`** from the **case packet** area. **`/justice/intake`**, **`/justice/chat`**, and **`/justice/chat-ai`** share **`src/lib/justice/commitIntakeToSessionAndServer.ts`** and **`src/lib/justice/normalizeCompanyWebsite.ts`** on form + scripted chat (optional company website: bare domains like `amazon.com` → `https://amazon.com`, sentinels like `none` / `n/a` → empty string). **Create commit (default):** **`/justice/intake`**, **`/justice/chat`**, and **new** **`/justice/chat-ai`** sessions — new case id, **`case_started`**, optional **`POST /api/justice/cases`**, **`intake_completed`**, then **`router.push("/justice/preview")`** (**`/justice/plan`** after preview continue). **Update commit:** **`/justice/chat-ai`** when an active case was hydrated (§2) — preserve case id/timeline, **`PATCH`** when signed in + UUID, no **`POST`**. **`/justice/chat`** and **`/justice/chat-ai`** use **`src/lib/justice/buildJusticeIntake.ts`** (**`buildJusticeIntakeFromParts`**, **`justiceIntakeToBuildJusticeIntakeParts`**, **`validateContactProofForIntake`**; §2). **`POST /api/justice/intake-chat`** powers AI turns on **`/justice/chat-ai`** (contract unchanged; §2).
- **Justice workspace hub (`/justice`):** **`/justice`** is shipped as a **thin**, **intentionally lightweight** Consumer Justice **workspace** landing — **not** a dashboard rewrite and **not** a replacement for **`/justice/plan`**. Copy explains that Surrenderless **organizes consumer issues into an action plan**, prepares **drafts, checklists, and records** for the user to use, and **does not file or submit complaints automatically**. Hub copy **recommends chat intake** and notes that **form intake** remains available for users who prefer the full structured form. **`src/app/justice/JusticeHubWorkspaceBody.tsx`** (single client mount **read-only**) renders the hub link list and, when **`readValidLocalJusticeIntake()`** finds valid **`sessionStorage`** intake, a compact **Current case** card — company name, category/product labels, submission-draft status from **`readTimeline`** using **`STORAGE_CASE_ID`**, and one primary link on the **Current case** card: **`/justice/chat-ai`**, **Continue in chat** — regardless of draft-review or prepared-packet state (§2 **Justice hub — Current case primary CTA routing (`/justice`)**); checklist deep links **Review submission draft** → **`/justice/preview`** and **Review prepared case packet** → **`/justice/packet`** unchanged. The static **Continue current action plan** hub card is **omitted** when a **Current case** snapshot exists so the dynamic card is the only “next step” CTA. **Current case** display **re-refreshes client-side** when the **tab becomes visible** (`document.visibilityState === "visible"` on **`visibilitychange`**), when the **browser window regains focus** (`window` **`focus`**), and on **`storage`** (e.g. cross-tab updates to **`sessionStorage`** / shared storage) so the card is **less likely to stay stale** after **chat**, **preview**, **plan**, or other flows that mutate local intake/timeline — still using only **existing** local helpers (**`readValidLocalJusticeIntake`**, **`readTimeline`**, **`STORAGE_CASE_ID`**); **no** hub case-list **`GET`**, **no** new routes, APIs, **`rules.ts`**, commit/preview/plan semantics, schemas, timeline event **definitions**, **`commitIntakeToSessionAndServer`**, or **`/justice/preview`** / **`/justice/plan`** behavior changes beyond their existing writes to **`sessionStorage`**. **Needs more info** chat routing on **Current case** — §2 **Justice hub — Needs more info chat routing CTA on Current case (`/justice`)**; compact **Current case** readiness checklist — §2 **Justice hub — Current case compact readiness checklist (`/justice`)**; approved next-step label + status on **Current case** — §2 **Justice hub — approved next-step label and status on Current case (`/justice`)**. With **no** local intake, link cards are **Start with chat intake** (**`/justice/chat-ai`**), **Use step-by-step chat** (**`/justice/chat`**; §2), **Continue current action plan**, **Start with form intake**, **Saved cases**. **No** case-list **`GET`**, **no** session mutation from the hub (reads only; active-case evidence **`GET`** for chat routing — §2 **Justice hub — Needs more info chat routing CTA on Current case (`/justice`)**). **`src/app/components/Header.tsx`** global nav order is **Justice** → **Chat intake** (**`/justice/chat-ai`**) → **Consumer case** (form intake → **`/justice/intake`**). **No** case **`GET`** on the hub, **no** duplication of **`rules.ts`** / plan readiness / destination logic, **no** new APIs, **no** auth changes, **no** schema or new DB tables, **no** automation, **no** filing or government submission from this surface. **Hub/header ordering** for chat-first entry is **UI-only** on this surface (see next bullet). **Preview-first intake finish** (**`/justice/intake`**, **`/justice/chat`**, and **`/justice/chat-ai`** → **`/justice/preview`**) is documented in §5; hub remains **read-only** (no commit from hub). **`commitIntakeToSessionAndServer`** **create** vs **update** modes: §2 **Chat AI intake — hydrate & update commit**.
- **Justice hub — handling requested on Current case (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** — **read-only legacy display** when **`!(snapshot.packetApproved && snapshot.approvedNextAction)`** but session has **`handling_requested_at`** (with §2 **Justice hub — approved next-step label and status on Current case (`/justice`)**). When valid local intake exists and **`justice_approved_next_action_v1`** has **`handling_requested_at`** for **`STORAGE_CASE_ID`** (via **`readSessionApprovedNextAction`** — §2 **Approved next action shared state helpers**), the **Current case** card (inside the primary **`Link`**) shows **Surrenderless handling requested** with recorded time; optional note (**`ApprovedNextActionHandlingRequestNoteReadOnly`**); optional **Acknowledged {time}** (**`ApprovedNextActionHandlingAcknowledgedReadOnly`**); **in-app tracking only** disclaimer. Below the card (legacy path only): **display-only** queue status (§2 **Justice hub — display-only derived handling queue status**) and **View in handling workbench** (§2 **Handling workbench — active-case back-link from hub**). When **`snapshot.packetApproved && snapshot.approvedNextAction`**, interactive request/note/ack — §2 **Justice hub — Surrenderless handling request and acknowledge on Current case (`/justice`)** — replaces in-card and below-card legacy handling UI for the approved case. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone. **No** routing changes. Refreshes on **focus**, **`storage`**, and **visibility**. **No** schema, migration, new API route, queue worker, filing record, timeline **type**, or automation.
- **Justice hub — display-only derived handling queue status (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** (**`ApprovedNextActionHandlingQueueStatusReadOnly`**, **`deriveHandlingQueueDisplayStatus`**) — **visibility-only** slice completing **queue-status parity** across every surface that shows handling (with §2 **Saved cases — handling requested visibility on All cases cards**, **Handling workbench — internal triage surface**, **Packet — display-only derived handling queue status**, **Chat AI intake — display-only derived handling queue status**, and **Plan — display-only derived handling queue status**). When the **Current case** snapshot has **`handlingRequestedAt`** / **`handling_requested_at`** (session **`justice_approved_next_action_v1`** via **`readSessionApprovedNextAction`**), renders **display-only derived queue status** from existing **`handling_requested_at`** and **`handling_acknowledged_at`** / **`handlingAcknowledgedAt`** only (**no** new persisted field, **no** writes): **Queue status: Awaiting internal triage.** when **`handling_acknowledged_at`** is absent; **Queue status: Acknowledged for internal tracking triage only** when **`handling_acknowledged_at`** is set. Placed below the **Current case** card (after in-card handling disclaimer content) and before **View in handling workbench** (§2 **Handling workbench — active-case back-link from hub**). When **`handling_requested_at`** is absent, **no** queue status line. **Does not** change **`STORAGE_CASE_ID`**, request/ack/note behavior, server **`GET`**, plan/preview CTA, chat-intake hub cards, hub refresh, or workbench link. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone. **No** schema, migration, new API route, queue worker, **`justice_case_filings`** row, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Justice hub — handled/open handling triage note on Current case (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** — **copy-only** slice (with §2 **Justice hub — handling requested on Current case**, **Justice hub — display-only derived handling queue status**, **Justice hub — Surrenderless handling request and acknowledge on Current case (`/justice`)**, **Approved next action — centralized handled/open handling triage note copy**, **Saved cases — handled/open handling triage note on All cases cards**). When **`snapshot.packetApproved && snapshot.approvedNextAction`** and hydrated **`approved_next_action`** has **`handling_requested_at`**, no **`handling_acknowledged_at`**, and **`status === "completed"`**, renders **`ApprovedNextActionHandlingHandledOpenTriageNote`** with variant **`inlineAck`** below queue status and above **View in handling workbench** (§2 **Justice hub — Surrenderless handling request and acknowledge on Current case (`/justice`)**). **Legacy** path when **`!(packetApproved && approvedNextAction)`** but session snapshot has **`handling_requested_at`** + completed + unacknowledged: variant **`redirect`** via **`showHandledOpenHandlingTriageNote`** — **no** **Mark acknowledged** on that legacy path. **Hidden** when **`status !== "completed"`**, when **`handling_acknowledged_at`** is set, or when **`handling_requested_at`** is absent. **Does not** change interactive request/note/ack writes (§2 **Justice hub — Surrenderless handling request and acknowledge on Current case (`/justice`)**), queue status, workbench link, focus/**`visibilitychange`**/**`storage`** refresh, or external-action behavior. **No** schema, migration, new API route, queue worker, **`justice_case_filings`**, timeline **type**, or external submission/contact/queue automation.
- **Justice hub — handling tracking status on Current case (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **read-only sync** slice (with §2 **Justice hub — display-only derived handling queue status (`/justice`)**, §2 **Justice hub — Surrenderless handling request and acknowledge on Current case (`/justice`)**, §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**, §2 **Saved cases — handling tracking status on All cases cards (`/justice/cases`)**, §2 **Plan — handling tracking status on prepared-packet handling callouts (`/justice/plan`)**, §2 **Chat AI intake — handling tracking status in Current action tracking (`/justice/chat-ai`)**, §2 **Packet — handling tracking status in prepared-action handling UI (`/justice/packet`)**). In **both** **Current case** handling blocks when **`handling_requested_at`** is set — **prepared-packet** path (**`snapshot.packetApproved && snapshot.approvedNextAction`**) and **legacy/session** path (**`snapshot.handlingRequestedAt`** with hydrated **`snapshot.approvedNextAction`**) — **`HubHandlingTrackingStatusReadOnly`** shows read-only **Handling tracking:** after **`ApprovedNextActionHandlingQueueStatusReadOnly`** — **Loading handling tracking context...** while **`hubReadinessLoading`**; when ready, **Handling tracking: {derived next step}** from page-local **`deriveHubManualActionNextStep`** / **`deriveHubHandlingTrackingLine`** mirroring workbench **`deriveManualActionNextStep`** gate order (basics + draft reviewed + prepared packet approved, **`approved_next_action`** status/outcome/ack/follow-up, **`evidenceCount`**, **`hasFilingRecord`** / **`hasConfirmationOnFile`** from **`filings`** parsed in extended **`refreshHubState`** — parallel **`GET /api/justice/evidence?case_id={id}`** + existing-contract **`GET /api/justice/filings?case_id={id}`** — same meaning as workbench **`handlingCaseHasFilingRecord`** / **`handlingCaseHasConfirmationOnFile`**); when gates are satisfied, **Handling tracking: Tracking complete for now.** — parity with workbench **Tracking complete** queue — §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**. Nearby subcopy: **In-app tracking only — not filed or submitted.** Placed before handled/open triage note (§2 **Justice hub — handled/open handling triage note on Current case (`/justice`)**) / **View in handling workbench**. **No** new API route, **`client_state`** fields, timeline writes, workers, or queues. **Does not** change request/note/ack writes, **Needs attention** membership, plan, packet, chat-ai, cases, handling workbench, or filing/submission/contact automation. **Read-only in-app sync only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, contacted anyone, or closed the real-world case. **No** schema, migration, new API route, queue worker, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**.
- **Justice hub — Surrenderless handling request and acknowledge on Current case (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **behavior** slice (with §2 **Justice hub — handling requested on Current case (`/justice`)**, §2 **Justice hub — display-only derived handling queue status (`/justice`)**, §2 **Justice hub — handled/open handling triage note on Current case (`/justice`)**, §2 **Handling workbench — active-case back-link from hub**, §2 **Plan — Surrenderless handling requested (`/justice/plan`)**, §2 **Packet — Surrenderless handling request and acknowledge on approved case packet surface (`/justice/packet`)**, §2 **Chat AI intake — handling requested in Current action tracking (`/justice/chat-ai`)**, §2 **Saved cases — acknowledge handling request from Needs attention (`/justice/cases`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). Makes **`/justice`** a **handling entry point** on the **Current case** approved-action area (outside the primary card **`Link`**) when **`snapshot.packetApproved`** and hydrated **`snapshot.approvedNextAction`** exist — closes hub-side **internal manual-handling handoff** without leaving the workspace landing. Page-local **`handleRequestSurrenderlessHandling`**, **`handleUpdateHandlingRequestNote`**, **`handleAcknowledgeHandlingRequest`** mirror plan/packet/chat: set **`handling_requested_at`** (ISO) + optional **`handling_request_note`**; note edit via **`applyHandlingRequestNoteToApprovedNextAction`** / **`omitClearedHandlingRequestNoteFromApprovedNextAction`**; acknowledge via **`acknowledgeHandlingRequestInApprovedNextAction`** — all persist through existing signed-in **UUID** **`GET /api/justice/cases/[id]`** + **`mergeClientStateWithApprovedNextAction`** / **`mergeClientStateWithAcknowledgedHandling`** + **`PATCH`** **`client_state`** (**`mergeApprovedNextActionTrackingFields`**, **`writeSessionApprovedNextAction`**, **`refreshHubState`** display sync) preserving **`status`**, **`started_at`**, **`completed_at`**, **`outcome_note`**, **`follow_up_*`**, **`label`**, **`href`**, **`approved_at`**, and other **`approved_next_action`** fields. Writes gated on current session case (**`STORAGE_CASE_ID === snapshot.caseId`**), **`packetApproved`**, and hydrated **`approvedNextAction`**. UI via shared **`ApprovedNextActionHandlingRequestBlock`**: when **`status !== "completed"`** and **no** **`handling_requested_at`** — **Request Surrenderless handling** + optional note; when **`handling_requested_at`** and **`status !== "completed"`** — same block with **`allowEditNote`**; when **`status === "completed"`** and **`handling_requested_at`** — **`ApprovedNextActionHandlingRequestedReadOnly`**, **`ApprovedNextActionHandlingQueueStatusReadOnly`**, **`ApprovedNextActionHandlingHandledOpenTriageNote`** (**`inlineAck`**) when unacknowledged, **View in handling workbench**, and **Mark acknowledged** + **`APPROVED_NEXT_ACTION_HANDLING_ACKNOWLEDGE_HELPER`** when **`handling_acknowledged_at`** absent. In-card handling lines hidden when interactive block is shown. **Legacy** read-only session display when **`!(packetApproved && approvedNextAction)`** but **`handling_requested_at`** exists — §2 **Justice hub — handling requested on Current case (`/justice`)**; approved-packet without handling request copy now says request from **action plan, case packet, or here on the hub**. **Does not** auto-request handling on packet approval; **does not** change plan, chat-ai, cases, packet inner card, handling workbench, membership predicates, routing, APIs, schema, timeline writes, workers, queues, filing, submission, or automation. **Internal triage tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Justice hub — Needs more info chat routing CTA on Current case (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **UI/navigation** slice (with §2 **Justice hub — Current case compact readiness checklist (`/justice`)**, §2 **Saved cases — Needs more info chat routing CTA (`/justice/cases`)**, §2 **Chat AI intake — hydrate & update commit**). When valid local intake exists (**Current case** card), uses **`isBasicCaseInfoReadyForEscalation`** from **`caseReadiness.ts`** on session intake (**no** predicate changes). When signed in and **`STORAGE_CASE_ID`** is a **UUID**, **`GET /api/justice/evidence?case_id={id}`** (same contract as saved cases/plan — **no** new API) on mount/focus/**`visibilitychange`**/**`storage`** refresh alongside the existing snapshot refresh; array length → saved proof/evidence count. Directly **below** the primary **Current case** **Continue in chat** link (outside the nested card link), one outline secondary **`Link`**: when **`!isBasicCaseInfoReadyForEscalation(intake)`** — **Update in chat** → **`/justice/chat-ai`**; when basics are complete, evidence count is loaded, and **`< 1`** — **Add proof in chat** → **`/justice/chat-ai`**; when ready (**basics** + at least one saved item) — **no** extra CTA. Active session case only — **no** **`activateCaseInSession`**. Primary **Current case** nested card target — §2 **Justice hub — Current case primary CTA routing (`/justice`)** (checklist inline links **unchanged**). Handling blocks below the card, hub link list, saved cases, plan, chat-ai, APIs, schema, and shared readiness logic **unchanged**. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Justice hub — Current case compact readiness checklist (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **UI/navigation** slice (with §2 **Justice hub — Needs more info chat routing CTA on Current case (`/justice`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Justice hub — Current case prepared packet reviewed checklist row (`/justice`)**, §2 **Saved cases — compact readiness checklist on All cases cards (`/justice/cases`)**, §2 **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**, §2 **Plan — Case readiness checklist proof CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist basics CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**, §2 **Plan — Case readiness approved-next visibility (`/justice/plan`)**, §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**). When **Current case** exists, adds compact **`<ul>`** under the primary card link and **before** existing secondary **Update in chat** / **Add proof in chat** buttons — mirrors plan **Case readiness** clarity on the hub entry surface. Uses **existing** state only (**no** new API, **no** schema): **`basicsReady`** from **`isBasicCaseInfoReadyForEscalation`** on session intake; **`evidenceCount`** after **`!evidenceLoading`** (same evidence **`GET`** as §2 **Justice hub — Needs more info chat routing CTA on Current case (`/justice`)**); **`snapshot.reviewed`** from session **`readTimeline`** / **`submission_draft_reviewed`**. Three rows: **Basic case info: yes/not yet** — when **`snapshot && !basicsReady`**, inline **Update in chat** → **`/justice/chat-ai`**; **Evidence: yes/not yet** (or **Evidence: loading...** while **`evidenceLoading`**) — when loaded and **`(evidenceCount ?? 0) < 1`**, inline **Add proof in chat** → **`/justice/chat-ai`**; **Submission draft reviewed: yes/not yet** — when **`snapshot && !snapshot.reviewed`**, inline **Review submission draft** → **`/justice/preview`**; fourth row when draft reviewed — §2 **Justice hub — Current case prepared packet reviewed checklist row (`/justice`)**. Same inline link classes as plan checklist CTAs (**`hubChecklistLinkCls`**). **Does not** change **`showUpdateInChat`**, **`showAddProofInChat`**, primary **Current case** card **`href`** / label (§2 **Justice hub — Current case primary CTA routing (`/justice`)**), evidence fetch, handling blocks, or hub link list. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** API, schema, helper, plan, preview, chat-ai, or cases changes on this slice. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Justice hub — Current case prepared packet reviewed checklist row (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **UI/navigation** slice (with §2 **Justice hub — Current case compact readiness checklist (`/justice`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**, §2 **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`)**, §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**, §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**, §2 **Plan — prepared packet approval surfaced (`/justice/plan`)**). Adds page-local **`STORAGE_PREPARED_PACKET_APPROVED_V1`** and **`readSessionPreparedPacketApproved(caseId)`** (same JSON map pattern as preview/plan/chat-ai). Extends **`readSnapshotFromLocalSession`** / **`CurrentCaseSnapshot`** with **`packetApproved: readSessionPreparedPacketApproved(caseId)`** when **`caseId`** exists — refreshed on existing **`refreshHubState`** (focus/**`storage`**/**`visibilitychange`** — **no** new listeners). In **Current case** compact checklist **`<ul>`**, **after** **Submission draft reviewed**, **only when** **`snapshot.reviewed`**: **Prepared case packet reviewed: yes** when **`packetApproved`**; **Prepared case packet reviewed: not yet** with inline **Review prepared case packet** → **`/justice/packet`** when not approved (**`hubChecklistLinkCls`**). **Navigation/readiness only** — closes hub recovery gap between draft review and packet approval (parity with chat-ai). **Does not** change evidence **`GET`**, handling blocks, approved-next labels, **Update in chat** / **Add proof in chat**, primary card **`href`** / **`primaryLabel`** (§2 **Justice hub — Current case primary CTA routing (`/justice`)**), routes, APIs, schema, handlers, or shared libs. **Session-only** for packet approval on hub (**no** case **`GET`** on this slice). **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, uploaded, stored, or contacted automatically.
- **Justice hub — Current case primary CTA routing (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **UI/navigation** slice (with §2 **Justice hub — Current case compact readiness checklist (`/justice`)**, §2 **Justice hub — Current case prepared packet reviewed checklist row (`/justice`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Chat AI intake — hydrate & update commit**). Page-local **`primaryHref`** **`"/justice/chat-ai"`** and **`primaryLabel`** **Continue in chat** on the nested **Current case** primary **`Link`** — regardless of **`snapshot.reviewed`** or **`snapshot.packetApproved`**. Checklist deep links **unchanged**: **Review submission draft** → **`/justice/preview`**; **Review prepared case packet** → **`/justice/packet`**. Preview **Continue** (§2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**) and plan/packet page behavior **unchanged** on this slice. **`snapshot.reviewed`** / **`snapshot.packetApproved`** remain for checklist rows and card copy only — **no** new API, **no** schema. Checklist **`<ul>`**, prepared-packet row, approved-next lines, handling blocks, evidence **`GET`**, **`showUpdateInChat`**, **`showAddProofInChat`**, static hub link list, APIs, schema, handlers, filing/submission/contact/queue/upload/storage/automatic-action behavior **unchanged** on this slice. **Navigation/readiness only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Justice hub — remove plan-first resume card when no Current case (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **UI/navigation** slice (with §2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Saved cases — intro copy chat-first (`/justice/cases`)**). Removes conditional hub link-list card **Continue current action plan** → **`/justice/plan`** shown when **`!snapshot`** (no **Current case** card). Resume when no local session intake: **Start with chat intake** → **`/justice/chat-ai`** and **Saved cases** → **`/justice/cases`** (**Open case** → chat — §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**). **Does not** change **Current case** card, checklist, handling blocks, **`primaryHref`** / **Continue in chat**, **Start with chat intake**, **Use step-by-step chat**, **Start with form intake**, APIs, schema, or **`/justice/plan`** page behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Justice hub — approved-packet handling request copy chat-first (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **UI/copy** slice (with §2 **Approval-to-action engine — approved packet action workbench link on justice hub Current case (`/justice`)**, §2 **Saved cases — approved-packet handling request copy chat-first (`/justice/cases`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Chat AI intake — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**). In both **Current case** approved-packet workbench helper blocks, second sentence **Request Surrenderless handling from chat intake, the case packet, or here on the hub when you want internal triage tracking.** replaces **Request Surrenderless handling from your action plan, case packet, or here on the hub when you want internal triage tracking.** — aligns with **Continue in chat** primary CTA (§2 **Justice hub — Current case primary CTA routing (`/justice`)**) and Saved cases/chat-ai helper copy (§2 **Saved cases — approved-packet handling request copy chat-first (`/justice/cases`)**). First sentence and **View on handling workbench** → **`/justice/handling`** **unchanged**. **Does not** change **Current case** card, **`primaryHref`**, checklist, handling tracking UI, hub link list, handling-request UI, APIs, schema, or filing/submission/contact/queue/upload/storage/automatic-action behavior. **Copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Justice hub — approved next-step label and status on Current case (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **visibility-only** slice (with §2 **Justice hub — approved next action display hydrate on Current case (`/justice`)**, §2 **Justice hub — handled-for-now timestamp on Current case (`/justice`)**, §2 **Saved cases — approved next-step label on All cases cards (`/justice/cases`)**, §2 **Saved cases — Needs attention approved-next-action label alignment (`/justice/cases`)**, §2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**, §2 **Plan — Case readiness approved-next visibility (`/justice/plan`)**, §2 **Plan — post-draft approved step label alignment (`/justice/plan`)**, §2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — Recap current-action label alignment (`/justice/chat-ai`)**, §2 **Approved next action shared state helpers**). In **`readSnapshotFromLocalSession`** / **`buildCurrentCaseSnapshot`**, reuses existing **`approvedNext`** from **`readSessionApprovedNextAction(caseId)`** (**`justice_approved_next_action_v1`** — §2 **Approved next action shared state helpers**); derives **`stepLabel`** from **`approvedNext?.label?.trim()`** and **`statusLabel`** from **`approvedNextActionStatusLabel(approvedNext?.status)`** (**Approved** / **Started** / **Handled**). On the **Current case** card (inside the primary **`Link`** — §2 **Justice hub — Current case primary CTA routing (`/justice`)** — after **Submission draft reviewed** / **not reviewed** and **before** in-card handling lines and **`{primaryLabel}`**), shows read-only **Next step: {label}** (bold **label**) when **`stepLabel`** exists and **Approved next action: {statusLabel}** when **`statusLabel`** exists — small neutral **`text-xs`** styling aligned with hub card copy. Signed-in **UUID** cases also server-hydrate approved next on **`refreshHubState`** — §2 **Justice hub — approved next action display hydrate on Current case (`/justice`)**; completed **handled-for-now** timestamp — §2 **Justice hub — handled-for-now timestamp on Current case (`/justice`)**. **Does not** add **Open {label}**, **Record action handled**, or **`PATCH`** on the hub. Readiness checklist, **Update in chat** / **Add proof in chat** CTAs, **display-only** handling queue status below the card, **View in handling workbench**, hub link list, Plan, Packet, Chat AI, Cases, APIs, and schema **unchanged** on this slice. **In-app tracking visibility only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, contacted anyone, uploaded, stored files, or taken automatic action on the user's behalf. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**.
- **Justice hub — approved next action display hydrate on Current case (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **display/hydrate** slice (with §2 **Justice hub — approved next-step label and status on Current case (`/justice`)**, §2 **Justice hub — handled-for-now timestamp on Current case (`/justice`)**, §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**, §2 **Chat AI intake — approved next action display hydrate (`/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In existing **`refreshHubState`** (mount, window **focus**, **`storage`**, tab **visibility** — same events as evidence **`GET`**), when valid local intake exists and **`STORAGE_CASE_ID`** is a signed-in **UUID**, **`GET /api/justice/cases/[id]`** reads server **`client_state`**, merges with session via **`hydrateApprovedNextActionForDisplay(caseId, client_state)`** (session fallback when **`GET`** fails or server has no **`approved_next_action`**), updates **Current case** snapshot **`approvedNextAction`** / **`stepLabel`** / **`statusLabel`** / handling display fields, and **`writeSessionApprovedNextAction`** to sync **`justice_approved_next_action_v1`**. Lets **Started** / **Handled** (and handling-request fields) written from **`/justice/handling`**, **`/justice/plan`**, **`/justice/chat-ai`**, or **`/justice/packet`** appear on the hub **without** requiring a plan/chat visit first. **Does not** add buttons, **`PATCH`**, **`handling_requested_at`** writes, outcome/follow-up UI, or show **completed** approved-packet actions in inbox-style lists (**`isApprovedPacketActionWithoutHandlingRequest`** still excludes **`status === "completed"`** for workbench nav). **Display correctness only** — **no** new API route, schema/migration, DB field, timeline **type**, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Justice hub — handled-for-now timestamp on Current case (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **read-only continuity display** slice (with §2 **Justice hub — approved next action display hydrate on Current case (`/justice`)**, §2 **Justice hub — approved next-step label and status on Current case (`/justice`)**, §2 **Justice hub — started-at read-only on Current case (`/justice`)**, §2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)**, §2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**, §2 **Chat AI intake — handled-for-now timestamp in Current action tracking (`/justice/chat-ai`)**, §2 **Packet — handled-for-now timestamp on approved next action card (`/justice/packet`)**, §2 **Plan — handled-for-now timestamp on prepared-packet approved callouts (`/justice/plan`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). On the **Current case** card (inside the primary **`Link`**), after **Approved next action: Handled**, when hydrated **`snapshot.approvedNextAction.status === "completed"`** and **`completed_at`** is present (from existing **`refreshHubState`** / **`hydrateApprovedNextActionForDisplay`** — **no** new **`GET`** or **`PATCH`**), shows read-only **Handled for now {formatted time}** via existing **`formatApprovedNextActionHandlingTimestamp`**. **Does not** add buttons, timeline events, or show **completed** approved-packet actions in **Needs attention** or handling workbench **Approved packet actions** inbox lists. **Does not** change primary CTA routing, checklist, handling blocks, workbench links, Plan, Packet, Chat AI, Cases, or APIs. **Display correctness only** — **no** new API route, schema/migration, DB field, timeline **type**, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Justice hub — started-at read-only on Current case (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **display-only** slice (with §2 **Justice hub — handled-for-now timestamp on Current case (`/justice`)**, §2 **Justice hub — approved next action display hydrate on Current case (`/justice`)**, §2 **Justice hub — approved next-step label and status on Current case (`/justice`)**, §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`)**, §2 **Saved cases — started-at read-only on All cases cards (`/justice/cases`)**, §2 **Approved next action started (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). On the **Current case** card (inside the primary **`Link`**), after **Approved next action: {status}** and before **Handled for now** / outcome / handling blocks, when hydrated **`snapshot.approvedNextAction.status === "started"`** and **`started_at`** is present (from existing **`refreshHubState`** / **`hydrateApprovedNextActionForDisplay`** — **no** new **`GET`** or **`PATCH`**), shows read-only **Opened {formatted time}** via existing **`formatApprovedNextActionHandlingTimestamp`** — parity with read-only **Handled for now {formatted time}** when **`status === "completed"`** (§2 **Justice hub — handled-for-now timestamp on Current case (`/justice`)**). Complements §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`)**, and §2 **Saved cases — started-at read-only on All cases cards (`/justice/cases`)**. **Does not** change primary CTA routing, checklist, handling blocks, workbench links, **Needs attention** card templates, plan, packet, chat-ai, cases, handling workbench, membership predicates, or any write paths. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Justice hub — outcome / follow-up read-only on Current case (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **read-only continuity display** slice (with §2 **Justice hub — approved next action display hydrate on Current case (`/justice`)**, §2 **Justice hub — handled-for-now timestamp on Current case (`/justice`)**, §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**, §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**, §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). On the **Current case** card (inside the primary **`Link`** only), reuses existing hydrated **`snapshot.approvedNextAction`** from **`refreshHubState`** / **`hydrateApprovedNextActionForDisplay`** (server **`client_state.approved_next_action`** merged with session — **no** new **`GET`** or **`PATCH`**). After **Handled for now** (§2 **Justice hub — handled-for-now timestamp on Current case (`/justice`)**), when **`outcome_note`** is present, shows read-only truncated outcome text (page-local **`truncateAttentionNote`**, max **200** — same cap as §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**). After in-card handling lines and before **`{primaryLabel}`**, when **`follow_up_needed === true`**, shows **Follow-up needed**; when **`follow_up_at`** is set, shows **`ApprovedNextActionFollowUpTimingLine`**. **Read-only only** — **no** forms, buttons, **`PATCH`**, timeline events, or changes to **Needs attention** builders, handling triage, **Clear follow-up**, inbox rules, or routing. Complements outcome/follow-up writes from §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)** and browse display on §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**. **Display correctness only** — **no** new API route, schema/migration, DB field, timeline **type**, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Justice workspace recovery routing (`/justice` fallback):** When a Consumer Justice page **cannot hydrate or resume a usable active case** (e.g. no valid local intake and **no server row** after **`GET /api/justice/cases`**, plan **GET**/list **hydrate** failures / empty list, **`useJusticeActionPageHydration`** unable to hydrate, or analogous **missing `STORAGE_CASE_ID` / intake** escapes on routed surfaces aligned with this pattern), **`router.replace("/justice")`** or **`router.push("/justice")`** (**and matching `Link` destinations** where the same UX intent applies) lands the user on the **`/justice`** workspace hub — the **recovery hub** for missing / **no-case** / **failed-resume** states (replacing earlier **`/justice/intake`**-only redirects for those outcomes). **`/justice`** preserves **chat-first recovery** (**Start with chat intake** is first on the hub) while **`Start with form intake`** stays one click away. **Routing / copy-only** slice: **no** changes to **`rules.ts`**, recommendation logic, destination ordering, schemas, **API** route implementations (handlers, payloads, responses), auth guards, **`commitIntakeToSessionAndServer`** or other **persistence**/session commit behavior, timeline event **semantics**, **`/justice/preview`** deterministic or AI-assisted draft **generation**, filing automation, or filing/submission flows.
- **Chat-first navigation promotion (UI only on hub/header):** **`/justice`** workspace and **`Header.tsx`** emphasize **AI conversational chat intake** first — **Justice** → **Chat intake** (**`/justice/chat-ai`**) → **Consumer case** (**`/justice/intake`**) in the header; **Start with chat intake** is the **first** hub card (**`/justice/chat-ai`**). A second hub card, **Use step-by-step chat**, links to **scripted** **`/justice/chat`** (§2 **Justice hub — step-by-step chat fallback**). **`/justice/chat`** remains **scripted** Q&A (not LLM-driven); **scripted chat behavior is unchanged** on this slice. **`/justice/chat-ai`** page copy presents conversational intake as the primary **Your consumer case** path (not a separate “prototype” label). **Preview-first intake finish** for **`/justice/intake`** and **`/justice/chat`:** after successful commit, **`router.push`** **`/justice/preview`**; **`/justice/chat-ai`** after successful signed-in **UUID** **create** **Continue** stays on **`/justice/chat-ai`** and flips to **Update in chat** (**`setIsUpdatingExistingCase(true)`** + **`sessionBaselinePartsRef`**); unsigned, non-UUID, or failed server persist still **`router.push`** **`/justice/preview`**; after successful draft review on preview, **Continue** routes to **`/justice/packet`** when the prepared packet is not approved and **`/justice/plan`** when it is (§2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**); users can also reach **`/justice/plan`** via hub, packet, or other surfaces (**`submission_draft_reviewed`** tracking unchanged). **Navigation slice only:** **no** new APIs, **no** prompt/parser changes, **no** LLM behavior changes, **no** auth changes on scripted chat, **no** schema or DB changes, **no** Supabase changes, **no** **`caseReadiness.ts`** changes, **no** plan **`rules.ts`** logic changes, **no** preview/plan behavior changes beyond existing flows. (Intake **commit** and **hydration** behavior for **`/justice/chat-ai`** are documented in §2 **Chat AI intake — hydrate & update commit**.)
- **Chat intake — shared `JusticeIntake` builder (scripted `/justice/chat` + AI `/justice/chat-ai` + structured alignment):** **`src/lib/justice/buildJusticeIntake.ts`** centralizes **`BuildJusticeIntakeParts`** defaults and conversion: **`defaultBuildJusticeIntakeParts()`**, forward **`buildJusticeIntakeFromParts`** (trim strings, **`normalizeCompanyWebsite`** on scripted/AI paths that use it, merge **`money_amount`** + **`desired_resolution`** into **`money_involved`** with separator **` — Desired outcome: `**, contact fields only when **`already_contacted === "yes"`**, optional **`consumer_us_state`**), and reverse **`justiceIntakeToBuildJusticeIntakeParts`** (hydrate saved **`JusticeIntake`** from **`sessionStorage`** — conservative split of **`money_involved`** on that separator or hyphen variant, safe contact enums, **`already_contacted`** only **`yes`** when stored as yes). **Scripted** **`/justice/chat`** and **AI** **`/justice/chat-ai`** both produce the **same `JusticeIntake`** shape before commit. **`parseIntakeChatAiResponse.ts`** imports shared **`defaultBuildJusticeIntakeParts`** (re-exported) instead of duplicating defaults. **Post-intake rail:** build intake → **`commitIntakeToSessionAndServer`** — **`/justice/intake`** and **`/justice/chat`** → **`/justice/preview`** → **`/justice/plan`**; **`/justice/chat-ai`** signed-in **UUID** **create** stays in chat for inline funnel, with **`/justice/preview`** fallback when continuity guards fail. **Still unchanged on this slice:** **`rules.ts`**, readiness, preview draft **generation**, filing/evidence APIs, Supabase schema, **`POST /api/justice/intake-chat`** contract, auto-filing/submission automation (§1).
- **Chat intake — shared contact-proof validation (scripted `/justice/chat` + AI `/justice/chat-ai`):** **`validateContactProofForIntake`** in **`src/lib/justice/buildJusticeIntake.ts`** (same module as the builder): when **`already_contacted === "yes"`**, **`contact_proof_type`** **`none`** or **`ticket`** requires non-empty **`contact_proof_text`** with fixed error strings (**“Describe your contact attempt before continuing.”** / **“Enter the ticket or case number before continuing.”**). Used by **scripted** chat and **AI** chat before commit (**`create`** or **`update`**). **Validation rules unchanged** on hydrate/update slice; **no** change to **`rules.ts`**, readiness, preview draft **generation**, or intake-chat API.
- **AI intake chat API (`POST /api/justice/intake-chat`):** **`src/app/api/justice/intake-chat/route.ts`** with **`buildJusticeIntakeAiPrompt.ts`** and **`parseIntakeChatAiResponse.ts`**. **Request:** `{ user_message, parts: BuildJusticeIntakeParts, conversation_history?: { role: "user" | "assistant"; content: string }[] }` — one user turn plus current partial parts and optional capped history. **Response:** `{ assistantMessage, parts }` — updated **`BuildJusticeIntakeParts`** (pre-commit; not full **`JusticeIntake`** until client runs **`buildJusticeIntakeFromParts`**). **Security:** same envelope as **`preview-draft`** — **`getUserOr401`**, **`rateLimit`**, **`OPENAI_API_KEY`** server-only, bounded body, JSON validation/clamping, OpenAI **`json_object`** response parsed before return. **No** Supabase / DB writes, **no** timeline writes, **no** case creation, **no** **`commitIntakeToSessionAndServer`** in the API. Consumed by **`/justice/chat-ai`** (§2). **Scripted `/justice/chat`** unchanged.
- **AI chat intake (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** — **primary AI conversational intake** UI (hub/header **Chat intake** and **Start with chat intake** → here; plan **Update in chat** → here; §2 **Chat-first navigation promotion** / **Justice-wide navigation alignment**). **Separate route** from **scripted** **`/justice/chat`** (scripted page **unchanged**; linked as **Use step-by-step chat**). **`/justice/intake`** remains the **structured form fallback** (**Edit structured form**). **Clerk** sign-in required (**`JusticeActionResumeSignInPrompt`** when signed out). On mount, **`readValidLocalJusticeIntake()`** (§2 **hydrate & update commit**): if valid **`STORAGE_INTAKE`** exists, **`justiceIntakeToBuildJusticeIntakeParts`** seeds **`parts`**, **Recap** shows the loaded case immediately, assistant copy explains the case is loaded and the user can describe changes; otherwise fresh **`defaultBuildJusticeIntakeParts()`** + new-case greeting. When **`isUpdatingExistingCase`**, compact **Active case** banner above the main chat card with compact readiness checklist and funnel-aware footer work links (§2 **Chat AI intake — active case context banner (Update in chat)**, §2 **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**); session changes surface in **Updated in this chat** and/or folded into **What happens next** when Continue handoff is visible (§2 **Chat AI intake — session change summary (Update in chat)**, §2 **Chat AI intake — Continue handoff session-change integration (Update in chat)**). When **`approved_next_action`** is present, **Current action tracking** in **Recap** with funnel-aware footer work links matching the **Active case** banner (§2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**). Local **`messages`** and **`parts`**; each send calls **`POST /api/justice/intake-chat`** with **`{ user_message, parts, conversation_history }`** (history = prior UI messages only; in-flight send guard §2; API error-copy polish §2). Shows assistant replies, compact **Recap** (including **Proof / evidence** guidance §2 and **Current action tracking** when applicable), OpenAI disclosure. **Continue to submission preview** blocked until basics present (§2 preview-readiness polish); **evidence not required** before preview. When complete: **`validateContactProofForIntake`** → **`buildJusticeIntakeFromParts`** → **`commitIntakeToSessionAndServer`** with **`mode: "update"`** when hydrated from an existing case, else **`create`** (**`commitLogLabel`**: **`justice chat-ai`**) → **`/justice/preview`** → **`/justice/plan`**. **Not filing automation** — same review/preview/plan rail as before. **Unchanged:** **`POST /api/justice/intake-chat`** contract, prompt/parser, **`rules.ts`**, readiness, preview draft **generation**, filing/evidence **APIs**, Supabase schema, scripted **`/justice/chat`** Q&A.
- **Chat AI intake — proof/evidence guidance (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/copy** slice in the **Recap** block (near **Save and continue in chat**). **Proof / evidence** panel explains that as the case is built **in this chat**, Surrenderless can organize proof such as **screenshots, receipts, order confirmations, emails, account pages, tracking pages, call notes, and chat transcripts** to strengthen the case. Copy frames this as the **chat-first workflow** (next step in the same case), not a separate manual detour. On **Update in chat** with a server-backed **UUID** case, a collapsed **Add a proof note** form can save one metadata row in-app with required **title** + **evidence_type** and optional **evidence date** / **description** (§2 **Chat AI intake — add proof note from Recap (Proof / evidence panel)**); on **fresh intake** (before first server-backed UUID case), the same form can **stage** metadata locally until **Continue to submission preview** (§2 **Chat AI intake — staged proof notes on fresh intake (Proof / evidence panel)**); when saved rows exist, a collapsed **Recent proof notes** block shows up to **3** metadata rows with inline **Edit** and **Delete** (§2 **Chat AI intake — recent saved proof preview in Recap (Proof / evidence panel)**); client-side **proof keyword nudge** after chat send may point users to **Add a proof note** and **prefill empty** title/description from the triggering message (§2 **Chat AI intake — proof keyword nudge in Recap (Proof / evidence panel)**); preview shows title, type, optional date, truncated description — **no** **`source_url`** / **`storage_note`** in chat. CTA **Organize evidence** → **`/justice/evidence`** (existing **`justice_case_evidence`** surface: title, type, optional date, description, optional **`source_url`** / **`storage_note`** — **metadata/notes/links only**; **file uploads are not available**; full list/edit/delete); return path **Update in chat** on **`/justice/evidence`** breadcrumb — §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**. **Evidence is optional before preview**; saving at least one item helps before **escalation/submission** (plan **ready to escalate** still uses evidence count §2); **nothing is filed automatically** from the app yet. **No** **`justice_case_evidence`** rows auto-created from chat; **no** evidence on **`BuildJusticeIntakeParts`**; **Continue** / commit (**create** / **update**) unchanged. **No** API, Supabase schema, **`rules.ts`**, **`caseReadiness.ts`**, preview generation, commit semantics, filing behavior, or intake-chat contract changes. Shown for **fresh intake** and **hydrated Update in chat**. Saved evidence count, recent proof preview, and inline edit/delete of displayed recent rows on **Update in chat** — §2 **Chat AI intake — saved evidence count in Recap (Proof / evidence panel)**, §2 **Chat AI intake — recent saved proof preview in Recap (Proof / evidence panel)**, §2 **Chat AI intake — edit recent proof note in Recap (Proof / evidence panel)**, §2 **Chat AI intake — delete recent proof note in Recap (Proof / evidence panel)**, §2 **Chat AI intake — proof keyword nudge in Recap (Proof / evidence panel)**, §2 **Chat AI intake — staged proof notes on fresh intake (Proof / evidence panel)**.
- **Chat AI intake — saved evidence count in Recap (Proof / evidence panel) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **chat-first visibility** slice (with §2 **Chat AI intake — proof/evidence guidance**). When **Update in chat** is active (**`isUpdatingExistingCase`**) and the user is signed in with a **UUID** **`STORAGE_CASE_ID`**, page-local **`loadSavedEvidencePreview`** calls existing **`GET /api/justice/evidence?case_id={id}`** (same pattern as **`/justice/plan`** saved-evidence preview and **`/justice/cases`** progress fetch — **no** new route, **no** schema) and sets **`savedEvidenceCount`** plus **`recentEvidenceRows`** (up to 3 newest — §2 **Chat AI intake — recent saved proof preview in Recap (Proof / evidence panel)**). In **Recap → Proof / evidence**, shows **Saved evidence: {N} item(s).** when **N > 0** or **No saved evidence yet.** when **N === 0**; **no** count line on **fresh intake**, signed-out users, or non-UUID case ids (generic panel copy only). Refetches on **window** **focus** and tab **visibility** → **visible** and after in-chat proof note save (§2 **Chat AI intake — add proof note from Recap (Proof / evidence panel)**) so returning from **`/justice/evidence`** or saving in chat updates the count and recent preview. **Does not** create **`justice_case_evidence`** rows from chat except via §2 **Chat AI intake — add proof note from Recap (Proof / evidence panel)**; change **`POST /api/justice/intake-chat`**, **`commitIntakeToSessionAndServer`**, preview-readiness gating, **Current action tracking**, handling UI, **Organize evidence** link, or existing guidance copy. **No** migration, worker, **`justice_case_filings`**, timeline **type**, or external submission/contact/queue automation.
- **Chat AI intake — add proof note from Recap (Proof / evidence panel) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **chat-first capture** slice (with §2 **Chat AI intake — proof/evidence guidance**, **Chat AI intake — saved evidence count in Recap (Proof / evidence panel)**, **Chat AI intake — recent saved proof preview in Recap (Proof / evidence panel)**, **Chat AI intake — edit recent proof note in Recap (Proof / evidence panel)**, **Chat AI intake — proof keyword nudge in Recap (Proof / evidence panel)**). When **Update in chat** is active (**`isUpdatingExistingCase`**) and the user is signed in with a **UUID** server-backed **`STORAGE_CASE_ID`** (**`canAddProofNoteInChat`** — same gate as **`loadSavedEvidencePreview`**: **`isLoaded`**, signed in, **`isUuid(caseId)`**), **Recap → Proof / evidence** shows a collapsed **`<details>`** **Add a proof note** mini-form: required **title** (max 500) and **evidence_type** select (**`JUSTICE_EVIDENCE_TYPES`** / **`JUSTICE_EVIDENCE_TYPE_LABELS`** from **`src/lib/justice/evidence.ts`**, default **`other`**); optional **evidence date** text input (max 200) and optional **description** textarea (max 8000). Submit **`POST /api/justice/evidence`** with **`case_id`**, **`title`**, **`evidence_type`**, and **`evidence_date`** / **`description`** only when trimmed non-empty — **no** new API, **no** schema; server appends **`evidence_added`** timeline via existing route. On success **`applyServerTimelineFromResponse`** (same pattern as **`/justice/evidence`** **`handleAdd`**), clears **title**, **evidence date**, and **description** ( **`evidence_type`** unchanged), shows **Proof note saved.**, refreshes **`loadSavedEvidencePreview`** (count + recent list — saved date/description appear in §2 **Chat AI intake — recent saved proof preview in Recap (Proof / evidence panel)** after refetch); on error, shows API **`error`** string; submit disabled while saving. **Metadata only** — honest copy (not file upload/storage); **no** **`source_url`** or **`storage_note`** in chat form; **no** filing, submission, external queue, or contact. **`/justice/evidence`** remains full list/edit/delete (**Organize evidence** link unchanged). On **Update in chat**, requires signed-in **UUID** **`STORAGE_CASE_ID`** (**`canAddProofNoteInChat`**). On **fresh intake**, stages locally via **`stagedProofNotes.ts`** (**`canStageProofNoteInChat`**) — §2 **Chat AI intake — staged proof notes on fresh intake (Proof / evidence panel)**; not signed-out. **No** auto-created evidence rows; **no** changes to **`POST /api/justice/intake-chat`** or **`buildJusticeIntakeAiPrompt.ts`** (§2 **Chat AI intake — proof keyword nudge in Recap (Proof / evidence panel)** is client-side in **`page.tsx`** only). **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — recent saved proof preview in Recap (Proof / evidence panel) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **chat-first visibility** slice (with §2 **Chat AI intake — proof/evidence guidance**, **Chat AI intake — saved evidence count in Recap (Proof / evidence panel)**, **Chat AI intake — add proof note from Recap (Proof / evidence panel)**, **Chat AI intake — edit recent proof note in Recap (Proof / evidence panel)**, **Chat AI intake — delete recent proof note in Recap (Proof / evidence panel)**, **Chat AI intake — proof keyword nudge in Recap (Proof / evidence panel)**). Reuses the same **`loadSavedEvidencePreview`** **`GET /api/justice/evidence?case_id={id}`** response ( **`created_at` desc** ) — **no** second fetch. When **Update in chat** is active, signed in, **UUID** **`STORAGE_CASE_ID`**, and **`savedEvidenceCount > 0`** (**`showRecentEvidencePreview`**), **Recap → Proof / evidence** shows collapsed **`<details>`** **Recent proof notes** (up to **3** rows from **`recentEvidenceRows`**; summary **(3 of N)** when **N > 3**). Each row: **title**, **evidence_type** label (**`JUSTICE_EVIDENCE_TYPE_LABELS`**), optional **evidence_date**, optional truncated **description** (~120 chars — **`CHAT_EVIDENCE_DESC_PREVIEW_MAX`**); per-row **Edit** → inline form (§2 **Chat AI intake — edit recent proof note in Recap (Proof / evidence panel)**) and **Delete** (§2 **Chat AI intake — delete recent proof note in Recap (Proof / evidence panel)**); values saved/edited/deleted in chat appear after **`loadSavedEvidencePreview`** refetch; **no** **`source_url`** or **`storage_note`** in chat. **`/justice/evidence`** remains full list/edit/delete for all rows and optional links (**Organize evidence** unchanged). Refreshes with **`loadSavedEvidencePreview`** on focus/visibility and after in-chat proof note save/edit/delete. **Metadata only** — **no** file upload/storage, filing, submission, external queue, or contact. **Not** on fresh intake before first server commit, signed-out, non-UUID, or **N === 0**. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — edit recent proof note in Recap (Proof / evidence panel) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **chat-first limited edit** slice (with §2 **Chat AI intake — recent saved proof preview in Recap (Proof / evidence panel)**, **Chat AI intake — add proof note from Recap (Proof / evidence panel)**). In **Recent proof notes**, each displayed row (max **3**) has **Edit**; one inline form at a time with **title**, **evidence_type**, optional **evidence date** (max 200), optional **description** (max 8000), **Save** / **Cancel** — hydrated from full **`recentEvidenceRows`** row (not truncated preview text). **`PATCH /api/justice/evidence/[id]`** (existing route — **no** new API, **no** schema) with **`title`**, **`evidence_type`**, **`evidence_date`** (trimmed string or **`null`** to clear), **`description`** (trimmed string or **`null`** to clear) only — **no** **`source_url`** or **`storage_note`**. On success clears edit state, **Proof note updated.**, refreshes **`loadSavedEvidencePreview`**; **no** **`applyServerTimelineFromResponse`** (evidence edit does not append timeline — same as **`/justice/evidence`** edit). **`/justice/evidence`** for full list, delete outside recent preview, and **`source_url`**/**`storage_note`** links. **Metadata only** — **no** file upload/storage, filing, submission, external queue, or contact. **Not** on fresh intake before first server commit, signed-out, non-UUID, or rows outside recent preview. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — delete recent proof note in Recap (Proof / evidence panel) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **chat-first limited delete** slice (with §2 **Chat AI intake — recent saved proof preview in Recap (Proof / evidence panel)**, **Chat AI intake — edit recent proof note in Recap (Proof / evidence panel)**). In **Recent proof notes**, each displayed row (max **3**) has **Delete** beside **Edit**; **`window.confirm`** before delete; **`DELETE /api/justice/evidence/[id]`** (existing route — **no** new API, **no** schema). On success **Proof note deleted.**, clears edit state if that row was being edited, refreshes **`loadSavedEvidencePreview`** (saved count + recent rows); visible error on failure; **no** **`applyServerTimelineFromResponse`** (delete does not append timeline); **no** **`source_url`** or **`storage_note`**. Rows not shown in recent preview and bulk management remain on **`/justice/evidence`**. **Metadata only** — **no** file upload/storage, filing, submission, external queue, or contact. **Not** on fresh intake before first server commit, signed-out, non-UUID, or **N === 0**. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — proof keyword nudge in Recap (Proof / evidence panel) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **client-side** slice (with §2 **Chat AI intake — add proof note from Recap (Proof / evidence panel)**). After a **successful** **`handleSend`** → **`POST /api/justice/intake-chat`** round trip, inspects the **latest user message** only (local regex in **`page.tsx`** — **no** API/schema changes; **`buildJusticeIntakeAiPrompt.ts`** unchanged). **Strong terms** (e.g. screenshot, receipt, invoice, tracking, confirmation, transcript, call note, chat log, account page); **negative guard** skips phrases like “no proof/evidence/screenshot”; **does not** match weak terms alone (proof, evidence, file, upload, email). When **`canAddProofNoteInChat`** and not yet offered this page session, shows a dismissible amber banner above **Add a proof note** guiding user to save a metadata proof note (**not** a file upload); **prefills** **Add a proof note** locally from the triggering user message (**empty fields only** — never overwrites an existing draft): **title** from trimmed/collapsed message capped ~**120** chars (word-safe ellipsis when truncated); **description** = full collapsed message only when longer than the title cap; **does not** infer **`evidence_type`** or **`evidence_date`**; opens the **Add a proof note** **`<details>`** for review; user must still click **Save proof note**. **Got it** dismisses the banner; banner hides after successful proof note save; **at most once** per page session. **Does not** auto-create **`justice_case_evidence`** rows. **Not** on fresh intake before first server commit. **LLM/system-prompt** proof nudges remain **future** work. **No** API/schema/intake-chat/prompt changes on this slice. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — staged proof notes on fresh intake (Proof / evidence panel) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** + **`src/lib/justice/stagedProofNotes.ts`** — **client-side staging** slice (with §2 **Chat AI intake — add proof note from Recap (Proof / evidence panel)**). On **fresh intake** (**`!isUpdatingExistingCase`**, signed in — **`canStageProofNoteInChat`**), **Recap → Proof / evidence** **Add a proof note** **stages** metadata locally in **`sessionStorage`** key **`justice_staged_proof_notes_v1`** (**`readStagedProofNotes`** / **`appendStagedProofNote`** / **`removeStagedProofNotesByClientIds`**) — **title**, **`evidence_type`**, optional **`evidence_date`**, optional **`description`** only; **no** **`POST /api/justice/evidence`** until flush; honest copy (**Staged on this device until you save your case in chat**); read-only **pending** staged count/list whenever signed in and staged rows exist (**`showStagedProofNotes`** — not only during **`canStageProofNoteInChat`**); mount always hydrates **`stagedProofNotes`** from **`readStagedProofNotes()`** even when loading **Update in chat**; honest copy (**pending upload — Continue to save to your case** on loaded case vs **on this device until you save your case in chat** on fresh intake); **no** staged edit/delete in this slice. On **Continue to submission preview** (**create** **`mode`**), **`commitIntakeToSessionAndServer`** returns **`{ caseId, serverPersisted }`** (**`CommitIntakeResult`**) so chat-ai confirms server case creation before flush; only when **`serverPersisted === true`** and **`caseId`** is a **UUID**, sequentially **`POST`s** staged rows via existing **`/api/justice/evidence`**; successfully flushed rows cleared from staging only after successful evidence **`POST`s**; failed/unflushed rows preserved with visible error. **Cross-mode flush retry:** when staged notes remain, **`readValidLocalJusticeIntake()`** exists, and **`STORAGE_CASE_ID`** is a **UUID** — regardless of **`isUpdatingExistingCase`** — **Continue** flushes to that existing **UUID** **without** another **`create`** commit (avoids duplicate server cases after partial flush failure or post-refresh **Update in chat**); normal fresh **create → flush → stay in chat** when signed-in **UUID** server persist succeeds; **`router.push("/justice/preview")`** fallback when continuity guards fail when no prior session case exists. **`clearLocalJusticeSession`** clears staged notes. **Update in chat** saved-evidence flow (**`GET`**, immediate **`POST`**, recent preview, edit/delete, keyword nudge) **unchanged**. **No** schema, new API, **`buildJusticeIntakeAiPrompt.ts`**, intake-chat, file upload/storage, filing, submission, contact, queue, or AI-inferred evidence rows; user must still stage/review — not auto-created from chat. **No** fresh-intake keyword nudge in this slice. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — active case context banner (Update in chat) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/copy** slice (with §2 **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — hydrate & update commit**, §2 **Saved cases — Needs more info chat routing CTA (`/justice/cases`)**, §2 **Justice hub — Needs more info chat routing CTA on Current case (`/justice`)**). When **`isUpdatingExistingCase`** (session intake hydrated on mount or after first signed-in **UUID** **create** **Continue**), shows compact **Active case** banner **after** page title/subtitle and **before** the main chat card so users know they are **continuing the same case**, not starting over. Uses **existing page/session data only** — **no** new API, schema, or prompt changes: headline **`parts.company_name.trim()`** or **Active case**; subline **category** label + truncated **`parts.purchase_or_signup`**; compact **Basic case info** / **Evidence** / **Submission draft reviewed** readiness checklist — §2 **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`)** (includes **Prepared case packet reviewed** when draft reviewed — §2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**); trimmed **focus** line from **`getPreviewBasicsMissing(parts)`** — **Still needed before preview: …** when preview basics incomplete; when escalation basics + loaded evidence are ready but draft not reviewed — **Review your submission draft before continuing.**; otherwise **Describe what to add or change, then save in chat.** Footer work links follow the current funnel — §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**. Reuses existing blue-bordered banner styling. **Does not** change **`handleSend`**, **`handleContinueToPreview`**, proof CRUD, staged proof logic, handoff panel, session-change summary, **`buildJusticeIntakeAiPrompt.ts`**, **`POST /api/justice/intake-chat`**, **`UPDATE_GREETING`**, hub, cases, plan, or **`caseReadiness.ts`**. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/navigation** slice (with §2 **Chat AI intake — active case context banner (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**, §2 **Justice hub — Current case compact readiness checklist (`/justice`)**, §2 **Justice hub — Current case prepared packet reviewed checklist row (`/justice`)**, §2 **Saved cases — compact readiness checklist on All cases cards (`/justice/cases`)**, §2 **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`)**, §2 **Plan — Case readiness checklist proof CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist basics CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**, §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**). When **`isUpdatingExistingCase`**, adds compact **`<ul>`** in the **Active case** banner after subline and **before** the trimmed focus line — mirrors hub/plan/cases readiness clarity on the chat work surface. Uses **existing** page/session data only (**no** new API, **no** schema): **`activeCaseBasicsReady`** from **`isBasicCaseInfoReadyForEscalation(buildJusticeIntakeFromParts(parts))`**; **`activeCaseEvidenceReady`** when **`showSavedEvidenceCount`** and **`(savedEvidenceCount ?? 0) >= 1`** (else **Evidence: loading…** until count loaded); **`activeCaseDraftReviewed`** from session **`readTimeline`** via page-local **`submissionDraftReviewedInTimeline`** (replaces prior standalone **Submission draft reviewed** / **Submission draft not reviewed yet** paragraph). Three rows: **Basic case info: yes/not yet** — inline **Continue in chat below** button focuses **`#chat-ai-input`** when not ready; **Evidence: yes/not yet** (or loading) — inline **Add proof below** opens existing **Proof / evidence** panel (**`setProofNoteDetailsOpen(true)`**, scroll to **`#chat-ai-proof-evidence-panel`**) when loaded and **`< 1`**; **Submission draft reviewed: yes/not yet** — inline **Review submission draft** → **`/justice/preview`** when not reviewed; fourth row when draft reviewed — §2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**. **`activeCaseChecklistLinkCls`** matches plan/hub/cases inline link styling. **Focus** line trimmed to avoid duplicating checklist status (preview-specific **Still needed before preview…** unchanged; draft-not-reviewed short line when basics + evidence ready). **Does not** change **`handleSend`**, **`handleContinueToPreview`**, proof CRUD, staged proof flush, handoff panel, session-change summary, hub, plan, cases, preview, or helper files. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/navigation** slice (with §2 **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — approved next action display hydrate (`/justice/chat-ai`)**, §2 **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`)**, §2 **Justice hub — Current case prepared packet reviewed checklist row (`/justice`)**, §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**, §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**, §2 **Plan — prepared packet approval surfaced (`/justice/plan`)**, §2 **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`)**). Adds page-local **`preparedPacketApproved`** state and **`readSessionPreparedPacketApproved(caseId)`** using existing **`justice_prepared_packet_approved_v1`** (**`STORAGE_PREPARED_PACKET_APPROVED_V1`** — mirrors preview/plan/packet). In the existing active-case load **`useEffect`** (same **`caseId`** + existing **`GET /api/justice/cases/[id]`** as approved-next hydrate — **no** new API): on session read sets **`preparedPacketApproved`** from **`readSessionPreparedPacketApproved`**; after successful **GET** sets **`true`** when session **or** **`parseJusticeCaseClientState(data.client_state).prepared_packet_approved === true`**; clears when not **`isUpdatingExistingCase`** or no **`caseId`**. In **Active case** compact checklist **`<ul>`**, **after** **Submission draft reviewed**, **only when** **`activeCaseDraftReviewed`**: **Prepared case packet reviewed: yes** when approved; **Prepared case packet reviewed: not yet** with inline **Review prepared case packet** → **`/justice/packet`** when not approved. **Navigation/readiness only** — closes chat-first gap between draft review and packet approval without leaving chat surface. **Does not** change proof CRUD, **Continue** handoff, **`handleSend`**, Recap **Current action tracking**, **Open** / **Record handled**, approved-next hydrate logic beyond reading **`prepared_packet_approved`** from the same **GET** payload, routes, APIs, schema, handlers, or shared libs. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, uploaded, stored, or contacted automatically.
- **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **visibility-only** slice (with §2 **Chat AI intake — active case context banner (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — active case banner outcome / follow-up read-only hints (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — approved next action display hydrate (`/justice/chat-ai`)**, §2 **Justice hub — approved next-step label and status on Current case (`/justice`)**, §2 **Saved cases — approved next-step label on All cases cards (`/justice/cases`)**, §2 **Approved next action shared state helpers**). When **`isUpdatingExistingCase`** and hydrated **`approvedNextAction`** exists, the **Active case** banner shows read-only lines **after** the readiness **`<ul>`** and **before** the trimmed focus line: **Next step: {label}** (bold **label**) when **`approvedNextAction.label?.trim()`**; **Approved next action: {Approved|Started|Handled}** via existing **`approvedNextActionStatusLabel(approvedNextAction.status)`** — **Hub/Cases wording** — same label pattern as **Recap → Current action tracking** after §2 **Chat AI intake — Recap current-action label alignment (`/justice/chat-ai`)**. Uses existing **`approvedNextAction`** state from **`hydrateApprovedNextActionForDisplay`** — **no** new API, **no** schema, **no** buttons. **Open {label}**, **Record action handled for now**, handling request, follow-up clear, proof CRUD, **Continue** handoff, **`handleApprovedNextActionOpen`**, **`handleMarkApprovedNextActionHandled`**, hydrate paths, routing, Plan, Packet, Hub, Cases unchanged. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Chat AI intake — active case banner outcome / follow-up read-only hints (Update in chat) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **read-only continuity display** slice (with §2 **Chat AI intake — active case context banner (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**, §2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)**, §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). When **`isUpdatingExistingCase`** and hydrated **`approvedNextAction`** exists, the **Active case** banner (only — **not** **Recap → Current action tracking**) shows compact read-only hints **after** **Next step** / **Approved next action** lines and **before** the trimmed focus line: truncated **`outcome_note`** (page-local **`truncateAttentionNote`**, max **200** — same cap as §2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)** and §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**); **Follow-up needed** when **`follow_up_needed === true`**; **`ApprovedNextActionFollowUpTimingLine`** when **`follow_up_at`** is set. Reuses existing **`approvedNextAction`** from **`hydrateApprovedNextActionForDisplay`** / server **`client_state.approved_next_action`** merge — **no** new **`GET`** or **`PATCH`**. **Read-only only** — **no** forms, buttons, or full outcome prose / **Record outcome / follow-up** form in the banner. Complements outcome/follow-up writes in §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)** and browse display on §2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)** / §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**. **Does not** change **Recap → Current action tracking**, packet, plan, hub, cases, **Needs attention** builders, handling triage, **Clear follow-up**, inbox rules, or routing. **Display correctness only** — **no** new API route, schema/migration, DB field, timeline **type**, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/navigation** slice (with §2 **Chat AI intake — active case context banner (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**). In the **Active case** banner footer link row, replaces fixed **Action plan** + **Submission preview** with funnel-aware primary work link from page-local **`resolveActiveCaseWorkHref`** / **`resolveActiveCaseWorkLabel`** using existing **`activeCaseDraftReviewed`** and **`preparedPacketApproved`** (**no** new API, **no** schema): when **`!activeCaseDraftReviewed`** — **Submission preview** → **`/justice/preview`**; when draft reviewed and **`!preparedPacketApproved`** — **Review prepared case packet** → **`/justice/packet`**; when **`preparedPacketApproved`** — **Action plan** → **`/justice/plan`**. Optional secondary link only when draft reviewed — **Action plan** → **`/justice/plan`** when primary is packet, **Submission preview** → **`/justice/preview`** when primary is plan; **omits** duplicate **Submission preview** when primary is already preview. **Recap → Current action tracking** footer reuses the same resolved targets — §2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**; page breadcrumb work link — §2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**. **Checklist** inline CTAs (**Continue in chat below**, **Add proof below**, **Review submission draft**, **Review prepared case packet**) **unchanged**. **Does not** change **`handleSend`**, **`handleContinueToPreview`**, handoff panel, Recap label/status/Open/Record handled, proof CRUD, routes beyond resolved targets, APIs, schema, filing, submission, contact, queue, upload, storage, or automatic-action behavior. **Navigation/readiness only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/navigation** slice (with §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — Recap current-action label alignment (`/justice/chat-ai`)**, §2 **Chat AI intake — approved next action display hydrate (`/justice/chat-ai`)**, §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**). In **Recap → Current action tracking** (**Update in chat** when **`approvedNextAction`** exists), replaces fixed footer **Action plan** + **Case packet** with the same funnel-aware targets already computed for the **Active case** banner — **`activeCaseWorkHref`** / **`activeCaseWorkLabel`** from **`resolveActiveCaseWorkHref`** / **`resolveActiveCaseWorkLabel`** (**`activeCaseDraftReviewed`** + **`preparedPacketApproved`** — **no** new helpers, **no** new API, **no** schema): when **`!activeCaseDraftReviewed`** — **Submission preview** → **`/justice/preview`**; when draft reviewed and **`!preparedPacketApproved`** — **Review prepared case packet** → **`/justice/packet`**; when **`preparedPacketApproved`** — **Action plan** → **`/justice/plan`**. Optional secondary via existing **`activeCaseSecondaryWorkLink`** only when non-null — **Action plan** when primary is packet, **Submission preview** when primary is plan; **omits** duplicate **Submission preview** when primary is already preview. Keeps existing emerald Recap footer link styling/classes. Page breadcrumb work link — §2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**. **Does not** change **`handleSend`**, **`handleContinueToPreview`**, handoff panel, proof CRUD, Active case checklist CTAs, **Open {label}**, **Record action handled for now**, handling UI, label/status lines, APIs, schema, filing, submission, contact, queue, upload, storage, or automatic-action behavior. **Navigation/readiness only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/navigation** slice (with §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — signed-in work-link funnel de-bias (Update in chat) (`/justice/chat-ai`)**, §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**, §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**). In the top breadcrumb row (**Home** · work link · **Step-by-step chat** · **Structured form**), replaces fixed **Action plan** → **`/justice/plan`** with funnel-aware **`activeCaseWorkLabel`** → **`activeCaseWorkHref`** when **`isUpdatingExistingCase`** — reuses existing **`resolveActiveCaseWorkHref`** / **`resolveActiveCaseWorkLabel`** from **`activeCaseDraftReviewed`** + **`preparedPacketApproved`** (**no** new helpers, **no** new API, **no** schema): when **`!activeCaseDraftReviewed`** — **Submission preview** → **`/justice/preview`**; when draft reviewed and **`!preparedPacketApproved`** — **Review prepared case packet** → **`/justice/packet`**; when **`preparedPacketApproved`** — **Action plan** → **`/justice/plan`**. **Fresh intake** keeps **Action plan** → **`/justice/plan`**. **No** secondary breadcrumb link. When **`chatFirstWorkLinkContinuity`** (**signed-in** + **`isUpdatingExistingCase`**) — §2 **Chat AI intake — signed-in work-link funnel de-bias (Update in chat) (`/justice/chat-ai`)** — breadcrumb uses **Justice workspace** → **`/justice`** instead. Closes outbound chat-first funnel continuity from **Packet** / **Evidence** / **Preview** **Update in chat** return paths. **Does not** change **`handleSend`**, **`handleContinueToPreview`**, Recap, Active case banner footers, proof CRUD, handling UI, session keys, routes, schema, filing, submission, contact, queue, upload, storage, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — signed-in work-link funnel de-bias (Update in chat) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/navigation** slice (with §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**). Page-local **`chatFirstWorkLinkContinuity`** (**`Boolean(isSignedIn) && isUpdatingExistingCase`**) when true: **hides** **Active case** banner footer funnel work-link row (**`activeCaseWorkHref`** / **`activeCaseSecondaryWorkLink`** → preview/packet/plan); **hides** **Recap → Current action tracking** footer funnel work-link row (same resolved targets); page breadcrumb middle link uses **Justice workspace** → **`/justice`** instead of funnel targets when **`chatFirstBreadcrumbContinuity`** — §2 **Chat AI intake — signed-in fresh intake breadcrumb chat-first (`/justice/chat-ai`)**. **`resolveActiveCaseWorkHref`** / **`resolveActiveCaseWorkLabel`** remain for unsigned **Update in chat** fallback only. **Checklist** deep links **unchanged**: **Review submission draft** → **`/justice/preview`**; **Review prepared case packet** → **`/justice/packet`**. Optional approved-action **Open {label} (optional)** deep links **unchanged**. **Does not** change **`handleContinueToPreview`**, stay-in-chat guards, **`handleSend`**, handoff panel, inline draft/packet approval, **Mark step opened** / **Record action handled**, proof CRUD, preview/packet/plan pages, APIs, schema, filing, submission, contact, queue, upload, storage, or automatic-action behavior. **Navigation/readiness only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — signed-in fresh intake breadcrumb chat-first (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/navigation** slice (with §2 **Chat AI intake — signed-in work-link funnel de-bias (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Saved cases — intro copy chat-first (`/justice/cases`)**). Page-local **`chatFirstBreadcrumbContinuity`** (**`Boolean(isSignedIn)`**) — **breadcrumb only**; **`chatFirstWorkLinkContinuity`** (**`Boolean(isSignedIn) && isUpdatingExistingCase`**) **unchanged**. When signed in (fresh intake or **Update in chat**), top breadcrumb middle link **Justice workspace** → **`/justice`**; signed-out fresh intake keeps **Action plan** → **`/justice/plan`**; signed-out **Update in chat** keeps funnel **`activeCaseWorkHref`** / **`activeCaseWorkLabel`** (§2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**). Closes signed-in fresh-intake plan-first breadcrumb exit on primary chat intake. **Does not** change banner/Recap work links, handoff panel, proof CRUD, **Continue** routing, Active case checklist, preview/packet/plan pages, APIs, schema, or filing/submission/contact/queue/upload/storage/automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — signed-out fresh intake breadcrumb chat-first (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/navigation** slice (with §2 **Chat AI intake — signed-in fresh intake breadcrumb chat-first (`/justice/chat-ai`)**, §2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**). Breadcrumb **`breadcrumbWorkHref`** / **`breadcrumbWorkLabel`**: **Justice workspace** → **`/justice`** when **`chatFirstBreadcrumbContinuity || !isUpdatingExistingCase`** — extends §2 **Chat AI intake — signed-in fresh intake breadcrumb chat-first (`/justice/chat-ai`)** to signed-out fresh-intake visitors; signed-out **Update in chat** keeps funnel **`activeCaseWorkHref`** / **`activeCaseWorkLabel`**. **`chatFirstBreadcrumbContinuity`**, **`chatFirstWorkLinkContinuity`**, **`resolveActiveCaseWorkHref`** / helpers, banner/Recap work links, **`handleSend`**, **`handleContinueToPreview`**, handoff panel, proof CRUD, checklist deep links, preview/packet/plan pages, APIs, schema, auth **unchanged** — **no** filing/submission/contact/queue/upload/storage/automatic-action claims.
- **Chat AI intake — resolveActiveCaseWorkHref post-packet-approved chat-first (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/navigation** slice (with §2 **Chat AI intake — signed-out fresh intake breadcrumb chat-first (`/justice/chat-ai`)**, §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Preview — session caseId Continue returns to chat (`/justice/preview`)**, §2 **Packet — session caseId approve returns to chat (`/justice/packet`)**). In page-local **`resolveActiveCaseWorkHref`** / **`resolveActiveCaseWorkLabel`**, post-packet-approved branch only: **`/justice/chat-ai`** / **Continue in chat** replaces **`/justice/plan`** / **Action plan** — **completes** signed-out **Update in chat** post-packet-approved funnel alignment with preview/packet session exits (#539–#540). Preview and packet branches **unchanged**. **`activeCaseSecondaryWorkLink`**, **`chatFirstWorkLinkContinuity`**, **`chatFirstBreadcrumbContinuity`**, breadcrumb hub logic, **`handleSend`**, **`handleContinueToPreview`**, handoff panel, proof CRUD, checklist deep links, preview/packet/plan pages, APIs, schema, auth **unchanged** — **no** filing/submission/contact/queue/upload/storage/automatic-action claims.
- **Chat AI intake — activeCaseSecondaryWorkLink mid-funnel plan de-bias (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/navigation** slice (with §2 **Chat AI intake — resolveActiveCaseWorkHref post-packet-approved chat-first (`/justice/chat-ai`)**, §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**). In page-local **`activeCaseSecondaryWorkLink`**, **`activeCaseDraftReviewed && !preparedPacketApproved`** branch only: **`null`** replaces secondary **Action plan** → **`/justice/plan`** — **removes** signed-out **Update in chat** mid-funnel plan skip-ahead before packet approval. Primary **Review prepared case packet** → **`/justice/packet`** unchanged; post-packet-approved secondary **Submission preview** → **`/justice/preview`** unchanged. **`resolveActiveCaseWorkHref`** / **`resolveActiveCaseWorkLabel`**, **`chatFirstWorkLinkContinuity`**, **`chatFirstBreadcrumbContinuity`**, breadcrumb hub logic, handoff panel, checklist deep links, preview/packet/plan pages, APIs, schema, auth **unchanged** — **no** filing/submission/contact/queue/upload/storage/automatic-action claims.
- **Chat AI intake — Recap current-action label alignment (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **copy/label-only** slice (with §2 **Chat AI intake — approved next action display hydrate (`/justice/chat-ai`)**, §2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`)**, §2 **Justice hub — approved next-step label and status on Current case (`/justice`)**, §2 **Saved cases — approved next-step label on All cases cards (`/justice/cases`)**, §2 **Saved cases — Needs attention approved-next-action label alignment (`/justice/cases`)**, §2 **Approved next action shared state helpers**). In **Recap → Current action tracking** (**Update in chat** when **`approvedNextAction`** exists), the status line uses **Approved next action: {Approved|Started|Handled}** via existing **`approvedNextActionStatusLabel(approvedNextAction.status)`** — **not** **Status:** — matching **Active case** banner, Hub, and **All cases** wording. **Next step: {label}** unchanged. Footer work links — §2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**. **Does not** change **Open {label}**, **Record action handled for now**, **`handleApprovedNextActionOpen`**, **`handleMarkApprovedNextActionHandled`**, hydrate, proof, **Continue** handoff, active-case banner checklist/focus, Plan, Packet, Hub, Cases, APIs, schema, or state handling. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Chat AI intake — session change summary (Update in chat) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** + **`src/lib/justice/summarizeBuildJusticeIntakePartsSessionChanges.ts`** — **client-only** slice (with §2 **Chat AI intake — hydrate & update commit**, §2 **Chat AI intake — active case context banner (Update in chat)**, §2 **Chat AI intake — saved evidence count in Recap (Proof / evidence panel)**, §2 **Chat AI intake — Continue handoff panel (What happens next)**, §2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)**, §2 **Preview — chat-update continuity card (Chat AI handoff)**). On existing-case hydrate, **`sessionBaselinePartsRef`** stores **`cloneBuildJusticeIntakeParts(justiceIntakeToBuildJusticeIntakeParts(intake))`** once in the mount **`useEffect`** — **not** reset after each chat message. **`sessionBaselineEvidenceCountRef`** captures the first successful **`loadSavedEvidencePreview`** count when **Update in chat** has a **UUID** case. **`useMemo`** calls **`summarizeBuildJusticeIntakePartsSessionChanges`** comparing baseline **`parts`** to current **`parts`**; when **`showSavedEvidenceCount`** and loaded count **increased** vs baseline, appends **Added proof note(s) this visit**. On successful **Save and continue in chat** (normal commit path — **not** staged-flush-retry), when **`isUpdatingExistingCase`** and **`sessionChangeLines.length > 0`**, **`writePreviewChatUpdateSummary`** in **`previewChatUpdateHandoff.ts`** stores lines in **`sessionStorage`** key **`justice_preview_chat_update_summary_v1`**; otherwise **`clearPreviewChatUpdateSummary`**. Shows compact **Updated in this chat** panel in **Recap** **only** when **`isUpdatingExistingCase`**, baseline exists, at least one change line, and **What happens next** is **not** yet visible (preview basics or contact proof still blocking — §2 **Chat AI intake — Continue handoff panel (What happens next)**); when the Continue handoff **is** visible, detected changes fold into the handoff instead (§2 **Chat AI intake — Continue handoff session-change integration (Update in chat)**) — **not** on **fresh intake** or when nothing changed. Bullets use clear labels such as **Company — updated**, **Website — updated**, **What happened — updated**, **Requested outcome — updated**, **Contact details — updated**, **Consumer state — updated**, plus proof line when applicable; short copy to review updates before saving in chat when the standalone panel shows. **Does not** change **`handleSend`**, **`handleContinueToPreview`**, **`commitIntakeToSessionAndServer`**, proof CRUD, staged proof logic, active-case banner, **`buildJusticeIntakeAiPrompt.ts`**, **`POST /api/justice/intake-chat`**, APIs, schema, timeline, hub, cases, or plan. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — Continue handoff session-change integration (Update in chat) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/copy** slice (with §2 **Chat AI intake — session change summary (Update in chat)**, §2 **Chat AI intake — Continue handoff panel (What happens next)**, §2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)**). When **What happens next** is visible and **`sessionChangeLines`** from **`summarizeBuildJusticeIntakePartsSessionChanges`** are non-empty, **`getContinueHandoffSteps`** receives **`sessionChangeLines`** and replaces generic **Save updates to your case.** with **Save your updates from this chat to your case:** plus each detected change as its own handoff step so users see what will save before **Save and continue in chat**; omits duplicate **Your N saved proof … stay on your case.** when handoff already includes **Added proof note(s) this visit**. Standalone blue **Updated in this chat** panel is **hidden** while handoff is visible (**`showSessionChangesPanel`** only when changes exist and handoff is not shown). **Does not** change **`handleSend`**, **`handleContinueToPreview`**, **`commitIntakeToSessionAndServer`**, proof CRUD, staged proof, active-case banner, session-change helper, **`buildJusticeIntakeAiPrompt.ts`**, APIs, schema, hub, cases, plan, or preview. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — Continue handoff panel (What happens next) (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/copy** slice above **Save and continue in chat** (with §2 **Chat AI intake — staged proof notes on fresh intake (Proof / evidence panel)**, §2 **AI chat intake — preview-readiness UX polish (`/justice/chat-ai`)**, §2 **Chat AI intake — active case context banner (Update in chat)**, §2 **Chat AI intake — session change summary (Update in chat)**, §2 **Chat AI intake — Continue handoff session-change integration (Update in chat)**, §2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)**). When **`getPreviewBasicsMissing`** is empty and **`validateContactProofForIntake`** passes, shows compact **What happens next** panel with short bullets from page-local **`getContinueHandoffSteps`** mirroring actual **`handleContinueToPreview`** branches: **fresh create** with no staged proof (**save case** → in-chat draft review → packet approval → current action tracking — §2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)**); **fresh create** with staged proof notes (**save case** → **save pending proof notes** → in-chat funnel); **staged proof flush retry** when staged notes + **`readValidLocalJusticeIntake()`** + **UUID** **`STORAGE_CASE_ID`** (**save pending proof notes** to existing case only — **no** duplicate **create**); **update mode** without staged retry and without session changes (**save updates** → in-chat funnel; when **`savedEvidenceCount > 0`**, notes saved proof items already on case); **update mode** with **`sessionChangeLines`** — **Save your updates from this chat to your case:** plus per-field change steps, then in-chat funnel (§2 **Chat AI intake — Continue handoff session-change integration (Update in chat)**). Copy explains **saving case/updates**, **pending proof notes** where applicable, and continuing **in chat** — **does not** claim filing, submission, contact, queueing, upload, or storage. Replaces separate staged-proof one-liner above **Continue**. **Save and continue in chat** disabled when **`submitting`**, **`loading`**, or required preview basics missing; existing amber **Still needed before preview** and contact-proof warning (amber one-line from **`validateContactProofForIntake`**) unchanged; contact-proof click validation unchanged. **No** schema, API, **`buildJusticeIntakeAiPrompt.ts`**, **`/justice/preview`**, **`/justice/plan`**, or **`handleContinueToPreview`** logic changes on this slice. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/copy** slice (with §2 **Chat AI intake — Continue handoff panel (What happens next)**, §2 **Chat AI intake — Continue handoff session-change integration (Update in chat)**, §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — inline submission draft review in Active case checklist (`/justice/chat-ai`)**, §2 **Chat AI intake — inline prepared packet approval in Active case checklist (`/justice/chat-ai`)**). When **`chatFirstContinuity`** (**signed-in** on **`/justice/chat-ai`**) is true, **`getContinueHandoffSteps`** uses in-chat funnel bullets — **Review your submission draft in the Active case checklist below**, **Approve your prepared packet in chat when ready**, **Continue next steps in Current action tracking below** — instead of **`previewStep`** / **`postPreviewFunnelStep`** preview → packet → plan wording. **Save and continue in chat** button label; **Active case** focus line and page subtitle say **save in chat** (not **continue to preview**); standalone **Updated in this chat** panel says **save in chat when ready**; **Review prepared case packet** checklist link hidden while inline packet approval block is shown (mirrors draft review). Unsigned / failed-persist fallback still uses preview-oriented **`previewStep`** / **`postPreviewFunnelStep`** when **`chatFirstContinuity`** is false. **Does not** change **`handleContinueToPreview`**, stay-in-chat guards, handoff visibility gates, proof/staged-proof logic, optional work links to preview/packet/plan, routes, **`commitIntakeToSessionAndServer`**, APIs, schema, or hub/cases open-case routing. **Copy/readiness only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Chat AI intake — proof panel continuation copy chat-first (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/copy** slice (with §2 **Chat AI intake — proof/evidence guidance (`/justice/chat-ai`)**, §2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)**, §2 **Chat AI intake — inline submission draft review in Active case checklist (`/justice/chat-ai`)**, §2 **Evidence — breadcrumb primary link chat-first (`/justice/evidence`)**). In **Recap → Proof / evidence**, replaces advisory paragraph **You can continue to your submission preview without proof for now.** with **You can continue in chat without proof for now. Review your submission draft in the Active case checklist when ready.** — aligns proof-panel continuation copy with signed-in handoff bullets and inline draft review (§2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)**); **does not** nudge users to **`/justice/preview`** as the primary next step from the proof panel. Escalation / proof-note encouragement sentence **unchanged**. **Does not** change proof CRUD, **Organize evidence** link, **Add a proof note** form, handoff panel, Active case checklist deep links, preview/packet/plan pages, APIs, schema, or filing/submission/contact/queue/upload/storage/automatic-action behavior. **Copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Preview — proof on your case card (Chat AI handoff continuity) (`/justice/preview`):** **`src/app/justice/preview/page.tsx`** only — **UI/copy** slice continuing the **Chat AI** proof flow after **Continue to submission preview** (with §2 **Chat AI intake — Continue handoff panel (What happens next)**, §2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)**, §2 **Chat AI intake — staged proof notes on fresh intake (Proof / evidence panel)**, §2 **Preview — approved-next read-only continuity (`/justice/preview`)**, §2 **Preview — approved-next session hydrate wiring (`/justice/preview`)**, §2 **Preview — chat-update continuity card (Chat AI handoff)**, §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**, §2 **Preview — pre-plan chat correction prompt (`/justice/preview`)**). Uses existing **`loadEvidence`** / **`GET /api/justice/evidence?case_id={id}`** state (**no** new API, **no** schema). **Intro copy** clarifies chat intake/case is saved, this page is for reviewing the draft, saved proof note **titles** are included when on the case, and **nothing is filed automatically**. Compact **Proof on your case** card after **Related action** and before **Deterministic draft**: **Loading saved proof notes...**; error state shows existing evidence load failure inside the card (**draft still shown**); when rows exist — count, metadata-only wording (**not file uploads**; titles in draft below), up to **3** recent rows (**title**, **`evidence_type`** label via **`JUSTICE_EVIDENCE_TYPE_LABELS`**, optional **`evidence_date`**); when empty after load — **No saved proof notes on this case yet.** + **Add proof in chat →** **`/justice/chat-ai`**. Replaces standalone evidence loading/error lines above the draft. **Does not** claim upload, storage, submission, contact, queueing, or filing. **No** **`buildSubmissionDraftPreview.ts`**, draft generation, review milestone, AI draft, continue-to-plan, intake-chat, or prompt changes on this slice. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Preview — chat-update continuity card (Chat AI handoff) (`/justice/preview`):** **`src/app/justice/preview/page.tsx`** + **`src/lib/justice/previewChatUpdateHandoff.ts`** — **client-only** slice (with §2 **Chat AI intake — session change summary (Update in chat)**, §2 **Chat AI intake — Continue handoff session-change integration (Update in chat)**, §2 **Preview — approved-next read-only continuity (`/justice/preview`)**, §2 **Preview — approved-next session hydrate wiring (`/justice/preview`)**, §2 **Preview — proof on your case card (Chat AI handoff continuity)**, §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**, §2 **Preview — pre-plan chat correction prompt (`/justice/preview`)**). When **Update in chat** **Continue** succeeds with detected **`sessionChangeLines`**, chat-ai writes **`{ lines }`** to short-lived **`sessionStorage`** **`justice_preview_chat_update_summary_v1`** via **`writePreviewChatUpdateSummary`**; otherwise **`clearPreviewChatUpdateSummary`**. After preview intake hydration is **ready**, **`readAndClearPreviewChatUpdateSummary`** loads valid lines and **immediately removes** the key (one-time card). Compact **From your chat update** card after intro paragraph and **before** **Related action** / **Proof on your case** when **`lines.length > 0`**: optional **`intake.company_name`** headline; bullet list of same session-change labels carried from Chat AI into saved case and draft review; footer **The draft below reflects your saved case. Nothing is filed automatically.**; **Update in chat** → **`/justice/chat-ai`**. **Not** shown for **fresh intake**, plan → preview, or update sessions with no detected changes. **Does not** change **`buildSubmissionDraftPreview`**, review milestone logic, evidence **`GET`**, AI draft behavior, continue-to-plan, commit, proof CRUD, staged proof, hub, cases, plan, APIs, schema, or prompt builders. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Preview — pre-plan chat correction prompt (`/justice/preview`):** **`src/app/justice/preview/page.tsx`** only — **UI/copy** slice (with §2 **Preview — approved-next read-only continuity (`/justice/preview`)**, §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**, §2 **Preview — chat-update continuity card (Chat AI handoff)**, §2 **Plan — post-draft chat continuity prompt after preview review (`/justice/plan`)**, §2 **Plan — post-draft re-preview continuity prompt after preview review (`/justice/plan`)**). In the bottom **review / Continue** card (when valid intake is loaded and main preview body is visible), after the **I reviewed…** checkbox and **before** the primary **Continue** control: chat-first correction prompt — **Need to fix something in the draft?** **Update in chat** → **`/justice/chat-ai`**; subline **Describe changes in chat, then return here to review your updated draft. Nothing is filed automatically.** Always shown on loaded preview (not gated on chat-update handoff). Primary button label and post-save navigation — §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**. Bookends plan post-draft lines (§2 **Plan — post-draft chat continuity prompt after preview review (`/justice/plan`)**, §2 **Plan — post-draft re-preview continuity prompt after preview review (`/justice/plan`)**) — fix in chat *before* marking draft reviewed vs fix *after* action plan shown vs re-check draft on preview after chat updates; read-only approved-next context on re-preview — §2 **Preview — approved-next read-only continuity (`/justice/preview`)**. **Does not** change **`submission-draft-reviewed`** / review milestone logic, draft-reviewed checkbox, **`buildSubmissionDraftPreview`**, evidence **`GET`**, AI draft behavior, chat-update handoff card, proof card, approved-next panel, APIs, schema, helpers, hub, cases, plan, or **`/justice/chat-ai`**. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`):** **`src/app/justice/preview/page.tsx`** only — **UI/navigation** slice (with §2 **Preview — pre-plan chat correction prompt (`/justice/preview`)**, §2 **Preview — signed-in Continue returns to chat (`/justice/preview`)**, §2 **Preview — approved-next read-only continuity (`/justice/preview`)**, §2 **Preview — approved-next session hydrate wiring (`/justice/preview`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Plan — prepared next action after draft review (`/justice/plan`)**, §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**). In **`handleContinueToPlan`**, after successful **`submission_draft_reviewed`** persistence (**`POST /api/justice/submission-draft-reviewed`** for owned **UUID** cases or **`appendSubmissionDraftReviewedOnce`**), **`router.push`** when **signed-in** and **`STORAGE_CASE_ID`** is a **UUID** — **`/justice/chat-ai`** (§2 **Preview — signed-in Continue returns to chat (`/justice/preview`)**); otherwise uses existing **`preparedPacketApproved`** (session **`justice_prepared_packet_approved_v1`** via §2 **Preview — approved-next session hydrate wiring (`/justice/preview`)** — **no** new API, **no** schema): **`/justice/plan`** when **`preparedPacketApproved`**; **`/justice/packet`** when not. Bottom review-card button label unchanged: **Continue to action plan** when packet approved; **Review prepared case packet** when not. **Does not** change proof card, chat-update card, approved-next panel, review checkbox, **`submission-draft-reviewed`** API contract, draft generation, evidence **`loadEvidence`**, checklist deep links, breadcrumb links, Plan, Packet, Chat AI, Cases, Hub handlers, or shared libs. **Navigation/readiness only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Preview — signed-in Continue returns to chat (`/justice/preview`):** **`src/app/justice/preview/page.tsx`** only — **UI/navigation** slice (with §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Chat AI intake — inline submission draft review in Active case checklist (`/justice/chat-ai`)**, §2 **Chat AI intake — inline prepared packet approval in Active case checklist (`/justice/chat-ai`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**). In **`handleContinueToPlan`**, after successful **`submission_draft_reviewed`** persistence, when **`isSignedIn`** and **`STORAGE_CASE_ID`** is a **UUID**, **`router.push("/justice/chat-ai")`** instead of packet/plan funnel — closes chat-first checklist loop (**Review submission draft** deep link → preview → **Continue** → back to chat for inline packet approval and Current action tracking). **Unsigned** / **non-UUID** / local-only cases keep **`preparedPacketApproved ? "/justice/plan" : "/justice/packet"`** fallback (§2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**). **Does not** change preview UI, draft generation, review checkbox, **`submission-draft-reviewed`** API, proof/chat-update/approved-next panels, checklist links on chat/hub/cases, packet/plan pages, or **`handleContinueToPreview`** stay-in-chat guards. **Navigation only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Preview — signed-in Continue button label chat-first (`/justice/preview`):** **`src/app/justice/preview/page.tsx`** only — **UI/copy** slice (with §2 **Preview — signed-in Continue returns to chat (`/justice/preview`)**, §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Chat AI intake — inline submission draft review in Active case checklist (`/justice/chat-ai`)**). Page-local **`signedInContinueToChat`** (**`isSignedIn && Boolean(caseId) && isUuid(caseId)`**) — render-time only; bottom **Continue** button label **Continue in chat** when **`signedInContinueToChat`**, else **Review prepared case packet** when **`!preparedPacketApproved`** or **Continue to action plan** when approved — matches existing **`handleContinueToPlan`** routing (§2 **Preview — signed-in Continue returns to chat (`/justice/preview`)**) without changing **`router.push`**. **Does not** change **`handleContinueToPlan`**, breadcrumb, review checkbox, draft body, proof/chat-update panels, approved-next panel, unsigned fallback routing, checklist deep links, APIs, schema, or filing/submission/contact/queue/upload/storage/automatic-action behavior. **Copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Preview — signed-in non-UUID Continue returns to chat (`/justice/preview`):** **`src/app/justice/preview/page.tsx`** only — **UI/navigation** slice (with §2 **Preview — signed-in Continue returns to chat (`/justice/preview`)**, §2 **Preview — signed-in Continue button label chat-first (`/justice/preview`)**, §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**, §2 **Chat AI intake — hydrate & update commit**). In **`handleContinueToPlan`**, after successful **`submission_draft_reviewed`** persistence, **`router.push("/justice/chat-ai")`** when **`isSignedIn && cid`** (any **`STORAGE_CASE_ID`**, not only **UUID**) — **completes** signed-in preview Continue chat-first routing; removes last programmatic **`/justice/plan`** redirect for signed-in users. **UUID** cases keep existing **`POST /api/justice/submission-draft-reviewed`** path; **non-UUID** cases keep **`appendSubmissionDraftReviewedOnce`** session append — persistence branches **unchanged**. **`signedInContinueToChat`** (**`isSignedIn && Boolean(caseId)`**) — **Continue in chat** label for any signed-in case (§2 **Preview — signed-in Continue button label chat-first (`/justice/preview`)** alignment). **Unsigned** / no **`caseId`** keeps **`preparedPacketApproved ? "/justice/plan" : "/justice/packet"`** fallback. **Does not** change review checkbox, breadcrumb, proof/chat-update/approved-next panels, pre-plan correction prompt, **`submission-draft-reviewed`** API contract, draft generation, evidence loading, packet/plan pages, APIs, schema, or auth. **Navigation only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Preview — session caseId Continue returns to chat (`/justice/preview`):** **`src/app/justice/preview/page.tsx`** only — **UI/navigation** slice (with §2 **Preview — signed-in non-UUID Continue returns to chat (`/justice/preview`)**, §2 **Preview — signed-in Continue button label chat-first (`/justice/preview`)**, §2 **Chat AI intake — hydrate & update commit**). In **`handleContinueToPlan`**, after successful **`submission_draft_reviewed`** persistence, **`router.push("/justice/chat-ai")`** when **`cid`** (any session **`STORAGE_CASE_ID`**, signed-in or unsigned) — **completes** preview Continue chat-first routing for all session-backed cases; removes last programmatic **`/justice/plan`** redirect when **`STORAGE_CASE_ID`** is present (extends §2 **Preview — signed-in non-UUID Continue returns to chat (`/justice/preview`)**). **UUID** server **`POST`** and **non-UUID** session append **unchanged**. Page-local **`continueToChat`** (**`Boolean(caseId)`**) — **Continue in chat** label for any session-backed case. **No** **`caseId`** keeps **`preparedPacketApproved ? "/justice/plan" : "/justice/packet"`** fallback. **Does not** change review checkbox, breadcrumb, proof/chat-update/approved-next panels, pre-plan correction prompt, **`submission-draft-reviewed`** API contract, draft generation, evidence loading, packet/plan pages, auth, APIs, or schema. **Navigation only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Preview — no-caseId Continue plan branch removal (`/justice/preview`):** **`src/app/justice/preview/page.tsx`** only — **UI/navigation** slice (with §2 **Preview — session caseId Continue returns to chat (`/justice/preview`)**, §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**). In **`handleContinueToPlan`**, when **`!cid`**, **`router.push("/justice/packet")`** only — removes dead **`preparedPacketApproved ? "/justice/plan" : "/justice/packet"`** ternary (**completes** removal of last consumer **`router.push("/justice/plan")`**). Bottom **Continue** when **`!continueToChat`**: **Review prepared case packet** only — removes dead **`Continue to action plan`** label branch. **`if (cid)`** → **`/justice/chat-ai`** and **Continue in chat** when **`continueToChat`** **unchanged**. **`submission_draft_reviewed`** persistence, UUID **`POST`** / session append, breadcrumb, proof/chat-update panels, pre-plan correction prompt, **`preparedPacketApproved`** hydrate, packet/plan pages, APIs, schema, auth **unchanged** — **no** filing/submission/contact/queue/upload/storage/automatic-action claims.
- **Preview — breadcrumb primary link chat-first (`/justice/preview`):** **`src/app/justice/preview/page.tsx`** only — **UI/navigation** slice (with §2 **Preview — signed-in Continue returns to chat (`/justice/preview`)**, §2 **Preview — pre-plan chat correction prompt (`/justice/preview`)**, §2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Justice-wide navigation alignment toward AI chat intake**). Top breadcrumb row when intake loaded: **Update in chat** → **`/justice/chat-ai`** (first link); **Justice workspace** → **`/justice`** (second); **Step-by-step chat** → **`/justice/chat`** and **Structured form** → **`/justice/intake`** **unchanged**. Replaces prior **Back to action plan** → **`/justice/plan`** first link + duplicate **Update in chat** second slot — pairs with §2 **Preview — signed-in Continue returns to chat (`/justice/preview`)** so checklist **Review submission draft** → preview exit returns to chat, not plan. **Does not** change draft generation, review checkbox, **Continue** routing, pre-plan correction prompt, proof/chat-update/approved-next panels, empty-state CTAs, APIs, schema, or checklist deep links — **no** filing/submission/contact/queue/upload/storage/automatic-action claims.
- **Preview — approved-next read-only continuity (`/justice/preview`):** **`src/app/justice/preview/page.tsx`** only — **visibility-only** slice (with §2 **Preview — approved-next session hydrate wiring (`/justice/preview`)**, §2 **Preview — chat-update continuity card (Chat AI handoff)**, §2 **Preview — proof on your case card (Chat AI handoff continuity)**, §2 **Plan — Case readiness approved-next visibility (`/justice/plan`)**, §2 **Justice hub — approved next-step label and status on Current case (`/justice`)**, §2 **Approved next action shared state helpers**). Session **`preparedPacketApproved`** / **`approvedNextAction`** state is set by §2 **Preview — approved-next session hydrate wiring (`/justice/preview`)** — **not** inline in render. When **`preparedPacketApproved && approvedNextAction`** and at least one of **label** or **status** is present, shows compact read-only panel **after** optional **From your chat update** card and **before** **Related action** / **Proof on your case**: **Next step: {label}** (bold **label**) when **`approvedNextAction.label?.trim()`**; **Approved next action: {Approved|Started|Handled}** via **`approvedNextActionStatusLabel(approvedNextAction.status)`** — same wording/style as Hub, Plan **Case readiness**, and Cases **All cases**. **No** buttons, **no** links, **no** **Open** action, **no** **Record handled** action on this panel. **Does not** change **`buildSubmissionDraftPreview`**, review checkbox, **`handleContinueToPlan`** routing (§2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**), **`appendSubmissionDraftReviewedOnce`**, proof card **`loadEvidence`**, AI draft, chat-update handoff, breadcrumb links, **`useJusticeActionPageHydration`**, routes, Plan, Packet, Chat AI, Cases, Hub, APIs, schema, or state handling. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Preview — approved-next session hydrate wiring (`/justice/preview`):** **`src/app/justice/preview/page.tsx`** only — **wiring-only** slice (with §2 **Preview — approved-next read-only continuity (`/justice/preview`)**, §2 **Approved next action shared state helpers**). Adds one **`useEffect`** on **`hydrationStatus === "ready"`** and **`caseId`** from **`STORAGE_CASE_ID`**: **`setPreparedPacketApproved(readSessionPreparedPacketApproved(caseId))`** (**`justice_prepared_packet_approved_v1`** — mirrors plan/packet) and **`setApprovedNextAction(hydrateApprovedNextActionForDisplay(caseId))`** (session **`justice_approved_next_action_v1`** via §2 **Approved next action shared state helpers**). When preview is **not** ready or **`caseId`** is absent, clears **`preparedPacketApproved`** (**`false`**) and **`approvedNextAction`** (**`undefined`**). Re-runs when **`caseId`** updates from existing session sync. Makes the existing read-only approved-next panel (§2 **Preview — approved-next read-only continuity (`/justice/preview`)**) appear when the active case has a prepared packet approved and an approved next action; **`preparedPacketApproved`** also drives Continue routing (§2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**). **Does not** change panel JSX, proof card, chat-update card, **`buildSubmissionDraftPreview`**, review checkbox, **`handleContinueToPlan`** navigation branches beyond existing session read, **`appendSubmissionDraftReviewedOnce`**, evidence **`loadEvidence`**, AI draft, breadcrumb links, **`useJusticeActionPageHydration`**, routes, Plan, Packet, Chat AI, Cases, Hub, APIs, schema, or handlers. **No** signed-in **`GET /api/justice/cases/[id]`** on this slice. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Plan — proof readiness continuity after preview review (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **UI/copy** slice continuing the **Chat AI → preview → plan** proof flow after **`submission_draft_reviewed`** (with §2 **Preview — proof on your case card (Chat AI handoff continuity)**, §2 **Plan — prepared next action after draft review (`/justice/plan`)**, §2 **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`)**, §2 **Plan — post-draft chat continuity prompt after preview review (`/justice/plan`)**, §2 **Plan — post-draft re-preview continuity prompt after preview review (`/justice/plan`)**, §2 **Plan — Case readiness checklist proof CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist basics CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**). Uses existing readiness fetch **`evidenceCount`** / **`evidenceRowsForPlan`** from **`GET /api/justice/evidence?case_id={id}`** (**no** new API, **no** schema). In **`showPostDraftReviewCallout`** (emerald **Surrenderless prepared your next step** card), adds one short proof line when signed in: **Loading saved proof notes...** while **`readinessLoading`**; when **`evidenceCount > 0`** — **Saved proof notes on your case: {N}. Metadata only — titles were included in your reviewed draft.**; when **`evidenceCount === 0`** — **No saved proof notes on this case yet. You can add short notes in chat before you escalate.** + **Add proof in chat** → **`/justice/chat-ai`**. **Evidence / proof** card empty state (signed in, not loading, no rows): **No saved proof notes on this case yet.** + **Add proof in chat →** **`/justice/chat-ai`**; existing **Add evidence** → **`/justice/evidence`** **unchanged**; collapsed saved-evidence list when **N > 0** **unchanged**. **Does not** change **`evidenceReady`**, **`readyToEscalate`**, post-draft gates, packet CTAs, handling/approval UI, fetch shape, or lib helpers. **Does not** claim upload, storage, submission, contact, queueing, or filing. **No** **`/justice/chat-ai`**, **`/justice/preview`**, schema, API, or prompt changes on this slice. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Plan — Case readiness checklist proof CTA (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **UI/navigation** slice (with §2 **Plan — proof readiness continuity after preview review (`/justice/plan`)**, §2 **Plan — Case readiness checklist basics CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**, §2 **Saved cases — Needs more info chat routing CTA (`/justice/cases`)**, §2 **Saved cases — compact readiness checklist on All cases cards (`/justice/cases`)**, §2 **Justice hub — Needs more info chat routing CTA on Current case (`/justice`)**). In the **Case readiness** checklist **Evidence added** row, keeps existing icon and **Evidence added: …** text; when signed in (**`isSignedIn`**), readiness load finished (**`!readinessLoading`**), and **`evidenceCount < 1`**, appends inline **Add proof in chat** → **`/justice/chat-ai`** (same inline link classes as §2 **Plan — Case readiness checklist basics CTA (`/justice/plan`)**). Closes the gap where users see **Needs more info** on the checklist but had to scroll to post-draft callout or **Evidence / proof** for a chat path when proof is missing. **Does not** change **`evidenceReady`**, **`readyToEscalate`**, **`basicsReady`**, readiness fetch logic, packet CTAs, post-draft callout copy, evidence-row behavior, breadcrumb **Update in chat**, handling/approval UI, or lib helpers. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** API, schema, helper, **`/justice/chat-ai`**, **`/justice/preview`**, hub, cases, or prompt changes on this slice. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Plan — Case readiness checklist basics CTA (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **UI/navigation** slice (with §2 **Plan — Case readiness checklist proof CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**, §2 **Saved cases — Needs more info chat routing CTA (`/justice/cases`)**, §2 **Justice hub — Needs more info chat routing CTA on Current case (`/justice`)**). In the **Case readiness** checklist **Basic case info present** row, keeps existing icon and label text; when signed in (**`isSignedIn`**), readiness load finished (**`!readinessLoading`**), and **`!basicsReady`** (same **`isBasicCaseInfoReadyForEscalation`** predicate as checklist — **does not** change **`basicsReady`** computation), appends inline **Update in chat** → **`/justice/chat-ai`** (mirrors §2 **Plan — Case readiness checklist proof CTA (`/justice/plan`)** inline link styling). Closes the gap where users see **Needs more info** because basics are incomplete but only had breadcrumb **Update in chat** at page top. **Does not** change **`basicsReady`**, **`readyToEscalate`**, **`evidenceReady`**, readiness fetch logic, packet CTAs, post-draft callout copy, evidence-row behavior, draft-review checklist row, or breadcrumb **Update in chat**. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** API, schema, helper, **`/justice/chat-ai`**, **`/justice/preview`**, hub, cases, or prompt changes on this slice. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Plan — Case readiness checklist draft-review CTA (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **UI/navigation** slice (with §2 **Plan — Case readiness checklist proof CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist basics CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**, §2 **Plan — Case readiness approved-next visibility (`/justice/plan`)**, §2 **Plan — post-draft re-preview continuity prompt after preview review (`/justice/plan`)**). In the **Case readiness** checklist, adds **Submission draft reviewed** row after basics/proof rows: **`draftReviewed`** from existing session **`timeline`** via **`timeline.some((e) => e.type === "submission_draft_reviewed")`** (**no** new API, **no** schema, **no** new timeline writes); **✓** / **○** icon; **Submission draft reviewed: yes** / **not yet**; when signed in (**`isSignedIn`**), readiness load finished (**`!readinessLoading`**), and **`!draftReviewed`**, appends inline **Review submission draft** → **`/justice/preview`** (same inline link classes as other checklist CTAs). When reviewed, **✓** only — **no** duplicate **Review again** link (post-draft callout §2 **Plan — post-draft re-preview continuity prompt after preview review (`/justice/plan`)** covers re-check after chat updates). Closes draft-review state gap on plan before escalation. Prepared packet reviewed row when draft reviewed — §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**. Post-approval approved-next read-only rows — §2 **Plan — Case readiness approved-next visibility (`/justice/plan`)**. **Does not** change **`basicsReady`**, **`evidenceReady`**, **`readyToEscalate`**, readiness fetch logic, packet CTAs, emerald **`showPostDraftReviewCallout`** copy, basics/evidence rows, breadcrumb, preview implementation, or **`showPostDraftReviewCallout`** gating. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** API, schema, helper, **`/justice/chat-ai`**, hub, cases, or prompt changes on this slice. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **UI/navigation** slice (with §2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**, §2 **Plan — Case readiness approved-next visibility (`/justice/plan`)**, §2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**, §2 **Justice hub — Current case prepared packet reviewed checklist row (`/justice`)**, §2 **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`)**, §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**, §2 **Plan — prepared packet approval surfaced (`/justice/plan`)**). In **Case readiness** **`<ul>`**, **after** **Submission draft reviewed** and **before** approved-next / **Filing recorded** rows, **only when** **`draftReviewed`**: **Prepared case packet reviewed: yes** when existing **`preparedPacketApproved`** (session **`justice_prepared_packet_approved_v1`** + case **`GET`** **`client_state.prepared_packet_approved`** via existing **`resolvePreparedPacketApproved`** — **no** new API, **no** schema); **Prepared case packet reviewed: not yet** when not approved; when signed in (**`isSignedIn`**), readiness load finished (**`!readinessLoading`**), and **`!preparedPacketApproved`**, inline **Review prepared case packet** → **`/justice/packet`** (same inline link classes as §2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**). **Navigation/readiness only** — closes plan checklist gap between draft review and packet approval (parity with chat-ai/hub/cases). **`{preparedPacketApproved && approvedNextAction ? (...)}`** approved-next block **unchanged**. **Does not** change **`readyToEscalate`**, **`basicsReady`**, **`evidenceReady`**, emerald **`showPostDraftReviewCallout`** copy, **`pickPreparedNextAction`**, post-draft CTAs, handlers, routing, APIs, schema, hub, cases, preview, or chat-ai. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Plan — Case readiness approved-next visibility (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **visibility-only** slice (with §2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**, §2 **Plan — post-draft approved step label alignment (`/justice/plan`)**, §2 **Preview — approved-next read-only continuity (`/justice/preview`)**, §2 **Preview — approved-next session hydrate wiring (`/justice/preview`)**, §2 **Saved cases — approved next-step label on All cases cards (`/justice/cases`)**, §2 **Justice hub — approved next-step label and status on Current case (`/justice`)**, §2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**, §2 **Approved next action shared state helpers**). In **Case readiness** **`<ul>`**, **after** **Prepared case packet reviewed** (§2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)** — when **`draftReviewed`**) and **before** **Filing recorded**, when **`preparedPacketApproved && approvedNextAction`**, adds read-only informational rows (neutral **•** bullet — **not** escalate gates): **Next step: {label}** (bold **label**) when **`approvedNextAction.label?.trim()`**; **Approved next action: {Approved|Started|Handled}** via **`approvedNextActionStatusLabel(approvedNextAction.status)`** when set — same label wording/style as Hub and **`CaseApprovedNextActionTracking`** on **All cases**. Uses existing **`approvedNextAction`** from **`hydrateApprovedNextActionForDisplay`** — **no** links or buttons on these rows. **Does not** change emerald **`showPostDraftReviewCallout`**, **Open {label}**, **Record action handled for now**, **`showApprovedNextActionCta`**, **`handleApprovedNextActionOpen`**, **`handleMarkApprovedNextActionHandled`**, handling request/ack, hydrate, routing, **`readyToEscalate`** / **`basicsReady`** / **`evidenceReady`**, APIs, schema, Chat AI, Cases, Hub, Packet, Preview, or state handling. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Plan — post-draft chat continuity prompt after preview review (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **UI/copy** slice (with §2 **Plan — proof readiness continuity after preview review (`/justice/plan`)**, §2 **Plan — prepared next action after draft review (`/justice/plan`)**, §2 **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`)**, §2 **Plan — post-draft re-preview continuity prompt after preview review (`/justice/plan`)**). In **`showPostDraftReviewCallout`** (emerald **Surrenderless prepared your next step** card), after the next-step explainer / readiness bridge and signed-in proof-readiness lines and **before** the primary packet/prep CTA, adds one short chat-first line: **Need to fix details or add more proof notes?** **Update in chat** → **`/justice/chat-ai`** — **Surrenderless keeps your case aligned with your draft and plan.** Shown whenever the post-draft callout is visible (including when proof notes already exist — not only empty-proof cases). Keeps the user inside the **chat-first** workflow after the action plan is shown while packet/prep remains the primary in-app next step. Reuses existing emerald callout link styling from the proof line. **Does not** change **`evidenceReady`**, **`readyToEscalate`**, **`recommendationText`**, **`pickPreparedNextAction`**, post-draft gates, packet approval, handling/approval UI, readiness fetch, evidence/proof card logic, or lib helpers. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** **`/justice/chat-ai`**, **`/justice/preview`**, **`/justice/packet`**, schema, API, or prompt changes on this slice. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Plan — post-draft re-preview continuity prompt after preview review (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **UI/copy** slice (with §2 **Plan — post-draft chat continuity prompt after preview review (`/justice/plan`)**, §2 **Preview — pre-plan chat correction prompt (`/justice/preview`)**, §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**, §2 **Preview — approved-next read-only continuity (`/justice/preview`)**, §2 **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`)**). In **`showPostDraftReviewCallout`** (same emerald card), **after** the **Update in chat** line and **before** the primary packet/prep CTA: **Updated your case in chat?** **Review submission draft again** → **`/justice/preview`**; subline **Confirm the draft matches your saved case before you escalate. Nothing is filed automatically.** Helps users who updated the case in chat after reviewing the draft re-check the submission draft before escalating; **Continue** from preview may route to **`/justice/packet`** or **`/justice/plan`** per §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**. Shown whenever the post-draft callout is visible (same gate as proof-readiness and **Update in chat** lines — **not** a separate gate). **Does not** change **`showPostDraftReviewCallout`** gating, **`evidenceReady`**, **`readyToEscalate`**, **`recommendationText`**, **`pickPreparedNextAction`**, packet CTAs, handling/approval UI, readiness fetch, evidence/proof card logic, preview **Continue** behavior, timeline writes, APIs, schema, helpers, hub, cases, **`/justice/chat-ai`**, or **`/justice/preview`** implementation beyond the in-app link target. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **UI/copy** slice (with §2 **Plan — prepared next action after draft review (`/justice/plan`)**, §2 **Plan — proof readiness continuity after preview review (`/justice/plan`)**, §2 **Plan — post-draft chat continuity prompt after preview review (`/justice/plan`)**, §2 **Plan — post-draft re-preview continuity prompt after preview review (`/justice/plan`)**, §2 **Plan — post-draft approved step label alignment (`/justice/plan`)**, §2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**, §2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**, §2 **Plan — Case readiness checklist proof CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist basics CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**). In **`showPostDraftReviewCallout`** (emerald **Surrenderless prepared your next step** card), after the main paragraph and **before** existing proof/chat continuity lines, adds page-local **`getPreparedNextStepExplainer`** copy from existing **`preparedPacketApproved`** and **`preparedNextAction.stepLabel`** (**no** new API, **no** schema): when packet **not** yet approved — **Opening your prepared review lets you check your saved case record in one place, then approve it for the next in-app step. Surrenderless does not file or submit for you.**; when **`preparedPacketApproved`** — **Your approved in-app step is {stepLabel} — use your case packet to continue tracking. Nothing is sent automatically.** When signed in (**`isSignedIn`**), readiness load finished (**`!readinessLoading`**), and **`!readyToEscalate`** (**`!basicsReady`** and/or **`!evidenceReady`**), adds **Before you escalate, finish readiness:** bridge with inline links using **`planChecklistLinkCls`** (same pattern as **Case readiness** checklist): **Update in chat** → **`/justice/chat-ai`** when **`!basicsReady`**; **Add proof in chat** → **`/justice/chat-ai`** when **`!evidenceReady`**; **Review submission draft** → **`/justice/preview`** when **`!draftReviewed`**. **Does not** hide or disable **Review prepared next step** / **View approved case packet** primary CTA or secondary prep link. **Does not** change **`showPostDraftReviewCallout`** gating, **`pickPreparedNextAction`**, **`handleViewApprovedCasePacketClick`**, **`recommendationText`** panel, full plan below, packet page, chat-ai, preview, hub, cases, or helper files. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Plan — post-draft approved step label alignment (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **copy/label-only** slice (with §2 **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`)**, §2 **Plan — prepared packet approval surfaced (`/justice/plan`)**, §2 **Plan — Case readiness approved-next visibility (`/justice/plan`)**, §2 **Justice hub — approved next-step label and status on Current case (`/justice`)**, §2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**, §2 **Saved cases — approved next-step label on All cases cards (`/justice/cases`)**, §2 **Preview — approved-next read-only continuity (`/justice/preview`)**). In **`showPostDraftReviewCallout`** emerald **Surrenderless prepared your next step** card, when **`preparedPacketApproved`**, the main paragraph step line uses **Next step: {approvedStepLabel}** — **not** **Approved next step:** — matching Hub, Cases, Chat AI **Active case** banner, Preview approved-next panel, and Plan **Case readiness** checklist (**Approved next action:** remains on status rows only). **Does not** change **`approvedStepLabel`**, **`approvedNextAction`** logic, **`(started)`** / **`(recorded as handled)`** suffixes, **Case readiness** status row **Approved next action:** copy, **Open {label}**, **Record action handled for now**, **`showApprovedNextActionCta`**, **`handleApprovedNextActionOpen`**, **`handleMarkApprovedNextActionHandled`**, handling request/ack, **`getPreparedNextStepExplainer`**, readiness bridge, chat/re-preview continuity lines, CTAs, routing, hydrate, Chat AI, Cases, Hub, Packet, Preview, APIs, schema, or session keys. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Chat AI intake — hydrate & update commit:** **`/justice/chat-ai`** is the in-app **Update in chat** path for an **active local case** (e.g. from **`/justice/plan`**, **Saved cases**, or hub — §2 **Saved cases — Needs more info chat routing CTA (`/justice/cases`)**, §2 **Justice hub — Needs more info chat routing CTA on Current case (`/justice`)**). **Hydration:** one-time mount read via **`readValidLocalJusticeIntake()`** → **`justiceIntakeToBuildJusticeIntakeParts`** in **`buildJusticeIntake.ts`** (shared with scripted/structured alignment); baseline **`parts`** snapshot for session change summary — §2 **Chat AI intake — session change summary (Update in chat)**. **Active case** orientation banner when hydrated — §2 **Chat AI intake — active case context banner (Update in chat)**; compact readiness checklist on **Update in chat** — §2 **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**; funnel-aware footer work links on **Active case** banner — §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**; same funnel-aware footer work links on **Recap → Current action tracking** when **`approvedNextAction`** exists — §2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**; read-only approved next-step label/status on **Active case** banner when **`approvedNextAction`** exists — §2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**. **Commit:** **`commitIntakeToSessionAndServer`** **`mode: "update"`** preserves **`STORAGE_CASE_ID`**, does **not** call **`newCaseId()`**, does **not** **`clearTimelineForNewCase`**, does **not** append **`case_started`**, does **not** clear FTC/mock keys, does **not** fire **`intake_completed`**, does **not** **`POST /api/justice/cases`**; writes **`STORAGE_INTAKE`**; when signed in and case id is a **UUID**, **`PATCH /api/justice/cases/[id]`** with `{ intake, timeline }` (same pattern as merchant save). **Create** **`mode`** (default) unchanged for new intake and for **`/justice/intake`** / **`/justice/chat`**. **`commitIntakeToSessionAndServer`** returns **`CommitIntakeResult`** **`{ caseId, serverPersisted }`** — **create** sets **`serverPersisted`** when signed-in **`POST /api/justice/cases`** succeeds with **`id`**; **update** sets **`serverPersisted`** when **`PATCH`** succeeds (callers may ignore; §2 **Chat AI intake — staged proof notes on fresh intake (Proof / evidence panel)** uses it before flushing staged proof). **No** new timeline event type for “intake updated” on this slice. **Limitations:** **`/justice/intake`** submit still uses **create** only (editing loaded session can still spawn a duplicate case — pre-existing); signed-out or non-UUID session ids get local intake update only (no server PATCH); **no** filing/submission automation.
- **AI chat intake — preview-readiness UX polish (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only. **Recap** adds **What happened** from **`parts.story`** (trimmed, truncated ~120 chars, or **—** when empty). **Preview readiness:** amber **Still needed before preview: …** lists only missing basics (**company**, **product/service**, **what happened**, **reply email**, **requested outcome**) via page-local **`getPreviewBasicsMissing`** — replaces generic all-fields copy. Shown above **Save and continue in chat** while incomplete; **Save and continue in chat** still blocks on incomplete basics but does **not** show a duplicate red copy of the same message (contact-proof errors remain red). Incomplete cases stay on **`/justice/chat-ai`**; complete signed-in intake uses **`commitIntakeToSessionAndServer`** (**`create`** or **`update`**) and stays in chat when continuity guards pass (§2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)**); preview fallback when guards fail.
- **AI chat intake — duplicate-send reliability guard (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — page-local **`sendInFlightRef`** in **`handleSend`**: return immediately when a send is already in flight (**`sendInFlightRef.current`** or **`loading`**); set ref **true** before **`fetch`**, clear in **`finally`** with **`loading`**. **Enter**-to-send only invokes **`handleSend`** when not loading and not in-flight. Prevents rapid **double-click** / fast **Enter** from starting overlapping **`POST /api/justice/intake-chat`** requests (stale **`conversation_history`**, duplicate bubbles, last-writer **`parts`**). **Normal send** unchanged; **failed-request retry** still works after the request finishes; **Recap** / preview-readiness / **Continue** → **`/justice/preview`** behavior unchanged. **Page-only reliability**: **no** API, prompt/parser, **`buildJusticeIntake.ts`**, commit helper, preview/plan, routing/hub/header, schemas, Supabase, readiness, **`rules.ts`**, or scripted **`/justice/chat`** changes.
- **AI chat intake — API error-copy polish (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — page-local **`formatIntakeChatApiError(status, serverError)`** in **`handleSend`**: for **`!res.ok`** responses, replaces raw server **`error`** strings (e.g. **`Unauthorized`**, **`Invalid model JSON`**, **`OPENAI_API_KEY not configured`**) with clearer user-facing retry guidance — **401** (session expired, sign in and resend), **429** (wait and retry), **502** (usable AI reply), **500** (including missing-key vs generic server error), **400**, **413** / too-large body, and a generic fallback. **Success path**, **input** clearing on success, **retry** after failure (input/messages/**`parts`** preserved), **duplicate-send** guard, **Recap**, and **Continue** → **`/justice/preview`** unchanged. Network catch and malformed-200 client messages unchanged. **Page-only polish**: **no** API route, prompt/parser, **`buildJusticeIntake.ts`**, commit helper, preview/plan, routing/hub/header, schemas, Supabase, readiness, **`rules.ts`**, or scripted **`/justice/chat`** changes.
- **AI intake — `purchase_or_signup` extraction (`buildJusticeIntakeAiPrompt.ts`):** **Issue found:** on **`/justice/chat-ai`**, a user said a **“phone case”** never arrived; the assistant repeated “phone case” in **`assistantMessage`** but returned **`parts.purchase_or_signup`** empty, so the recap **Product / service** line stayed blank and the pre-preview basics gate blocked. **Fix (prompt-only):** **`src/lib/justice/buildJusticeIntakeAiPrompt.ts`** now explicitly instructs the model to populate **`purchase_or_signup`** whenever the product, service, order item, subscription, account, charge target, or thing purchased/charged is **clearly stated** (with examples such as phone case, Prime, checking account, internet plan) — without inventing when unstated. **No** change to **`parseIntakeChatAiResponse.ts`**, **`/justice/chat-ai`** page, API route behavior, commit helper, preview/plan, or scripted chat. **Smoke-tested:** **`/justice/chat-ai`** now maps **“phone case”** into **Product / service** and a complete intake reaches **`/justice/preview`**.
- **Justice hub — step-by-step chat fallback (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** lists **Start with chat intake** → **`/justice/chat-ai`** first, then **Use step-by-step chat** → **`/justice/chat`** (scripted one-question-at-a-time intake). **`Header.tsx`** **Chat intake** → **`/justice/chat-ai`**. **Hub/header/navigation/copy only**: **no** behavior change to either chat route’s intake logic, **`POST /api/justice/intake-chat`**, prompt/parser, **`buildJusticeIntake.ts`**, commit helper, preview/plan, schemas, Supabase, readiness, **`rules.ts`**, or scripted **`/justice/chat`** Q&A. **Smoke-tested:** hub **Start with chat intake** opens **`/justice/chat-ai`**; **Use step-by-step chat** opens **`/justice/chat`**.
- **Justice-wide navigation alignment toward AI chat intake (UI/navigation/copy + chat-ai hydrate):** Extends hub/header promotion (§2) to **preview**, **plan**, **packet**, **evidence**, **prep**, and **chat** surfaces so **`/justice/chat-ai`** is the primary **Chat intake** / **Update in chat** path and fallbacks are explicit. **`/justice/preview`** (`preview/page.tsx`): no-case empty state primary **Chat intake** → **`/justice/chat-ai`**; **Step-by-step chat** → **`/justice/chat`**; **Structured form** → **`/justice/intake`**; footer breadcrumb matches. **`/justice/plan`** (`plan/page.tsx`): breadcrumb **Update in chat** → **`/justice/chat-ai`** (hydrates active case when local intake exists; §2 **Chat AI intake — hydrate & update commit**), **Edit structured form** → **`/justice/intake`** second; signed-out **Start a new case** → **`/justice`** hub. **`/justice/packet`** (`packet/page.tsx`): breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** in main and no-**`caseId`** empty-state rows — §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)** — completes **Packet → Update in chat → Chat AI** continuity. **`/justice/evidence`** (`evidence/page.tsx`): breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** — §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)** — completes **Organize evidence** return loop from chat-ai. **`/justice/cases`** (`cases/page.tsx`): breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** — §2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)** — matches Preview/Evidence/Packet chat-first breadcrumb continuity (PR **#376** / commit **`eede473`**). **`/justice/handling`** (`handling/page.tsx`): breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Saved cases** and before **Action plan** — §2 **Handling workbench — Update in chat breadcrumb return (`/justice/handling`)** — matches Preview/Evidence/Packet/Saved cases chat-first breadcrumb continuity (PR **#378** / commit **`115e295`**). **`/justice/merchant`** (`merchant/page.tsx`): breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** — §2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)** — matches Preview/Evidence/Packet/Saved cases/Handling chat-first breadcrumb continuity (PR **#380** / commit **`608a515`**). **`/justice/payment-dispute`** (`payment-dispute/page.tsx`): breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** — §2 **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`)** — matches Merchant chat-first breadcrumb continuity (PR **#382** / commit **`151f93d`**). **`/justice/bbb`** (`bbb/page.tsx`): breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** — §2 **BBB — Update in chat breadcrumb return (`/justice/bbb`)** — matches Merchant/Payment dispute chat-first breadcrumb continuity (PR **#384** / commit **`2fdf4f8`**). **`/justice/cfpb`** (`cfpb/page.tsx`): breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** — §2 **CFPB — Update in chat breadcrumb return (`/justice/cfpb`)** — matches BBB/Merchant/Payment dispute chat-first breadcrumb continuity (PR **#386** / commit **`5de8335`**). **`/justice/fcc`** (`fcc/page.tsx`): breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** — §2 **FCC — Update in chat breadcrumb return (`/justice/fcc`)** — matches CFPB/BBB/Merchant/Payment dispute chat-first breadcrumb continuity (PR **#388** / commit **`35e3e7c`**). **`/justice/dot`** (`dot/page.tsx`): breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** — §2 **DOT — Update in chat breadcrumb return (`/justice/dot`)** — matches FCC/CFPB/BBB/Merchant/Payment dispute chat-first breadcrumb continuity (PR **#390** / commit **`d56f7be`**). **`/justice/demand-letter`** (`demand-letter/page.tsx`): breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** — §2 **Demand letter — Update in chat breadcrumb return (`/justice/demand-letter`)** — matches DOT/FCC/CFPB/BBB/Merchant/Payment dispute chat-first breadcrumb continuity (PR **#392** / commit **`051c35a`**). **`/justice/state-ag`** (`state-ag/page.tsx`): breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** — §2 **State AG — Update in chat breadcrumb return (`/justice/state-ag`)** — matches Demand letter/DOT/FCC/CFPB/BBB/Merchant/Payment dispute chat-first breadcrumb continuity (PR **#394** / commit **`7154ef5`**). **`/justice/ftc-review`** (`ftc-review/page.tsx`): breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** — §2 **FTC review — Update in chat breadcrumb return (`/justice/ftc-review`)** — matches State AG/Demand letter/DOT/FCC/CFPB/BBB/Merchant/Payment dispute chat-first breadcrumb continuity (PR **#396** / commit **`25144d7`**); success-panel **Back to action plan** button unchanged. **`/justice/intake`** (`intake/page.tsx`): breadcrumb **Home · Chat intake · Step-by-step chat** → **`/`**, **`/justice/chat-ai`**, **`/justice/chat`** — §2 **Structured intake — chat-first breadcrumb links (`/justice/intake`)** — keeps structured intake connected back to the chat-first flow (PR **#398** / commit **`2c35e2d`**). **`/justice/chat`** (`chat/page.tsx`): breadcrumb **Home · Chat intake · Action plan · Structured form** → **`/`**, **`/justice/chat-ai`**, **`/justice/plan`**, **`/justice/intake`** — §2 **Step-by-step chat — chat-first breadcrumb return (`/justice/chat`)** — gives scripted step-by-step chat a visible return path to primary AI chat intake (PR **#400** / commit **`809412b`**). **Prep pages** (`bbb`, `cfpb`, `fcc`, `dot`, `demand-letter`): missing-field hints link **chat intake** → **`/justice/chat-ai`** first and **structured form** → **`/justice/intake`**. **`/justice/chat-ai`** (`chat-ai/page.tsx`) breadcrumb: when **`isUpdatingExistingCase`**, funnel-aware **`activeCaseWorkLabel`** → **`activeCaseWorkHref`** (same **`resolveActiveCaseWorkHref`** / **`resolveActiveCaseWorkLabel`** as Active case banner — §2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**); fresh intake keeps **Action plan** → **`/justice/plan`**; **Structured form** → **`/justice/intake`**; **`/justice/chat`** (`chat/page.tsx`): §2 **Step-by-step chat — chat-first breadcrumb return (`/justice/chat`)**; subtitle **Use structured form** → **`/justice/intake`** unchanged. **Routes not removed:** **`/justice/chat`** (scripted fallback), **`/justice/intake`** (structured fallback). **Navigation slice:** **no** changes to **`rules.ts`**, readiness, preview draft **generation**, filing/evidence APIs, Supabase schema, or **`POST /api/justice/intake-chat`** contract. **Hydrate/update slice** (separate bullets): **`buildJusticeIntake.ts`** reverse mapper + **`commitIntakeToSessionAndServer`** **`update`** mode — **no** auto-filing.
- **Saved cases — Start new case routing (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** — **Start new case** invokes **`clearLocalJusticeSession()`** and **`href`** **`/justice`** workspace hub (**not** **`/justice/intake`**), aligning **Saved cases** repeat entry with the **workspace / recovery hub** (**chat intake** first on the hub; **form intake** remains available there). Low-risk link **Handling workbench** → **`/justice/handling`** (§2 **Handling workbench — internal triage surface**). Prep **edit** hints now follow §2 **Justice-wide navigation alignment** (chat intake first, structured form fallback). **Routing only**: **no** **`rules.ts`**, schemas, **API** implementations, auth, filing automation, **`/justice/preview`** / **`/justice/plan`** flows, **`commitIntakeToSessionAndServer`**, or timeline **behavior** / **semantics** changes.
- **Saved cases — Needs attention list alignment (`/justice/cases`):** On **`src/app/justice/cases/page.tsx`**, the **Needs attention** section builds from **`filteredSortedCases`** (same **search**, **status** filter, and **sort** as **All cases**), not the unfiltered **`cases`** array — so **search** / **status** filters do not show attention cards for cases hidden from the visible list. **Needs attention** includes **open follow-up tasks** (incomplete **`justice_case_tasks`** rows via **`buildAttentionItems`**), read-only **approved packet action** cards when **`prepared_packet_approved`** is set and **`approved_next_action`** matches **`parseApprovedPacketActionWithoutHandlingRequest`** (§2 **Approval-to-action engine — approved packet action visibility on Saved cases Needs attention (`/justice/cases`)**), **approved next action handling-request** cards when **`handling_requested_at`** is set and **`status !== "completed"`** (§2 **Saved cases — Needs attention for handling requested**), and, when present, **approved next action follow-up** cards from **`client_state.approved_next_action.follow_up_needed`** (§2 **Saved cases — Needs attention from approved next action follow-up**, **Saved cases — Needs attention opens approved action surface**, **Saved cases — clear Needs attention follow-up**). **`hasNeedsAttentionContent`** includes approved-packet-action cards. It is **not** the per-case **“Needs more info”** readiness badge on **All cases** cards (**`caseReadiness`** / **`isBasicCaseInfoReadyForEscalation`** + evidence count). **Client-side Saved cases UI alignment only**: **no** change to task **semantics**, case **readiness**, **pagination** / **Load more**, **API** route implementations, schemas, auth, **`rules.ts`**, **routes**, **open** / **archive** / **save label** behavior, or timeline **behavior**; **`tasksByCaseId`** fetch unchanged.
- **Saved cases — Needs attention from approved next action follow-up (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only. **`GET /api/justice/cases`** already returns **`client_state`**; page-local **`buildFollowUpAttentionItems`** parses **`approved_next_action`** and surfaces cases where **`follow_up_needed === true`** in the **Needs attention** section (alongside open **tasks**). Compact cards show case **label** or **company** + **product/service**, read-only **Next step: {label}** when **`approved_next_action.label`** is set (§2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**), **Approved next action: {Approved|Started|Handled}** via **`approvedNextActionStatusLabel`** when **`status`** is set (§2 **Saved cases — Needs attention approved-next-action label alignment (`/justice/cases`)**), user-paced follow-up timing when **`follow_up_at`** is set (§2 **Approved next action follow-up timing**), truncated **outcome note**, **Follow-up needed**, **Open action** (§2 **Saved cases — Needs attention opens approved action surface**), and **Clear follow-up** (§2 **Saved cases — clear Needs attention follow-up**). Task rows in **Needs attention** still use **Open case** → **`/justice/plan`**. Copy states **in-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. Empty state remains **No open follow-up tasks.** when there are no follow-up-needed cases and no open tasks. **All cases** list **Open case** behavior unchanged. **Search** / **filter** / **sort**, labels, archive, and **Load more** unchanged. **Tracking scaffold** toward §1 **chat-first → prepare → approve → submit/queue/track** — **not** the final manual workflow and **not** follow-up automation. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **no** changes to **`rules.ts`**, **`caseReadiness.ts`**, **`commitIntakeToSessionAndServer`**, or plan/packet surfaces beyond existing tracking UI.
- **Saved cases — Needs attention opens approved action surface (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only. **`openApprovedNextActionFollowUp`** activates the case in session (**`STORAGE_CASE_ID`**, intake, timeline — same **`activateCaseInSession`** as **`openCase`**) then routes to the most relevant in-app surface: **`router.push(approved_next_action.href)`** when **`href`** is present and an internal **`/justice/*`** route (**`isInternalJusticeHref`**); otherwise **`router.push("/justice/chat-ai")`** (§2 **Saved cases — Needs attention follow-up default chat-first routing (`/justice/cases`)**). **Needs attention** follow-up cards use CTA **`approvedNextActionFollowUpOpenLabel`** — **Open action** when explicit internal **`href`** (e.g. **`/justice/merchant`**), **Open tracking** on default. **All cases** **Open case** routes to **`/justice/chat-ai`** via **`openUpdateInChat`** — §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**. **Search** / **filter** / **sort**, labels, archive, and **Load more** unchanged. **Tracking / navigation scaffold** toward §1 **chat-first → prepare → approve → submit/queue/track** — **not** the final manual workflow. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, or contacted automatically.
- **Saved cases — Needs attention follow-up default chat-first routing (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **UI/navigation** slice (with §2 **Saved cases — Needs attention opens approved action surface (`/justice/cases`)**, §2 **Saved cases — Needs attention from approved next action follow-up (`/justice/cases`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Chat AI intake — approved next action display hydrate (`/justice/chat-ai`)**, §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**). In **`resolveApprovedNextActionFollowUpHref`**, explicit valid internal **`next.href`** (**`isInternalJusticeHref`**) **unchanged** (e.g. **`/justice/merchant`** → **Open action**); fallback only changes from **`"/justice/plan"`** to **`"/justice/chat-ai"`** so **Needs attention** follow-up **Open tracking** lands on **Update in chat** with **Current action tracking** (follow-up timing, outcome note, **Mark follow-up handled**). **`activateCaseInSession`** before navigate **unchanged**. Checklist deep links, **Open case**, filters, card layout, **Clear follow-up**, handling-request cards, explicit **`href`** actions, APIs, schema **unchanged** — **no** filing/submission/contact/queue/upload/storage/automatic-action claims.
- **Saved cases — clear Needs attention follow-up (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only. On approved-next-action **Needs attention** cards, **Clear follow-up** (**`clearApprovedNextActionFollowUp`**) sets **`approved_next_action.follow_up_needed`** to **false** (omits the flag on persist) while **preserving** **`outcome_note`**, **`follow_up_at`**, **`status`**, **`href`**, **`label`**, **`approved_at`**, **`started_at`**, **`completed_at`**, and other **`approved_next_action`** fields. Updates in-memory **`cases`** list **`client_state`** immediately; for signed-in **UUID** cases, **`GET` + merge + `PATCH`** on existing **`/api/justice/cases/[id]`** with merged **`client_state`** (same pattern as plan/packet); syncs **`justice_approved_next_action_v1`** session map when present. After clear, the card **disappears from Needs attention** (no longer **`follow_up_needed === true`**) but the case **remains in All cases** (may still show approved-next-action tracking there without **Follow-up needed**). **Open action** and **All cases** **Open case** unchanged. **Search** / **filter** / **sort**, archive, and **Load more** unchanged. Copy: clears from **Needs attention** only; notes and dates stay saved; **not** automatic filing or submission. **Tracking / navigation scaffold** toward §1 **chat-first → prepare → approve → submit/queue/track** — **not** the final manual workflow. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, or contacted automatically.
- **Saved cases — record action handled on Needs attention approved-packet-action cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **behavior** slice (with §2 **Approval-to-action engine — approved packet action visibility on Saved cases Needs attention (`/justice/cases`)**, §2 **Saved cases — clear Needs attention follow-up (`/justice/cases`)**, §2 **Approved next action completed / handled (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Plan — prepared packet approval surfaced (`/justice/plan`)**, §2 **Packet — record action handled on approved next action card (`/justice/packet`)**, §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**, §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). On **Needs attention → Approved packet action** cards only ( **`buildApprovedPacketActionAttentionItems`** ), when **`next.status === "started"`**, adds **Record action handled for now** via page-local **`markApprovedPacketActionHandled`**: builds next **`approved_next_action`** preserving existing fields and setting **`status: "completed"`** and **`completed_at`** (ISO) through **`mergeApprovedNextActionTrackingFields`** + **`mergeClientStateWithApprovedNextAction`**, optimistic in-memory **`cases`** update, signed-in **UUID** **`GET` + merge + `PATCH`** **`client_state`** (same pattern as **`clearApprovedNextActionFollowUp`** / **`acknowledgeHandlingRequest`**), and **`writeSessionApprovedNextAction`** when the row is the active browser case (**`sessionCaseId === caseRow.id`**) — mirrors plan **`handleMarkApprovedNextActionHandled`** (§2 **Plan — prepared packet approval surfaced (`/justice/plan`)**), packet (§2 **Packet — record action handled on approved next action card (`/justice/packet`)**), chat-ai (§2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**), and workbench **`markApprovedPacketActionHandled`** (§2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**). Placed after status / optional **Opened {formatted time}** and before **Open case** / **View on handling workbench**, with **Tracking only — not automatic filing or submission.** subcopy. Card leaves **Needs attention** after write because **`parseApprovedPacketActionWithoutHandlingRequest`** already excludes **`completed`** — membership predicate unchanged. **Does not** change **`CaseApprovedNextActionTracking`** on **All cases**, handling-request **Needs attention** record-handled — §2 **Saved cases — record action handled on Needs attention handling-request cards (`/justice/cases`)**, plan, packet, chat-ai, hub, handling workbench, routing, or **`buildApprovedPacketActionAttentionItems`** membership. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior.
- **Saved cases — record action handled on Needs attention handling-request cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **behavior** slice (with §2 **Saved cases — Needs attention for handling requested (`/justice/cases`)**, §2 **Saved cases — acknowledge handling request from Needs attention (`/justice/cases`)**, §2 **Saved cases — record action handled on Needs attention approved-packet-action cards (`/justice/cases`)**, §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**, §2 **Approved next action completed / handled (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). On **Needs attention → handling-request** cards only (**`buildHandlingRequestedAttentionItems`**), when **`next.status === "started"`**, adds **Record action handled for now** reusing page-local **`markApprovedPacketActionHandled`** — same **`approved_next_action`** persist path: preserves **`handling_requested_at`**, **`handling_request_note`**, **`handling_acknowledged_at`**, **`started_at`**, **`outcome_note`**, **`follow_up_*`**, **`label`**, **`href`**, **`approved_at`**, and other fields; sets **`status: "completed"`** + **`completed_at`** (ISO) via **`mergeApprovedNextActionTrackingFields`** + **`mergeClientStateWithApprovedNextAction`**, optimistic **`setCases`**, signed-in **UUID** **`GET` + merge + `PATCH`** on existing **`/api/justice/cases/[id]`**, and **`writeSessionApprovedNextAction`** when **`sessionCaseId === caseRow.id`** — mirrors approved-packet **Needs attention** record-handled (§2 **Saved cases — record action handled on Needs attention approved-packet-action cards (`/justice/cases`)**) and workbench handling-request triage cards (§2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**). Shows optional **Opened {formatted time}** when **`started_at`** is set and **Tracking only — not automatic filing or submission.** subcopy. Card leaves **Needs attention** after write via existing **`isHandlingAwaitingTriageApprovedNextAction`** (**`status !== "completed"`** predicate) — membership predicate unchanged. **Does not** change **Mark acknowledged**, request handling, note edit, **`CaseApprovedNextActionTracking`** on **All cases**, approved-packet **Needs attention** cards, plan, packet, chat-ai, hub, handling workbench, routing, or **`buildHandlingRequestedAttentionItems`** membership. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior.
- **Saved cases — readiness status hints (`/justice/cases`):** Each case card under **All cases** shows the **Ready to escalate** / **Needs more info** status (green vs amber) plus existing **Evidence** / **Filings** / **Open tasks** / **Next due** stat rows. A short **display-only** helper line from page-local **`caseReadinessHint`** appears **only when the case is not ready** (**Needs more info**), explaining whether **basic case details** (company, category, product/service, what happened, desired outcome) or **evidence** (**Add at least one proof note in chat, or add evidence from the evidence page.** when basics are complete but **`evidenceCount === 0`**) is still missing — using the **same predicates already on the card** (**`isBasicCaseInfoReadyForEscalation(row.intake)`** and **`progressById[row.id]?.evidenceCount`**). **Ready** cases **do not** repeat the extra “basic case details and at least one evidence item are saved” sentence under the green label (reduces card clutter); readiness **logic** is unchanged. Direct chat routing on **Needs more info** cards — §2 **Saved cases — Needs more info chat routing CTA (`/justice/cases`)**; compact per-card readiness checklist — §2 **Saved cases — compact readiness checklist on All cases cards (`/justice/cases`)**; prepared packet reviewed row when draft reviewed — §2 **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`)**. **Display-only Saved cases UI polish**; **no** change to readiness **logic** in **`caseReadiness.ts`**, **search** / **filter** / **sort**, **Needs attention** list, **APIs**, **pagination**, **open** / **archive** / **save label** behavior, **routes**, schemas, auth, **`rules.ts`**, timeline, or **`commitIntakeToSessionAndServer`**.
- **Saved cases — Needs more info chat routing CTA (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **UI/navigation** slice (with §2 **Saved cases — compact readiness checklist on All cases cards (`/justice/cases`)**, §2 **Saved cases — readiness status hints (`/justice/cases`)**, §2 **Chat AI intake — hydrate & update commit**). On **All cases** cards in **Needs more info** state (same **`isBasicCaseInfoReadyForEscalation`** + **`progressById[row.id]?.evidenceCount`** predicates as the readiness badge — **unchanged**), directly under **`caseReadinessHint`**, adds one outline secondary control: when **`!isBasicCaseInfoReadyForEscalation(row.intake)`** — **Update in chat**; when basics are complete and **`evidenceCount < 1`** — **Add proof in chat**. Both call page-local **`openUpdateInChat(row)`** — **`activateCaseInSession(row)`** ( **`STORAGE_CASE_ID`**, intake, timeline, approved-next-action session hydrate — same as **`openCase`**) then **`router.push("/justice/chat-ai")`** so users can fix details or add proof notes without opening the plan first. Reuses existing card secondary button styling (outline, consistent with **Archive**). **`Open case`** routing — §2 **Saved cases — Open case chat-first routing (`/justice/cases`)** (label unchanged). **Evidence-only** **`caseReadinessHint`** copy updated to mention proof notes in chat and **`/justice/evidence`** (display-only — **no** predicate changes). **Does not** change **`isReadyToEscalate`**, status filters, **Needs attention** builders/cards, APIs, schema, hub, plan, chat-ai, or **`caseReadiness.ts`**. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Saved cases — compact readiness checklist on All cases cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **UI/navigation** slice (with §2 **Saved cases — Needs more info chat routing CTA (`/justice/cases`)**, §2 **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`)**, §2 **Justice hub — Current case compact readiness checklist (`/justice`)**, §2 **Plan — Case readiness checklist proof CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist basics CTA (`/justice/plan`)**, §2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**). On each **All cases** card when **`progressById[row.id]`** is loaded, adds compact **`<ul>`** after existing progress stats (**Evidence** / **Filings** / **Open tasks** / **Next due**) and **before** **Ready to escalate** / **Needs more info** — mirrors hub/plan readiness clarity. Uses **existing** data only (**no** new API, **no** schema): **`isBasicCaseInfoReadyForEscalation(row.intake)`**; **`progressById[row.id]?.evidenceCount`**; page-local **`caseDraftReviewed(row)`** from **`row.timeline`** / **`submission_draft_reviewed`**. Three rows: **Basic case info: yes/not yet** — inline **Update in chat** via **`openUpdateInChat(row)`** (**`activateCaseInSession`** then **`/justice/chat-ai`**) when not ready; **Evidence: yes/not yet** — inline **Add proof in chat** via **`openUpdateInChat(row)`** when **`(evidenceCount ?? 0) < 1`**; **Submission draft reviewed: yes/not yet** — inline **Review submission draft** via page-local **`openReviewSubmissionDraft(row)`** (**`activateCaseInSession`** then **`/justice/preview`**) when **`!caseDraftReviewed(row)`**; fourth row when draft reviewed — §2 **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`)**. Same inline link classes as plan/hub checklist CTAs. **Does not** change **`isReadyToEscalate`**, **`caseMatchesStatusFilter`**, **`caseReadinessHint`**, **`openCase`**, evidence/progress fetch, or existing **Needs more info** outline **Update in chat** / **Add proof in chat** buttons. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** API, schema, helper file, hub, plan, preview, or chat-ai changes on this slice. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **UI/navigation** slice (with §2 **Saved cases — compact readiness checklist on All cases cards (`/justice/cases`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**, §2 **Justice hub — Current case prepared packet reviewed checklist row (`/justice`)**, §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**, §2 **Approved next action shared state helpers**). Page-local **`casePreparedPacketApproved(row)`** returns **`parseJusticeCaseClientState(row.client_state).prepared_packet_approved === true`** (list **`GET`** already returns **`client_state`** — **no** new API, **no** schema). In **All cases** compact checklist **`<ul>`**, **after** **Submission draft reviewed**, **only when** **`caseDraftReviewed(row)`**: **Prepared case packet reviewed: yes** when **`casePreparedPacketApproved(row)`**; **Prepared case packet reviewed: not yet** when not approved, with inline button **Review prepared case packet** via **`openReviewPreparedPacket(row)`** — **`activateCaseInSession(row)`** then **`router.push("/justice/packet")`**; reuses **`casesChecklistLinkCls`**. **Navigation/readiness only** — closes multi-case inventory gap between draft review and packet approval (parity with chat-ai/hub). **Does not** change **Needs attention** builders/cards (beyond shared **`openCase`** destination — §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**), **`CaseApprovedNextActionTracking`** approved-next labels, filters, search/sort, **`isReadyToEscalate`**, **`caseReadinessHint`**, evidence/progress fetch, or existing **Needs more info** outline **Update in chat** / **Add proof in chat** buttons. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** API, schema, helper file, hub, plan, preview, or chat-ai changes on this slice. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Saved cases — Open case chat-first routing (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **UI/navigation** slice (with §2 **Saved cases — compact readiness checklist on All cases cards (`/justice/cases`)**, §2 **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`)**, §2 **Saved cases — Needs more info chat routing CTA (`/justice/cases`)**, §2 **Saved cases — breadcrumb primary link chat-first (`/justice/cases`)**, §2 **Chat AI intake — hydrate & update commit**). **`openCase(row)`** delegates to **`openUpdateInChat(row)`** — **`activateCaseInSession(row)`** (**`STORAGE_CASE_ID`**, intake, timeline, approved-next-action session hydrate) then **`router.push("/justice/chat-ai")`** — regardless of draft-review or prepared-packet state. Checklist deep links **unchanged**: **`openReviewSubmissionDraft(row)`** → **`/justice/preview`**; **`openReviewPreparedPacket(row)`** → **`/justice/packet`**. Preview **Continue** (§2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**) **unchanged** on this slice. **`caseDraftReviewed`** / **`casePreparedPacketApproved`** remain for checklist rows only — **no** new API, **no** schema. **Open case** button label, card layout, **Needs attention** inclusion/builders, **Open action** / **`openApprovedNextActionFollowUp`**, approved-next labels, filters/search, archive, **Load more**, APIs, handlers, filing/submission/contact/queue/upload/storage/automatic-action behavior **unchanged** on this slice. **Navigation/readiness only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Saved cases — breadcrumb primary link chat-first (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **UI/navigation** slice (with §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Saved cases — Needs attention follow-up default chat-first routing (`/justice/cases`)**, §2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Packet — breadcrumb primary link chat-first (`/justice/packet`)**). Top breadcrumb row: **Update in chat** → **`/justice/chat-ai`** (first link); **Justice workspace** → **`/justice`** (second); **Home** → **`/`** **unchanged**. Replaces prior **Back to action plan** → **`/justice/plan`** first link + duplicate **Update in chat** second slot — aligns Saved cases exit with **Open case** / **Open tracking** chat-first routing (§2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Saved cases — Needs attention follow-up default chat-first routing (`/justice/cases`)**). **Does not** change intro copy, **`openCase`**, **`openUpdateInChat`**, checklist deep links, filters, card layout, Needs attention builders, APIs, schema — **no** filing/submission/contact/queue/upload/storage/automatic-action claims.
- **Saved cases — intro copy chat-first (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **UI/copy** slice (with §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Saved cases — breadcrumb primary link chat-first (`/justice/cases`)**). Page intro under **Saved cases** heading: **Open a case you saved while signed in to continue in chat.** replaces **… continue your action plan.** — aligns hero copy with **`openCase`** → **`openUpdateInChat`** → **`/justice/chat-ai`** (§2 **Saved cases — Open case chat-first routing (`/justice/cases`)**); breadcrumb **Update in chat** first link unchanged (§2 **Saved cases — breadcrumb primary link chat-first (`/justice/cases`)**). **Does not** change **`openCase`**, **`openUpdateInChat`**, breadcrumb, **Open tracking**, checklist deep links, Needs attention builders, filters, card layout, handling copy, APIs, schema, or filing/submission/contact/queue/upload/storage/automatic-action behavior. **Copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Saved cases — approved-packet handling request copy chat-first (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **UI/copy** slice (with §2 **Approval-to-action engine — approved packet action workbench link on All cases cards (`/justice/cases`)**, §2 **Saved cases — intro copy chat-first (`/justice/cases`)**, §2 **Chat AI intake — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**). In **`CaseApprovedNextActionTracking`** approved-packet workbench helper ( **All cases** cards and **Needs attention** approved-packet blocks when **`showApprovedPacketActionWorkbench`** ), second sentence **Request Surrenderless handling from chat intake when you want internal triage tracking.** replaces **Request handling from your action plan when you want internal triage tracking.** — aligns with chat-first **Open case** routing (§2 **Saved cases — Open case chat-first routing (`/justice/cases`)**) and chat-ai helper copy (§2 **Chat AI intake — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**). First sentence (**approved case packet and next in-app step — not a Surrenderless handling request**) and **View on handling workbench** → **`/justice/handling`** **unchanged**. **Does not** change **`openCase`**, **`openUpdateInChat`**, breadcrumb, handling tracking UI, Needs attention builders, filters, card layout, APIs, schema, or filing/submission/contact/queue/upload/storage/automatic-action behavior. **Copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Saved cases — Needs attention for handling requested (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only. **`buildHandlingRequestedAttentionItems`** (from **`filteredSortedCases`**, same **search** / **status** filter / **sort** as **All cases**) surfaces cases matching **`isHandlingAwaitingTriageApprovedNextAction`** (§2 **Approved next action — shared handling awaiting-triage membership predicate**) in **Needs attention** (alongside open **tasks** and approved-next-action **follow-up** cards). Compact cards show case **label** or **company** + **product/service**, **Surrenderless handling requested**, recorded time, optional **`handling_request_note`**, a **display-only derived queue status** line (**`ApprovedNextActionHandlingQueueStatusReadOnly`** — always **Queue status: Awaiting internal triage.** in this inbox because cards exclude **`handling_acknowledged_at`**; derived from existing **`handling_requested_at`** / **`handling_acknowledged_at`** only, **no** new persisted field), read-only **Next step: {label}** when **`approved_next_action.label`** is set (§2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**), **Approved next action: {Approved|Started|Handled}** when **`status`** is set (§2 **Saved cases — Needs attention approved-next-action label alignment (`/justice/cases`)**), when **`status === "started"`** optional **Opened {formatted time}** and **Record action handled for now** — §2 **Saved cases — record action handled on Needs attention handling-request cards (`/justice/cases`)**, honest **in-app tracking only** copy, **Open case** via **`openCase`** funnel — §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, **View in handling workbench** → **`/justice/handling`** (§2 **Saved cases — View in handling workbench on Needs attention handling cards**), and **Mark acknowledged** (§2 **Saved cases — acknowledge handling request from Needs attention** — **unchanged**). **Which cases appear** in **Needs attention** **unchanged**; **no** new filters or sort options. **Follow-up** **Needs attention** behavior **unchanged**. **No** schema, migration, new API route, queue worker, **`justice_case_filings`** row, timeline **type**, browser automation, filing automation, or **`/api/submit-form`**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Saved cases — acknowledge handling request from Needs attention (`/justice/cases`):** **`src/app/justice/cases/page.tsx`**, **`src/lib/justice/types.ts`**, **`src/lib/justice/approvedNextActionState.ts`**. Optional **`handling_acknowledged_at`** (ISO) on existing **`approved_next_action`** in **`justice_cases.client_state`** only — **in-app triage ack**, not handled/submitted/queued externally. **Mark acknowledged** on handling-request **Needs attention** cards sets **`handling_acknowledged_at`** via **`acknowledgeHandlingRequestInApprovedNextAction`** + **`mergeClientStateWithAcknowledgedHandling`** and existing **`GET` + merge + `PATCH`** (preserves **`handling_requested_at`**, note, status, href, label, timestamps, follow-up fields). Card leaves **Needs attention**; **All cases** still shows **Surrenderless handling requested** with optional **Acknowledged {time} — internal tracking triage only**. Copy: **Acknowledged means internal tracking triage only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone. **No** schema, migration, new API route, queue worker, filing record, timeline **type**, or automation.
- **Saved cases — View in handling workbench on Needs attention handling cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **card-level navigation-only** slice (with §2 **Handling workbench — internal triage surface** and the page-level **Handling workbench** link — §2 **Saved cases — Start new case routing**). On each handling-request **Needs attention** card from **`buildHandlingRequestedAttentionItems`** (§2 **Saved cases — Needs attention for handling requested**), a secondary **View in handling workbench** control (**`Link`** → **`/justice/handling`**, outline secondary styling consistent with **Mark acknowledged**) appears between **Open case** and **Mark acknowledged**. **No** **`STORAGE_CASE_ID`** activation or change on click; **no** query/hash/deep-link; workbench remains the **full** handling-request list (**not** filtered to one case). After **Mark acknowledged**, the card leaves **Needs attention** and no longer shows this control. **Needs attention** inclusion rules, **display-only derived queue status**, **Mark acknowledged** persistence, **Open case** behavior, header **Handling workbench** link, **follow-up**/**task** **Needs attention** cards, and **search** / **filter** / **sort** **unchanged**. **No** schema, migration, new API route, queue worker, **`justice_case_filings`** row, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, **`/api/submit-form`**, or new persistence; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Saved cases — approved next-step label on All cases cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **visibility-only** slice (with §2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**, §2 **Saved cases — handling requested visibility on All cases cards**, §2 **Saved cases — Needs attention approved-next-action label alignment (`/justice/cases`)**, §2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**, §2 **Plan — Case readiness approved-next visibility (`/justice/plan`)**, §2 **Approved next action shared state helpers**). In page-local **`CaseApprovedNextActionTracking`** on **All cases** cards, when **`client_state.approved_next_action.label`** is present (via existing **`parseApprovedNextActionFromClientState`** — §2 **Approved next action shared state helpers**), shows read-only **Next step: {label}** (bold **label**) **above** the existing **Approved next action: {Approved|Started|Handled}** line from **`approvedNextActionStatusLabel`** — **status logic unchanged** (when the block renders and which status label appears); same read-only **Next step** / **Approved next action** pair on **Needs attention** handling-request and follow-up cards — §2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**; same **Approved next action:** label pattern on those cards — §2 **Saved cases — Needs attention approved-next-action label alignment (`/justice/cases`)**; same read-only **Next step** / **Approved next action** pair on **Plan** **Case readiness** when **`preparedPacketApproved`** — §2 **Plan — Case readiness approved-next visibility (`/justice/plan`)**. Completed **handled-for-now** timestamp on **All cases** browse cards — §2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**. Existing handling blocks (**Surrenderless handling requested**, **display-only** queue status, handled/open triage note, **View in handling workbench**, disclaimers, follow-up timing), **Needs attention** inclusion/builders, **search** / **filter** / **sort**, labels, archive, **Load more**, **Open case**, APIs, schema, Plan, Packet, Chat AI, and Hub **unchanged** on this slice. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **in-app tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, contacted anyone, uploaded, stored files, or taken automatic action on the user's behalf.
- **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **read-only browse/history display** slice (with §2 **Saved cases — approved next-step label on All cases cards (`/justice/cases`)**, §2 **Saved cases — started-at read-only on All cases cards (`/justice/cases`)**, §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**, §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**, §2 **Justice hub — approved next action display hydrate on Current case (`/justice`)**, §2 **Packet — handled-for-now timestamp on approved next action card (`/justice/packet`)**, §2 **Plan — handled-for-now timestamp on prepared-packet approved callouts (`/justice/plan`)**, §2 **Approved next action completed / handled (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In **`CaseApprovedNextActionTracking`** on **All cases** cards only, when **`approved_next_action.status === "completed"`** and **`completed_at`** is set on list **`GET`** **`client_state`**, shows read-only **Handled for now {formatted time}** via existing **`formatApprovedNextActionHandlingTimestamp`** — **in-app tracking browse/history only**; complements existing **Approved next action: Handled** status line. **Does not** add buttons, **`PATCH`**, timeline events, or show **completed** approved-packet actions in **Needs attention** or handling workbench **Approved packet actions** inbox lists (**`parseApprovedPacketActionWithoutHandlingRequest`** unchanged). **Does not** change **Needs attention** builders, handling-request blocks, workbench links, **Open case**, search/filter/sort, refetch-on-focus, plan/chat/packet/hub surfaces, or APIs. **No** new API route, schema/migration, DB field, timeline **type**, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Saved cases — started-at read-only on All cases cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **display-only** slice (with §2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**, §2 **Saved cases — approved next-step label on All cases cards (`/justice/cases`)**, §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`)**, §2 **Approved next action started (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In **`CaseApprovedNextActionTracking`** on **All cases** cards only, when **`approved_next_action.status === "started"`** and **`started_at`** is set on list **`GET`** **`client_state`**, shows read-only **Opened {formatted time}** via existing **`formatApprovedNextActionHandlingTimestamp`** after **Approved next action: {status}** and before outcome/handling blocks — parity with read-only **Handled for now {formatted time}** when **`status === "completed"`** (§2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**); uses existing **`approved_next_action.started_at`** from **`parseApprovedNextActionFromClientState`** (**no** new **`GET`** or **`PATCH`**). Complements §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**, and §2 **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`)**. **Does not** change **Needs attention** card templates, builders, handling-request blocks, workbench links, **Open case**, search/filter/sort, refetch-on-focus, hub, plan, packet, chat-ai, handling workbench, membership predicates, or any write paths. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Saved cases — outcome note on All cases cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **read-only browse/history display** slice (with §2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**, §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**, §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Saved cases — Needs attention from approved next action follow-up (`/justice/cases`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In **`CaseApprovedNextActionTracking`** on **All cases** cards only, when list **`GET`** **`client_state.approved_next_action.outcome_note`** is present (via existing **`parseApprovedNextActionFromClientState`** — **no** new **`GET`** or **`PATCH`**), shows read-only truncated outcome text (page-local **`truncateAttentionNote`**, max **200** chars — same as **Needs attention** follow-up cards) after **Handled for now** and before handling/follow-up lines. Complements existing **Follow-up needed** and user-paced follow-up timing on **All cases** browse cards — §2 **Approved next action follow-up timing (user-paced labels)**. **Does not** change **Needs attention** builders, **Clear follow-up**, **Open action**, inbox inclusion rules, handling blocks, workbench links, **Open case**, search/filter/sort, or APIs. **Display correctness only** — **no** new API route, schema/migration, DB field, timeline **type**, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Saved cases — Needs attention approved-next-action label alignment (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **display/copy-only** slice (with §2 **Saved cases — Needs attention for handling requested**, §2 **Saved cases — Needs attention from approved next action follow-up**, §2 **Saved cases — approved next-step label on All cases cards (`/justice/cases`)**, §2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**, §2 **Justice hub — approved next-step label and status on Current case (`/justice`)**, §2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — Recap current-action label alignment (`/justice/chat-ai`)**, §2 **Approved next action shared state helpers**). On **Needs attention** **handling-requested** and **follow-up** cards, the approved-next-action status line uses **Approved next action: {Approved|Started|Handled}** via existing **`approvedNextActionStatusLabel(next.status)`** with **`font-medium`** label span matching **`CaseApprovedNextActionTracking`** on **All cases** — **not** **Status:**. **Next step: {label}** on those cards — §2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**. **Does not** change **`buildHandlingRequestedAttentionItems`**, **`buildFollowUpAttentionItems`**, **`openApprovedNextActionFollowUp`**, **`clearApprovedNextActionFollowUp`**, **`acknowledgeHandlingRequest`**, hydrate, routing, filters, search/sort, **Open case**, APIs, schema, Plan, Packet, Hub, Chat AI, or state handling. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **display-only** slice (with §2 **Saved cases — Needs attention approved-next-action label alignment (`/justice/cases`)**, §2 **Saved cases — approved next-step label on All cases cards (`/justice/cases`)**, §2 **Saved cases — Needs attention for handling requested**, §2 **Saved cases — Needs attention from approved next action follow-up**, §2 **Justice hub — approved next-step label and status on Current case (`/justice`)**, §2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**, §2 **Approved next action shared state helpers**). In **`handlingRequestedAttentionItems.map`** and **`followUpAttentionItems.map`**, when **`next.label?.trim()`** exists, adds read-only **Next step: {label}** (bold **label**, **`mt-1 text-xs text-neutral-600 dark:text-neutral-400`**) **before** the existing **Approved next action: {status}** line — same pair as **`CaseApprovedNextActionTracking`** on **All cases** and Hub/Chat/Plan/Preview approved-next surfaces. **Does not** change **`buildHandlingRequestedAttentionItems`**, **`buildFollowUpAttentionItems`**, **`openApprovedNextActionFollowUp`**, **`clearApprovedNextActionFollowUp`**, **`acknowledgeHandlingRequest`**, hydrate, routing, filters, search/sort, **Open case**, APIs, schema, handlers, shared libs, Plan, Packet, Hub, or Chat AI. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, uploaded, stored, or contacted automatically.
- **Saved cases — handling requested visibility on All cases cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** — **visibility-only** slice (with §2 **Saved cases — approved next-step label on All cases cards**). Page-local **`CaseApprovedNextActionTracking`** (existing **All cases** approved-next-action block) parses **`client_state.approved_next_action`** via **`parseApprovedNextActionFromClientState`** (§2 **Approved next action shared state helpers**). When **`approved_next_action.label`** is set, read-only **Next step: {label}** appears above the status line — §2 **Saved cases — approved next-step label on All cases cards**. When **`handling_requested_at`** is set, the card shows **Surrenderless handling requested** and recorded date/time via shared **`formatHandlingRecordedInline`**, optional **`handling_request_note`**, a **display-only derived queue status** line (**`ApprovedNextActionHandlingQueueStatusReadOnly`** / **`deriveHandlingQueueDisplayStatus`** — **no** new persisted field; derived only from existing **`handling_requested_at`** and **`handling_acknowledged_at`** on **`approved_next_action`**): **Queue status: Awaiting internal triage.** when **`handling_acknowledged_at`** is absent, **Queue status: Acknowledged for internal tracking triage only** when **`handling_acknowledged_at`** is set, plus optional **Acknowledged {time} — internal tracking triage only** timestamp line when acknowledged. Cards **without** **`handling_requested_at`** show **no** queue status line (**unchanged** otherwise). When handled (**`status === "completed"`**) with handling requested and unacknowledged, optional handled/open triage note — §2 **Saved cases — handled/open handling triage note on All cases cards**. Per-card **View in handling workbench** when handling is requested — §2 **Saved cases — View in handling workbench on All cases handling-request cards**. List **`client_state`** refreshes on tab refocus — §2 **Saved cases — refetch active cases on window focus**. Copy: **in-app tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone yet. **Needs attention** handling-request cards also show **Queue status: Awaiting internal triage.** (§2 **Saved cases — Needs attention for handling requested**). **Open case**, **Archive**, **Save label**, **search** / **filter** / **sort**, and **Load more** **unchanged**. **No** schema, migration, new API route, queue worker, **`justice_case_filings`** row, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Saved cases — handled/open handling triage note on All cases cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** — **copy** slice (with §2 **Saved cases — handling requested visibility on All cases cards**, **Saved cases — Mark acknowledged on All cases handling-request cards (`/justice/cases`)**, **Approved next action — centralized handled/open handling triage note copy**). In **`CaseApprovedNextActionTracking`**, when **`client_state.approved_next_action`** has **`handling_requested_at`**, no **`handling_acknowledged_at`**, and **`status === "completed"`** (**`showHandledOpenHandlingTriageNote`**), renders **`ApprovedNextActionHandlingHandledOpenTriageNote`** with variant **`inlineAck`** when **All cases** interactive acknowledge is enabled — §2 **Saved cases — Mark acknowledged on All cases handling-request cards (`/justice/cases`)**. Placed after **`ApprovedNextActionHandlingQueueStatusReadOnly`** and before **View in handling workbench** (§2 **Saved cases — View in handling workbench on All cases handling-request cards**) and the generic **in-app tracking only** disclaimer. **Hidden** when **`status !== "completed"`**, when **`handling_acknowledged_at`** is set, or when **`handling_requested_at`** is absent. **Does not** change **Needs attention** inclusion rules (**`isHandlingAwaitingTriageApprovedNextAction`**), **Mark acknowledged** on active handling **Needs attention** cards — §2 **Saved cases — acknowledge handling request from Needs attention**, **Open case**, **search** / **filter** / **sort**, APIs, persistence, workbench **Awaiting** / nested handled / **Acknowledged** membership, queue-status derivation, or hub/packet/plan/chat handled/open notes. **No** schema, migration, new API route, queue worker, **`justice_case_filings`**, timeline **type**, or external submission/contact/queue automation.
- **Saved cases — Mark acknowledged on All cases handling-request cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **behavior** slice (with §2 **Saved cases — handling requested visibility on All cases cards**, §2 **Saved cases — handled/open handling triage note on All cases cards**, §2 **Saved cases — acknowledge handling request from Needs attention**, §2 **Saved cases — record action handled on Needs attention handling-request cards**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). Closes the **Saved cases post-handled acknowledgment loop**: after **Record action handled for now** on **Needs attention → handling-request** cards (§2 **Saved cases — record action handled on Needs attention handling-request cards**) drops a case from **Needs attention**, the same case remains on **All cases** with **`handling_requested_at`**, **`status === "completed"`**, and no **`handling_acknowledged_at`**. In **`CaseApprovedNextActionTracking`** on **All cases** cards only, when that handled/open state applies, shows **`ApprovedNextActionHandlingHandledOpenTriageNote`** variant **`inlineAck`**, **Mark acknowledged**, and **`APPROVED_NEXT_ACTION_HANDLING_ACKNOWLEDGE_HELPER`** after **View in handling workbench** — reuses page-local **`acknowledgeHandlingRequest`** + **`acknowledgeHandlingRequestInApprovedNextAction`** + **`mergeClientStateWithAcknowledgedHandling`** + signed-in **UUID** **`GET` + merge + `PATCH`** **`client_state`** (same pattern as **Needs attention** handling-request cards — §2 **Saved cases — acknowledge handling request from Needs attention**), optimistic **`setCases`** via **`applyAcknowledgedHandlingToCaseRow`**, and **`writeSessionApprovedNextAction`** when **`sessionCaseId === caseRow.id`**. Preserves **`handling_requested_at`**, **`handling_request_note`**, **`status`**, **`completed_at`**, **`started_at`**, **`outcome_note`**, **`follow_up_*`**, **`label`**, **`href`**, **`approved_at`**, and other **`approved_next_action`** fields. After ack, queue status shows **Acknowledged for internal tracking triage only**; handled/open triage note and button hide. **Does not** change **`isHandlingAwaitingTriageApprovedNextAction`**, **Needs attention** builders/membership, request handling, note edit, record-handled on **Needs attention**, plan, packet, chat-ai, hub, handling workbench, routing, or **`CaseApprovedNextActionTracking`** when acknowledge props are not passed. **Internal triage tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior.
- **Saved cases — View in handling workbench on All cases handling-request cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **card-level navigation-only** slice (with §2 **Saved cases — handling requested visibility on All cases cards**, **Saved cases — handled/open handling triage note on All cases cards**, **Saved cases — View in handling workbench on Needs attention handling cards**, **Approval-to-action engine — approved packet action workbench link on All cases cards (`/justice/cases`)**, and **Handling workbench — internal triage surface**). In **`CaseApprovedNextActionTracking`**, when **`client_state.approved_next_action.handling_requested_at`** is set, a read-only **View in handling workbench** **`Link`** → **`/justice/handling`** (emerald **`text-xs`** underline styling, same as plan/chat) appears after queue status, optional handled/open triage note (§2 **Saved cases — handled/open handling triage note on All cases cards**), and **`ApprovedNextActionHandlingAcknowledgedReadOnly`**, and before the existing disclaimer. Includes **handled** (**`status === "completed"`**) + unacknowledged handling requests that are **not** in **Needs attention** or workbench **Awaiting** but may appear in the nested **Handled — open handling request** list (§2 **Handling workbench — read-only handled/open-request rows under Awaiting**). **No** **`STORAGE_CASE_ID`** activation or change on click; **no** query/hash/deep-link/filter; workbench remains the **full** handling-request list. Cards **without** **`handling_requested_at`** show **no** handling-request card-level workbench link (approved-packet-action link — §2 **Approval-to-action engine — approved packet action workbench link on All cases cards (`/justice/cases`)**). **Does not** change **`buildHandlingRequestedAttentionItems`** or **Needs attention** inclusion rules, workbench **Awaiting** / nested handled / **Acknowledged** membership, **Mark acknowledged** on **Needs attention** or active workbench **Awaiting** rows, queue-status derivation, **`handling_request_note`** display, page-level **Handling workbench** link (§2 **Saved cases — Start new case routing**), **Open case**, **search** / **filter** / **sort**, pagination, APIs, or external-action copy. **No** schema, migration, new API route, queue worker, **`justice_case_filings`** row, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, **`/api/submit-form`**, or new persistence; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Saved cases — handling tracking status on All cases cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **read-only sync** slice (with §2 **Saved cases — handling requested visibility on All cases cards**, §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**, §2 **Chat AI intake — handling tracking status in Current action tracking (`/justice/chat-ai`)**, §2 **Packet — handling tracking status in prepared-action handling UI (`/justice/packet`)**). On **All cases** cards only, when **`client_state.approved_next_action.handling_requested_at`** is set, **`CaseApprovedNextActionTracking`** shows read-only **Handling tracking:** after queue status — **Loading handling tracking context...** while existing per-case progress fetches are in flight; when ready, **Handling tracking: {derived next step}** from page-local **`deriveCasesManualActionNextStep`** / **`deriveCasesHandlingTrackingLine`** mirroring workbench **`deriveManualActionNextStep`** gate order (case row readiness, **`approved_next_action`** status/outcome/ack/follow-up, evidence count, **`hasFilingRecord`** / **`hasConfirmationOnFile`** parsed from already-fetched **`GET /api/justice/filings?case_id={id}`** rows — same meaning as workbench **`handlingCaseHasFilingRecord`** / **`handlingCaseHasConfirmationOnFile`**); when gates are satisfied, **Handling tracking: Tracking complete for now.** — parity with workbench **Tracking complete** queue membership — §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**. Nearby subcopy: **In-app tracking only — not filed or submitted.** **View in handling workbench** link — §2 **Saved cases — View in handling workbench on All cases handling-request cards** — **unchanged**. Reuses existing Saved cases list **`GET`** + evidence/filings/tasks progress fetches — **no** new **`GET`**, **`POST`**, **`PATCH`**, **`client_state`** fields, timeline writes, workers, or queues. **Does not** change **Needs attention** membership (**`isHandlingAwaitingTriageApprovedNextAction`**, **`buildHandlingRequestedAttentionItems`**, follow-up builders), acknowledge writes, **`/justice/handling`** workbench pages, hub, packet, plan, chat-ai, routing, or filing/submission/contact automation. **Read-only in-app sync only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, contacted anyone, or closed the real-world case. **No** schema, migration, new API route, queue worker, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**.
- **Saved cases — refetch active cases on window focus (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **data-refresh** slice (with §2 **Handling workbench — refetch active cases on window focus**; enabled by plan/chat **Mark acknowledged** — §2 **Plan — acknowledge handling request in Surrenderless handling UI**, **Chat AI intake — acknowledge handling request in Current action tracking**). Extracts existing signed-in **`GET /api/justice/cases`** load into shared **`loadCases(signal, listLimit?)`** (**unchanged** endpoint and **`parseJusticeCasesListEnvelope`**). **Initial mount** still calls **`loadCases`** once with **`CASES_PAGE_LIMIT`** when signed in (**unchanged**). On **`window` `focus`** and on **`document` `visibilitychange`** when **`visibilityState === "visible"`**, re-runs **`loadCases`**, replaces in-memory **`cases`** and **`has_more`** on success, and preserves existing load-error behavior (**`Could not load cases.`** + empty list on failure). Refetch uses **`offset=0`** and **`limit = Math.max(casesLengthRef.current, CASES_PAGE_LIMIT)`** so a user who used **Load more** does **not** shrink back to only the first page after refocus (still subject to API **`MAX_LIST_LIMIT`** cap). **`refreshSessionCaseIdFromStorage`** on the same events keeps **Current case in this browser** aligned. **Purpose:** when the user returns to an open **Saved cases** tab after **Mark acknowledged** on **`/justice/plan`** or **`/justice/chat-ai`**, **Needs attention**, **All cases** handling blocks (**`CaseApprovedNextActionTracking`**), **display-only** queue status, and handled/open triage notes (§2 **Saved cases — handled/open handling triage note on All cases cards**) rebucket from fresh **`client_state`** without a full page reload. **Cases-local Mark acknowledged** still updates **`cases`** immediately via **`applyAcknowledgedHandlingToCaseRow`** — refocus refetch is additive, not required for on-page ack. **Does not** change **Needs attention** inclusion (**`isHandlingAwaitingTriageApprovedNextAction`**), **Mark acknowledged** on active handling **Needs attention** cards, **Open case**, **search** / **filter** / **sort**, **Load more** append semantics, archive/labels, workbench **Awaiting** / nested handled / **Acknowledged** membership, APIs, schema, persistence, or external-action behavior. **No** migration, worker, **`justice_case_filings`**, timeline **type**, or external submission/contact/queue automation; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — internal triage surface (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** + **`src/lib/justice/approvedNextActionState.ts`** — **operational** in-app surface toward §1 **submit/queue/track** ownership using **existing** saved-case data only (**no** new schema, migration, API route, queue worker, **`justice_case_filings`** row, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**). Signed-in users load active saved cases via existing paginated **`GET /api/justice/cases`** (page through **`has_more`**). **Approved packet actions** (packet approved without **`handling_requested_at`**) appear in a separate section **above** triage buckets — §2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)** — with user-controlled **started** / **completed** status writes on **`ApprovedPacketActionCaseCard`** only — §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)** — **not** Surrenderless handling requests. **Two read-only triage sections:** **Awaiting internal triage** — same **active-case** rule as **Saved cases** **Needs attention** via **`isHandlingAwaitingTriageApprovedNextAction`** (§2 **Approved next action — shared handling awaiting-triage membership predicate**, **Saved cases — Needs attention for handling requested**, **Handling workbench — align Awaiting internal triage with Saved cases Needs attention**); **Acknowledged** — **`handling_requested_at`** and **`handling_acknowledged_at`** both present (any **`status`**). Each card shows case **label** or **company**, **product/service**, **approved next action** status + **label**, **Surrenderless handling requested**, request recorded time, optional **`handling_request_note`**, **display-only** queue status (**`ApprovedNextActionHandlingQueueStatusReadOnly`** / **`deriveHandlingQueueDisplayStatus`**), acknowledgement timestamp line when set (**`ApprovedNextActionHandlingAcknowledgedReadOnly`**), and honest **in-app tracking only** disclaimer — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone. **Per-card navigation (session-first):** **Open action plan**, **Open case packet**, **Update in chat**, and **Open approved step** (when **`approved_next_action.href`** is an internal **`/justice/*`** route and **not** only **`/justice/plan`**) each call **`activateCaseInSession`** first (**`STORAGE_CASE_ID`**, **`STORAGE_INTAKE`**, timeline, **`hydrateApprovedNextActionForDisplay`** + **`writeSessionApprovedNextAction`**) — copy **Opens this case in your browser session first.** — then **`router.push`**: **Open action plan** → **`/justice/plan`**; **Open case packet** → **`/justice/packet`** (same selected case); **Update in chat** → **`/justice/chat-ai`** (loaded-case recap / **Current action tracking**, **not** fresh intake); **Open approved step** → stored internal **`href`**. **Acknowledged** cards include the same navigation buttons **without** **Mark acknowledged**. **Mark acknowledged** on **Awaiting internal triage** only uses the same **`acknowledgeHandlingRequestInApprovedNextAction`** + **`mergeClientStateWithAcknowledgedHandling`** + **`GET` + merge + `PATCH`** pattern as **`/justice/cases`** Needs attention (§2 **Saved cases — acknowledge handling request from Needs attention**). **No** global header link yet; entry from **Saved cases** **Handling workbench** link. **`/justice/cases`** Needs attention / **All cases** behavior **unchanged**. **No** schema, API, worker, filing, timeline, external queue, submission, contact, or automation behavior added by navigation. **Bidirectional loop:** **`/justice`** (hub), **`/justice/plan`**, **`/justice/chat-ai`**, **`/justice/packet`**, **`/justice/cases`** **Needs attention** handling-request cards, and **`/justice/cases`** **All cases** **`CaseApprovedNextActionTracking`** blocks show **View in handling workbench** → **`/justice/handling`** when handling is requested (active-case surfaces when the active case has **`handling_requested_at`**; **Saved cases** on per-card **Needs attention** rows — §2 **Saved cases — View in handling workbench on Needs attention handling cards**; **All cases** per-card handling block — §2 **Saved cases — View in handling workbench on All cases handling-request cards**; **Handling workbench — active-case back-link from hub**, **Handling workbench — active-case back-link from plan and chat-ai**, **Handling workbench — active-case back-link from packet**).
- **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only manual-handling context** slice (with §2 **Handling workbench — internal triage surface (`/justice/handling`)**, §2 **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — submission draft reviewed read-only on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**, §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)**, §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). On **`HandlingWorkbenchCaseCard`** handling-request triage cards only (**not** **`ApprovedPacketActionCaseCard`**), when **`item.next.status !== "completed"`**, after handling request / queue / acknowledge UI and **before** the existing **in-app tracking only** disclaimer, shows read-only truncated **`outcome_note`** (page-local **`truncateAttentionNote`**, max **200**), **Follow-up needed** when **`follow_up_needed === true`**, and **`ApprovedNextActionFollowUpTimingLine`** when **`follow_up_at`** is set — from existing **`item.next`** on workbench list **`GET /api/justice/cases`** **`client_state.approved_next_action`** via **`parseApprovedNextActionFromClientState`** (**no** new **`GET`** or **`PATCH`**). When **`status === "completed"`**, outcome/follow-up write form — §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**. Complements outcome/follow-up writes from §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)** / §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)** / §2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)** and browse display on §2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)** / §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**. **Read-only when not completed** — **no** outcome writes except via §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**. **Does not** change **`buildHandlingWorkbenchItems`**, **`isHandlingAwaitingTriageApprovedNextAction`**, **Needs attention** builders, approved-packet inbox rules, acknowledge **`PATCH`** behavior, **`handling_request_note`** edit rules, **Clear follow-up**, packet, plan, hub, cases, chat, or routing. **Display correctness only** when read-only — **no** new API route, schema/migration, DB field, timeline **type**, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **behavior** slice (with §2 **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — internal triage surface (`/justice/handling`)**, §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**, §2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)**, §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), when **`item.next.status === "completed"`**, shows compact **Record outcome / follow-up** form (**outcome / note** textarea, **Follow-up needed** checkbox, optional **follow-up date (your pace)**, **Save tracking note**) after handling request / queue / acknowledge UI and **before** the existing **in-app tracking only** disclaimer — replaces read-only outcome lines for completed cards (§2 **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`)**). Persists **`outcome_note`**, **`follow_up_needed`**, and **`follow_up_at`** on existing **`approved_next_action`** via **`mergeApprovedNextActionTrackingFields`**, **`mergeClientStateWithApprovedNextAction`**, optimistic **`setCases`** + **`writeSessionApprovedNextAction`**, and signed-in **UUID** **`GET` + merge + `PATCH`** **`/api/justice/cases/[id]`** **`client_state`** — same pattern as workbench **Mark acknowledged**. Copy: **Tracking only — not automatic filing or submission.** Clear follow-up when **`follow_up_needed`** — §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**. **Does not** edit **`handling_request_note`**, change **`isHandlingAwaitingTriageApprovedNextAction`**, **Needs attention** membership predicates, approved-packet inbox rules, acknowledge **`PATCH`** semantics, routing, or nested **Mark acknowledged** behavior. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **behavior** slice (with §2 **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**, §2 **Saved cases — Needs attention from approved next action follow-up (`/justice/cases`)**, §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), when **`item.next.follow_up_needed === true`**, shows secondary **Mark follow-up handled** below the outcome form/read-only outcome block and **before** disclaimer/nav, with helper **Clears this from Saved cases Needs attention. Outcome notes and dates stay saved. Tracking only — not automatic filing or submission.** Persists via **`clearFollowUpFromApprovedNextAction`**, **`mergeClientStateWithClearedFollowUp`**, optimistic **`setCases`** + **`writeSessionApprovedNextAction`**, and signed-in **UUID** **`GET` + merge + `PATCH`** **`/api/justice/cases/[id]`** **`client_state`** — same pattern as §2 **Saved cases — Needs attention from approved next action follow-up (`/justice/cases`)** **Clear follow-up** / §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)** **Mark follow-up handled**. Removes **`follow_up_needed`** only; preserves **`outcome_note`** and **`follow_up_at`**. **Does not** edit **`handling_request_note`**, change **`buildHandlingWorkbenchItems`**, **`isHandlingAwaitingTriageApprovedNextAction`**, handling-request **Needs attention** builders, acknowledge **`PATCH`** semantics, routing, or outcome write form semantics. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **behavior** slice (with §2 **Handling workbench — internal triage surface (`/justice/handling`)**, §2 **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**, §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**, §2 **Approved next action completed / handled (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), when **`item.next.status === "started"`**, shows **Opened for next step.** + secondary **Record action handled for now** after handling request / queue / acknowledge UI and **before** outcome form/read-only outcome block, with helper **Tracking only — not automatic filing or submission.** Persists **`status: "completed"`** and **`completed_at`** on existing **`approved_next_action`** via **`mergeApprovedNextActionTrackingFields`** (preserves **`handling_requested_at`** and **`handling_acknowledged_at`**), **`mergeClientStateWithApprovedNextAction`**, optimistic **`setCases`** + **`writeSessionApprovedNextAction`**, and signed-in **UUID** **`GET` + merge + `PATCH`** **`/api/justice/cases/[id]`** **`client_state`** — same pattern as **`ApprovedPacketActionCaseCard`** **Record action handled for now** (§2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**) and §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**. Requires **`status === "started"`** — §2 **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`)** when **`status === "approved"`**. After save, card may move from **Awaiting internal triage** to nested **Handled — open handling request** (when unacknowledged) and outcome/follow-up write form becomes available — §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**; clear follow-up when **`follow_up_needed`** — §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**. **Does not** change **`ApprovedPacketActionCaseCard`** membership rules, **`isHandlingAwaitingTriageApprovedNextAction`**, handling-request **Needs attention** builders, acknowledge **`PATCH`** semantics, **`handling_request_note`** edit rules, or routing. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **behavior** slice (with §2 **Handling workbench — internal triage surface (`/justice/handling`)**, §2 **Handling workbench — Open approved step visibility on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**, §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**, §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**, §2 **Packet — Open approved step marks started tracking (`/justice/packet`)**, §2 **Approved next action started (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), when **`item.next.status === "approved"`** and an internal approved-step **`href`** exists, **Open approved step** persists **`status: "started"`** and **`started_at`** (preserves **`handling_requested_at`** and **`handling_acknowledged_at`** via **`mergeApprovedNextActionTrackingFields`**) via existing page-local **`openApprovedPacketStep`** / **`persistApprovedPacketNextAction`** + optimistic **`setCases`** + **`writeSessionApprovedNextAction`** + signed-in **UUID** **`GET` + merge + `PATCH`** **`/api/justice/cases/[id]`** **`client_state`**, then navigates to the stored internal **`href`** — same started persist path as **`ApprovedPacketActionCaseCard`** (§2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**) and §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**. Button shows **Saving…** while persisting; helper when **`status === "approved"`**: **Tracking only — not automatic filing or submission.** Hidden once **`status`** is **`"started"`** or **`"completed"`** — §2 **Handling workbench — Open approved step visibility on handling-request triage cards (`/justice/handling`)**. Unlocks on-card **Record action handled for now** — §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**; then outcome/follow-up write — §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**; clear follow-up when **`follow_up_needed`** — §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**. **Does not** change **`ApprovedPacketActionCaseCard`** membership rules, **`isHandlingAwaitingTriageApprovedNextAction`**, handling-request **Needs attention** builders, acknowledge **`PATCH`** semantics, **`handling_request_note`** edit rules, or other nav button semantics. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — Open approved step visibility on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **UI/visibility** slice (with §2 **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — internal triage surface (`/justice/handling`)**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), **Open approved step** and its **Tracking only — not automatic filing or submission.** helper are shown only while **`item.next.status === "approved"`** and an internal approved-step **`href`** exists — hidden once the action is **`"started"`** or **`"completed"`** so operators are not prompted to re-open after §2 **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`)**. Started cards use **Opened for next step.** + **Record action handled for now** — §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**; completed cards use outcome/follow-up write — §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)** and clear follow-up when **`follow_up_needed`** — §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**. Other session-first nav (**Open action plan**, **Open case packet**, **Update in chat**) unchanged. **Does not** change started persist behavior when **`status === "approved"`**, **`ApprovedPacketActionCaseCard`**, membership predicates, acknowledge **`PATCH`**, outcome/clear-follow-up writes, or routing except hiding the redundant button after started/completed. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only manual-handling handoff context** slice (with §2 **Handling workbench — internal triage surface (`/justice/handling`)**, §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**, §2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**, §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), after the approved-action status line and **before** handling-request UI: when **`item.next.status === "completed"`** and **`completed_at`** is set, read-only **Handled for now {formatted time}** via existing **`formatApprovedNextActionHandlingTimestamp`**; when **`status === "started"`** and **`started_at`** is set, read-only **Opened {formatted time}** alongside **Opened for next step.** — §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**; packet-approved and draft-reviewed signals — §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)** (**single source** after §2 **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`)**). **Awaiting internal triage** section helpers updated so handled-open cases are directed to nested **Handled — open handling request** and workbench **Mark acknowledged** (§2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**), not plan/chat-only. **Does not** change **`isHandlingAwaitingTriageApprovedNextAction`**, **Needs attention** membership, approved-packet inbox rules, acknowledge **`PATCH`** semantics, **`handling_request_note`** edit rules, outcome/follow-up fields, plan/chat/cases/hub/packet routing, or nested **Mark acknowledged** behavior. **Display correctness only** — **no** new API route, schema/migration, DB field, timeline **type**, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — submission draft reviewed read-only on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only manual-handling handoff context** slice (with §2 **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`)**, §2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**, §2 **Saved cases — compact readiness checklist on All cases cards (`/justice/cases`)**, §2 **Preview → plan review tracking**). Originally shipped standalone **Submission draft reviewed** / **Submission draft not reviewed yet** lines on **`HandlingWorkbenchCaseCard`** using page-local **`caseDraftReviewed`** (**`SUBMISSION_DRAFT_REVIEWED_TIMELINE_ID`** from **`timeline.ts`** — same pattern as **`cases/page.tsx`**). Standalone lines **removed** — draft-reviewed signal now shown only inside §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)** checklist (**single source** after §2 **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`)**). Read-only review context only — **not** filed or submitted. **Does not** change **`isHandlingAwaitingTriageApprovedNextAction`**, **Needs attention** membership, approved-packet inbox rules, acknowledge **`PATCH`** semantics, **`handling_request_note`** edit rules, outcome/follow-up fields, routing, or nested **Mark acknowledged** behavior. **Does not** write timeline events, call **`POST /api/justice/submission-draft-reviewed`**, or add new API/schema/worker/queue/submission/filing/automation. **Display correctness only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only manual-handling handoff context** slice (with §2 **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — submission draft reviewed read-only on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`)**, §2 **Saved cases — compact readiness checklist on All cases cards (`/justice/cases`)**, §2 **Plan — Case readiness checklist (`/justice/plan`)**, **`src/lib/justice/caseReadiness.ts`**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), after the approved-action status / **Handled for now** lines and **before** handling-request UI, shows compact read-only **Manual review readiness (approval funnel)** with three lines — **Basic case info: yes / not yet** via **`isBasicCaseInfoReadyForEscalation(caseRow.intake)`**; **Submission draft reviewed: yes / not yet** via existing **`caseDraftReviewed`**; **Prepared case packet approved: yes / not yet** via **`parseJusticeCaseClientState(caseRow.client_state).prepared_packet_approved`** — all from already-loaded workbench list **`GET /api/justice/cases`** (**`intake`**, **`timeline`**, **`client_state`** only; **no** new **`GET`** or **`PATCH`**). Rollup **Ready for manual review** when all three are true; otherwise **Needs more before manual review**. Subcopy **Read-only — not filed or submitted. Evidence is not shown here.** — evidence is **intentionally omitted** from this slice (no cases-style progress fetch). **Single source** for packet-approved and draft-reviewed signals after §2 **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`)**. **Does not** change **`isHandlingAwaitingTriageApprovedNextAction`**, **Needs attention** membership, approved-packet inbox rules, acknowledge **`PATCH`** semantics, **`handling_request_note`** edit rules, outcome/follow-up fields, routing, or nested **Mark acknowledged** behavior. **Display correctness only** — **no** new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — saved manual filing records read-only on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only operator visibility** slice (with §2 **Handling workbench — internal triage surface (`/justice/handling`)**, §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)**, §2 **Saved filings — plan preview**, §2 **Packet — filing records (`JusticeFilingRecords`)**, **`@/lib/justice/filings`** **`JusticeCaseFilingRow`**). After workbench list **`GET /api/justice/cases`** load, page-local **`useEffect`** fetches existing packet filing rows **only** for handling-request case ids (**`allHandlingItems`** — **not** **`approvedPacketActionItems`**) via existing **`GET /api/justice/filings?case_id={id}`** per id (**no** new route, schema, or **`POST` / `PATCH` / `DELETE`** from workbench). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), after approval-funnel readiness and **before** handling-request UI: when rows exist, collapsed read-only **Saved manual filings** block (**destination**, optional **filed date**, truncated **confirmation** / **notes** — **intentionally omits `filing_url`**; same preview pattern as §2 **Saved filings — plan preview**). When approval-funnel rollup is **Ready for manual review** and no rows exist after fetch, honest line **Manual filing not recorded yet. Add or edit filing records from the case packet after external submission.** Subcopy under **Open case packet** (when shown): **Manual filing records are added on the packet. Surrenderless does not file or submit automatically.** **Open case packet** remains the write path — **`/justice/packet`** **`JusticeFilingRecords`** unchanged. **No** inline filing form on workbench; **no** filing **`POST` / `PATCH`** from workbench. **Does not** change **`ApprovedPacketActionCaseCard`**, filing CRUD, packet filing forms, plan, chat-ai, packet, hub, cases, routing, APIs, schema, timeline writes, workers, queues, filing automation, submission automation, or external action behavior. **Read-only visibility into existing packet filing records** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — saved proof/evidence readiness on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only pre-action readiness** slice (with §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — saved manual filing records read-only on handling-request triage cards (`/justice/handling`)**, §2 **Saved cases — compact readiness checklist on All cases cards (`/justice/cases`)**, §2 **Plan — saved-evidence preview**, §2 **Chat AI intake — saved evidence count in Recap (Proof / evidence panel)**). Extends the existing handling-request per-case fetch (**`allHandlingItems`** only — **not** **`approvedPacketActionItems`**) to **`Promise.all`** existing **`GET /api/justice/filings?case_id={id}`** + **`GET /api/justice/evidence?case_id={id}`** per id — **evidence count only** (array length; **no** full evidence rows rendered). Stores **`evidenceCountByCaseId`**. On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), in **Manual review readiness (approval funnel)** checklist: read-only **Evidence on case: yes ({N} saved proof note(s))** or **Evidence on case: not yet** — **separate** from **Ready for manual review** / **Needs more before manual review** rollup (unchanged: basics + draft reviewed + packet approved only). When **Ready for manual review**, fetch complete, and count **0**, honest line **No saved proof on case yet. Review evidence on the case packet before external manual action.** Checklist subcopy updated to **read-only metadata only — not filed or submitted** (proof note titles/details not shown). **No** inline evidence form; **no** evidence **`POST` / `PATCH` / `DELETE`** from workbench — evidence writes remain on **`/justice/packet`**, **`/justice/evidence`**, and chat-ai proof panels. **Does not** change **`ApprovedPacketActionCaseCard`**, evidence CRUD, filing CRUD, packet forms, plan, chat-ai, packet, hub, cases, routing, APIs, schema, timeline writes, workers, queues, filing automation, submission automation, or external action behavior. **Read-only metadata for pre-action readiness** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — case handoff summary on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only operator visibility** slice (with §2 **Handling workbench — saved proof/evidence readiness on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — saved manual filing records read-only on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)**, §2 **Packet — prepared action review framing (`/justice/packet`)**, §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), after readiness/proof/filing blocks and **before** handling-request queue controls, collapsed read-only **Case handoff summary** (`<details>`) from already-loaded workbench list **`GET /api/justice/cases`** data only (**`intake`**, **`approved_next_action.label`** — **no** new **`GET`**, **`POST`**, **`PATCH`**, destination links, or external-site links). Metadata-only lines when present: **Company** (only when not already the card title), humanized **Issue category** from **`problem_category`**, **Product / service** (only when not already shown on the card), truncated **Story** snippet (~**200** chars via page-local **`truncateAttentionNote`**), **Next step** from **`item.next.label`**. Subcopy **Read-only summary — not filed or submitted. Open case packet for the full bundle.** — full complaint packet, copy/download/print, evidence rows, filing CRUD, and prepared-action bundle remain on **`/justice/packet`** (§2 **Packet — prepared action review framing (`/justice/packet`)**). **No** copy/download/print on workbench; **no** inline edits. **Does not** change **`ApprovedPacketActionCaseCard`**, evidence CRUD, filing CRUD, packet forms, plan, chat-ai, packet, hub, cases, routing, APIs, schema, timeline writes, workers, queues, filing automation, submission automation, or external action behavior. **Operator visibility only** — Surrenderless has **not** prepared, filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — internal operator note on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** + **`src/lib/justice/types.ts`** + **`src/lib/justice/approvedNextActionState.ts`** — **workbench-only internal triage note** slice (with §2 **Handling workbench — internal triage surface (`/justice/handling`)**, §2 **Handling workbench — case handoff summary on handling-request triage cards (`/justice/handling`)**, §2 **Saved cases — acknowledge handling request from Needs attention (`/justice/cases`)**). Adds optional **`handling_operator_note`** on existing **`client_state.approved_next_action`** JSON (**`JusticeApprovedNextAction`** — **no** new API route, DB migration, or timeline write). **`approvedNextActionState.ts`**: parse, **`applyHandlingOperatorNoteToApprovedNextAction`**, preserve through **`mergeApprovedNextActionTrackingFields`**, **`mergeClientStateWithApprovedNextAction`**, **`resolveApprovedNextAction`**, and **`mergeApprovedNextActionForHydrate`** alongside existing fields (**`status`**, **`started_at`**, **`completed_at`**, **`outcome_note`**, **`follow_up_*`**, **`label`**, **`href`**, **`approved_at`**, **`handling_requested_at`**, **`handling_request_note`**, **`handling_acknowledged_at`**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), after user **`handling_request_note`** read-only display and **before** queue status: read-only **Internal operator note** when set; compact **Save internal note** form when absent or **Edit internal note**; persists via signed-in **UUID** **`GET` + merge + `PATCH`** **`/api/justice/cases/[id]`** + optimistic **`setCases`** + **`writeSessionApprovedNextAction`** when active browser case — same pattern as outcome/acknowledge writes. Copy: **Internal triage only — not filed, submitted, or sent to the consumer.** — **distinct from user-facing `handling_request_note`**; **does not** reuse **`handling_request_note`** for operator notes. **Not** surfaced on plan, chat-ai, packet, hub, or cases in this slice. **Does not** change **`ApprovedPacketActionCaseCard`**, evidence CRUD, filing CRUD, packet forms, routing, workers, queues, filing automation, submission automation, or external action behavior. **Internal operator tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted the consumer.
- **Handling workbench — external manual action readiness on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only operator prep context** slice (with §2 **Handling workbench — saved proof/evidence readiness on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — saved manual filing records read-only on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — case handoff summary on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)**, §2 **Plan — prepared next action after draft review (`/justice/plan`)**, §2 **Packet — prepared action review framing (`/justice/packet`)**, **`src/lib/justice/rules.ts`** **`computeJusticeDestinations`**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), after **Case handoff summary** and **before** handling-request queue controls, when per-case fetch is complete (**`filingsReady`**): read-only **Ready for external manual action: yes / not yet** from page-local **`readyForExternalManualAction = readyForManualReview && (evidenceCount ?? 0) > 0`** — **separate** from unchanged **`readyForManualReview`** rollup (basics + draft reviewed + packet approved only). When **`readyForExternalManualAction`**, collapsed read-only **Suggested manual channels** lists first **1–3** **`status === "recommended"`** destination **labels** from **`computeJusticeDestinations(caseRow.intake, { manualFtc: ftcUnlockedFromIntake(caseRow.intake), useCompanyContactLabels: true })`** — **label strings only**; **no** **`href`**, **`internalRoute`**, links, buttons, or navigation; block omitted when no recommended destinations. Subcopy **Read-only — Surrenderless does not file or submit automatically. Use Open case packet or Open approved step for prep surfaces.** — prep/full bundle remain on **`/justice/packet`** (§2 **Packet — prepared action review framing (`/justice/packet`)**). **No** new **`GET`**, **`POST`**, **`PATCH`**, or **`client_state`** fields. **Does not** change **`ApprovedPacketActionCaseCard`**, evidence CRUD, filing CRUD, packet forms, plan, chat-ai, packet, hub, cases, routing, APIs, schema, timeline writes, workers, queues, filing automation, submission automation, or external action behavior. **Read-only operator prep context only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — filing confirmation tracking on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only post-external-action operator tracking** slice (with §2 **Handling workbench — saved manual filing records read-only on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — saved proof/evidence readiness on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — external manual action readiness on handling-request triage cards (`/justice/handling`)**, §2 **Saved filings — plan preview**, §2 **Packet — filing records (`JusticeFilingRecords`)**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), after saved filings / external-action readiness and **before** handling-request queue controls, when per-case fetch is complete (**`filingsReady`**): compact read-only rollup from existing **`savedFilings`** — **Manual filing recorded: yes / not yet** (**`hasFilingRecord`**), **Confirmation on file: yes / not yet** (**`hasConfirmationOnFile`** from **`confirmation_number`** on any row); collapsed **Saved manual filings** preview remains the detail view. When **`item.next.status === "started"`** or **`"completed"`**, honest nudges: no filing → **Manual filing not recorded yet. Add filing records from the case packet after external submission.**; filing without confirmation → **No confirmation number on file yet. Add or edit filing records from the case packet after external submission.** Subcopy **Read-only tracking — not filed or submitted. Filing and confirmation writes stay on the case packet.** — **`/justice/packet`** **`JusticeFilingRecords`** unchanged. **No** new **`GET`**, **`POST`**, **`PATCH`**, or **`client_state`** fields; **no** **`filing_url`**; **no** inline filing form. **Does not** change **`ApprovedPacketActionCaseCard`**, evidence CRUD, filing CRUD, packet forms, plan, chat-ai, packet, hub, cases, routing, APIs, schema, timeline writes, workers, queues, filing automation, submission automation, or external action behavior. **Read-only operator tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only progress and next-step guidance** slice (with §2 **Handling workbench — saved proof/evidence readiness on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — saved manual filing records read-only on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — external manual action readiness on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — filing confirmation tracking on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), after filing confirmation rollup and **before** handling-request queue controls, collapsed read-only **Manual action progress** (`<details>`) from existing workbench list + per-case filing/evidence fetch — compact yes/not yet lines: **Ready for manual review**, **Ready for external manual action** (when **`filingsReady`**), **Action opened**, **Manual filing recorded** / **Confirmation on file** (when **`filingsReady`**), **Outcome recorded** (when **`status === "completed"`**), **Handling acknowledged** (when **`handling_requested_at`**); page-local **`deriveManualActionNextStep`** yields one plain-text **Next step:** from first incomplete lifecycle step (prep → open approved step → filing → confirmation → outcome → acknowledge → **Tracking complete for now**). **No** new buttons, external links, **`GET`**, **`POST`**, **`PATCH`**, or **`client_state`** fields. Existing filing preview, external-action readiness, confirmation rollup, outcome form, and acknowledgement controls **unchanged**. Subcopy **Read-only progress — not filed or submitted. Writes stay on the case packet, approved step, and existing workbench actions.** **Does not** change **`ApprovedPacketActionCaseCard`**, evidence CRUD, filing CRUD, packet forms, plan, chat-ai, packet, hub, cases, routing, APIs, schema, timeline writes, workers, queues, filing automation, submission automation, or external action behavior. **Read-only progress and next-step guidance only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — follow-up carry-forward on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only workbench queue visibility** slice (with §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**, §2 **Saved cases — Needs attention from approved next action follow-up (`/justice/cases`)**, §2 **Approved next action follow-up timing**). Page-level **`followUpHandlingItems`** from existing **`allHandlingItems`** where **`follow_up_needed === true`**, sorted by **`follow_up_at`** ascending (missing dates last; same tie-break as Saved cases follow-up attention). New **Follow-up tracking** section after **Acknowledged** — count, subcopy **User-paced follow-up tracking only — not automatic contact.**, reuses **`HandlingWorkbenchCaseCard`** with existing list + per-case filing/evidence fetch props; cards may also appear under **Acknowledged** (no dedupe). On **`HandlingWorkbenchCaseCard`**, extends **Manual action progress** with **Follow-up open: yes / not yet** when **`follow_up_needed === true`** or **`status === "completed"`**, optional **`ApprovedNextActionFollowUpTimingLine`** when **`follow_up_needed`**; extends **`deriveManualActionNextStep`** so after outcome/ack branches, open follow-up → **Review follow-up timing and mark follow-up handled when complete.** before **Tracking complete for now.** Existing outcome form and **Mark follow-up handled** (**`clearFollowUpFromApprovedNextAction`**) **unchanged** — **no** new write paths. **No** new **`GET`**, **`POST`**, **`PATCH`**, or **`client_state`** fields. **Does not** change **`ApprovedPacketActionCaseCard`**, packet, plan, chat-ai, cases, hub, routing, APIs, schema, timeline writes, workers, queues, filing automation, submission automation, or external action behavior. **Read-only workbench queue visibility plus existing follow-up clear write only** — Surrenderless has **not** contacted, filed, submitted, sent, **queued externally**, or auto-chased follow-ups.
- **Handling workbench — operator handoff queue tiers on Awaiting internal triage (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only queue ordering** slice (with §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — saved proof/evidence readiness on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — external manual action readiness on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — internal triage surface (`/justice/handling`)**, §2 **Saved cases — Needs attention for handling requested (`/justice/cases`)**). On **Awaiting internal triage** handling-request section only (**not** **`HandlingWorkbenchCaseCard`** internals, **`ApprovedPacketActionCaseCard`**, **Handled — open handling request**, **Acknowledged**, or **Follow-up tracking**): page-local **`awaitingHandoffTiers`** from existing **`awaitingItems`** + **`filingsByCaseId`** / **`evidenceCountByCaseId`** when **`filingsReady`** — nested sublists with counts in tier order: **Ready for external manual action** (**`readyForExternalManualAction`**), **Needs saved proof before external action** (**`readyForManualReview`** + **`evidenceCount === 0`**), **Needs prep before manual review** (**`!readyForManualReview`**), **Other awaiting handling requests** (catch-all); within each tier **`sortByHandlingRequestedAtDesc`**. When **`!filingsReady`**, flat list unchanged + **Loading proof and filing context for handoff prioritization…** — no premature external-ready/proof tiers. Subcopy **Read-only prioritization for operator handoff — not automatic filing, submission, or contact.** **No** new badges, card actions, **`GET`**, **`POST`**, **`PATCH`**, or **`client_state`** fields. **Does not** change **`HandlingWorkbenchCaseCard`** blocks, manual action progress, follow-up tracking, handled-open/acknowledged sections, existing writes, packet, plan, chat-ai, cases, hub, routing, APIs, schema, timeline writes, workers, queues, filing automation, submission automation, or external action behavior. **Read-only queue ordering only** — Surrenderless has **not** filed, submitted, contacted, or auto-prioritized external action.
- **Handling workbench — confirmation follow-up queue on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only post-external-action queue ordering** slice (with §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — filing confirmation tracking on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — saved manual filing records read-only on handling-request triage cards (`/justice/handling`)**, §2 **Packet — filing records (`JusticeFilingRecords`)**). Page-level **`postExternalConfirmationTiers`** from existing **`allHandlingItems`** + **`filingsByCaseId`** when **`filingsReady`** — includes **`status === "started"`** or **`"completed"`** with no saved manual filing and/or no confirmation on file; nested sublists with counts: **Manual filing not recorded yet**, **Confirmation not on file yet**; within each **`sortByHandlingRequestedAtDesc`**. New **Confirmation follow-up** section before **Follow-up tracking** — count, subcopy **Read-only tracking after external manual action — not filed or submitted. Filing and confirmation writes stay on the case packet.** — reuses **`HandlingWorkbenchCaseCard`** with existing props; cards may duplicate across sections (no dedupe). When **`!filingsReady`**, **Loading filing context for confirmation follow-up…** — no premature tiers. **No** new **`GET`**, **`POST`**, **`PATCH`**, **`client_state`** fields, badges, or card actions. **Does not** change **`HandlingWorkbenchCaseCard`** internals, operator handoff tiers, manual action progress, confirmation rollup on cards, follow-up tracking, handled-open/acknowledged sections, existing writes, packet, plan, chat-ai, cases, hub, routing, APIs, schema, timeline writes, workers, queues, filing automation, submission automation, or external action behavior. **Read-only queue ordering only** — Surrenderless has **not** filed, submitted, contacted, or auto-chased confirmations.
- **Handling workbench — outcome closure queue on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only closure queue visibility** slice (with §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**, §2 **Handling workbench — confirmation follow-up queue on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — follow-up carry-forward on handling-request triage cards (`/justice/handling`)**). Page-level **`outcomeClosureTiers`** from existing **`allHandlingItems`** — nested sublists with counts: **Outcome not recorded yet** (**`status === "completed"`** + no **`outcome_note`**), **Handling request not acknowledged yet** (**`status === "completed"`** + **`outcome_note`** + **`handling_requested_at`** + no **`handling_acknowledged_at`**); within each **`sortByHandlingRequestedAtDesc`**. New **Outcome closure** section after **Confirmation follow-up**, before **Follow-up tracking** — count, subcopy **Read-only closure tracking — not filed or submitted. Outcome and acknowledgement writes stay on existing workbench actions.** — reuses **`renderHandlingWorkbenchCaseCard`**; cards may duplicate across sections (no dedupe). Nested **Handled — open handling request**, outcome form, and **Mark acknowledged** **unchanged**. **No** new **`GET`**, **`POST`**, **`PATCH`**, **`client_state`** fields, badges, or card actions. **Does not** change **`HandlingWorkbenchCaseCard`** internals, operator handoff tiers, confirmation follow-up, follow-up tracking, packet, plan, chat-ai, cases, hub, routing, APIs, schema, timeline writes, workers, queues, filing automation, submission automation, or external action behavior. **Read-only queue visibility only** — Surrenderless has **not** filed, submitted, contacted, or auto-closed cases.
- **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **read-only carried-to-completion visibility** slice (with §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — confirmation follow-up queue on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome closure queue on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — follow-up carry-forward on handling-request triage cards (`/justice/handling`)**). Page-level **`trackingCompleteHandlingItems`** from existing **`allHandlingItems`** + **`filingsByCaseId`** / **`evidenceCountByCaseId`** when **`filingsReady`** — includes items where page-local **`deriveHandlingManualActionNextStepForItem`** (same inputs as **`HandlingWorkbenchCaseCard`**) yields **`deriveManualActionNextStep`** → **Tracking complete for now.**; **`sortByHandlingRequestedAtDesc`**. New **Tracking complete** section after **Follow-up tracking** — count, subcopy **Read-only — in-app handling tracking caught up for now; not filed or submitted. Reopen a case if external follow-up or new action is needed.** — reuses **`renderHandlingWorkbenchCaseCard`**; cards may duplicate across sections (no dedupe). When **`!filingsReady`**, **Loading proof and filing context for tracking-complete list…** — no premature complete assignment. **Tracking complete** means in-app handling tracking is caught up for now — **not** that Surrenderless filed, submitted, contacted, or that the real-world case is closed. **No** new **`GET`**, **`POST`**, **`PATCH`**, **`client_state`** fields, badges, or card actions. **Does not** change **`HandlingWorkbenchCaseCard`** internals, operator handoff tiers, confirmation/outcome/follow-up queues, packet, plan, chat-ai, cases, hub, routing, APIs, schema, timeline writes, workers, queues, filing automation, submission automation, or external action behavior. **Read-only visibility only**.
- **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **display-only** slice (with §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — submission draft reviewed read-only on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — internal triage surface (`/justice/handling`)**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), removes redundant standalone **Packet approved for next action** and standalone **Submission draft reviewed** / **Submission draft not reviewed yet** lines that duplicated §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)** checklist rows — **Manual review readiness (approval funnel)** remains the **single source** for **Basic case info**, **Submission draft reviewed**, **Prepared case packet approved**, and **Ready for manual review** / **Needs more before manual review** rollup from existing list **`GET /api/justice/cases`** data only. **Does not** change behavior, persistence, membership predicates, acknowledge **`PATCH`**, **Open approved step**, record handled, outcome/follow-up write, clear follow-up, routing, or **`ApprovedPacketActionCaseCard`**. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **display-only** slice (with §2 **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`)**, §2 **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**, §2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**, §2 **Approved next action started (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), when **`item.next.status === "started"`** and **`started_at`** is set, shows read-only **Opened {formatted time}** via existing **`formatApprovedNextActionHandlingTimestamp`** alongside **Opened for next step.** — parity with read-only **Handled for now {formatted time}** when **`status === "completed"`** (§2 **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`)**); uses existing **`approved_next_action.started_at`** from already-loaded workbench list **`GET /api/justice/cases`** **`client_state`** only (**no** new **`GET`** or **`PATCH`**). Complements §2 **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`)**, §2 **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`)**, and **Record action handled for now** — §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**. **Does not** change behavior, persistence, membership predicates, **Open approved step**, record handled, outcome/follow-up write, clear follow-up, acknowledge **`PATCH`**, routing, or **`ApprovedPacketActionCaseCard`**. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **display-only** slice (with §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**, §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**, §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, §2 **Approved next action started (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). On **`ApprovedPacketActionCaseCard`** only (**Approved packet actions** section — **not** **`HandlingWorkbenchCaseCard`** triage cards), inside the existing **`showRecordHandled`** block when **`next.status === "started"`** and **`started_at`** is set, shows read-only **Opened {formatted time}** via existing **`formatApprovedNextActionHandlingTimestamp`** below **Opened for next step.** and before **Record action handled for now** — mirrors **`HandlingWorkbenchCaseCard`** triage-card started-at display — §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**; uses existing **`approved_next_action.started_at`** from already-loaded workbench list **`GET /api/justice/cases`** **`client_state`** only (**no** new **`GET`** or **`PATCH`**). Complements §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, and §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**. **Does not** change **Open approved step**, record handled, outcome/follow-up write, clear follow-up, acknowledge **`PATCH`**, routing, membership predicates, **`HandlingWorkbenchCaseCard`**, plan, packet, chat-ai, cases, or hub. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — approved-packet handling request copy chat-first (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **UI/copy** slice (with §2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)**, §2 **Saved cases — approved-packet handling request copy chat-first (`/justice/cases`)**, §2 **Justice hub — approved-packet handling request copy chat-first (`/justice`)**, §2 **Packet — approved-packet handling request copy chat-first (`/justice/packet`)**, §2 **Chat AI intake — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**). In **`ApprovedPacketActionCaseCard`** helper and **Approved packet actions** section intro, plan-first handling-request clause **Request Surrenderless handling from chat intake when you want internal triage tracking.** (card) / **request Surrenderless handling from chat intake when you want internal triage tracking.** (section intro) replaces **Request handling from your action plan…** / **request handling from your action plan…** — completes approved-packet handling copy parity with hub/cases/packet (§2 **Saved cases — approved-packet handling request copy chat-first (`/justice/cases`)**, §2 **Packet — approved-packet handling request copy chat-first (`/justice/packet`)**). First sentence on card and section intro lead-in **unchanged**. **Does not** change **Open action plan**, **Open case packet**, **Update in chat**, handling-request triage cards, record-handled / acknowledge UI, navigation handlers, APIs, schema, or filing/submission/contact/queue/upload/storage/automatic-action behavior. **Copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Handling workbench — approved-packet card Update in chat primary navigation (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **UI/navigation** slice (with §2 **Handling workbench — approved-packet handling request copy chat-first (`/justice/handling`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Chat AI intake — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**). On **`ApprovedPacketActionCaseCard`** only (**Approved packet actions** section — **not** **`HandlingWorkbenchCaseCard`** triage cards), reorders nav buttons: **Update in chat** first with **`navButtonPrimaryCls`**; **Open action plan** second with **`navButtonSecondaryCls`**; **Open case packet** and **Open approved step** **unchanged** (secondary). Same handlers — **`onOpenChat`** → existing **`openChat`** / **`navigateWithCase(row, "/justice/chat-ai")`**, **`onOpenActionPlan`** → **`/justice/plan`**, **`onOpenPacket`** → **`/justice/packet`** — aligns primary CTA with chat-first consumer surfaces (§2 **Handling workbench — approved-packet handling request copy chat-first (`/justice/handling`)**). **Does not** change approved-packet helper/section copy, **`HandlingWorkbenchCaseCard`** button order, handling-request triage cards, **Mark acknowledged** UI, **`navigateWithCase`** / session activation, record-handled behavior, APIs, schema, or other workbench sections. **Navigation emphasis only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Handling workbench — triage card Update in chat primary navigation (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **UI/navigation** slice (with §2 **Handling workbench — approved-packet card Update in chat primary navigation (`/justice/handling`)**, §2 **Handling workbench — approved-packet handling request copy chat-first (`/justice/handling`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Chat AI intake — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**). On **`HandlingWorkbenchCaseCard`** only (**Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** — **not** **`ApprovedPacketActionCaseCard`**), reorders nav buttons: **Update in chat** first with **`navButtonPrimaryCls`**; **Open action plan** second with **`navButtonSecondaryCls`**; **Open case packet** unchanged (secondary, same **`compactNavigation`** gate); manual filing records note, **Open approved step**, and **Mark acknowledged** **unchanged**. Same handlers — **`onOpenChat`** → existing **`openChat`** / **`navigateWithCase(row, "/justice/chat-ai")`**, **`onOpenActionPlan`** → **`/justice/plan`**, **`onOpenPacket`** → **`/justice/packet`** — aligns triage-card primary CTA with chat-first consumer surfaces and **`ApprovedPacketActionCaseCard`** nav (§2 **Handling workbench — approved-packet card Update in chat primary navigation (`/justice/handling`)**). **Does not** change **`ApprovedPacketActionCaseCard`** button order, approved-packet helper/section copy, **Mark acknowledged** UI/behavior, **`navigateWithCase`** / session activation, record-handled behavior, APIs, schema, or other workbench sections. **Navigation emphasis only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Handling workbench — Awaiting intro acknowledge copy chat-first ordering (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **UI/copy** slice (with §2 **Approved next action — handled/open triage redirect note chat-first acknowledge ordering (`approvedNextActionHandlingDisplay.tsx`)**, §2 **Handling workbench — triage card Update in chat primary navigation (`/justice/handling`)**, §2 **Handling workbench — completed-unacknowledged handling count in Awaiting helper (`/justice/handling`)**). In **Awaiting internal triage** section intro only (~1960–1964), acknowledge clause **acknowledge from chat intake or your action plan** replaces **acknowledge from your action plan or chat intake** — aligns page-local helper with **`HANDLING_HANDLED_OPEN_TRIAGE_NOTE_REDIRECT`** chat-first ordering (§2 **Approved next action — handled/open triage redirect note chat-first acknowledge ordering (`approvedNextActionHandlingDisplay.tsx`)**) and triage-card **Update in chat** primary nav (§2 **Handling workbench — triage card Update in chat primary navigation (`/justice/handling`)**). **`HANDLING_HANDLED_OPEN_TRIAGE_NOTE_REDIRECT`** / **`INLINE_ACK`**, **`resolveHandlingTrackingContextualLink`**, **Mark acknowledged** UI/behavior, triage card button order, empty-state copy, approved-packet sections, APIs, schema, routing, styling **unchanged**. **Copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Handling workbench — empty-state packet-approval copy chat-first (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **UI/copy** slice (with §2 **Handling workbench — Awaiting intro acknowledge copy chat-first ordering (`/justice/handling`)**, §2 **Packet — signed-in approve returns to chat (`/justice/packet`)**, §2 **Chat AI intake — inline prepared packet approval in Active case checklist (`/justice/chat-ai`)**, §2 **Justice hub — approved-packet handling request copy chat-first (`/justice`)**). In **`!hasAnyWorkbenchContent`** empty state only (~1890–1892), approval clause **Approve your prepared case packet from chat intake or on the case packet, or request Surrenderless handling when an approved next action is active.** replaces **Approve your prepared case packet on the action plan, or request Surrenderless handling when an approved next action is active.** — aligns with chat-first packet approval on **`/justice/chat-ai`** (§2 **Chat AI intake — inline prepared packet approval in Active case checklist (`/justice/chat-ai`)**) and **`/justice/packet`** (§2 **Packet — signed-in approve returns to chat (`/justice/packet`)**); hub handling-request copy leads with chat intake (§2 **Justice hub — approved-packet handling request copy chat-first (`/justice`)**). **Awaiting internal triage** intro, triage card nav, **`redirect`** / **`inlineAck`** notes, approved-packet sections, **Mark acknowledged** behavior, APIs, schema, routing, styling **unchanged**. **Copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Handling workbench — active-case back-link from plan and chat-ai:** **`src/app/justice/plan/page.tsx`**, **`src/app/justice/chat-ai/page.tsx`** — **navigation-only** slice closing the loop with §2 **Handling workbench — internal triage surface** (handling-request path only — approved-packet-action workbench nav without **`handling_requested_at`** on plan — §2 **Approval-to-action engine — approved packet action workbench link on action plan prepared-packet callouts (`/justice/plan`)**; on chat — §2 **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**). When the active **`approved_next_action`** has **`handling_requested_at`**, a small read-only link **View in handling workbench** → **`/justice/handling`** appears below the existing handling UI on **`/justice/plan`** (both prepared-packet handling callouts) and in **Recap → Current action tracking** on **`/justice/chat-ai`** (**Update in chat** / loaded case only). **Fresh chat intake** (no loaded case / no **`handling_requested_at`**) does **not** show the handling-request link. **No** session activation on link click (workbench is the full handling-request list); **no** claim of filing, submission, external queue, contact, or automation. Request handling, note edit/clear, acknowledge, workbench CTAs, hydration, queue status, and disclaimers **unchanged**. **No** schema, migration, new API route, queue worker, **`justice_case_filings`** row, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Handling workbench — active-case back-link from packet (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **navigation-only** slice completing the workbench loop with §2 **Handling workbench — active-case back-link from plan and chat-ai** (handling-request path only — approved-packet-action workbench nav without **`handling_requested_at`** — §2 **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`)**). When the hydrated **`approved_next_action`** for the active packet case has **`handling_requested_at`**, a small read-only link **View in handling workbench** → **`/justice/handling`** appears immediately below **`ApprovedNextActionHandlingRequestedReadOnly`** in the prepared-action area (same emerald **`text-xs`** link styling as plan/chat). When **`handling_requested_at`** is absent, the handling-request link is hidden (approved-packet-action link — §2 **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`)**). **No** **`STORAGE_CASE_ID`** activation or change on link click; **no** request/ack/note edit on packet; packet approval, timeline, filings, and other packet copy **unchanged**. Complements workbench **Open case packet** and §2 **Packet — handling requested visibility (prepared-action area)** — **in-app tracking only**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone. **No** schema, migration, new API route, queue worker, **`justice_case_filings`** row, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Handling workbench — active-case back-link from hub (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **navigation-only** slice completing workbench access from all common handling entry surfaces (with §2 **Handling workbench — active-case back-link from plan and chat-ai** and **Handling workbench — active-case back-link from packet**; handling-request path only — approved-packet-action workbench nav without **`handling_requested_at`** — §2 **Approval-to-action engine — approved packet action workbench link on justice hub Current case (`/justice`)**). When the **Current case** snapshot has **`handling_requested_at`** (session **`justice_approved_next_action_v1`** for **`STORAGE_CASE_ID`** via **`readSessionApprovedNextAction`**), a small read-only link **View in handling workbench** → **`/justice/handling`** appears below the **Current case** card (same emerald **`text-xs`** link styling as plan/chat/packet; placed **outside** the card’s primary **`/justice/plan`** or **`/justice/preview`** link to avoid nested links). When **`handling_requested_at`** is absent, the handling-request link is hidden (approved-packet-action link — §2 **Approval-to-action engine — approved packet action workbench link on justice hub Current case (`/justice`)**). **No** **`STORAGE_CASE_ID`** activation or change on click; **no** request/ack/note edit on hub; **Current case** loading, plan/preview CTA, and chat-intake hub cards **unchanged**. Supports **chat-first recovery** on the workspace hub — **in-app tracking only**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone. **No** schema, migration, new API route, queue worker, **`justice_case_filings`** row, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Handling workbench — align Awaiting internal triage with Saved cases Needs attention (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **membership-alignment** slice (with §2 **Saved cases — Needs attention for handling requested**; enabled by plan/chat **Mark acknowledged** — §2 **Plan — acknowledge handling request in Surrenderless handling UI**, **Chat AI intake — acknowledge handling request in Current action tracking**). **Awaiting internal triage** now includes only cases where **`handling_requested_at`** is set, **`handling_acknowledged_at`** is absent, and **`approved_next_action.status !== "completed"`** (same rule as **`buildHandlingRequestedAttentionItems`** on **`/justice/cases`**; later centralized — §2 **Approved next action — shared handling awaiting-triage membership predicate**).
- **Approved next action — shared handling awaiting-triage membership predicate (`approvedNextActionState.ts`):** **`src/lib/justice/approvedNextActionState.ts`**, **`src/app/justice/cases/page.tsx`**, **`src/app/justice/handling/page.tsx`** — **no-UI refactor** slice. Exports **`isHandlingAwaitingTriageApprovedNextAction(next)`** — a type guard on existing **`JusticeApprovedNextAction`** fields only (**no** schema, API, persistence, or copy changes). Returns **true** when **`handling_requested_at`** is set, **`handling_acknowledged_at`** is absent, and **`approved_next_action.status !== "completed"`** (active handling request awaiting internal triage). **`buildHandlingRequestedAttentionItems`** on **`/justice/cases`** and **Awaiting internal triage** bucketing on **`/justice/handling`** call this predicate instead of duplicating inline checks — **behavior identical** to before: active unacknowledged non-completed handling requests appear in **Saved cases** **Needs attention** and workbench **Awaiting**; handled (**`status === "completed"`**) + unacknowledged handling requests remain excluded from those inboxes (nested **Handled — open handling request** — §2 **Handling workbench — read-only handled/open-request rows under Awaiting**); acknowledged requests leave **Needs attention**/**Awaiting**/nested handled and appear in workbench **Acknowledged**. **Does not** change UI, disclaimers, queue status, workbench links, chat/plan helper notes, read-only handled handling on chat, **Mark acknowledged**, filters/sort/pagination, nested handled membership rules, or external-action copy. **No** migration, worker, **`justice_case_filings`**, timeline **type**, or external submission/contact/queue automation. **`status === "completed"`** (handled) + handling requested + unacknowledged cases **no longer** appear in **Awaiting**; they remain on **All cases** (§2 **Saved cases — handling requested visibility on All cases cards**) and can be **Mark acknowledged** from **`/justice/plan`** or **`/justice/chat-ai`**; after ack they appear in workbench **Acknowledged**. Minimal static helper copy under **Awaiting internal triage** explains completed approved actions are acknowledged from plan or chat, not this inbox. Page still loads all cases with **`handling_requested_at`** for **Acknowledged** and empty-state detection (**`allHandlingItems`**). **Does not** change **Acknowledged** section rules, per-card CTAs, session highlight, **Mark acknowledged** on awaiting rows, queue status, notes, disclaimers, pagination, APIs, **Needs attention** inclusion, chat/plan/cases ack persistence, or filters. **No** schema, migration, new API route, queue worker, **`justice_case_filings`**, timeline **type**, external submission/contact/queue automation, or **`/api/submit-form`**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Approved next action — centralized handled/open handling triage note copy (`approvedNextActionHandlingDisplay.tsx`):** **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`**, **`src/app/justice/JusticeHubWorkspaceBody.tsx`**, **`src/app/justice/packet/page.tsx`**, **`src/app/justice/plan/page.tsx`**, **`src/app/justice/chat-ai/page.tsx`**, **`src/app/justice/cases/page.tsx`** (§2 **Saved cases — handled/open handling triage note on All cases cards**), **`src/app/justice/handling/page.tsx`** (§2 **Handling workbench — handled/open redirect note on nested Handled — open cards**) — **no-behavior-change** copy refactor on hub/packet/plan/chat; **cases** and workbench nested **Handled — open** cards add **`redirect`** note only. Centralizes handled/open triage note strings and rendering in **`ApprovedNextActionHandlingHandledOpenTriageNote`** (with **`formatHandlingHandledOpenTriageNote`**, **`HANDLING_HANDLED_OPEN_TRIAGE_NOTE_REDIRECT`**, **`HANDLING_HANDLED_OPEN_TRIAGE_NOTE_INLINE_ACK`**, variant **`"redirect" | "inlineAck"`**). **Display conditions unchanged** on every surface: show only when **`handling_requested_at`** is set, **`handling_acknowledged_at`** is absent, and **`approved_next_action.status === "completed"`** (handled approved action with open handling request). **`redirect`** variant (**hub**, **packet**, **All cases** **`CaseApprovedNextActionTracking`**, nested workbench **Handled — open** **`HandlingWorkbenchCaseCard`**): not listed in workbench **Awaiting** or **Saved cases** **Needs attention**; acknowledge from **action plan** or **chat intake**; external-action disclaimer. **`inlineAck`** variant (**plan** both prepared-packet handling callouts, **chat** loaded-case **Current action tracking**): not listed in **Awaiting** or **Saved cases** **Needs attention**; **Mark acknowledged below**; same disclaimer. Replaces page-local constants and duplicated inline **`<p>`** blocks — **user-facing wording per variant unchanged** on hub/packet/plan/chat. **Does not** change when notes appear, **Mark acknowledged** on plan/chat/cases/workbench (still **no** ack on hub/packet/**All cases** handled/open or nested workbench handled/open), queue status, workbench links, hydration/**PATCH**, APIs, schema, persistence, or external-action claims. **No** migration, worker, **`justice_case_filings`**, timeline **type**, or external submission/contact/queue automation.
- **Approved next action — handled/open triage redirect note chat-first acknowledge ordering (`approvedNextActionHandlingDisplay.tsx`):** **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** only — **UI/copy** slice (with §2 **Approved next action — centralized handled/open handling triage note copy (`approvedNextActionHandlingDisplay.tsx`)**, §2 **Handling workbench — triage card Update in chat primary navigation (`/justice/handling`)**, §2 **Handling workbench — handled/open redirect note on nested Handled — open cards (`/justice/handling`)**, §2 **Justice hub — handled/open handling triage note on Current case (`/justice`)**, §2 **Saved cases — handled/open handling triage note on All cases cards (`/justice/cases`)**, §2 **Packet — handled/open handling triage note in prepared-action area (`/justice/packet`)**). In **`HANDLING_HANDLED_OPEN_TRIAGE_NOTE_REDIRECT`** only, acknowledge clause **Acknowledge it from chat intake or the action plan for internal triage only.** replaces **Acknowledge it from the action plan or chat intake for internal triage only.** — chat-first ordering on every **`ApprovedNextActionHandlingHandledOpenTriageNote`** **`redirect`** surface (handling workbench **Awaiting internal triage** cards, hub/packet legacy **`redirect`**, **All cases** when inline ack disabled). **`HANDLING_HANDLED_OPEN_TRIAGE_NOTE_INLINE_ACK`**, **`resolveHandlingTrackingContextualLink`**, **Mark acknowledged** UI/behavior, handling workbench **Awaiting internal triage** intro copy, empty-state copy, APIs, schema, routing, styling **unchanged**. **Copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Handling workbench — completed-unacknowledged handling count in Awaiting helper (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **discoverability-only** slice (with §2 **Handling workbench — align Awaiting internal triage with Saved cases Needs attention**, §2 **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`)**). From **`allHandlingItems`**, computes **`completedUnacknowledgedCount`** for cases where **`handling_requested_at`** is set, **`handling_acknowledged_at`** is absent, and **`approved_next_action.status === "completed"`**. When count **> 0**, extends the existing **Awaiting internal triage** helper with one line pointing to nested **Handled — open handling request** (§2 **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`)**). Those cases **remain excluded** from **Awaiting** and **Saved cases** **Needs attention**; they stay visible on **All cases** (§2 **Saved cases — handling requested visibility on All cases cards**). Per-case identification — §2 **Handling workbench — read-only handled/open-request rows under Awaiting**; workbench **Mark acknowledged** on nested rows — §2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**. **No** third top-level workbench section, **no** change to **Awaiting** or **Acknowledged** membership, active **Awaiting** per-card CTAs, queue status, notes, disclaimers, pagination, APIs, **Needs attention** rules, or chat/plan/cases ack behavior. **No** schema, migration, new API route, queue worker, **`justice_case_filings`**, timeline **type**, external submission/contact/queue automation, or **`/api/submit-form`**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — read-only handled/open-request rows under Awaiting (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **discoverability + navigation** slice (with §2 **Handling workbench — completed-unacknowledged handling count in Awaiting helper**, §2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**, and **Handling workbench — align Awaiting internal triage with Saved cases Needs attention**). From **`allHandlingItems`**, builds **`completedUnacknowledgedItems`** with the same membership as **`completedUnacknowledgedCount`**: **`handling_requested_at`** set, **`handling_acknowledged_at`** absent, **`approved_next_action.status === "completed"`** (handled approved action with an open handling request). When **`completedUnacknowledgedItems.length > 0`**, renders a **nested** block **under** **Awaiting internal triage** (below existing **Awaiting** helper/count copy) — **not** a third top-level section. Subheading **Handled — open handling request**; section helper updated — §2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**. Reuses **`HandlingWorkbenchCaseCard`** with **`compactNavigation`** (**Open action plan**, **Update in chat** only — **no** **Open case packet** or **Open approved step** on these rows). Cards show the same read-only handling fields (label/company, product, approved-action status, **Surrenderless handling requested**, recorded time, optional **`handling_request_note`**, **display-only** queue status, disclaimer). Per-card handled/open triage note — §2 **Handling workbench — handled/open redirect note on nested Handled — open cards**. **Session highlight** (**Current case in this browser**) applies the same as other workbench cards. Workbench **Mark acknowledged** on nested rows — §2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**; plan/chat/cases ack paths unchanged. After ack, rows leave the nested list and appear in **Acknowledged** (immediate local update + refetch on focus). **Does not** change **Awaiting** membership, **Acknowledged** membership, **Saved cases** **Needs attention** rules, queue-status derivation, pagination, **`handling_request_note`** edit/clear, or external-action copy on other surfaces. **No** schema, migration, new API route, queue worker, **`justice_case_filings`**, timeline **type**, external submission/contact/queue automation, or **`/api/submit-form`**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — handled/open redirect note on nested Handled — open cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** — **copy-only** slice (with §2 **Handling workbench — read-only handled/open-request rows under Awaiting**, §2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**, **Approved next action — centralized handled/open handling triage note copy**). In **`HandlingWorkbenchCaseCard`**, when **`handling_requested_at`**, no **`handling_acknowledged_at`**, and **`status === "completed"`** (**`showHandledOpenHandlingTriageNote`**), renders **`ApprovedNextActionHandlingHandledOpenTriageNote`** after **`ApprovedNextActionHandlingQueueStatusReadOnly`** and before **`ApprovedNextActionHandlingAcknowledgedReadOnly`** and the generic disclaimer / nav buttons. Active **Awaiting internal triage** cards use variant **`redirect`** (**`HANDLING_HANDLED_OPEN_TRIAGE_NOTE_REDIRECT`**); nested **Handled — open handling request** rows with workbench **Mark acknowledged** use variant **`inlineAck`** (**`HANDLING_HANDLED_OPEN_TRIAGE_NOTE_INLINE_ACK`**) — §2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**; **not** shown on **Acknowledged** cards (**`handling_acknowledged_at`** set). **Does not** change **Awaiting** / **Acknowledged** membership (**`isHandlingAwaitingTriageApprovedNextAction`**), **Mark acknowledged** on active **Awaiting** rows, **Open action plan** / **Update in chat** on nested rows, refetch on focus (§2 **Handling workbench — refetch active cases on window focus**), session highlight (§2 **Handling workbench — current browser-session case highlight**), **Saved cases** behavior, APIs, schema, persistence, or hub/packet/plan/chat/cases handled/open notes. **No** migration, worker, **`justice_case_filings`**, timeline **type**, or external submission/contact/queue automation.
- **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **behavior** slice (with §2 **Handling workbench — read-only handled/open-request rows under Awaiting (`/justice/handling`)**, §2 **Handling workbench — internal triage surface (`/justice/handling`)**, §2 **Plan — acknowledge handling request in Surrenderless handling UI (`/justice/plan`)**, §2 **Chat AI intake — acknowledge handling request in Current action tracking (`/justice/chat-ai`)**, §2 **Saved cases — acknowledge handling request from Needs attention (`/justice/cases`)**, §2 **Approved next action — centralized handled/open handling triage note copy**). On nested **`completedUnacknowledgedItems`** under **Awaiting internal triage** only — handled approved-next-action cases with **`approved_next_action.status === "completed"`**, **`handling_requested_at`** set, and **`handling_acknowledged_at`** absent — enables existing **Mark acknowledged** on **`HandlingWorkbenchCaseCard`** via **`showMarkAcknowledged`** + **`onAcknowledge`** calling page-local **`acknowledgeHandlingRequest`** (**`acknowledgeHandlingRequestInApprovedNextAction`** + **`mergeClientStateWithAcknowledgedHandling`** + signed-in **UUID** **`GET` + merge + `PATCH`** on existing **`/api/justice/cases/[id]`** — same path as active **Awaiting internal triage** cards and §2 **Plan — acknowledge handling request in Surrenderless handling UI** / §2 **Chat AI intake — acknowledge handling request in Current action tracking** / §2 **Saved cases — acknowledge handling request from Needs attention**). Nested section helper no longer says acknowledgement is plan/chat-only; per-card **`inlineAck`** handled/open triage note aligns with **Mark acknowledged below** copy. **`APPROVED_NEXT_ACTION_HANDLING_DISCLAIMER`** and **`APPROVED_NEXT_ACTION_HANDLING_ACKNOWLEDGE_HELPER`** unchanged. After success, row leaves nested list and appears in **Acknowledged** (existing bucketing). **Does not** change **`isHandlingAwaitingTriageApprovedNextAction`**, **Needs attention** membership, approved-packet inbox rules, outcome/follow-up fields, plan/chat/cases/hub/packet write surfaces, or routing. **No** schema, migration, new API route, queue worker, browser automation, filing automation, timeline **type**, external submission/contact/queue automation, or **`/api/submit-form`**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — current browser-session case highlight (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **visibility-only** slice. Reads the active browser-session case id from existing **`STORAGE_CASE_ID`** (**`justice_case_id`** in **`sessionStorage`**); when a workbench card’s case id matches, shows read-only **Current case in this browser** below the card title in **Awaiting internal triage**, the nested **Handled — open handling request** list (§2 **Handling workbench — read-only handled/open-request rows under Awaiting**), and **Acknowledged**. Refreshes on page load, when the case list finishes loading, on window **focus** / tab **visibility** (with §2 **Handling workbench — refetch active cases on window focus** — session id + case list), and when workbench per-card navigation calls **`activateCaseInSession`**. **Does not** change **Awaiting** membership (§2 **Handling workbench — align Awaiting internal triage with Saved cases Needs attention**), **Acknowledged** membership, **Awaiting** helper count copy (§2 **Handling workbench — completed-unacknowledged handling count in Awaiting helper**), nested handled/open-request membership or CTAs (§2 **Handling workbench — read-only handled/open-request rows under Awaiting**), routing, filtering, persistence, **Mark acknowledged**, **`handling_request_note`** edit/clear, queue status, disclaimers, schema, API, worker, **`justice_case_filings`**, timeline **type**, external queue, submission, contact, or automation behavior. Complements §2 **Handling workbench — internal triage surface** and **Handling workbench — active-case back-link from plan and chat-ai** so users returning from a case surface can spot the active session case in the list — **in-app tracking only**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Handling workbench — refetch active cases on window focus (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **data-refresh** slice (with §2 **Handling workbench — current browser-session case highlight**, **Saved cases — refetch active cases on window focus**; enabled by plan/chat **Mark acknowledged** — §2 **Plan — acknowledge handling request in Surrenderless handling UI**, **Chat AI intake — acknowledge handling request in Current action tracking**). Extracts existing paginated **`GET /api/justice/cases`** load into shared **`loadCases(signal)`** (**`fetchAllActiveCases`** — **unchanged** endpoint and pagination). **Initial mount** still calls **`loadCases`** once when signed in (**unchanged**). On **`window` `focus`** and on **`document` `visibilitychange`** when **`visibilityState === "visible"`**, re-runs **`loadCases`**, replaces in-memory **`cases`** on success, and preserves existing load-error behavior (**`Could not load cases.`** + empty list on failure). Keeps **`refreshSessionCaseIdFromStorage`** on the same events so **Current case in this browser** stays aligned. **Purpose:** when the user returns to an open workbench tab after **Mark acknowledged** on **`/justice/plan`** or **`/justice/chat-ai`**, **Awaiting internal triage**, nested **Handled — open handling request**, and **Acknowledged** rebucket from fresh **`client_state`** without a full page reload. **Workbench-local Mark acknowledged** still updates **`cases`** immediately via **`applyAcknowledgedHandlingToCaseRow`** — refocus refetch is additive, not required for on-page ack. **Does not** change UI, copy, routes, APIs, schema, persistence, **`isHandlingAwaitingTriageApprovedNextAction`** / nested handled membership rules, per-card CTAs, queue status, notes, disclaimers, **Mark acknowledged** semantics, plan/chat/cases ack, or external-action behavior. **No** migration, worker, **`justice_case_filings`**, timeline **type**, or external submission/contact/queue automation; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Preview → plan review tracking:** When the user confirms review on **`/justice/preview`** and continues to **`/justice/plan`**, a non-filing timeline milestone **`submission_draft_reviewed`** is recorded (**`POST /api/justice/submission-draft-reviewed`**: Clerk auth, UUID **`case_id`**, **`userOwnsJusticeCase`**, idempotent append to **`justice_cases.timeline`** via **`appendCaseTimelineEntry`**; **session-only** or non-UUID cases use **`appendSubmissionDraftReviewedOnce`** in **`timeline.ts`**). **`/justice/plan`** shows **Submission draft reviewed.** in the timeline status summary when that event applies. **Tracking** for **`submission_draft_reviewed`** remains as documented above; **no** auto-created tasks, **no** preview-page changes beyond the existing review flow.
- **Plan — prepared next action after draft review (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **UI/copy + shared `pickPreparedNextAction` from `preparedNextAction.ts`** (§2 **Approval-to-action engine — plan prepared next action shared helper usage (`/justice/plan`)**) after preview→plan (**no** changes to **`rules.ts`**, **`caseReadiness.ts`**, APIs, schema, filing automation, queue workers, timeline **types**, preview generation, **`chat-ai`**, **`commitIntakeToSessionAndServer`**, or real submission behavior). When **`showPostDraftReviewCallout`** is true (**`submission_draft_reviewed`** in timeline, case **not** merchant-resolved, user has **not** moved on via prep-opened / complaint-filed / **`ftc_practice_completed`**), a single emerald card **below the action-plan title** and **above** the **Current case** summary frames **Surrenderless prepared your next step** from the reviewed draft: **`stepLabel`** names the current focus (from contact state + first **`recommended`** / **`available`** destination with **`internalRoute`** via **`computeJusticeDestinations`**); copy states the case is assembled for **in-app review**, **nothing filed automatically**, and Surrenderless has **not** submitted, filed, or contacted anyone; includes **proof readiness continuity** line from existing **`evidenceCount`** (§2 **Plan — proof readiness continuity after preview review (`/justice/plan`)**); **next-step explainer** and **readiness bridge** when not ready to escalate (§2 **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`)**); packet landing mirrors pre-approval explainer + bridge (§2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**) and post-approval approved-step clarity after approval (§2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**). **Primary CTA** (one obvious button): **`Review prepared next step`** → **`/justice/packet`** (case packet aggregate review — intake, timeline, evidence, filings, tasks — **not** a government filing); when **prepared packet approval** is already persisted (§2 **Plan — prepared packet approval surfaced**), primary becomes **`View approved case packet`** and an inner **Prepared packet approved for next action** callout appears. **Secondary** (when applicable): **`Open {stepLabel} preparation`** → **`detailHref`** (**`/justice/merchant`** when not yet contacted; else that first routable prep **`internalRoute`**). Manual prep pages, external filing, and recording remain available via the **full action plan below** and packet — **not** the main framing of the card. **No** inline **`recommendationText`** in the card (reduces directory-of-options feel). **`preparedNextAction`** is computed as a **plain const** after intake is loaded (not **`useMemo`** past early returns). **Interim scaffolding** toward §1 (user eventually **only chats**; Surrenderless prepares / submits-or-queues / tracks where feasible) — **does not** imply Surrenderless actually files or submits yet.
- **Plan — prepared packet approval surfaced (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** — when the user returns from **`/justice/packet`** after approving, the plan **recognizes persisted approval** from existing case data: on hydrate, existing **`GET /api/justice/cases/[id]`** (by **`STORAGE_CASE_ID`**) and **`GET /api/justice/cases`** list-resume paths read **`client_state.prepared_packet_approved`** and, when present, **`client_state.approved_next_action`** (**`label`**, **`href`**, **`status`** **`"approved"`**, **`"started"`**, or **`"completed"`**, **`approved_at`**, **`started_at`**, **`completed_at`**, optional **`outcome_note`**, **`follow_up_needed`**, **`follow_up_at`** — §2 **Approved next action target**, **Approved next action started**, **Approved next action completed / handled**, **Approved next action outcome / follow-up tracking**); page-local **`justice_prepared_packet_approved_v1`** and **`justice_approved_next_action_v1`** session maps remain fallback for local / non-UUID cases. **No** new API route, schema, migration, queue worker, filing automation, browser automation, or timeline **type**; **no** changes to **`rules.ts`**, **`caseReadiness.ts`**, evidence/filing **APIs**, preview generation, or **`commitIntakeToSessionAndServer`**. When approved (not yet started): inner emerald **Prepared packet approved for next action**, **Next step: {label}** in main post-draft paragraph (§2 **Plan — post-draft approved step label alignment (`/justice/plan`)**), green **`View approved case packet`** button, optional **`Open {label}`**. When **`status === "started"`** (§2): **Next action started**, **Opened for next step**, and **Continue approved case packet** after return/refresh. Completed **handled-for-now** timestamp on prepared-packet approved callouts — §2 **Plan — handled-for-now timestamp on prepared-packet approved callouts (`/justice/plan`)**. Copy states Surrenderless has **not** filed, submitted, sent, or contacted anyone automatically. **Interim scaffolding** toward §1 **chat-first → prepare → approve → start/track** — in-app state only, not submission.
- **Plan — handled-for-now timestamp on prepared-packet approved callouts (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **read-only continuity display** slice (with §2 **Plan — prepared packet approval surfaced (`/justice/plan`)**, §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, §2 **Approved next action completed / handled (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**, §2 **Justice hub — handled-for-now timestamp on Current case (`/justice`)**, §2 **Chat AI intake — handled-for-now timestamp in Current action tracking (`/justice/chat-ai`)**, §2 **Packet — handled-for-now timestamp on approved next action card (`/justice/packet`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In **both** prepared-packet approved inner emerald callouts — nested inner card inside **`showPostDraftReviewCallout && preparedPacketApproved`** and standalone **`preparedPacketApproved`** callout when post-draft callout is off — after the existing completed prose block and before the **`approvedNextActionStarted`** block / **`ApprovedNextActionTrackingSummary`**, when hydrated **`approvedNextAction.status === "completed"`** and **`completed_at`** is present (from existing page hydrate via **`hydrateApprovedNextActionForDisplay`** — **no** new **`GET`** or **`PATCH`**), shows read-only **Handled for now {formatted time}** via existing **`formatApprovedNextActionHandlingTimestamp`**. **Does not** change **`ApprovedNextActionOutcomeTrackingForm`**, **Record action handled for now**, handling blocks, workbench links, or show **completed** approved-packet actions in **Needs attention** or handling workbench **Approved packet actions** inbox lists. **Display correctness only** — **no** new API route, schema/migration, DB field, timeline **type**, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **display-only** slice (with §2 **Plan — prepared packet approval surfaced (`/justice/plan`)**, §2 **Plan — handled-for-now timestamp on prepared-packet approved callouts (`/justice/plan`)**, §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**, §2 **Approved next action started (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In **both** prepared-packet approved inner emerald callouts — nested inner card inside **`showPostDraftReviewCallout && preparedPacketApproved`** and standalone **`preparedPacketApproved`** callout when post-draft callout is off — inside the existing **`approvedNextActionStarted`** block, when **`approvedNextAction.status === "started"`** and **`started_at`** is set, shows read-only **Opened {formatted time}** via existing **`formatApprovedNextActionHandlingTimestamp`** below **Opened for next step.** and before **Record action handled for now** — parity with read-only **Handled for now {formatted time}** when **`status === "completed"`** (§2 **Plan — handled-for-now timestamp on prepared-packet approved callouts (`/justice/plan`)**); uses existing **`approved_next_action.started_at`** from already-hydrated **`approvedNextAction`** / **`hydrateApprovedNextActionForDisplay`** (**no** new **`GET`** or **`PATCH`**). Complements §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)** and §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**. **Does not** change **Record action handled for now**, **Open {label}**, handling blocks, outcome/follow-up write, clear follow-up, workbench links, chat-ai, packet, cases, hub, handling workbench, membership predicates, or any write paths. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Plan — clear follow-up on prepared-packet callouts (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **behavior** slice (with §2 **Plan — prepared packet approval surfaced (`/justice/plan`)**, §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Saved cases — clear Needs attention follow-up (`/justice/cases`)**, §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**, §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In **both** prepared-packet approved inner emerald callouts — nested inner card inside **`showPostDraftReviewCallout && preparedPacketApproved`** and standalone **`preparedPacketApproved`** callout when post-draft callout is off — when **`approved_next_action.status === "completed"`** and **`follow_up_needed === true`**, shows **Mark follow-up handled** below **`ApprovedNextActionTrackingSummary`** / **`ApprovedNextActionOutcomeTrackingForm`**. **`handleClearApprovedNextActionFollowUp`** calls existing **`clearFollowUpFromApprovedNextAction`** (omits **`follow_up_needed`** only; preserves **`outcome_note`**, **`follow_up_at`**, **`status`**, **`href`**, **`label`**, **`approved_at`**, **`started_at`**, **`completed_at`**, and other **`approved_next_action`** fields) and persists via existing **`persistApprovedNextAction`** (**`mergeApprovedNextActionTrackingFields`** + **`GET` + merge + `PATCH`** **`client_state`** for signed-in **UUID** cases; **`justice_approved_next_action_v1`** session sync). After clear, case leaves **Saved cases** **Needs attention** follow-up list (no longer **`follow_up_needed === true`**) but outcome notes and dates remain on plan and **All cases**. Helper copy: **Clears this from Needs attention on Saved cases. Your outcome note and dates stay saved. Not automatic filing or submission.** — mirrors chat-ai / cases / handling workbench clear-follow-up pattern. **Does not** change handling workbench, **`isHandlingAwaitingTriageApprovedNextAction`**, **Needs attention** builders (read-only membership from **`follow_up_needed`**), acknowledge **`PATCH`**, open-step or record-handled writes, routing, or **`ApprovedNextActionOutcomeTrackingForm`** save semantics. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Packet — prepared action review framing (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/copy** aligned with the post–draft-review handoff from **`/justice/plan`** (plan primary CTA **`Review prepared next step`** → **`/justice/packet`**; §2 **Plan — prepared next action**, §2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**, §2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**, §2 **Packet — started-state record-handled copy alignment (`/justice/packet`)**, §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**). Page-local **`showPreparedActionPacketFraming`** mirrors the plan callout gates (**`submission_draft_reviewed`**, not merchant-resolved, not moved on via prep-opened / complaint-filed / **`ftc_practice_completed`**) using **`readTimeline`**, **`isMerchantResolved`**, and the same prep/filed type lists — **no** new timeline writes, **no** APIs, schema, migrations, queue workers, filing automation, browser automation, or timeline **type** changes; **no** changes to **`buildPacketPlainText`**, copy/download/print, evidence/filing **APIs**, **`rules.ts`**, preview, or **`commitIntakeToSessionAndServer`**. When framing is active, an emerald card below the title states Surrenderless assembled the packet from the reviewed submission draft for **in-app preparation and review** — **Your prepared action review** before approval, **Your approved case packet** after (§2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**); the page subtitle shifts to prepared-review wording; copy clarifies **nothing has been filed, submitted, or sent automatically** and Surrenderless has **not** submitted, filed, or contacted anyone; users may record external filings manually in the filing section below and **Surrenderless does not submit or queue** government complaints yet; **pre-approval explainer** and **readiness bridge** when not ready to escalate (§2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**); **Back to action plan** → **`/justice/plan`**. **All existing** packet content remains: case summary, requested resolution, timeline, saved evidence, **`JusticeFilingRecords`**, **`JusticeCaseTasks`**, and the full copy/download/print bundle. **Interim scaffolding** toward §1 (**chat-first → prepare → approve → eventually submit/queue/track**) — **not** real submission or filing by Surrenderless. **Prepared packet approval** (below) is a separate checkpoint on the same page when framing is active.
- **Packet — prepared review explainer and readiness bridge (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/copy** slice (with §2 **Packet — prepared action review framing (`/justice/packet`)**, §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**, §2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**, §2 **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`)**, §2 **Plan — prepared next action after draft review (`/justice/plan`)**). In **`showPreparedActionPacketFraming && !packetApproved`** emerald **Your prepared action review** card, after existing explanatory paragraphs and **before** **Back to action plan** / **Approve for next action** checkpoint, adds page-local **`getPreparedPacketReviewExplainer`** from render-time shared **`pickPreparedNextAction`** **`stepLabel`** from **`preparedNextAction.ts`** (same destination inputs as **`handleApprovePreparedPacket`** — **`computeJusticeDestinations`**, contact state; **no** new API, **no** schema): names next in-app step — **When you approve below, Surrenderless marks this packet ready for {stepLabel} — the next in-app step from your reviewed draft. Surrenderless does not file or submit for you.** When signed in (**`isSignedIn`**) and **`!readyToEscalate`** (**`!basicsReady`** via **`isBasicCaseInfoReadyForEscalation(intake)`** and/or **`!evidenceReady`** from existing **`loadEvidence`** **`evidence.length`**), adds **Before you approve, finish readiness:** bridge with **`packetChecklistLinkCls`** inline links: **Update in chat** → **`/justice/chat-ai`** when **`!basicsReady`**; **Add proof in chat** → **`/justice/chat-ai`** when **`!evidenceReady`**; **Review submission draft** → **`/justice/preview`** when **`!draftReviewed`** (**`submission_draft_reviewed`** timeline). Adds chat correction line: **Need to fix details or add proof notes?** **Update in chat** → **`/justice/chat-ai`**. **Does not** change **`handleApprovePreparedPacket`**, approval persistence/**`client_state`**, **`showPreparedActionPacketFraming`** gates, packet body, copy/download/print, **`JusticeFilingRecords`**, **`JusticeCaseTasks`**, APIs, schema, **`rules.ts`**, plan, preview, chat-ai, hub, cases. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/copy** slice (with §2 **Packet — prepared action review framing (`/justice/packet`)**, §2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**, §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**, §2 **Packet — Open approved step marks started tracking (`/justice/packet`)**, §2 **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`)**). When **`showPreparedActionPacketFraming && packetApproved && approvedNextAction?.label`**, top emerald card retitles to **Your approved case packet** and replaces pre-approval review copy with post-approval context using page-local **`getApprovedPacketNextStepExplainer`** from **`approvedNextAction.label`** and **`status`** (**no** new API, **no** schema): **approved** — **Your approved in-app step is {stepLabel} — open it below or continue from your action plan. Nothing is sent automatically.**; **started** — **`getApprovedPacketNextStepExplainer`** and matching inline copy direct users to **Record handled status from your action plan or chat intake when ready** (§2 **Packet — started-state record-handled copy alignment (`/justice/packet`)**); **completed** — existing handled/completed wording. Bold **`{stepLabel}`** in UI. Keeps **Need to fix details or add proof notes?** **Update in chat** → **`/justice/chat-ai`**. In **Packet approved for next action** inner card when approved and not started/completed, adds line before **Open {label}**: opens in-app preparation/tracking for that step — **nothing is filed or sent automatically** (**Open {label}** started-marking behavior — §2 **Packet — Open approved step marks started tracking (`/justice/packet`)**). **Does not** change **`handleApprovePreparedPacket`**, approval persistence/**`client_state`**, **`showPreparedActionPacketFraming`** gates, packet body, copy/download/print, **`JusticeFilingRecords`**, **`JusticeCaseTasks`**, APIs, schema, **`rules.ts`**, plan, preview, chat-ai, hub, cases. **Does not** claim filing, submission, contact, queueing, upload, storage, or automatic action by Surrenderless. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Packet — prepared packet approval checkpoint (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** + **`src/lib/justice/preparedNextAction.ts`** (shared **`pickPreparedNextAction`** / **`buildApprovedNextActionTarget`** — §2 **Approval-to-action engine — prepared next action shared helpers (`preparedNextAction.ts`)**) + **`JusticeCaseClientState`** in **`src/lib/justice/types.ts`** (`prepared_packet_approved?: boolean` on existing **`justice_cases.client_state`** JSONB). When **`showPreparedActionPacketFraming`** applies (§2 **Packet — prepared action review framing (`/justice/packet`)**, §2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**, §2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**, §2 **Packet — Open approved step marks started tracking (`/justice/packet`)**, §2 **Packet — signed-in approve returns to chat (`/justice/packet`)**), **Approve for next action** appears below the prepared-review card: checkbox **I reviewed this prepared packet**, button **Approve prepared packet for next action**; after approval, emerald **Packet approved for next action** with **Continue from action plan** → **`/justice/plan`** (when **`approved_next_action.status === "started"`** from plan, packet **Open {label}**, or prior visit — §2 **Packet — approved next action started surfaced**, §2 **Packet — Open approved step marks started tracking (`/justice/packet`)** — shows **Next action started** / **Opened for next step** instead). **Persistence:** on load, signed-in users with a **UUID** **`STORAGE_CASE_ID`** **`GET /api/justice/cases/[id]`** and hydrate approved state from **`client_state.prepared_packet_approved`** and **`approved_next_action`** via **`hydrateApprovedNextActionForDisplay`** (including **`started`** and **`handling_*`** — §2 **Approved next action shared state helpers**); on approve, UI updates immediately, session maps are written with **`mergeApprovedNextActionTrackingFields`** so existing **`handling_requested_at`**, **`handling_request_note`**, and **`handling_acknowledged_at`** are preserved when writing a new approval target, then **`GET` + merge + `PATCH`** existing **`/api/justice/cases/[id]`** with merged **`client_state`** (no new API route; **no** schema/migration; **no** new timeline **type** or **`timeline`** writes); when signed in with a **UUID** case, successful **`PATCH`** routes to **`/justice/chat-ai`** (§2 **Packet — signed-in approve returns to chat (`/justice/packet`)**). **Session fallback** remains for local / non-UUID cases. **No** queue workers, filing automation, browser automation, or changes to **`rules.ts`**, **`caseReadiness.ts`**, evidence/filing **APIs**, preview **`submission_draft_reviewed`**, or **`commitIntakeToSessionAndServer`**. Copy states **nothing filed, submitted, sent, or contacted automatically**; Surrenderless does **not** submit or queue yet — scaffolding toward §1 **prepare → approve → start/track**, not real submission. Packet body, copy/download/print, and filing/task CRUD **unchanged**.
- **Packet — signed-in approve returns to chat (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/navigation** slice (with §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**, §2 **Packet — breadcrumb primary link chat-first (`/justice/packet`)**, §2 **Preview — signed-in Continue returns to chat (`/justice/preview`)**, §2 **Chat AI intake — inline prepared packet approval in Active case checklist (`/justice/chat-ai`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`)**). In **`handleApprovePreparedPacket`**, after session/local approval writes and successful signed-in **UUID** **`GET` + merge + `PATCH`** **`client_state`**, **`router.push("/justice/chat-ai")`** — closes chat-first checklist loop (**Review prepared case packet** deep link → packet → **Approve** → back to chat for **Current action tracking**). **Unsigned** / **non-UUID** stays on packet after approve (**unchanged**). **`handleApprovePreparedPacketFromChat`** on **`/justice/chat-ai`** **unchanged** (inline approve stays in chat). Approve checkbox/button text, packet body, handling workbench links, checklist deep links, APIs, schema **unchanged** — **no** filing/submission/contact/queue/upload/storage/automatic-action claims.
- **Packet — signed-in non-UUID approve returns to chat (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/navigation** slice (with §2 **Packet — signed-in approve returns to chat (`/justice/packet`)**, §2 **Preview — signed-in non-UUID Continue returns to chat (`/justice/preview`)**, §2 **Chat AI intake — hydrate & update commit**). In **`handleApprovePreparedPacket`**, after session/local approval writes and state updates, when **`isLoaded && isSignedIn && caseId && !isUuid(caseId)`**, **`router.push("/justice/chat-ai")`** — **completes** signed-in preview→packet→chat checklist routing for session-only case ids (pairs with §2 **Preview — signed-in non-UUID Continue returns to chat (`/justice/preview`)**). **UUID** signed-in path keeps existing **`GET` + merge + `PATCH`** **`client_state`** then **`router.push("/justice/chat-ai")`** (§2 **Packet — signed-in approve returns to chat (`/justice/packet`)**). **Unsigned** / no **`caseId`** stays on packet after approve. **`handleApprovePreparedPacketFromChat`**, approve checkbox/button, packet content, emerald **Continue in chat** links, breadcrumb, handling links, **`pickPreparedNextAction`** / **`buildApprovedNextActionTarget`**, APIs, schema, auth **unchanged** — **no** filing/submission/contact/queue/upload/storage/automatic-action claims.
- **Packet — session caseId approve returns to chat (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/navigation** slice (with §2 **Packet — signed-in non-UUID approve returns to chat (`/justice/packet`)**, §2 **Preview — session caseId Continue returns to chat (`/justice/preview`)**, §2 **Chat AI intake — hydrate & update commit**). In **`handleApprovePreparedPacket`**, after session/local approval writes and state updates, **`router.push("/justice/chat-ai")`** when **`caseId && (!isSignedIn || !isUuid(caseId))`** — **completes** session-backed preview→packet→chat checklist routing for unsigned/local cases (pairs with §2 **Preview — session caseId Continue returns to chat (`/justice/preview`)** (#539)). **Signed-in UUID** path keeps existing **`GET` + merge + `PATCH`** **`client_state`** then **`router.push("/justice/chat-ai")`** (§2 **Packet — signed-in approve returns to chat (`/justice/packet`)**). **No** **`caseId`** unchanged (handler returns early). **`handleApprovePreparedPacketFromChat`**, approve checkbox/button, packet content, emerald **Continue in chat** links, breadcrumb, handling links, **`pickPreparedNextAction`** / **`buildApprovedNextActionTarget`**, APIs, schema, auth **unchanged** — **no** filing/submission/contact/queue/upload/storage/automatic-action claims.
- **Packet — breadcrumb primary link chat-first (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/navigation** slice (with §2 **Packet — signed-in approve returns to chat (`/justice/packet`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**, §2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**). Top breadcrumb row in **main** render and **no-`caseId`** empty state: **Update in chat** → **`/justice/chat-ai`** (first link); **Justice workspace** → **`/justice`** (second); **Evidence** → **`/justice/evidence`** and **Saved cases** → **`/justice/cases`** **unchanged**. Replaces prior **Back to action plan** → **`/justice/plan`** first link + duplicate **Update in chat** second slot — pairs with §2 **Packet — signed-in approve returns to chat (`/justice/packet`)** and §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)** so checklist **Review prepared case packet** → packet exit returns to chat, not plan. **Does not** change approve checkbox/button, **Approve** → chat routing, in-card **Back to action plan** / **Continue from action plan** links, handling workbench links, packet body, APIs, schema, or checklist deep links — **no** filing/submission/contact/queue/upload/storage/automatic-action claims.
- **Packet — approved-packet handling request copy chat-first (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/copy** slice (with §2 **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`)**, §2 **Saved cases — approved-packet handling request copy chat-first (`/justice/cases`)**, §2 **Justice hub — approved-packet handling request copy chat-first (`/justice`)**, §2 **Chat AI intake — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**, §2 **Packet — signed-in approve returns to chat (`/justice/packet`)**). In the approved-packet emerald helper card when **`isApprovedPacketActionWithoutHandlingRequest`**, second sentence **Request Surrenderless handling from chat intake when you want internal triage tracking.** replaces **Request handling from your action plan when you want internal triage tracking.** — aligns with chat-first **Approve** → **`/justice/chat-ai`** routing (§2 **Packet — signed-in approve returns to chat (`/justice/packet`)**) and Saved cases/hub helper copy (§2 **Saved cases — approved-packet handling request copy chat-first (`/justice/cases`)**, §2 **Justice hub — approved-packet handling request copy chat-first (`/justice`)**). First sentence and **View on handling workbench** → **`/justice/handling`** **unchanged**. **Does not** change **Continue from action plan**, breadcrumbs, **Update in chat**, approve → chat routing, handling-request UI, packet body, APIs, schema, or filing/submission/contact/queue/upload/storage/automatic-action behavior. **Copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Packet — approved next action started surfaced (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **display/hydrate** slice (with §2 **Packet — Open approved step marks started tracking (`/justice/packet`)**, §2 **Packet — started-state record-handled copy alignment (`/justice/packet`)**). When **`approved_next_action.status`** is **`"started"`** (from **`/justice/plan`**, packet or chat **Open {label}**, or prior session/server state), **`/justice/packet`** hydrates that state from existing **`justice_cases.client_state`** (signed-in **UUID** **`GET /api/justice/cases/[id]`**) or **`justice_approved_next_action_v1`** session fallback; **`hydrateApprovedNextActionForDisplay`** (built on **`resolveApprovedNextAction`**) coalesces server + session for display and **does not downgrade** **`started`** back to **`approved`**. In the existing top approval / prepared-action area (when framing is active and the packet is already approved), **`status === "started"`** shows **Next action started**, **Opened for next step**, and copy that the approved in-app step was opened (with stored **label** when present); post-approval emerald card uses **`getApprovedPacketNextStepExplainer`** **started** variant with **action plan or chat intake** record-handled guidance (§2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**, §2 **Packet — started-state record-handled copy alignment (`/justice/packet`)**); **`status === "approved"`** keeps prior **Packet approved for next action** wording and **Open {label}** CTA with in-app prep/tracking clarifier (§2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**; started write on click — §2 **Packet — Open approved step marks started tracking (`/justice/packet`)**). **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **no** changes to case summary, timeline, evidence, **`JusticeFilingRecords`**, **`JusticeCaseTasks`**, copy/download/print, or the packet approval checkbox flow; **nothing** filed, submitted, sent, or contacted automatically. Keeps **packet** aligned with **plan** in **prepare → approve → start/track** (§2 **Approved next action started** on plan).
- **Packet — started-state record-handled copy alignment (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **copy-only** slice (with §2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**, §2 **Packet — approved next action started surfaced (`/justice/packet`)**, §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**). When **`approved_next_action.status === "started"`**, started-state guidance no longer says record handled **from your action plan when ready** alone: page-local **`getApprovedPacketNextStepExplainer`** **started** branch and duplicate inline started-state copy in the post-approval explainer (and matching inner-card started branch) now say **from your action plan or chat intake when ready** — aligned with chat **Record action handled for now** (§2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**) and existing handling/workbench **action plan or chat intake** wording. **Does not** change **`handleApprovedNextActionOpen`**, **`handleApprovePreparedPacket`**, hydrate, **`persistApprovedNextActionClientState`**, routing, buttons, Open/Link behavior, Plan, Chat AI, Hub, Cases, APIs, schema, or state handling. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Packet — approved-step explainer chat-first (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/copy** slice (with §2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**, §2 **Packet — started-state record-handled copy alignment (`/justice/packet`)**, §2 **Packet — signed-in approve returns to chat (`/justice/packet`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Preview — signed-in Continue button label chat-first (`/justice/preview`)**). In page-local **`getApprovedPacketNextStepExplainer`** **approved** branch and matching visible JSX in top emerald **Your approved case packet** explainer, **open it below or continue in chat. Nothing is sent automatically.** replaces **open it below or continue from your action plan. Nothing is sent automatically.** — aligns with chat-first **Approve** → **`/justice/chat-ai`** routing (§2 **Packet — signed-in approve returns to chat (`/justice/packet`)**) and hub/preview **Continue in chat** CTAs. **Started** / **completed** branches **unchanged**. **Does not** change **Continue from action plan**, **Back to action plan**, approve → chat routing, handling-request UI, breadcrumbs, **Update in chat**, **Open {label}**, APIs, schema, or navigation handlers. **Copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Packet — started-state record-handled copy chat-first ordering (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/copy** slice (with §2 **Packet — started-state record-handled copy alignment (`/justice/packet`)**, §2 **Packet — approved-step explainer chat-first (`/justice/packet`)**, §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, §2 **Packet — signed-in approve returns to chat (`/justice/packet`)**). In page-local **`getApprovedPacketNextStepExplainer`** **started** branch and matching visible JSX in top emerald **Your approved case packet** explainer, record-handled location clause **from chat intake, or from your action plan when ready** replaces **from your action plan, or from chat intake when ready** — chat-first ordering after §2 **Packet — started-state record-handled copy alignment (`/justice/packet`)** added both destinations. **Approved** / **completed** branches **unchanged**. **Does not** change **Continue from action plan**, **Back to action plan**, approve → chat routing, handling-request UI, breadcrumbs, **Update in chat**, **Open {label}**, record-handled behavior, APIs, schema, or navigation handlers. **Copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Packet — approved-step started explainer plan de-bias (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/copy** slice (with §2 **Packet — started-state record-handled copy chat-first ordering (`/justice/packet`)**, §2 **Packet — approved-step explainer chat-first (`/justice/packet`)**, §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, §2 **Packet — signed-in approve returns to chat (`/justice/packet`)**). In page-local **`getApprovedPacketNextStepExplainer`** **started** branch and matching visible JSX in top emerald **Your approved case packet** explainer, record-handled location clause **from chat intake when ready** replaces **from chat intake, or from your action plan when ready** — removes action-plan destination after §2 **Packet — started-state record-handled copy chat-first ordering (`/justice/packet`)** kept both. **Approved** / **completed** branches **unchanged**. **Does not** change **Back to action plan**, approve → chat routing, handling-request UI, breadcrumbs, **Update in chat**, inner approved card started copy, **Open {label}**, record-handled behavior, APIs, schema, auth, plan page, or navigation handlers. **Copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Packet — inner approved card Continue in chat link (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/navigation** slice (with §2 **Packet — approved-step explainer chat-first (`/justice/packet`)**, §2 **Packet — signed-in approve returns to chat (`/justice/packet`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Preview — signed-in Continue button label chat-first (`/justice/preview`)**). On inner emerald **Packet approved for next action** card, **`Link`** **Continue in chat** → **`/justice/chat-ai`** replaces **Continue from action plan** → **`/justice/plan`** — aligns with top explainer **continue in chat** (§2 **Packet — approved-step explainer chat-first (`/justice/packet`)**) and **Approve** → chat routing (§2 **Packet — signed-in approve returns to chat (`/justice/packet`)**). Same link styling/classes. **Does not** change **Back to action plan**, top emerald explainers, **View on handling workbench**, handling-request UI, **Open {label}**, record-handled behavior, breadcrumbs, **Update in chat**, APIs, schema, or navigation handlers. **Navigation only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Packet — inner approved card approved-state body copy chat-first (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/copy** slice (with §2 **Packet — inner approved card Continue in chat link (`/justice/packet`)**, §2 **Packet — approved-step explainer chat-first (`/justice/packet`)**, §2 **Packet — signed-in approve returns to chat (`/justice/packet`)**, §2 **Justice hub — Current case primary CTA routing (`/justice`)**). On inner emerald **Packet approved for next action** card **approved** branch only (**`!approvedNextActionStarted && !approvedNextActionCompleted`**), body clause **Continue in chat when you are ready for the next in-app step.** replaces **Continue from your action plan when you are ready for the next in-app step.** — aligns body copy with **Continue in chat** link (§2 **Packet — inner approved card Continue in chat link (`/justice/packet`)**) and top explainer **continue in chat** (§2 **Packet — approved-step explainer chat-first (`/justice/packet`)**). **Started** / **completed** branches, **Continue in chat** link/href, **Back to action plan**, top emerald explainers, approve → chat routing, handling-request UI, record-handled behavior, **Open {label}**, APIs, schema, styling/classes **unchanged**. **Copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Packet — top emerald card Continue in chat link (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/navigation** slice (with §2 **Packet — inner approved card Continue in chat link (`/justice/packet`)**, §2 **Packet — approved-step explainer chat-first (`/justice/packet`)**, §2 **Packet — signed-in approve returns to chat (`/justice/packet`)**, §2 **Packet — breadcrumb primary link chat-first (`/justice/packet`)**, §2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**). In top emerald **Your prepared action review** / **Your approved case packet** explainer card only, footer **`Link`** **Continue in chat** → **`/justice/chat-ai`** replaces **Back to action plan** → **`/justice/plan`** — closes chat-first checklist loop (**Review prepared case packet** → packet → **Continue in chat** → chat) and aligns with inner approved card **Continue in chat** (§2 **Packet — inner approved card Continue in chat link (`/justice/packet`)**) and **Approve** → chat routing (§2 **Packet — signed-in approve returns to chat (`/justice/packet`)**). Same link styling/classes. **Does not** change breadcrumb row, **Approve for next action** checkpoint, inner approved card, handling UI, filing records, **`handleApprovePreparedPacket`** routing, explainer copy, **Update in chat** breadcrumb, APIs, schema, or navigation handlers. **Navigation only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Packet — handled-for-now timestamp on approved next action card (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **read-only continuity display** slice (with §2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**, §2 **Packet — approved next action started surfaced (`/justice/packet`)**, §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, §2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)**, §2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**, §2 **Justice hub — handled-for-now timestamp on Current case (`/justice`)**, §2 **Chat AI intake — handled-for-now timestamp in Current action tracking (`/justice/chat-ai`)**, §2 **Plan — handled-for-now timestamp on prepared-packet approved callouts (`/justice/plan`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In the **`packetApproved`** inner emerald **Next action recorded as handled** card only, after the existing completed prose block and before **`ApprovedNextActionTrackingSummary`**, when hydrated **`approvedNextAction.status === "completed"`** and **`completed_at`** is present (from existing page hydrate via **`hydrateApprovedNextActionForDisplay`** — **no** new **`GET`** or **`PATCH`**), shows read-only **Handled for now {formatted time}** via existing **`formatApprovedNextActionHandlingTimestamp`**. **Does not** change top explainer copy, **`ApprovedNextActionTrackingSummary`**, outcome/follow-up form (§2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)**), **Open {label}**, handling blocks, workbench links, approval checkpoint, or show **completed** approved-packet actions in **Needs attention** or handling workbench **Approved packet actions** inbox lists. **Display correctness only** — **no** new API route, schema/migration, DB field, timeline **type**, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Packet — started-at read-only on approved next action card (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **display-only** slice (with §2 **Packet — handled-for-now timestamp on approved next action card (`/justice/packet`)**, §2 **Packet — record action handled on approved next action card (`/justice/packet`)**, §2 **Packet — approved next action started surfaced (`/justice/packet`)**, §2 **Packet — Open approved step marks started tracking (`/justice/packet`)**, §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**, §2 **Approved next action started (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In the **`packetApproved`** inner emerald approved-next-action card only — **Packet approved for next action** / **Next action started** — inside the existing **`approvedNextActionStarted`** block, when **`approvedNextAction.status === "started"`** and **`started_at`** is set, shows read-only **Opened {formatted time}** via existing **`formatApprovedNextActionHandlingTimestamp`** below **Opened for next step.** and before **Record action handled for now** (§2 **Packet — record action handled on approved next action card (`/justice/packet`)**) / **Open {label}** / completed **`ApprovedNextActionTrackingSummary`** blocks — parity with read-only **Handled for now {formatted time}** when **`status === "completed"`** (§2 **Packet — handled-for-now timestamp on approved next action card (`/justice/packet`)**); uses existing **`approved_next_action.started_at`** from already-hydrated **`approvedNextAction`** / **`hydrateApprovedNextActionForDisplay`** (**no** new **`GET`** or **`PATCH`**). Complements §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, and §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**. **Does not** change **Open {label}**, top explainer copy, handling blocks, workbench links, approval checkpoint, outcome/follow-up form, clear follow-up, chat-ai, plan, cases, hub, handling workbench, membership predicates, or any write paths. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Packet — record action handled on approved next action card (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **behavior** slice (with §2 **Packet — Open approved step marks started tracking (`/justice/packet`)**, §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, §2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)**, §2 **Packet — started-state record-handled copy alignment (`/justice/packet`)**, §2 **Approved next action completed / handled (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Plan — prepared packet approval surfaced (`/justice/plan`)**, §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**, §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In the **`packetApproved`** inner emerald approved-next-action card only, inside the existing **`approvedNextActionStarted`** block when **`approvedNextAction.status === "started"`**, adds **Record action handled for now** calling page-local **`handleMarkApprovedNextActionHandled`**: builds next **`approved_next_action`** preserving existing fields and setting **`status: "completed"`** and **`completed_at`** (ISO), persists via existing **`persistApprovedNextAction`** (**`mergeApprovedNextActionTrackingFields`**, **`writeSessionApprovedNextAction`**, signed-in **UUID** **`GET` + merge + `PATCH`** **`client_state`** through **`persistApprovedNextActionClientState`**) — mirrors §2 **Plan — prepared packet approval surfaced (`/justice/plan`)** **`handleMarkApprovedNextActionHandled`**. Placed after **Opened {formatted time}** (§2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**) and before **Open {label}** / completed **`ApprovedNextActionTrackingSummary`** blocks, with **Tracking only — not automatic filing or submission.** subcopy. Started-state explainer copy updated so record-handled is not plan/chat-only (§2 **Packet — started-state record-handled copy alignment (`/justice/packet`)**). **Does not** change **`handleApprovedNextActionOpen`**, outcome/follow-up form, clear follow-up, handling blocks, workbench links, approval checkpoint, plan, chat-ai, cases, hub, handling workbench, membership predicates, or routing. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior.
- **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **behavior** slice (with §2 **Packet — record action handled on approved next action card (`/justice/packet`)**, §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**, §2 **Plan — prepared packet approval surfaced (`/justice/plan`)**, §2 **Packet — clear follow-up on approved next action card (`/justice/packet`)**, §2 **Packet — handled-for-now timestamp on approved next action card (`/justice/packet`)**, §2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)**, §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**, §2 **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In the **`packetApproved`** inner emerald **Next action recorded as handled** card only, when **`packetApproved`** and hydrated **`approvedNextAction.status === "completed"`**, shows page-local **`ApprovedNextActionOutcomeTrackingForm`** after existing **`ApprovedNextActionTrackingSummary`** — same field semantics as chat/plan: **`outcome_note`**, **`follow_up_needed`**, **`follow_up_at`** only; **`handleSaveApprovedNextActionTracking`** builds the next **`approved_next_action`** object and persists via existing **`persistApprovedNextAction`** (**`mergeApprovedNextActionTrackingFields`**, **`mergeClientStateWithApprovedNextAction`**, signed-in **UUID** **`GET` + merge + `PATCH`** on existing **`/api/justice/cases/[id]`**, **`writeSessionApprovedNextAction`**). Clear follow-up when **`follow_up_needed`** — §2 **Packet — clear follow-up on approved next action card (`/justice/packet`)**. Copy: **Tracking only — not automatic filing or submission.** **Does not** change **`status`**, **`completed_at`**, handling-request fields, **`ApprovedNextActionTrackingSummary`**, handling blocks, workbench links, approval checkpoint, hub/cases/handling/workbench read-only surfaces, chat/plan write paths, or show **completed** approved-packet actions in **Needs attention** or handling workbench **Approved packet actions** inbox lists. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Packet — clear follow-up on approved next action card (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **behavior** slice (with §2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)**, §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Plan — clear follow-up on prepared-packet callouts (`/justice/plan`)**, §2 **Saved cases — clear Needs attention follow-up (`/justice/cases`)**, §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**, §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In the **`packetApproved`** inner emerald **Next action recorded as handled** card only, when **`approved_next_action.status === "completed"`** and **`follow_up_needed === true`**, shows **Mark follow-up handled** below **`ApprovedNextActionOutcomeTrackingForm`**. **`handleClearApprovedNextActionFollowUp`** calls existing **`clearFollowUpFromApprovedNextAction`** (omits **`follow_up_needed`** only; preserves **`outcome_note`**, **`follow_up_at`**, **`status`**, **`href`**, **`label`**, **`approved_at`**, **`started_at`**, **`completed_at`**, and other **`approved_next_action`** fields) and persists via existing **`persistApprovedNextAction`** (**`mergeApprovedNextActionTrackingFields`** + **`GET` + merge + `PATCH`** **`client_state`** for signed-in **UUID** cases; **`justice_approved_next_action_v1`** session sync). After clear, case leaves **Saved cases** **Needs attention** follow-up list (no longer **`follow_up_needed === true`**) but outcome notes and dates remain on packet and **All cases**. Helper copy: **Clears this from Needs attention on Saved cases. Your outcome note and dates stay saved. Not automatic filing or submission.** — mirrors plan / chat-ai / cases / handling workbench clear-follow-up pattern. **Does not** dedupe **`ApprovedNextActionTrackingSummary`** and **`ApprovedNextActionOutcomeTrackingForm`**; **does not** change plan, chat-ai, cases, handling workbench, **`isHandlingAwaitingTriageApprovedNextAction`**, **Needs attention** builders (read-only membership from **`follow_up_needed`**), acknowledge **`PATCH`**, open-step or record-handled writes, routing, or outcome-form save semantics. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Packet — Open approved step marks started tracking (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **behavior** slice mirroring §2 **Approved next action started (`/justice/plan`)** (with §2 **Packet — approved next action started surfaced (`/justice/packet`)**, §2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**, §2 **Packet — started-state record-handled copy alignment (`/justice/packet`)**, §2 **Packet — handled-for-now timestamp on approved next action card (`/justice/packet`)**). When the packet is approved and **`approved_next_action.status`** is **`"approved"`** (not **`started`** or **`completed`**), **Open {label}** in the **Packet approved for next action** inner card is a **`<button>`** calling page-local **`handleApprovedNextActionOpen`**: builds **`approved_next_action`** with existing **`label`** / **`href`**, sets **`status: "started"`** and **`started_at`** (preserves **`approved_at`**), merges tracking fields via **`mergeApprovedNextActionTrackingFields`**, writes **`justice_approved_next_action_v1`** and **`justice_prepared_packet_approved_v1`** session maps, updates React state, **`await`**s page-local **`persistApprovedNextActionClientState`** (**`GET` + **`mergeClientStateWithApprovedNextAction`** + **`PATCH`** existing **`/api/justice/cases/[id]`** **`client_state`**) for signed-in **UUID** cases, then **`router.push`** to the approved **`href`**. If already **`completed`**, navigates only — **no** status downgrade. When **`started`** or **`completed`**, **Open {label}** remains plain **`Link`** navigation — **does not** downgrade **`started`** → **`approved`** or **`completed`** to a lower state. **Does not** change approval checkpoint, **`handleApprovePreparedPacket`**, post-approval explainer copy, **`showPreparedActionPacketFraming`** gates, packet body, copy/download/print, **`JusticeFilingRecords`**, **`JusticeCaseTasks`**, prep routes, APIs, schema, **`rules.ts`**, plan, preview, chat-ai, hub, cases. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Packet — handling requested visibility (prepared-action area) (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** — **read-only mirror** of plan/chat/cases (§2 **Plan — Surrenderless handling requested**, **Chat AI intake — handling requested in Current action tracking**, **Saved cases — handling requested visibility on All cases cards**). When **`showPreparedActionPacketFraming`** is active and **`client_state.approved_next_action.handling_requested_at`** is set (hydrated via **`hydrateApprovedNextActionForDisplay`** — §2 **Approved next action shared state helpers**), **`ApprovedNextActionHandlingRequestedReadOnly`** shows **Surrenderless handling requested**, recorded time, optional **`handling_request_note`**, and optional **Acknowledged {time} — internal tracking triage only** when **`handling_acknowledged_at`** is set; when **`handling_requested_at`** is set, **display-only derived queue status** via **`ApprovedNextActionHandlingQueueStatusReadOnly`** (§2 **Packet — display-only derived handling queue status**) and **View in handling workbench** → **`/justice/handling`** (§2 **Handling workbench — active-case back-link from packet**) appear in the handling fragment. Copy: **in-app tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone yet. **No** request button, **no** new persistence, **no** change to **Approve prepared packet for next action** flow. **No** schema, migration, new API route, queue worker, **`justice_case_filings`** row, timeline **type**, browser automation, filing automation, or **`/api/submit-form`**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Packet — display-only derived handling queue status (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** (**`ApprovedNextActionHandlingQueueStatusReadOnly`**, **`deriveHandlingQueueDisplayStatus`**) — **visibility-only** slice aligning packet with §2 **Saved cases — handling requested visibility on All cases cards**, **Handling workbench — internal triage surface**, **Chat AI intake — display-only derived handling queue status**, and **Plan — display-only derived handling queue status**. When **`approved_next_action.handling_requested_at`** is set (hydrated **`approved_next_action`** in the prepared-action handling fragment), renders **display-only derived queue status** from existing **`handling_requested_at`** and **`handling_acknowledged_at`** only (**no** new persisted field, **no** writes): **Queue status: Awaiting internal triage.** when **`handling_acknowledged_at`** is absent; **Queue status: Acknowledged for internal tracking triage only** when **`handling_acknowledged_at`** is set. Placed after **`ApprovedNextActionHandlingRequestedReadOnly`** and before handled/open triage note (§2 **Packet — handled/open handling triage note in prepared-action area**, when applicable) and **View in handling workbench** (§2 **Handling workbench — active-case back-link from packet**). When **`handling_requested_at`** is absent, **no** queue status line. **Does not** change packet approval, timeline, **`JusticeFilingRecords`**, **`JusticeCaseTasks`**, follow-up UI, workbench link, or other packet copy. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone. **No** schema, migration, new API route, queue worker, **`justice_case_filings`** row, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Packet — handled/open handling triage note in prepared-action area (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** — **copy-only** slice (with §2 **Packet — Surrenderless handling request and acknowledge on approved case packet surface (`/justice/packet`)**, §2 **Approved next action — centralized handled/open handling triage note copy**). In the prepared-action handling fragment when **`showPreparedActionPacketFraming`** is active and hydrated **`approved_next_action`** has **`handling_requested_at`**, no **`handling_acknowledged_at`**, and **`status === "completed"`**, renders **`ApprovedNextActionHandlingHandledOpenTriageNote`** — **`inlineAck`** when **`packetApproved`** (§2 **Packet — Surrenderless handling request and acknowledge on approved case packet surface (`/justice/packet`)**); legacy **`redirect`** when handling is visible before packet approval — after **display-only** queue status (§2 **Packet — display-only derived handling queue status**) and before **View in handling workbench** (§2 **Handling workbench — active-case back-link from packet**). **Hidden** when **`status !== "completed"`**, when **`handling_acknowledged_at`** is set, or when **`handling_requested_at`** is absent. **Does not** change packet approval/**PATCH**, timeline, filings, tasks, hydrate paths, or external-action behavior. **No** schema, migration, new API route, queue worker, **`justice_case_filings`**, timeline **type**, or external submission/contact/queue automation.
- **Packet — handling tracking status in prepared-action handling UI (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **read-only sync** slice (with §2 **Packet — Surrenderless handling request and acknowledge on approved case packet surface (`/justice/packet`)**, §2 **Packet — display-only derived handling queue status (`/justice/packet`)**, §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**, §2 **Saved cases — handling tracking status on All cases cards (`/justice/cases`)**, §2 **Plan — handling tracking status on prepared-packet handling callouts (`/justice/plan`)**, §2 **Justice hub — handling tracking status on Current case (`/justice`)**, §2 **Chat AI intake — handling tracking status in Current action tracking (`/justice/chat-ai`)**, §2 **Approved next action — handling tracking contextual action links (justice surfaces)**). In **both** prepared-action handling blocks when **`approved_next_action.handling_requested_at`** is set — **`packetApproved && approvedNextAction`** path and legacy **`handling_requested_at`** path before packet approval — **`PacketHandlingTrackingStatusReadOnly`** shows read-only **Handling tracking:** after **`ApprovedNextActionHandlingQueueStatusReadOnly`** — **Loading handling tracking context...** while **`packetHandlingReadinessLoading`** (**`evidenceLoading`** or **`filingsLoading`** on existing packet fetches); when ready, **Handling tracking: {derived next step}** from page-local **`derivePacketManualActionNextStep`** / **`derivePacketHandlingTrackingLine`** mirroring workbench **`deriveManualActionNextStep`** gate order (**`handlingTrackingBasicsReady`**, **`handlingTrackingDraftReviewed`**, **`packetApproved`**, **`approved_next_action`** status/outcome/ack/follow-up, **`evidence.length`**, **`hasFilingRecord`** / **`hasConfirmationOnFile`** from already-fetched **`filings`** — same meaning as workbench **`handlingCaseHasFilingRecord`** / **`handlingCaseHasConfirmationOnFile`**); when gates are satisfied, **Handling tracking: Tracking complete for now.** — parity with workbench **Tracking complete** queue — §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**. Nearby subcopy: **In-app tracking only — not filed or submitted.** Placed before handled/open triage note (§2 **Packet — handled/open handling triage note in prepared-action area (`/justice/packet`)**) / **View in handling workbench**. Reuses existing packet **`GET /api/justice/evidence`** + **`GET /api/justice/filings`** — **no** new **`GET`**, **`POST`**, **`PATCH`**, **`client_state`** fields, timeline writes, workers, or queues. **Does not** change **Request Surrenderless handling**, note edit, **Mark acknowledged**, outcome/follow-up forms, **Record action handled**, **Open** step, workbench link, **`JusticeFilingRecords`** CRUD, or other packet copy. **Read-only in-app sync only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, contacted anyone, or closed the real-world case. **No** schema, migration, new API route, queue worker, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**.
- **Approved next action — handling tracking contextual action links (justice surfaces):** **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** + **`src/app/justice/packet/page.tsx`**, **`src/app/justice/plan/page.tsx`**, **`src/app/justice/cases/page.tsx`**, **`src/app/justice/chat-ai/page.tsx`**, **`src/app/justice/JusticeHubWorkspaceBody.tsx`** — **read-only navigation** slice (with §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — confirmation follow-up queue on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome closure queue on handling-request triage cards (`/justice/handling`)**, §2 **Saved cases — handling tracking status on All cases cards (`/justice/cases`)** (#470), §2 **Plan — handling tracking status on prepared-packet handling callouts (`/justice/plan`)** (#471), §2 **Justice hub — handling tracking status on Current case (`/justice`)** (#472), §2 **Chat AI intake — handling tracking status in Current action tracking (`/justice/chat-ai`)** (#473), §2 **Packet — handling tracking status in prepared-action handling UI (`/justice/packet`)** (#474), §2 **Saved cases — session-aware handling tracking contextual links on All cards (`/justice/cases`)**). Below **Handling tracking:** on all five user surfaces when **`handling_requested_at`** is set, **`ApprovedNextActionHandlingTrackingContextualLink`** renders one honest navigation link from shared **`resolveHandlingTrackingContextualLink`** keyed on existing derived step strings (**`HANDLING_TRACKING_STEP_*`** — parity with workbench **`deriveManualActionNextStep`**): readiness → **`/justice/chat-ai`** or **`/justice/packet`**; open approved step → **`approved_next_action.href`** or **`/justice/packet`**; filing/confirmation → **`/justice/packet#packet-filings`** (same-page **`#packet-filings`** on packet); outcome / unacknowledged ack / follow-up → **`/justice/plan`** when not already on-plan with controls visible; **Tracking complete for now.** → **no** link. Suppresses redundant same-surface links (e.g. plan outcome when outcome form is on-page; **Mark acknowledged** when button already visible). **`id="packet-filings"`** wraps **`JusticeFilingRecords`** on **`/justice/packet`**. Link copy only — **Open filing records**, **Open approved step**, **Record outcome on action plan**, **Review follow-up on action plan**, etc. — **never** filed/submitted/contacted/closed. **Read-only navigation only** — **no** new API route, **`client_state`** fields, timeline writes, workers, queues, gate derivation changes, request/note/ack/outcome/follow-up writes, **Record action handled**, **Open** step, workbench link, or **`JusticeFilingRecords`** CRUD behavior. **Does not** change **`/justice/handling`** workbench pages. **No** schema, migration, queue worker, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**.
- **Approved next action — handling tracking Mark acknowledged contextual link chat-first routing (`approvedNextActionHandlingDisplay.tsx`):** **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** only — **UI/navigation** slice (with §2 **Approved next action — handling tracking contextual action links (justice surfaces)**, §2 **Approved next action — handled/open triage redirect note chat-first acknowledge ordering (`approvedNextActionHandlingDisplay.tsx`)**, §2 **Handling workbench — Awaiting intro acknowledge copy chat-first ordering (`/justice/handling`)**, §2 **Chat AI intake — acknowledge handling request in Current action tracking (`/justice/chat-ai`)**, §2 **Saved cases — session-aware handling tracking contextual links on All cards (`/justice/cases`)**). In **`resolveHandlingTrackingContextualLink`** **`HANDLING_TRACKING_STEP_MARK_ACKNOWLEDGED`** branch only: when **`markAcknowledgedOnScreen`**, return **`null`** (**unchanged**); when **`surface === "chat-ai"`**, return **`null`** (mirror prior on-plan suppression); otherwise **`{ href: "/justice/chat-ai", label: "Mark acknowledged in chat" }`** replaces **`{ href: "/justice/plan", label: "Mark acknowledged on action plan" }`** — aligns contextual ack navigation with chat-first acknowledge copy (§2 **Approved next action — handled/open triage redirect note chat-first acknowledge ordering (`approvedNextActionHandlingDisplay.tsx`)**) and **Mark acknowledged** in chat Current action tracking (§2 **Chat AI intake — acknowledge handling request in Current action tracking (`/justice/chat-ai`)**). **`ApprovedNextActionHandlingTrackingContextualLink`** consumers on hub, packet, plan, chat-ai, **All cases** (**`openHandlingTrackingContextualLink`** session activation — §2 **Saved cases — session-aware handling tracking contextual links on All cards (`/justice/cases`)**) pick up new target automatically — **no** page-file edits. **`RECORD_OUTCOME`**, **`REVIEW_FOLLOW_UP`**, filing/confirmation hash links, **`REVIEW_PACKET`** / **`OPEN_APPROVED`**, **`redirect`** / **`inlineAck`** triage notes, handling workbench copy, on-screen **Mark acknowledged** UI/behavior, gate derivation, APIs, schema, styling **unchanged**. **Navigation only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Approved next action — handling tracking Record outcome contextual link chat-first routing (`approvedNextActionHandlingDisplay.tsx`):** **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** only — **UI/navigation** slice (with §2 **Approved next action — handling tracking Mark acknowledged contextual link chat-first routing (`approvedNextActionHandlingDisplay.tsx`)**, §2 **Approved next action — handling tracking contextual action links (justice surfaces)**, §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**, §2 **Saved cases — session-aware handling tracking contextual links on All cards (`/justice/cases`)**). In **`resolveHandlingTrackingContextualLink`** **`HANDLING_TRACKING_STEP_RECORD_OUTCOME`** branch only: when **`surface === "plan"`**, return **`null`** (**unchanged** — outcome form on-page); when **`surface === "chat-ai"`**, return **`null`**; otherwise **`{ href: "/justice/chat-ai", label: "Record outcome in chat" }`** replaces **`{ href: "/justice/plan", label: "Record outcome on action plan" }`** — aligns contextual outcome navigation with **Record outcome / follow-up** in chat Current action tracking (§2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**) and Mark acknowledged chat-first routing (§2 **Approved next action — handling tracking Mark acknowledged contextual link chat-first routing (`approvedNextActionHandlingDisplay.tsx`)**). **`ApprovedNextActionHandlingTrackingContextualLink`** consumers pick up new target automatically — **no** page-file edits. **`HANDLING_TRACKING_STEP_MARK_ACKNOWLEDGED`** branch, **`REVIEW_FOLLOW_UP`**, filing/confirmation hash links, **`REVIEW_PACKET`** / **`OPEN_APPROVED`**, **`redirect`** / **`inlineAck`** triage notes, handling workbench copy, outcome form UI/behavior on each surface, gate derivation, APIs, schema, styling **unchanged**. **Navigation only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Approved next action — handling tracking Review follow-up contextual link chat-first routing (`approvedNextActionHandlingDisplay.tsx`):** **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** only — **UI/navigation** slice (with §2 **Approved next action — handling tracking Record outcome contextual link chat-first routing (`approvedNextActionHandlingDisplay.tsx`)**, §2 **Approved next action — handling tracking Mark acknowledged contextual link chat-first routing (`approvedNextActionHandlingDisplay.tsx`)**, §2 **Approved next action — handling tracking contextual action links (justice surfaces)**, §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**, §2 **Saved cases — session-aware handling tracking contextual links on All cards (`/justice/cases`)**). In **`resolveHandlingTrackingContextualLink`** **`HANDLING_TRACKING_STEP_REVIEW_FOLLOW_UP`** branch only: when **`surface === "plan"`**, return **`null`** (**unchanged** — **Mark follow-up handled** on-page); when **`surface === "chat-ai"`**, return **`null`**; otherwise **`{ href: "/justice/chat-ai", label: "Review follow-up in chat" }`** replaces **`{ href: "/justice/plan", label: "Review follow-up on action plan" }`** — completes chat-first routing for handling-tracking contextual links (Mark acknowledged, Record outcome, Review follow-up) and aligns with **Mark follow-up handled** in chat Current action tracking (§2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**). **`ApprovedNextActionHandlingTrackingContextualLink`** consumers pick up new target automatically — **no** page-file edits. **`HANDLING_TRACKING_STEP_MARK_ACKNOWLEDGED`** / **`RECORD_OUTCOME`** branches, filing/confirmation hash links, **`REVIEW_PACKET`** / **`OPEN_APPROVED`**, **`redirect`** / **`inlineAck`** triage notes, handling workbench copy, follow-up form/button UI/behavior on each surface, gate derivation, APIs, schema, styling **unchanged**. **Navigation only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Saved cases — session-aware handling tracking contextual links on All cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** — **read-only navigation** slice (with §2 **Approved next action — handling tracking contextual action links (justice surfaces)** (#475), §2 **Saved cases — handling tracking status on All cases cards (`/justice/cases`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**, §2 **Saved cases — Needs attention opens approved action surface (`/justice/cases`)**, §2 **Saved cases — handling tracking on Needs attention handling-request cards (`/justice/cases`)**). On **All cases** cards only, **`CaseApprovedNextActionTracking`** passes **`onNavigate`** to **`ApprovedNextActionHandlingTrackingContextualLink`** — **`openHandlingTrackingContextualLink(caseRow, href)`** calls **`activateCaseInSession(caseRow)`** first (**`STORAGE_CASE_ID`**, intake, timeline, approved-next-action session hydrate — same as **`openCase`** / **`openApprovedNextActionFollowUp`** / **`openUpdateInChat`**), then **`router.push(resolvedHref)`** for resolver targets including **`/justice/packet#packet-filings`**, **`/justice/plan`**, **`/justice/chat-ai`**, and internal **`approved_next_action.href`**. Hub/plan/packet/chat-ai contextual links remain plain **`<Link>`** (**unchanged**). **Does not** change **`resolveHandlingTrackingContextualLink`** mapping, link labels, gate derivation, **Needs attention** membership, **Open case** / funnel routing, acknowledge writes, or other Saved cases handlers. **Read-only navigation only** — **no** new API route, **`client_state`** fields, timeline writes, workers, queues, filing/submission/contact automation. **No** schema, migration, queue worker, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**.
- **Saved cases — handling tracking on Needs attention handling-request cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **read-only sync + navigation** slice (with §2 **Saved cases — Needs attention for handling requested (`/justice/cases`)**, §2 **Saved cases — handling tracking status on All cases cards (`/justice/cases`)** (#470), §2 **Approved next action — handling tracking contextual action links (justice surfaces)** (#475), §2 **Saved cases — session-aware handling tracking contextual links on All cards (`/justice/cases`)** (#476), §2 **Saved cases — handling tracking on approved-packet-action cards (`/justice/cases`)**, §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — confirmation follow-up queue on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome closure queue on handling-request triage cards (`/justice/handling`)**). On **Needs attention → handling-request** cards only (**`handlingRequestedAttentionItems`** / **`buildHandlingRequestedAttentionItems`** — membership **unchanged** via **`isHandlingAwaitingTriageApprovedNextAction`**), after **`ApprovedNextActionHandlingQueueStatusReadOnly`** and before disclaimers/buttons: read-only **Handling tracking:** — **Loading handling tracking context...** while **`progressById[caseRow.id]`** is undefined; when ready, **Handling tracking: {derived next step}** from existing **`deriveCasesHandlingTrackingLine`** (workbench **`deriveManualActionNextStep`** parity); subcopy **In-app tracking only — not filed or submitted.**; **`ApprovedNextActionHandlingTrackingContextualLink`** with **`onNavigate`** → **`openHandlingTrackingContextualLink`** (**`activateCaseInSession`** then **`router.push`** — #476 pattern). **`markAcknowledgedOnScreen={true}`** suppresses gate-6 link when **Mark acknowledged** is on-card. Reuses existing **`progressById`** fetches — **no** new **`GET`**, **`POST`**, **`PATCH`**, **`client_state`** fields, timeline writes, workers, or queues. **Does not** change **All cases** cards, queue status, **Mark acknowledged**, **Open case**, workbench links, acknowledge writes, **Record action handled**, or other **Needs attention** card types. **Read-only navigation only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, contacted anyone, or closed the real-world case. **No** schema, migration, queue worker, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**.
- **Saved cases — handling tracking on approved-packet-action cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **read-only sync + navigation** slice (with §2 **Approval-to-action engine — approved packet action visibility on Saved cases Needs attention (`/justice/cases`)**, §2 **Saved cases — handling tracking status on All cases cards (`/justice/cases`)** (#470), §2 **Approved next action — handling tracking contextual action links (justice surfaces)** (#475), §2 **Saved cases — session-aware handling tracking contextual links on All cards (`/justice/cases`)** (#476), §2 **Saved cases — handling tracking on Needs attention handling-request cards (`/justice/cases`)** (#477), §2 **Packet — handling tracking on approved-packet-action prepared-action UI (`/justice/packet`)**, §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — confirmation follow-up queue on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome closure queue on handling-request triage cards (`/justice/handling`)**). On **approved-packet-action** cards without **`handling_requested_at`** — **All cases** **`CaseApprovedNextActionTracking`** when **`parseApprovedPacketActionWithoutHandlingRequest(clientState)`** and **Needs attention → Approved packet action** cards (**`approvedPacketActionAttentionItems`** / **`buildApprovedPacketActionAttentionItems`** — membership **unchanged**): after status / **`approved_at`** / opened-at lines and before **Record action handled** / disclaimers / buttons, read-only **Handling tracking:** — **Loading handling tracking context...** while **`progressLoading && progressById[caseRow.id]`** is undefined; when ready, **Handling tracking: {derived next step}** from existing **`deriveCasesHandlingTrackingLine`** (workbench **`deriveManualActionNextStep`** parity — gate 6 ack only when **`handling_requested_at`** is set, so **not** shown on this path); subcopy **In-app tracking only — not filed or submitted.**; **`ApprovedNextActionHandlingTrackingContextualLink`** with **`onNavigate`** → **`openHandlingTrackingContextualLink`** (#476 pattern). **`markAcknowledgedOnScreen={false}`** — **no** inline **Mark acknowledged** on these cards. **No** queue status lines — not a Surrenderless handling request. Reuses existing **`progressById`** fetches — **no** new **`GET`**, **`POST`**, **`PATCH`**, **`client_state`** fields, timeline writes, workers, or queues. **Does not** change handling-request cards (#477), **Record action handled**, **Open case**, workbench links, request/note/ack, outcome/follow-up writes, hub, plan, packet, chat-ai, or workbench. **Read-only navigation only** — does **not** imply Surrenderless handling request, filing, submission, or automation. **No** schema, migration, queue worker, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**.
- **Packet — handling tracking on approved-packet-action prepared-action UI (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **read-only sync + navigation** slice (with §2 **Packet — handling tracking status in prepared-action handling UI (`/justice/packet`)** (#474), §2 **Saved cases — handling tracking on approved-packet-action cards (`/justice/cases`)** (#478), §2 **Saved cases — handling tracking on Needs attention handling-request cards (`/justice/cases`)** (#477), §2 **Approved next action — handling tracking contextual action links (justice surfaces)** (#475), §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — confirmation follow-up queue on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome closure queue on handling-request triage cards (`/justice/handling`)**). **`PacketHandlingTrackingStatusReadOnly`** now renders when **`preparedPacketApproved`** and **`handling_requested_at`** is absent (approved-packet-action path) — same component, **`derivePacketHandlingTrackingLine`** / **`derivePacketManualActionNextStep`** gate parity with workbench **`deriveManualActionNextStep`**; reuses existing evidence + filings fetches (**`packetHandlingReadinessLoading`**). On **`packetApproved && approvedNextAction`** prepared-action block, **`else`** branch after optional handling-request UI (mirrors handling-request **`PacketHandlingTrackingStatusReadOnly`** placement in same block — legacy **`handling_requested_at`**-only block **unchanged** when packet not approved): read-only **Handling tracking:** — **Loading handling tracking context...** while readiness loading; when ready, derived step or **Tracking complete for now.**; subcopy **In-app tracking only — not filed or submitted.**; **`ApprovedNextActionHandlingTrackingContextualLink`** with **`surface="packet"`** (same-page **`#packet-filings`** for filing gates). **`markAcknowledgedOnScreen={false}`** — **no** inline **Mark acknowledged** on this path. **No** queue status — does **not** imply Surrenderless handling request. **Does not** change handling-request path, **`JusticeFilingRecords`** CRUD, request/note/ack writes, outcome/follow-up writes, **Record action handled**, **Open** step, workbench links, hub, plan, chat-ai, cases, or workbench. **Read-only navigation only** — **no** filing/submission automation. **No** schema, migration, API route, queue worker, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**.
- **Plan — handling tracking on approved-packet-action prepared-packet callouts (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **read-only sync + navigation** slice (with §2 **Plan — handling tracking status on prepared-packet handling callouts (`/justice/plan`)** (#471), §2 **Approval-to-action engine — approved packet action workbench link on action plan prepared-packet callouts (`/justice/plan`)**, §2 **Packet — handling tracking on approved-packet-action prepared-action UI (`/justice/packet`)** (#479), §2 **Saved cases — handling tracking on approved-packet-action cards (`/justice/cases`)** (#478), §2 **Approved next action — handling tracking contextual action links (justice surfaces)** (#475), §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — confirmation follow-up queue on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome closure queue on handling-request triage cards (`/justice/handling`)**). **`PlanHandlingTrackingStatusReadOnly`** now renders when **`preparedPacketApproved`** and **`handling_requested_at`** is absent (approved-packet-action path) — same component guard as **`PacketHandlingTrackingStatusReadOnly`** (**`showApprovedPacketActionPath`**); **`derivePlanHandlingTrackingLine`** / **`derivePlanManualActionNextStep`** gate parity with workbench **`deriveManualActionNextStep`**; reuses existing plan readiness fetch (**`readinessLoading`**, evidence, filings). In **both** prepared-packet approved emerald inner callouts — **`showPostDraftReviewCallout`** inner **`preparedPacketApproved`** block and standalone **`preparedPacketApproved`** block — after **`isApprovedPacketActionWithoutHandlingRequest`** workbench copy and **before** handling-request UI: read-only **Handling tracking:** — **Loading handling tracking context...** while **`readinessLoading`**; when ready, derived step or **Tracking complete for now.**; subcopy **In-app tracking only — not filed or submitted.**; **`ApprovedNextActionHandlingTrackingContextualLink`** with **`surface="plan"`**. **`markAcknowledgedOnScreen={false}`** — **no** inline **Mark acknowledged** on this path. **No** queue status — does **not** imply Surrenderless handling request. **Does not** change handling-request path, **Request Surrenderless handling**, note edit, ack writes, outcome/follow-up writes, **Record action handled**, workbench links, hub, packet, chat-ai, cases, or workbench. **Read-only navigation only** — **no** filing/submission automation. **No** schema, migration, API route, queue worker, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**.
- **Justice hub — handling tracking on approved-packet-action Current case (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **read-only sync + navigation** slice (with §2 **Justice hub — handling tracking status on Current case (`/justice`)** (#472), §2 **Approval-to-action engine — approved packet action workbench link on justice hub Current case (`/justice`)**, §2 **Packet — handling tracking on approved-packet-action prepared-action UI (`/justice/packet`)** (#479), §2 **Plan — handling tracking on approved-packet-action prepared-packet callouts (`/justice/plan`)**, §2 **Saved cases — handling tracking on approved-packet-action cards (`/justice/cases`)** (#478), §2 **Approved next action — handling tracking contextual action links (justice surfaces)** (#475), §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — confirmation follow-up queue on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome closure queue on handling-request triage cards (`/justice/handling`)**). **`HubHandlingTrackingStatusReadOnly`** now renders when **`snapshot.packetApproved`** and **`handling_requested_at`** is absent on hydrated **`snapshot.approvedNextAction`** (approved-packet-action path) — same component guard as **`PacketHandlingTrackingStatusReadOnly`**; **`deriveHubHandlingTrackingLine`** / **`deriveHubManualActionNextStep`** gate parity with workbench **`deriveManualActionNextStep`**; reuses existing hub evidence + filings fetch (**`hubReadinessLoading`**). When **`packetApproved && approvedNextAction`**, after **`isApprovedPacketActionWithoutHandlingRequest`** workbench copy and **before** handling-request UI: read-only **Handling tracking:** — loading line while **`hubReadinessLoading`**; when ready, derived step or **Tracking complete for now.**; subcopy **In-app tracking only — not filed or submitted.**; **`ApprovedNextActionHandlingTrackingContextualLink`** with **`surface="hub"`**. **`markAcknowledgedOnScreen={false}`**. **No** queue status — does **not** imply Surrenderless handling request. **Does not** change handling-request path, request/note/ack writes, **Current case** primary CTA, checklist, legacy handling blocks, plan, packet, chat-ai, cases, or workbench. **Read-only navigation only** — **no** filing/submission automation. **No** schema, migration, API route, queue worker, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**.
- **Chat AI intake — handling tracking on approved-packet-action Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **read-only sync + navigation** slice (with §2 **Chat AI intake — handling tracking status in Current action tracking (`/justice/chat-ai`)** (#473), §2 **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**, §2 **Packet — handling tracking on approved-packet-action prepared-action UI (`/justice/packet`)** (#479), §2 **Plan — handling tracking on approved-packet-action prepared-packet callouts (`/justice/plan`)**, §2 **Saved cases — handling tracking on approved-packet-action cards (`/justice/cases`)** (#478), §2 **Approved next action — handling tracking contextual action links (justice surfaces)** (#475), §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — confirmation follow-up queue on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — outcome closure queue on handling-request triage cards (`/justice/handling`)**). **`ChatHandlingTrackingStatusReadOnly`** now renders when **`preparedPacketApproved`** and **`handling_requested_at`** is absent on loaded-case **`approvedNextAction`** (approved-packet-action path) — same component guard as **`PacketHandlingTrackingStatusReadOnly`**; **`deriveChatHandlingTrackingLine`** / **`deriveChatManualActionNextStep`** gate parity with workbench **`deriveManualActionNextStep`**; reuses existing chat evidence + filings fetch (**`chatHandlingReadinessLoading`**). In **Recap → Current action tracking** (**Update in chat** only), after **`isApprovedPacketActionWithoutHandlingRequest`** workbench copy and **before** handling-request UI: read-only **Handling tracking:** — loading line while **`chatHandlingReadinessLoading`**; when ready, derived step or **Tracking complete for now.**; subcopy **In-app tracking only — not filed or submitted.**; **`ApprovedNextActionHandlingTrackingContextualLink`** with **`surface="chat-ai"`**. **`markAcknowledgedOnScreen={false}`**. **No** queue status — does **not** imply Surrenderless handling request. **Does not** change handling-request path, **Request Surrenderless handling**, note edit, ack writes, outcome/follow-up writes, **Record action handled**, proof CRUD, Continue handoff, hub, plan, packet, cases, or workbench. **Read-only navigation only** — **no** filing/submission automation. **No** schema, migration, API route, queue worker, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**.
- **Packet — Surrenderless handling request and acknowledge on approved case packet surface (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **behavior** slice (with §2 **Packet — handling requested visibility (prepared-action area) (`/justice/packet`)**, §2 **Packet — display-only derived handling queue status (`/justice/packet`)**, §2 **Packet — handled/open handling triage note in prepared-action area (`/justice/packet`)**, §2 **Handling workbench — active-case back-link from packet (`/justice/packet`)**, §2 **Plan — Surrenderless handling requested (`/justice/plan`)**, §2 **Chat AI intake — handling requested in Current action tracking (`/justice/chat-ai`)**, §2 **Saved cases — acknowledge handling request from Needs attention (`/justice/cases`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In the **prepared-action / post-approval handling area** when **`packetApproved`** and hydrated **`approvedNextAction`** exist, closes packet-side **internal manual-handling handoff** without leaving the approved case packet surface — mirrors plan/chat handling writes: page-local **`handleRequestSurrenderlessHandling`** sets **`handling_requested_at`** (ISO) + optional **`handling_request_note`**; **`handleUpdateHandlingRequestNote`** via **`applyHandlingRequestNoteToApprovedNextAction`** / **`omitClearedHandlingRequestNoteFromApprovedNextAction`**; **`handleAcknowledgeHandlingRequest`** via **`acknowledgeHandlingRequestInApprovedNextAction`** — all persist through existing **`persistApprovedNextAction`** (**`mergeApprovedNextActionTrackingFields`**, session sync, signed-in **UUID** **`GET` + merge + `PATCH`** **`client_state`**) preserving **`status`**, **`started_at`**, **`completed_at`**, **`outcome_note`**, **`follow_up_needed`**, **`follow_up_at`**, **`label`**, **`href`**, **`approved_at`**, and other **`approved_next_action`** fields. UI via shared **`ApprovedNextActionHandlingRequestBlock`**: when **`status !== "completed"`** and **no** **`handling_requested_at`** — **Request Surrenderless handling** + optional note; when **`handling_requested_at`** and **`status !== "completed"`** — same block with **`allowEditNote`**; when **`status === "completed"`** and **`handling_requested_at`** — **`ApprovedNextActionHandlingRequestedReadOnly`**, **`ApprovedNextActionHandlingQueueStatusReadOnly`**, **`ApprovedNextActionHandlingHandledOpenTriageNote`** (**`inlineAck`**) when unacknowledged, **View in handling workbench**, and **Mark acknowledged** when **`handling_acknowledged_at`** absent. **Does not** auto-request handling on packet approval; **does not** change inner **`packetApproved`** approved-next-action card writes, plan, chat-ai, cases, hub, handling workbench, membership predicates, or routing. **Internal triage tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior.
- **Approved next action target (`client_state` — packet + plan):** **`src/lib/justice/types.ts`** (**`JusticeApprovedNextAction`**, **`approved_next_action`** on **`JusticeCaseClientState`**), **`src/app/justice/packet/page.tsx`**, **`src/app/justice/plan/page.tsx`**. On **`/justice/packet`** approval (when prepared framing is active), Surrenderless stores **both** packet-approved state and **which in-app step** was approved for, using existing **`justice_cases.client_state`** only: **`approved_next_action`** with optional **`label`**, **`href`**, **`status: "approved"`**, **`approved_at`** (ISO). Target is derived at approve time from shared **`pickPreparedNextAction`** / **`buildApprovedNextActionTarget`** in **`preparedNextAction.ts`** (§2 **Approval-to-action engine — prepared next action shared helpers (`preparedNextAction.ts`)**) — contact state + first **`recommended`** / **`available`** destination with **`internalRoute`** via **`computeJusticeDestinations`** on **`/justice/packet`** (e.g. merchant/company contact **`/justice/merchant`** or a prep route; **`href`** falls back to **`/justice/packet`** when no prep route); **`/justice/plan`** uses shared **`pickPreparedNextAction`** for post-draft **`stepLabel`** / **`detailHref`** (§2 **Approval-to-action engine — plan prepared next action shared helper usage (`/justice/plan`)**); packet approval still uses **`buildApprovedNextActionTarget`** at attach time only. **Hydrate:** packet **`GET /api/justice/cases/[id]`** restores **`approved_next_action`** (including **`started`** when present) after refresh; syncs **`justice_approved_next_action_v1`** session map when server has data. **Plan:** existing case **`GET`** hydrate paths read **`approved_next_action`** and surface the **exact stored label/href** in approved callouts when the user returns from packet — not a recomputed destination. **Session fallback:** **`justice_approved_next_action_v1`** for local / non-UUID. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or new timeline **type**; **nothing** filed, submitted, sent, or contacted automatically. **Interim scaffolding** toward §1 **chat-first → prepare → approve → start/track** — remembers approval **intent**, not execution.
- **Approval-to-action engine — prepared next action shared helpers (`preparedNextAction.ts`):** **`src/lib/justice/preparedNextAction.ts`** — **refactor-only groundwork** slice (PR **#402** / commit **`bc61aef`**; with §2 **Approved next action target (`client_state` — packet + plan)**, §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**, §2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). Exports **`PreparedNextActionPick`**, **`pickPreparedNextAction`** (contact state + first **`recommended`** / **`available`** destination with **`internalRoute`** → **`detailHref`** + **`stepLabel`**), and **`buildApprovedNextActionTarget`** (→ **`JusticeApprovedNextAction`** with **`label`**, **`href`** fallback **`/justice/packet`**, **`status: "approved"`**, **`approved_at`**) using existing **`JusticeApprovedNextAction`** / **`JusticeDestination`** from **`types.ts`**. **`/justice/packet`** imports shared helpers for prepared-next-action selection and **`approved_next_action`** target building in **`handleApprovePreparedPacket`**, pre-approval explainers, and render-time **`preparedNextAction`** — **canonical packet approval attach point** unchanged. **`/justice/plan`** wired to shared **`pickPreparedNextAction`** — §2 **Approval-to-action engine — plan prepared next action shared helper usage (`/justice/plan`)**. **Preserves** existing packet approval behavior and **`approved_next_action`** shape; **no** auto-**`handling_requested_at`**, **no** timeline events, **no** new API route, schema/migration, queue worker, filing/submission/contact/upload/storage automation, or DB tables. **Refactor only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **Interim scaffolding** toward §1 **approval-to-action engine** — packet and plan share prepared next-action selection logic; **`buildApprovedNextActionTarget`** remains packet attach only, not execution.
- **Approval-to-action engine — plan prepared next action shared helper usage (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **refactor-only** slice (PR **#404** / commit **`037f892`**; with §2 **Approval-to-action engine — prepared next action shared helpers (`preparedNextAction.ts`)**, §2 **Plan — prepared next action after draft review (`/justice/plan`)**, §2 **Approved next action target (`client_state` — packet + plan)**). Removes page-local duplicate **`pickPreparedNextAction`** where covered by shared **`pickPreparedNextAction`** from **`preparedNextAction.ts`**; plan keeps page-local **`href: "/justice/packet"`** and **`buttonLabel: "Review prepared next step"`** wrapper around shared **`detailHref`** + **`stepLabel`** for post-draft callout CTAs — **same** **`stepLabel`** / **`detailHref`** / primary **Review prepared next step** → **`/justice/packet`** / secondary prep link behavior as before. **`buildApprovedNextActionTarget`** remains packet-only (§2 **Packet — prepared packet approval checkpoint (`/justice/packet`)** — approval attach point unchanged). **Preserves** existing plan behavior; packet and plan now share the same prepared next-action selection logic. **No** auto-**`handling_requested_at`**, **no** timeline events, **no** packet approval behavior change, **no** UI copy changes beyond refactor wiring, **no** new API route, schema/migration, queue worker, filing/submission/contact/upload/storage automation, or DB tables. **Refactor only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone.
- **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** + **`src/lib/justice/approvedNextActionState.ts`** — **first real approval-to-action behavior slice** (PR **#406** / commit **`b83acdd`**; with §2 **Handling workbench — internal triage surface (`/justice/handling`)**, §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**, §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**, §2 **Approval-to-action engine — approved packet action visibility on Saved cases Needs attention (`/justice/cases`)**, §2 **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**, §2 **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`)**, §2 **Approval-to-action engine — approved packet action workbench link on action plan prepared-packet callouts (`/justice/plan`)**, §2 **Approval-to-action engine — approved packet action workbench link on justice hub Current case (`/justice`)**, §2 **Approval-to-action engine — approved packet action workbench link on All cases cards (`/justice/cases`)**). Exports **`parseApprovedPacketActionWithoutHandlingRequest`** and **`isApprovedPacketActionWithoutHandlingRequest`** — membership from existing **`client_state`** only: **`prepared_packet_approved === true`**, parseable **`approved_next_action`** with **`label`** or **`href`**, **no** **`handling_requested_at`**, **`status !== "completed"`** — **not** a Surrenderless handling-request queue signal. Signed-in paginated **`GET /api/justice/cases`** loads cases; **`buildApprovedPacketActionItems`** + **`sortByApprovedAtDesc`** surface matching rows in **Approved packet actions** **above** **Awaiting internal triage** / **Acknowledged** triage buckets. **`ApprovedPacketActionCaseCard`** shows case **label** or **company**, **product/service**, **Next step: {label}**, **Status**, **`approved_at`** when present, honest copy (**approved case packet and next in-app step — not a Surrenderless handling request**; request handling from action plan when you want internal triage tracking), session-first navigation — **Open action plan**, **Open case packet**, **Update in chat**, **Open approved step** when internal **`href`** (not plan-only), and user-controlled **started** / **completed** status writes — §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**. Empty state covers both approved-packet actions and handling requests. **Does not** set **`handling_requested_at`** on packet approval; **does not** change **`buildHandlingWorkbenchItems`**, **`isHandlingAwaitingTriageApprovedNextAction`**, **Request Surrenderless handling** on plan/chat, **Needs attention** handling-request rules, ack/triage semantics, packet approval persistence, or triage bucket membership. **No** new **`client_state`** field, timeline **type**, API route, worker, **`justice_case_filings`**, filing/submission/contact/upload/storage automation, or schema migration — **operational visibility + in-app tracking writes** toward §1 **prepare → approve → track**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **user-controlled in-app tracking** slice (with §2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)**, §2 **Approved next action started (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Approved next action completed / handled (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**, §2 **Handling workbench — internal triage surface (`/justice/handling`)**). On **`ApprovedPacketActionCaseCard`** only (approved-packet section — **not** handling-request triage cards): when **`approved_next_action.status === "approved"`**, **Open approved step** persists **`status: "started"`** and **`started_at`** (if missing) via **`mergeApprovedNextActionTrackingFields`** + **`mergeClientStateWithApprovedNextAction`**, optimistic **`setCases`** + **`writeSessionApprovedNextAction`**, then existing session-first navigation to the approved internal **`href`**; when **`status === "started"`**, shows **Opened for next step.**, **Record action handled for now** (sets **`status: "completed"`** + **`completed_at`**), and **Tracking only — not automatic filing or submission.** subcopy; signed-in **UUID** cases use the same **`GET` + merge + `PATCH`** **`client_state`** pattern as **Mark acknowledged** on **Awaiting internal triage** rows. **Does not** set **`handling_requested_at`**, **`handling_acknowledged_at`**, **`outcome_note`**, **`follow_up_needed`**, **`follow_up_at`**, or change **`label`**, **`href`**, **`approved_at`**. Completed cards drop from **Approved packet actions** when **`status === "completed"`** (existing **`parseApprovedPacketActionWithoutHandlingRequest`** predicate). **Outcome / follow-up** tracking remains on **`/justice/plan`**, **`/justice/chat-ai`**, and **`/justice/cases`** for now. **Does not** change handling-request triage membership, **Mark acknowledged** on **Awaiting** rows, plan/chat/packet primary write surfaces, packet approval attach, or workbench nav links on other surfaces. **No** filing, submission, external queue, worker, timeline **type**, new API route, schema/migration, DB field, or persistence-model change beyond existing **`approved_next_action`** updates on **`justice_cases.client_state`**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Approval-to-action engine — approved packet action visibility on Saved cases Needs attention (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **read-only inbox parity** slice (PR **#408** / commit **`cef391f`**; with §2 **Saved cases — record action handled on Needs attention approved-packet-action cards (`/justice/cases`)**, §2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)**, §2 **Saved cases — Needs attention list alignment (`/justice/cases`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**, §2 **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**, §2 **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`)**, §2 **Approval-to-action engine — approved packet action workbench link on action plan prepared-packet callouts (`/justice/plan`)**, §2 **Approval-to-action engine — approved packet action workbench link on justice hub Current case (`/justice`)**, §2 **Approval-to-action engine — approved packet action workbench link on All cases cards (`/justice/cases`)**). **`buildApprovedPacketActionAttentionItems`** (from **`filteredSortedCases`**, same **search** / **status** filter / **sort** as **All cases**) uses shared **`parseApprovedPacketActionWithoutHandlingRequest`** — same membership as workbench **Approved packet actions**: **`prepared_packet_approved === true`**, parseable **`approved_next_action`** with **`label`** or **`href`**, **no** **`handling_requested_at`**, **`status !== "completed"`**. Sorted by **`approved_at`** descending when present. Compact **Needs attention** cards (**Approved packet action** title, blue border — **not** emerald handling-request styling) show case **label** or **company**, **product/service**, **Next step: {label}**, **Approved next action: {Approved|Started|Handled}**, **`approved_at`** when present, honest copy (**approved case packet and next in-app step — not a Surrenderless handling request**), generic **in-app tracking only** disclaimer, **Open case** via existing **`openCase`** funnel (§2 **Saved cases — Open case chat-first routing (`/justice/cases`)**), and navigation-only **View on handling workbench** → **`/justice/handling`** (**no** session change on link click). When **`status === "started"`**, record-handled write — §2 **Saved cases — record action handled on Needs attention approved-packet-action cards (`/justice/cases`)**. **`hasNeedsAttentionContent`** includes this list. Rendered **before** handling-request **Needs attention** cards. **Does not** set **`handling_requested_at`**; **does not** change **`buildHandlingRequestedAttentionItems`**, **`isHandlingAwaitingTriageApprovedNextAction`**, handling-request card copy/queue status/**Mark acknowledged**, follow-up/task attention, packet approval persistence, or **Request Surrenderless handling** on plan/chat. **No** queue status, **no** **Mark acknowledged**, **no** new **`client_state`** field, timeline **type**, API route, worker, **`justice_case_filings`**, filing/submission/contact/upload/storage automation, or schema migration — **operational visibility** (plus started-state record-handled write — §2 **Saved cases — record action handled on Needs attention approved-packet-action cards (`/justice/cases`)**) toward §1 **prepare → approve → track**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **navigation-only** slice (PR **#410** / commit **`2090248`**; with §2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)**, §2 **Approval-to-action engine — approved packet action visibility on Saved cases Needs attention (`/justice/cases`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**, §2 **Handling workbench — active-case back-link from plan and chat-ai**). In **Recap → Current action tracking** when **`isUpdatingExistingCase`** and hydrated **`approvedNextAction`** exist, **`isApprovedPacketActionWithoutHandlingRequest`** on **`{ prepared_packet_approved: preparedPacketApproved, approved_next_action: approvedNextAction }`** — same membership as workbench **Approved packet actions** and cases **Needs attention** approved-packet cards: **`prepared_packet_approved === true`**, parseable **`approved_next_action`** with **`label`** or **`href`**, **no** **`handling_requested_at`**, **`status !== "completed"`**. Shows read-only honest copy (**approved case packet and next in-app step — not a Surrenderless handling request**; request handling below when you want internal triage tracking) and navigation-only **View on handling workbench** → **`/justice/handling`** (**no** **`STORAGE_CASE_ID`** activation on click). Rendered after Open/started/completed tracking blocks and **before** handling-request UI. Mutually exclusive with existing **View in handling workbench** when **`handling_requested_at`** is set (§2 **Handling workbench — active-case back-link from plan and chat-ai**). **Does not** auto-set **`handling_requested_at`**, change **Request Surrenderless handling**, queue status, **Mark acknowledged**, handling-request workbench link, hydrate/PATCH, or workbench triage membership. **No** queue status, **no** **Mark acknowledged**, **no** new **`client_state`** field, timeline **type**, API route, worker, **`justice_case_filings`**, filing/submission/contact/upload/storage automation, or schema migration — **operational visibility only**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **navigation-only** slice (PR **#412** / commit **`40e56f9`**; with §2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)**, §2 **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**, §2 **Handling workbench — active-case back-link from packet**). Inside the **`packetApproved`** emerald **Packet approved for next action** card, **`isApprovedPacketActionWithoutHandlingRequest`** on **`{ prepared_packet_approved: packetApproved, approved_next_action: approvedNextAction }`** — same membership as workbench **Approved packet actions**, cases **Needs attention** approved-packet cards, and chat **Current action tracking**: **`prepared_packet_approved === true`**, parseable **`approved_next_action`** with **`label`** or **`href`**, **no** **`handling_requested_at`**, **`status !== "completed"`**. Shows read-only honest copy (**approved case packet and next in-app step — not a Surrenderless handling request**; request handling from your action plan when you want internal triage tracking) and navigation-only **View on handling workbench** → **`/justice/handling`** (**no** **`STORAGE_CASE_ID`** activation on click). Rendered after Open/started/completed content and **before** **Continue from action plan**. Mutually exclusive with existing **View in handling workbench** when **`handling_requested_at`** is set (§2 **Handling workbench — active-case back-link from packet**, §2 **Packet — handling requested visibility (prepared-action area)**). **Does not** auto-set **`handling_requested_at`**, change handling-request workbench link, queue status, **Mark acknowledged**, **`handleApprovePreparedPacket`**, approval persistence, hydrate/PATCH, or workbench triage membership. **No** queue status, **no** **Mark acknowledged**, **no** new **`client_state`** field, timeline **type**, API route, worker, **`justice_case_filings`**, filing/submission/contact/upload/storage automation, or schema migration — **operational visibility only**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Approval-to-action engine — approved packet action workbench link on action plan prepared-packet callouts (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **navigation-only** slice (PR **#414** / commit **`33e8fad`**; with §2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)**, §2 **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`)**, §2 **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**, §2 **Handling workbench — active-case back-link from plan and chat-ai**). In **both** prepared-packet approved emerald inner cards — **`showPostDraftReviewCallout`** inner **`preparedPacketApproved`** block and standalone **`preparedPacketApproved`** block when post-draft callout is off — **`isApprovedPacketActionWithoutHandlingRequest`** on **`{ prepared_packet_approved: preparedPacketApproved, approved_next_action: approvedNextAction }`** — same membership as workbench **Approved packet actions**, cases **Needs attention** approved-packet cards, chat **Current action tracking**, and packet **Packet approved for next action** card: **`prepared_packet_approved === true`**, parseable **`approved_next_action`** with **`label`** or **`href`**, **no** **`handling_requested_at`**, **`status !== "completed"`**. Shows read-only honest copy (**approved case packet and next in-app step — not a Surrenderless handling request**; request Surrenderless handling when you want internal triage tracking) and navigation-only **View on handling workbench** → **`/justice/handling`** (**no** **`STORAGE_CASE_ID`** activation on click). Rendered after Open/started/completed content and **before** handling-request UI. Mutually exclusive with existing **View in handling workbench** when **`handling_requested_at`** is set (§2 **Handling workbench — active-case back-link from plan and chat-ai**, §2 **Plan — Surrenderless handling requested**). **Does not** auto-set **`handling_requested_at`**, change handling-request workbench link, queue status, **Mark acknowledged**, **`Request Surrenderless handling`** / handling UI, approval persistence, hydrate/PATCH, or workbench triage membership. **No** queue status, **no** **Mark acknowledged**, **no** new **`client_state`** field, timeline **type**, API route, worker, **`justice_case_filings`**, filing/submission/contact/upload/storage automation, or schema migration — **operational visibility only**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Approval-to-action engine — approved packet action workbench link on justice hub Current case (`/justice`):** **`src/app/justice/JusticeHubWorkspaceBody.tsx`** only — **navigation-only** slice (PR **#416** / commit **`e046e4f`**; with §2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)**, §2 **Approval-to-action engine — approved packet action workbench link on action plan prepared-packet callouts (`/justice/plan`)**, §2 **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`)**, §2 **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**, §2 **Approval-to-action engine — approved packet action workbench link on All cases cards (`/justice/cases`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**, §2 **Handling workbench — active-case back-link from hub**). When valid local intake exists and the **Current case** snapshot has **`packetApproved`** (session **`justice_prepared_packet_approved_v1`**) and session **`approvedNextAction`** from **`readSessionApprovedNextAction`**, **`isApprovedPacketActionWithoutHandlingRequest`** on **`{ prepared_packet_approved: packetApproved, approved_next_action: approvedNextAction }`** — same membership as workbench **Approved packet actions**, cases **Needs attention** approved-packet cards, chat **Current action tracking**, plan **both** prepared-packet approved emerald inner callouts, packet **Packet approved for next action** card, and **All cases** **`CaseApprovedNextActionTracking`**: **`prepared_packet_approved === true`**, parseable **`approved_next_action`** with **`label`** or **`href`**, **no** **`handling_requested_at`**, **`status !== "completed"`**. Shows read-only honest copy (**approved case packet and next in-app step — not a Surrenderless handling request**; request Surrenderless handling from your action plan when you want internal triage tracking) and navigation-only **View on handling workbench** → **`/justice/handling`** (**no** **`STORAGE_CASE_ID`** activation on click). Rendered below the **Current case** card checklist and secondary CTAs, **outside** the primary **`/justice/plan`** / **`/justice/preview`** / **`/justice/packet`** card link (same outer placement as handling-request **View in handling workbench**). Mutually exclusive with existing **View in handling workbench** when **`handling_requested_at`** is set (§2 **Handling workbench — active-case back-link from hub**, §2 **Justice hub — handling requested on Current case**). **Does not** auto-set **`handling_requested_at`**, change handling-request workbench link, queue status, **Mark acknowledged**, **Current case** primary CTA, checklist, hydrate/PATCH, or workbench triage membership. **No** queue status, **no** **Mark acknowledged**, **no** new **`client_state`** field, timeline **type**, API route, worker, **`justice_case_filings`**, filing/submission/contact/upload/storage automation, or schema migration — **operational visibility only**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Approval-to-action engine — approved packet action workbench link on All cases cards (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **navigation-only** slice (with §2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)**, §2 **Approval-to-action engine — approved packet action visibility on Saved cases Needs attention (`/justice/cases`)**, §2 **Approval-to-action engine — approved packet action workbench link on justice hub Current case (`/justice`)**, §2 **Approval-to-action engine — approved packet action workbench link on action plan prepared-packet callouts (`/justice/plan`)**, §2 **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`)**, §2 **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**, §2 **Saved cases — View in handling workbench on All cases handling-request cards**). In **`CaseApprovedNextActionTracking`** on **All cases** cards, **`parseApprovedPacketActionWithoutHandlingRequest(clientState)`** from existing list **`GET`** **`client_state`** — same membership as workbench **Approved packet actions**, **Needs attention** approved-packet cards, hub **Current case**, chat **Current action tracking**, plan prepared-packet callouts, and packet **Packet approved for next action** card. Shows read-only honest copy (**approved case packet and next in-app step — not a Surrenderless handling request**; request handling from your action plan when you want internal triage tracking) and navigation-only **View on handling workbench** → **`/justice/handling`** (**no** case activation on click). Rendered after handling-request UI and **before** follow-up/disclaimer. Mutually exclusive with **View in handling workbench** when **`handling_requested_at`** is set. **Does not** auto-set **`handling_requested_at`**, change handling-request workbench link, queue status, **Mark acknowledged**, **Open case**, **Needs attention** cards, or workbench triage membership. **No** new API route, **`client_state`** field, timeline **type**, worker, filing/submission/contact/upload/storage automation, or schema migration — **operational visibility only**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Approved next action started (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`):** **`src/app/justice/plan/page.tsx`** (primary write on plan); **`src/app/justice/packet/page.tsx`** (write on packet **Open {label}** — §2 **Packet — Open approved step marks started tracking (`/justice/packet`)**); **`src/app/justice/chat-ai/page.tsx`** (write on chat **Open {label}** — §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**); **`src/lib/justice/types.ts`** — **`JusticeApprovedNextAction.status`** includes **`"started"`** with optional **`started_at`** (full union **`"approved" | "started" | "completed"`** — §2 **Approved next action completed / handled**; packet surfaces **`started`** on hydrate — §2 **Packet — approved next action started surfaced**). On **`/justice/plan`**, the green **`View approved case packet`** control is a **`<button>`** (not **`Link` + `onClick`**): **`handleViewApprovedCasePacketClick`** builds **`approved_next_action`** with existing **`label`** / **`href`**, sets **`status: "started"`** and **`started_at`**, writes **`justice_approved_next_action_v1`** and **`justice_prepared_packet_approved_v1`** immediately, updates React state, **`await`**s **`GET` + merge + `PATCH`** on existing **`/api/justice/cases/[id]`** **`client_state`** for signed-in **UUID** cases, then **`router.push(next.href || "/justice/packet")`**. Secondary **`Open {label}`** on plan uses the same started persistence. On **`/justice/packet`** and **`/justice/chat-ai`**, **`handleApprovedNextActionOpen`** mirrors that pattern before routing to the approved prep **`href`** (§2 **Packet — Open approved step marks started tracking (`/justice/packet`)**, §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**). **`resolveApprovedNextAction`** merges server + session and **does not downgrade** **`started`** to **`approved`** (or **`completed`** to a lower state). After return/refresh, callouts read **Next action started** / **Opened for next step** when **`status === "started"`**. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, or contacted automatically. **Interim scaffolding** toward §1 **prepare → approve → start/track**.
- **Approved next action completed / handled (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`):** **`src/lib/justice/types.ts`**, **`src/app/justice/plan/page.tsx`**, **`src/app/justice/packet/page.tsx`**, **`src/app/justice/chat-ai/page.tsx`** (write on chat — §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**). Extends **`approved_next_action`** on existing **`justice_cases.client_state`** only (plus **`justice_approved_next_action_v1`** / **`justice_prepared_packet_approved_v1`** session fallbacks for local / non-UUID cases). **`status`** supports **`"approved" | "started" | "completed"`** with optional **`completed_at`** (ISO); **`label`**, **`href`**, **`approved_at`**, and **`started_at`** stay intact. **Tracking scaffold** toward §1 **chat-first → prepare → approve → submit/queue/track** — **not** the final manual workflow and **not** real submit/queue/follow-up automation yet; long-term goal remains: user **only chats** with Surrenderless while it gathers facts, structures the case, organizes proof, prepares actions, gets approval, submits/queues where feasible, and tracks follow-up inside the app. On **`/justice/plan`**, when **`status === "started"`**, **Record action handled for now** sets **`status: "completed"`** and **`completed_at`**, updates session immediately, and **`await`**s **`GET` + merge + `PATCH`** on existing **`/api/justice/cases/[id]`** for signed-in **UUID** cases (**`persistApprovedNextActionClientState`**). On **`/justice/chat-ai`**, **Recap → Current action tracking** exposes **Open {label}** when **`status === "approved"`** (§2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**), **Record action handled for now** when **`status === "started"`** (§2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**), and **completed-step clarity** when **`status === "completed"`** (§2 **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`)**). Copy: **Next action recorded as handled**; **in-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **`resolveApprovedNextAction`** on plan, packet, and chat **does not downgrade** **`completed`** → **`started`** → **`approved`**. **`/justice/packet`** hydrates and shows handled/completed state in the approval area (record handled from plan or chat). Outcome / follow-up fields when **`completed`** — §2 **Approved next action outcome / follow-up tracking**. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **no** changes to **`rules.ts`**, **`caseReadiness.ts`**, evidence/filing **APIs**, preview generation, or **`commitIntakeToSessionAndServer`**.
- **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`):** **`src/lib/justice/types.ts`**, **`src/app/justice/plan/page.tsx`**, **`src/app/justice/packet/page.tsx`**, **`src/app/justice/chat-ai/page.tsx`** (write on chat — §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**; write on packet — §2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)**; clear follow-up on plan — §2 **Plan — clear follow-up on prepared-packet callouts (`/justice/plan`)**; clear follow-up on packet — §2 **Packet — clear follow-up on approved next action card (`/justice/packet`)**). When **`approved_next_action.status === "completed"`**, **`JusticeApprovedNextAction`** also supports optional **`outcome_note`**, **`follow_up_needed`**, and **`follow_up_at`** (ISO) on existing **`justice_cases.client_state`** only, with **`justice_approved_next_action_v1`** session fallback for local / non-UUID cases. **Tracking scaffold** toward §1 **chat-first → prepare → approve → submit/queue/track** — **not** the final manual workflow; long-term goal remains: user **only chats** with Surrenderless while it gathers facts, prepares actions, gets approval, submits/queues where feasible, and tracks follow-up inside the app. On **`/justice/plan`**, a compact **Record outcome / follow-up** form (**outcome / note** textarea, **Follow-up needed** checkbox, optional **follow-up date (your pace)**, **Save tracking note**) writes session immediately and **`await`**s **`GET` + merge + `PATCH`** on existing **`/api/justice/cases/[id]`** for signed-in **UUID** cases; saved note and follow-up target **survive refresh** via hydrate; **Mark follow-up handled** on both prepared-packet emerald callouts when **`follow_up_needed`** — §2 **Plan — clear follow-up on prepared-packet callouts (`/justice/plan`)** (mirrors §2 **Saved cases — clear Needs attention follow-up (`/justice/cases`)**, chat-ai clear follow-up in **Current action tracking**, §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**, §2 **Packet — clear follow-up on approved next action card (`/justice/packet`)**). On **`/justice/chat-ai`**, the same compact form in **Recap → Current action tracking** when **`status === "completed"`** — chat-first write path on already-hydrated **`approvedNextAction`** (§2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**). On **`/justice/packet`**, the same compact form in the **`packetApproved`** inner emerald **Next action recorded as handled** card when **`status === "completed"`** — packet write path after **`ApprovedNextActionTrackingSummary`** (§2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)**); **Mark follow-up handled** when **`follow_up_needed`** — §2 **Packet — clear follow-up on approved next action card (`/justice/packet`)**. **`resolveApprovedNextAction`** merges **`outcome_note`** / **`follow_up_*`** from server + session without dropping **`completed`**. Hub, cases, and handling workbench show read-only outcome/follow-up continuity from hydrated **`client_state`** — §2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)**, §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**, §2 **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`)**. User-paced **follow-up timing** labels when **`follow_up_at`** is set — §2 **Approved next action follow-up timing (user-paced labels)**. Copy states **in-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **no** changes to **`rules.ts`**, **`caseReadiness.ts`**, evidence/filing **APIs**, preview generation, or **`commitIntakeToSessionAndServer`**.
- **Plan — Surrenderless handling requested (`/justice/plan` only):** **`src/lib/justice/types.ts`**, **`src/lib/justice/approvedNextActionState.ts`**, **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`**, **`src/app/justice/plan/page.tsx`**. Optional **`handling_requested_at`** (ISO), **`handling_request_note`**, and **`handling_acknowledged_at`** on existing **`approved_next_action`** in **`justice_cases.client_state`** only — **in-app ownership marker** toward §1 **chat-first → submit/queue/track**, **not** a queue worker or filing record. When **`handling_requested_at`** is set, plan always shows the handling block (read-only **`ApprovedNextActionHandlingRequestedReadOnly`** when **`status === "completed"`**; **`ApprovedNextActionHandlingRequestBlock`** with **`allowEditNote`** while not completed). When **`handling_requested_at`** is **missing** and **`status !== "completed"`**, plan shows **Request Surrenderless handling** with an **optional short note** (prompt: *What do you want Surrenderless to handle when this workflow exists?*); click sets **`handling_requested_at`** and, when provided, **`handling_request_note`** via existing **`GET` + merge + `PATCH`** **`client_state`** (**`persistApprovedNextAction`** uses **`mergeApprovedNextActionTrackingFields`** + **`mergeClientStateWithApprovedNextAction`**) plus **`justice_approved_next_action_v1`** session sync; load uses **`hydrateApprovedNextActionForDisplay`**. After request, **Save request note** can update or clear **`handling_request_note`** only (**`applyHandlingRequestNoteToApprovedNextAction`**; blank removes the note, **`handling_requested_at`** unchanged). When **`handling_acknowledged_at`** is set (from plan **Mark acknowledged**, chat, Saved cases, or workbench — §2 **Plan — acknowledge handling request in Surrenderless handling UI**, **Chat AI intake — acknowledge handling request in Current action tracking**), plan shows read-only **Acknowledged {time} — internal tracking triage only** in the handling block. When **`handling_requested_at`** is set, **display-only derived queue status** via **`ApprovedNextActionHandlingQueueStatusReadOnly`** (§2 **Plan — display-only derived handling queue status**), **View in handling workbench** → **`/justice/handling`** (§2 **Handling workbench — active-case back-link from plan and chat-ai**), and **Mark acknowledged** when **`handling_acknowledged_at`** is absent (§2 **Plan — acknowledge handling request in Surrenderless handling UI**) appear in both prepared-packet handling callouts. Saved cases, packet, and hub mirror the same read-only ack line (packet/hub without plan back-link on earlier slices). When **`handling_requested_at`** is absent, **no** queue status line. Disclaimer: **in-app tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone yet. **Approved** / **started** / **handled** / outcome follow-up behavior **unchanged**. **No** schema, migration, new API route, queue worker, browser automation, **`justice_case_filings`** rows, **`/api/submit-form`**, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Plan — acknowledge handling request in Surrenderless handling UI (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — completes the **request → triage → acknowledge** loop on the action plan (with §2 **Chat AI intake — acknowledge handling request in Current action tracking**, **Saved cases — acknowledge handling request from Needs attention**, **Handling workbench — internal triage surface**). Uses existing **`acknowledgeHandlingRequestInApprovedNextAction`** and **`persistApprovedNextAction`** (**`mergeApprovedNextActionTrackingFields`** + **`mergeClientStateWithApprovedNextAction`** + **`GET` + merge + `PATCH`** **`client_state`** for signed-in **UUID** cases; **`justice_approved_next_action_v1`** session sync). In **both** prepared-packet handling callouts where **`handling_requested_at`** is set, when **`handling_acknowledged_at`** is **not** set, shows secondary **Mark acknowledged** (with **`APPROVED_NEXT_ACTION_HANDLING_ACKNOWLEDGE_HELPER`**) **below** **View in handling workbench**. After success: **`ApprovedNextActionHandlingAcknowledgedReadOnly`** shows **Acknowledged {time} — internal tracking triage only**, **`ApprovedNextActionHandlingQueueStatusReadOnly`** shows **Queue status: Acknowledged for internal tracking triage only**, **Mark acknowledged** disappears, **View in handling workbench** remains. **Not** shown without **`handling_requested_at`** or when already acknowledged. **Does not** change **Request Surrenderless handling**, **Save request note** / note clear, workbench link, prepared-packet approval, **approved** / **started** / **handled** tracking, plan CTAs, chat/cases/workbench behavior, **Needs attention** inclusion, workbench section membership/filtering, schema, migration, new API route, queue worker, **`justice_case_filings`**, timeline **type**, external submission/contact/queue automation, or **`/api/submit-form`**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Plan — display-only derived handling queue status (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** (**`ApprovedNextActionHandlingQueueStatusReadOnly`**, **`deriveHandlingQueueDisplayStatus`**) — **visibility-only** slice aligning plan with §2 **Saved cases — handling requested visibility on All cases cards**, **Handling workbench — internal triage surface**, **Packet — display-only derived handling queue status**, and **Chat AI intake — display-only derived handling queue status**. Wherever plan handling UI already appears with **`approved_next_action.handling_requested_at`** set (both prepared-packet handling callouts — **`ApprovedNextActionHandlingRequestBlock`** or **`ApprovedNextActionHandlingRequestedReadOnly`**), renders **display-only derived queue status** from existing **`handling_requested_at`** and **`handling_acknowledged_at`** only (**no** new persisted field, **no** writes): **Queue status: Awaiting internal triage.** when **`handling_acknowledged_at`** is absent; **Queue status: Acknowledged for internal tracking triage only** when **`handling_acknowledged_at`** is set. Placed after the handling request/read-only block and before **View in handling workbench** (§2 **Handling workbench — active-case back-link from plan and chat-ai**); handled-action open-handling triage copy when applicable — §2 **Plan — handled-action open-handling triage note in Surrenderless handling UI**; **Mark acknowledged** sits below the workbench link when unacknowledged (§2 **Plan — acknowledge handling request in Surrenderless handling UI**). When **`handling_requested_at`** is absent, **no** queue status line. **Does not** change request handling, save/clear note, acknowledgement persistence semantics, hydration, **`GET` + merge + `PATCH`**, workbench link, packet/preview flow, plan CTAs, or non-handling plan UI. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone. **No** schema, migration, new API route, queue worker, **`justice_case_filings`** row, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Plan — handling tracking status on prepared-packet handling callouts (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** only — **read-only sync** slice (with §2 **Plan — Surrenderless handling requested (`/justice/plan`)**, §2 **Plan — display-only derived handling queue status (`/justice/plan`)**, §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**, §2 **Saved cases — handling tracking status on All cases cards (`/justice/cases`)**, §2 **Justice hub — handling tracking status on Current case (`/justice`)**, §2 **Chat AI intake — handling tracking status in Current action tracking (`/justice/chat-ai`)**, §2 **Packet — handling tracking status in prepared-action handling UI (`/justice/packet`)**). In **both** prepared-packet emerald handling callouts when **`approved_next_action.handling_requested_at`** is set, **`PlanHandlingTrackingStatusReadOnly`** shows read-only **Handling tracking:** after queue status — **Loading handling tracking context...** while existing **`readinessLoading`**; when ready, **Handling tracking: {derived next step}** from page-local **`derivePlanManualActionNextStep`** / **`derivePlanHandlingTrackingLine`** mirroring workbench **`deriveManualActionNextStep`** gate order (basics + draft reviewed + prepared packet approved, **`approved_next_action`** status/outcome/ack/follow-up, **`evidenceCount`**, **`hasFilingRecord`** / **`hasConfirmationOnFile`** from already-fetched **`filings`** — same meaning as workbench **`handlingCaseHasFilingRecord`** / **`handlingCaseHasConfirmationOnFile`**); when gates are satisfied, **Handling tracking: Tracking complete for now.** — parity with workbench **Tracking complete** queue — §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**. Nearby subcopy: **In-app tracking only — not filed or submitted.** Placed after **`ApprovedNextActionHandlingQueueStatusReadOnly`** and before handled/open triage note (§2 **Plan — handled-action open-handling triage note in Surrenderless handling UI**) / **View in handling workbench**. Reuses existing plan readiness fetch (**evidence**, **filings**, **tasks**) — **no** new **`GET`**, **`POST`**, **`PATCH`**, **`client_state`** fields, timeline writes, workers, or queues. **Does not** change **Request Surrenderless handling**, note edit, **Mark acknowledged**, outcome/follow-up writes, **Record action handled**, **Needs attention** membership, hub, packet, chat-ai, cases, handling workbench, or filing/submission/contact automation. **Read-only in-app sync only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, contacted anyone, or closed the real-world case. **No** schema, migration, new API route, queue worker, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**.
- **Plan — handled-action open-handling triage note in Surrenderless handling UI (`/justice/plan`):** **`src/app/justice/plan/page.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** — **copy-only** slice (with §2 **Approved next action — centralized handled/open handling triage note copy**). In **both** prepared-packet handling callouts where **`handling_requested_at`** is set, when **`handling_acknowledged_at`** is absent and **`approved_next_action.status === "completed"`**, renders **`ApprovedNextActionHandlingHandledOpenTriageNote`** with variant **`inlineAck`** after **Queue status** and before **View in handling workbench** / **Mark acknowledged**. **Hidden** when **`status !== "completed"`**, when already acknowledged, or when **`handling_requested_at`** is absent. **Does not** change **Request Surrenderless handling**, note edit/clear, ack persistence, workbench link, queue status, or external-action copy. **No** schema, migration, new API route, queue worker, **`justice_case_filings`**, timeline **type**, external submission/contact/queue automation, or **`/api/submit-form`**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Approved next action follow-up timing (user-paced labels):** **`src/lib/justice/approvedNextActionFollowUp.tsx`** (**`getApprovedNextActionFollowUpTiming`**, **`ApprovedNextActionFollowUpTimingLine`**), **`src/app/justice/cases/page.tsx`**, **`src/app/justice/plan/page.tsx`**, **`src/app/justice/packet/page.tsx`**. Uses existing **`approved_next_action.follow_up_at`** only (local calendar day compare). When a follow-up date is present, surfaces gentle **user-paced** labels — **Follow-up date passed**, **Follow-up today**, **Upcoming follow-up** — plus formatted date; **not** **overdue** language and **not** deadline enforcement. Copy on **`/justice/plan`** clarifies the date is an optional **tracking aid** and **move at your own pace**. Labels appear on **`/justice/cases`** (**All cases** approved-next-action line — **Next step: {label}** when set (§2 **Saved cases — approved next-step label on All cases cards**), **Approved next action: {status}**, including **Surrenderless handling requested** when **`handling_requested_at`** is set, §2 **Saved cases — handling requested visibility on All cases cards** — and **Needs attention** handling-request and follow-up cards — **Next step: {label}** when set (§2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**)), **`/justice/plan`** tracking summary + form context, and **`/justice/packet`** tracking summary. **Tracking scaffold** toward §1 **chat-first → prepare → approve → submit/queue/track** — **not** the final manual workflow. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, or contacted automatically.
- **Approved next action shared state helpers (`approvedNextActionState.ts`):** **`src/lib/justice/approvedNextActionState.ts`** centralizes **`approved_next_action`** parsing from unknown **`client_state`** / session values (**`parseApprovedNextAction`**, **`parseApprovedNextActionFromClientState`**, **`parseJusticeCaseClientState`**), status display labels (**`approvedNextActionStatusLabel`** — **Approved** / **Started** / **Handled**), handling awaiting-triage membership (**`isHandlingAwaitingTriageApprovedNextAction`** — §2 **Approved next action — shared handling awaiting-triage membership predicate**), approved-packet action without handling request (**`parseApprovedPacketActionWithoutHandlingRequest`**, **`isApprovedPacketActionWithoutHandlingRequest`** — §2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)**, §2 **Approval-to-action engine — approved packet action visibility on Saved cases Needs attention (`/justice/cases`)**, §2 **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**, §2 **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`)**, §2 **Approval-to-action engine — approved packet action workbench link on action plan prepared-packet callouts (`/justice/plan`)**, §2 **Approval-to-action engine — approved packet action workbench link on justice hub Current case (`/justice`)**), follow-up clearing (**`clearFollowUpFromApprovedNextAction`** — removes **`follow_up_needed`** while preserving **`outcome_note`**, **`follow_up_at`**, **`handling_requested_at`**, **`handling_request_note`**, **`handling_acknowledged_at`**, **`status`**, **`href`**, **`label`**, **`approved_at`**, **`started_at`**, **`completed_at`**, and other fields), handling-request note updates (**`applyHandlingRequestNoteToApprovedNextAction`** — sets or clears **`handling_request_note`** only), cleared-follow-up **`client_state`** merge (**`mergeClientStateWithClearedFollowUp`** — preserves **`prepared_packet_approved`**), plan/packet persist merge (**`mergeClientStateWithApprovedNextAction`** — preserves prior fields unless incoming action omits **`handling_request_note`** intentionally; forces **`prepared_packet_approved`** on plan persist), session + server resolve (**`resolveApprovedNextAction`** — merges **`justice_approved_next_action_v1`** with **`client_state`**; **does not downgrade** **`completed`** → **`started`** → **`approved`**), display hydrate (**`hydrateApprovedNextActionForDisplay`** — coalesces **`handling_acknowledged_at`** from session, resolved action, and raw **`client_state`** for plan/chat/packet UI), tracking-field preserve on persist (**`mergeApprovedNextActionTrackingFields`** — keeps **`handling_requested_at`** / **`handling_acknowledged_at`** when a patch omits them), and session map read/write (**`STORAGE_APPROVED_NEXT_ACTION_V1`** / **`justice_approved_next_action_v1`**). **`/justice/chat-ai`**, **`/justice/cases`**, **`/justice/plan`**, and **`/justice/packet`** import these helpers (chat-ai **Current action tracking** approved-packet-action workbench link (§2 **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**) / **Open {label}** when **`approved`** / **Record action handled for now** / **completed-step clarity** / **Mark follow-up handled** / **Request Surrenderless handling** / **Mark acknowledged** — §2 **Chat AI intake — approved next action display hydrate**, **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**, **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`)**, **Chat AI intake — handling requested in Current action tracking**, **Chat AI intake — acknowledge handling request in Current action tracking**; cases **Needs attention** approved-packet-action cards (§2 **Approval-to-action engine — approved packet action visibility on Saved cases Needs attention (`/justice/cases`)**) + handling-request attention + **Clear follow-up** / **All cases** **`CaseApprovedNextActionTracking`** — **Next step: {label}** when set on **All cases** (§2 **Saved cases — approved next-step label on All cases cards**) and on **Needs attention** handling-request and follow-up cards (§2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**), **`handling_requested_at`** visibility (§2 **Saved cases — handling requested visibility on All cases cards**); plan **approved** / **started** / **handled** / outcome follow-up / **Request Surrenderless handling** / **Mark acknowledged** persist (§2 **Plan — acknowledge handling request in Surrenderless handling UI**); plan prepared-packet approved-packet-action workbench link (§2 **Approval-to-action engine — approved packet action workbench link on action plan prepared-packet callouts (`/justice/plan`)**); packet **approval** hydrate, approved-packet-action workbench link (§2 **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`)**), and return-to-plan; hub **Current case** approved-packet-action workbench link (§2 **Approval-to-action engine — approved packet action workbench link on justice hub Current case (`/justice`)**)). **`/justice/handling`** uses **`parseApprovedPacketActionWithoutHandlingRequest`** for workbench **Approved packet actions** (§2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)**). Reduces duplicated parsing/resolving/merge logic across justice surfaces and keeps handling request/note/ack visible when session and server disagree. **No** schema, migration, API route, queue worker, automation, filing behavior, or timeline **type**; **nothing** filed, submitted, sent, queued, or contacted automatically. **No** schema, migration, API route, queue worker, automation, filing behavior, or timeline **type**; **nothing** filed, submitted, sent, queued, or contacted automatically.
- **Chat AI intake — approved next action display hydrate (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **hydration parity** with **`/justice/plan`** and **`/justice/packet`** (§2 **Approved next action shared state helpers**). When **Update in chat** loads a case, **Current action tracking** seeds from **`hydrateApprovedNextActionForDisplay(caseId)`** immediately; for signed-in **UUID** cases, after **`GET /api/justice/cases/[id]`**, **`hydrateApprovedNextActionForDisplay(caseId, client_state)`** coalesces **`handling_requested_at`**, **`handling_request_note`**, and **`handling_acknowledged_at`** with session/server/raw **`client_state`** (status merge still via **`resolveApprovedNextAction`** inside the helper; **does not downgrade** **`completed`** → **`started`** → **`approved`**). Same **GET** also sets **`preparedPacketApproved`** for **Active case** checklist — §2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**. Hydrated action updates React state and **`writeSessionApprovedNextAction`** keeps the session map aligned. **Request Surrenderless handling**, **Save request note**, **Mark acknowledged**, **Record action handled for now**, and **Mark follow-up handled** persist through **`mergeApprovedNextActionTrackingFields`** / **`mergeClientStateWithAcknowledgedHandling`** / **`mergeClientStateWithApprovedNextAction`** so patches that omit handling tracking fields do not drop them. **Record action handled** — §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**. **Open approved step** when **`status === "approved"`** — §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**. **Active case banner** read-only **Next step** / **Approved next action** when **`approvedNextAction`** exists — §2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**; **Recap** status line uses same **Approved next action:** label — §2 **Chat AI intake — Recap current-action label alignment (`/justice/chat-ai`)** (**Open** / **Record handled** remain in Recap only). **Current action tracking** still appears for loaded cases; **Mark follow-up handled** still shows when **`follow_up_needed === true`** and **keeps** the tracking box visible. **Handling-request display + request** — §2 **Chat AI intake — handling requested in Current action tracking**. **No** other routes change on this slice; **no** schema, migration, API route, queue worker, automation, filing behavior, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Chat AI intake — handling requested in Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** — **chat-first** slice aligned with §2 **Plan — Surrenderless handling requested**. When **`approved_next_action`** exists in **Recap** **Current action tracking** and **`handling_requested_at`** is **missing** (and **`status !== "completed"`**), shows **Request Surrenderless handling** with the same **optional short note** as plan; click sets **`approved_next_action.handling_requested_at`** (ISO) and optional **`handling_request_note`** while **preserving** **`status`**, **`href`**, **`label`**, timestamps, **`outcome_note`**, and **`follow_up_*`**. Updates React state and **`justice_approved_next_action_v1`** immediately; for signed-in **UUID** cases, **`GET` + merge + `PATCH`** existing **`/api/justice/cases/[id]`** **`client_state`** via **`mergeClientStateWithApprovedNextAction`** (same pattern as plan and **Mark follow-up handled**); request **survives refresh** via **`hydrateApprovedNextActionForDisplay`**; handling request/note/clear-follow-up persists use **`mergeApprovedNextActionTrackingFields`**. After request, **Save request note** can update or clear **`handling_request_note`** only while **`status !== "completed"`** (§2 **Chat AI intake — read-only handling UI when approved action is handled**); when **`status === "completed"`** with **`handling_requested_at`**, handling display is read-only via **`ApprovedNextActionHandlingRequestedReadOnly`**. When **`handling_acknowledged_at`** is set, shows read-only **Acknowledged {time} — internal tracking triage only**. When **`handling_requested_at`** is set on a **loaded** case, **display-only derived queue status** via **`ApprovedNextActionHandlingQueueStatusReadOnly`** (§2 **Chat AI intake — display-only derived handling queue status**), **View in handling workbench** → **`/justice/handling`** (§2 **Handling workbench — active-case back-link from plan and chat-ai**), and **Mark acknowledged** when **`handling_acknowledged_at`** is absent (§2 **Chat AI intake — acknowledge handling request in Current action tracking**); **not** shown on **fresh intake** (no loaded case / no **`handling_requested_at`**). Copy: **in-app tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone yet. **Current action tracking** otherwise **unchanged** (next step, **Approved next action** status line — §2 **Chat AI intake — Recap current-action label alignment (`/justice/chat-ai`)**, outcome note, follow-up timing, **Action plan** / **Case packet** links). **Mark follow-up handled** **unchanged**. **No** schema, migration, new API route, queue worker, **`justice_case_filings`** row, timeline **type**, browser automation, filing automation, or **`/api/submit-form`**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Chat AI intake — acknowledge handling request in Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **chat-first triage** slice using the **existing** **`approved_next_action`** acknowledgement flow (§2 **Saved cases — acknowledge handling request from Needs attention**, **Handling workbench — internal triage surface** — **`acknowledgeHandlingRequestInApprovedNextAction`**, **`mergeClientStateWithAcknowledgedHandling`**, **`mergeApprovedNextActionTrackingFields`**). When **Update in chat** loads a case and **Recap → Current action tracking** has hydrated **`approved_next_action`** with **`handling_requested_at`** set and **`handling_acknowledged_at`** **not** set, shows secondary **Mark acknowledged** (with **`APPROVED_NEXT_ACTION_HANDLING_ACKNOWLEDGE_HELPER`**) below **View in handling workbench**. Click updates React state and **`justice_approved_next_action_v1`** immediately, then **`GET` + merge + `PATCH /api/justice/cases/[id]`** **`client_state`** for signed-in **UUID** cases; re-hydrates from PATCH response when present. After success: **`ApprovedNextActionHandlingAcknowledgedReadOnly`** shows **Acknowledged {time} — internal tracking triage only** (inside **`ApprovedNextActionHandlingRequestBlock`**), **`ApprovedNextActionHandlingQueueStatusReadOnly`** shows **Queue status: Acknowledged for internal tracking triage only**, **Mark acknowledged** disappears, **View in handling workbench** remains. **Fresh intake** / no loaded case → **no** **Mark acknowledged**. **Does not** change **Request Surrenderless handling**, **Save request note** / note clear, workbench link, **Mark follow-up handled**, **`commitIntakeToSessionAndServer`**, display hydrate (§2 **Chat AI intake — approved next action display hydrate**), plan/packet links, **Needs attention** inclusion rules, workbench section membership/filtering, schema, migration, new API route, queue worker, **`justice_case_filings`**, timeline **type**, external submission/contact/queue automation, or **`/api/submit-form`**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Chat AI intake — display-only derived handling queue status (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** (**`ApprovedNextActionHandlingQueueStatusReadOnly`**, **`deriveHandlingQueueDisplayStatus`**) — **visibility-only** slice aligning **loaded-case** chat with §2 **Saved cases — handling requested visibility on All cases cards**, **Handling workbench — internal triage surface**, **Packet — display-only derived handling queue status**, and **Plan — display-only derived handling queue status**. In **Recap → Current action tracking**, when **`approved_next_action.handling_requested_at`** is set (hydrated **`approved_next_action`** — §2 **Chat AI intake — approved next action display hydrate**), renders **display-only derived queue status** from existing **`handling_requested_at`** and **`handling_acknowledged_at`** only (**no** new persisted field, **no** writes): **Queue status: Awaiting internal triage.** when **`handling_acknowledged_at`** is absent; **Queue status: Acknowledged for internal tracking triage only** when **`handling_acknowledged_at`** is set. Placed after the handling request/read-only block (**`ApprovedNextActionHandlingRequestBlock`** or **`ApprovedNextActionHandlingRequestedReadOnly`** — §2 **Chat AI intake — read-only handling UI when approved action is handled**) and before **View in handling workbench** (§2 **Handling workbench — active-case back-link from plan and chat-ai**); handled-action open-handling triage copy when applicable — §2 **Chat AI intake — handled-action open-handling triage note in Current action tracking**; **Mark acknowledged** sits below the workbench link when unacknowledged (§2 **Chat AI intake — acknowledge handling request in Current action tracking**). **Fresh intake** / no loaded case / no **`handling_requested_at`** → **no** queue status line. **Does not** change request handling, save/clear note, acknowledgement persistence semantics, hydration, **`GET` + merge + `PATCH`**, workbench link, or other recap copy. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone. **No** schema, migration, new API route, queue worker, **`justice_case_filings`** row, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Chat AI intake — handled-action open-handling triage note in Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** + **`src/lib/justice/approvedNextActionHandlingDisplay.tsx`** — **copy-only** slice (with §2 **Approved next action — centralized handled/open handling triage note copy**). In **Recap → Current action tracking** for a **loaded** case, when **`handling_requested_at`**, no **`handling_acknowledged_at`**, and **`status === "completed"`**, renders **`ApprovedNextActionHandlingHandledOpenTriageNote`** with variant **`inlineAck`** after **Queue status** and before **View in handling workbench** / **Mark acknowledged**. **Hidden** on **fresh intake** or when conditions above are not met. **Does not** change **Request Surrenderless handling**, note edit/clear, ack persistence, workbench link, queue status, or external-action copy. **No** schema, migration, new API route, queue worker, **`justice_case_filings`**, timeline **type**, external submission/contact/queue automation, or **`/api/submit-form`**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Chat AI intake — handling tracking status in Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **read-only sync** slice (with §2 **Chat AI intake — handling requested in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — display-only derived handling queue status (`/justice/chat-ai`)**, §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**, §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**, §2 **Saved cases — handling tracking status on All cases cards (`/justice/cases`)**, §2 **Plan — handling tracking status on prepared-packet handling callouts (`/justice/plan`)**, §2 **Justice hub — handling tracking status on Current case (`/justice`)**, §2 **Packet — handling tracking status in prepared-action handling UI (`/justice/packet`)**). In **loaded-case** **Recap → Current action tracking** when **`isUpdatingExistingCase`** and hydrated **`approved_next_action.handling_requested_at`** is set, **`ChatHandlingTrackingStatusReadOnly`** shows read-only **Handling tracking:** after **`ApprovedNextActionHandlingQueueStatusReadOnly`** — **Loading handling tracking context...** while **`chatHandlingReadinessLoading`**; when ready, **Handling tracking: {derived next step}** from page-local **`deriveChatManualActionNextStep`** / **`deriveChatHandlingTrackingLine`** mirroring workbench **`deriveManualActionNextStep`** gate order (basics + draft reviewed + prepared packet approved, **`approved_next_action`** status/outcome/ack/follow-up, **`savedEvidenceCount`**, **`hasFilingRecord`** / **`hasConfirmationOnFile`** from **`savedFilings`** parsed in extended **`loadSavedEvidencePreview`** — parallel **`GET /api/justice/evidence?case_id={id}`** + existing-contract **`GET /api/justice/filings?case_id={id}`** — same meaning as workbench **`handlingCaseHasFilingRecord`** / **`handlingCaseHasConfirmationOnFile`**); when gates are satisfied, **Handling tracking: Tracking complete for now.** — parity with workbench **Tracking complete** queue — §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**. Nearby subcopy: **In-app tracking only — not filed or submitted.** Placed before handled/open triage note (§2 **Chat AI intake — handled-action open-handling triage note in Current action tracking**) / **View in handling workbench**. **No** new API route, **`client_state`** fields, timeline writes, workers, or queues. **Does not** change **Request Surrenderless handling**, note edit, **Mark acknowledged**, outcome/follow-up forms, **Record action handled**, **Open approved step**, workbench link, or other recap copy. **Read-only in-app sync only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, contacted anyone, or closed the real-world case. **No** schema, migration, new API route, queue worker, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**.
- **Chat AI intake — inline manual filing and confirmation capture in Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **write-capable completion** slice (with §2 **Chat AI intake — handling tracking status in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — handling tracking on approved-packet-action Current action tracking (`/justice/chat-ai`)**, §2 **Packet — filing records CRUD on approved case packet (`/justice/packet`)**, §2 **Approved next action — handling tracking contextual action links (justice surfaces)**). In **loaded-case** **Recap → Current action tracking**, when signed in and **`STORAGE_CASE_ID`** is a **UUID**, **`ChatHandlingTrackingStatusReadOnly`** shows compact in-chat **`ChatManualFilingCaptureForm`** when derived step is **`HANDLING_TRACKING_STEP_ADD_FILING`** or **`HANDLING_TRACKING_STEP_ADD_CONFIRMATION`** — **both** approved-packet-action and handling-request paths. **Add filing:** **`POST /api/justice/filings`** — required **destination** (prefilled from **`approved_next_action.label`** when present), optional **`filed_at`**, **`confirmation_number`**, **`notes`** — same body shape as **`JusticeFilingRecords`**; **`applyServerTimelineFromResponse`** on POST success. **Add confirmation:** **`PATCH /api/justice/filings/[id]`** on first filing row missing **`confirmation_number`** — **`confirmation_number`** and/or **`notes`**. After save: **`refreshChatFilings`** → existing **`loadSavedEvidencePreview`** refetch; user stays on **`/justice/chat-ai`**; derived **Handling tracking:** advances on-page. Honest copy: **Record a manual action** / **This records what was done outside Surrenderless. It does not submit or file anything for you.** Suppresses **`ApprovedNextActionHandlingTrackingContextualLink`** to **`/justice/packet#packet-filings`** while inline form is shown. **Does not** change schema, add API routes, **`client_state`** writes, handling request/ack, outcome/follow-up, **Record action handled**, proof CRUD, packet **`JusticeFilingRecords`**, plan, hub, cases, workbench, or automation. **Manual record only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Chat AI intake — archive case on tracking complete in Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **write-capable completion** slice (with §2 **Chat AI intake — inline manual filing and confirmation capture in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — handling tracking status in Current action tracking (`/justice/chat-ai`)**, §2 **Saved cases — archive on All cases cards (`/justice/cases`)**). In **loaded-case** **Recap → Current action tracking**, when signed in, **`STORAGE_CASE_ID`** is a **UUID**, and derived **Handling tracking:** step is **`HANDLING_TRACKING_STEP_COMPLETE`** (**Tracking complete for now.**), **`ChatHandlingTrackingStatusReadOnly`** shows compact **Close this case** / **Archive case** block — **`PATCH /api/justice/cases/[id]`** with **`{ archived_at: ISO }`** (same as **`archiveCase`** on **`/justice/cases`**). Honest copy: **This archives the case in Surrenderless. It does not submit, file, or contact anyone.** On success: **`clearLocalJusticeSession()`** (same keys as **Start new case** on plan/cases) then **`router.push("/justice")`** — does **not** leave an archived case as the active browser session. **Both** approved-packet-action and handling-request tracking paths. **Does not** change schema, add API routes, handling ladder gates, filing CRUD, outcome/follow-up/ack writes, **`/justice/cases`** archive UI, workbench, or automation. **Archive record only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Chat AI intake — inline submission draft review in Active case checklist (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **write-capable funnel** slice (with §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**, §2 **Chat AI intake — inline prepared packet approval in Active case checklist (`/justice/chat-ai`)**, §2 **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`)**). In **Update in chat** **Active case** checklist when signed in, **`STORAGE_CASE_ID`** is a **UUID**, and submission draft is **not** reviewed (**`submissionDraftReviewedInTimeline`**), shows compact **`ChatInlineSubmissionDraftReviewBlock`**: deterministic draft from **`buildSubmissionDraftPreview`** using **`resolveChatPreviewDestination`** (**`computeJusticeDestinations`** + **`STORAGE_FTC_MANUAL_UNLOCK`** + **`cfpbLikelyRelevant`** / **`fccLikelyRelevant`** — preview parity); truncated **Show more** / **Show less**; checkbox **I reviewed the submission draft shown above.**; **Mark draft reviewed** button. Mirrors preview **`handleContinueToPlan`** persistence: signed-in **`POST /api/justice/submission-draft-reviewed`** with **`destination_label`** + **`used_ai: false`**, or **`appendSubmissionDraftReviewedOnce`** — **`applyServerTimelineFromResponse`** + local **`submissionDraftReviewOverride`** so checklist unlocks **inline prepared packet approval** without leaving chat. Optional **`Link`** **Open full submission preview** → **`/justice/preview`** — no auto-navigation. Checklist **Review submission draft** link hidden while inline block is shown. **`handleContinueToPreview`** for loaded signed-in **UUID** **Update in chat** saves and fresh signed-in **UUID** **create** uses **`shouldStayInChatAfterSignedInUuidCaseSave`** — stays on **`/justice/chat-ai`** after save instead of **`router.push("/justice/preview")`** (fresh **create** also **`setIsUpdatingExistingCase(true)`** + **`sessionBaselinePartsRef`**). Honest copy: deterministic draft **not filed or sent automatically**. **Does not** change schema, add API routes, preview/packet/handling pages, or automation. **In-app review record only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Chat AI intake — inline prepared packet approval in Active case checklist (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **write-capable funnel** slice (with §2 **Packet — prepared packet approval checkpoint (`/justice/packet`)**, §2 **Chat AI intake — inline submission draft review in Active case checklist (`/justice/chat-ai`)**, §2 **Chat AI intake — archive case on tracking complete in Current action tracking (`/justice/chat-ai`)**, §2 **Approval-to-action engine — prepared next action shared helpers (`preparedNextAction.ts`)**, §2 **Approved next action target (`client_state` — packet + plan)**). In **Update in chat** **Active case** checklist when signed in, **`STORAGE_CASE_ID`** is a **UUID**, submission draft is reviewed (**`submissionDraftReviewedInTimeline`** or inline override), and **`prepared_packet_approved`** is not set, shows compact **Approve prepared packet** block — checkbox **I reviewed this prepared packet** + **Approve prepared packet** button. Mirrors **`handleApprovePreparedPacket`** on **`/justice/packet`**: **`pickPreparedNextAction`** / **`buildApprovedNextActionTarget`** / **`mergeApprovedNextActionTrackingFields`** from session intake; **`writePreparedPacketApproved`** + **`writeSessionApprovedNextAction`**; signed-in **`GET` + merge + `PATCH`** **`/api/justice/cases/[id]`** **`client_state`** with **`prepared_packet_approved: true`** and **`approved_next_action`**. Honest copy: **This approves the packet inside Surrenderless. It does not submit, file, or contact anyone.** User stays on **`/justice/chat-ai`**; local **`preparedPacketApproved`** / **`approvedNextAction`** update so **Recap → Current action tracking** can appear without a packet visit. **Does not** change schema, add API routes, packet page UI, handling workbench, or automation. **In-app approval only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Chat AI intake — mark step opened stays in chat in Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **continuity** slice (with §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — inline manual filing and confirmation capture in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — archive case on tracking complete in Current action tracking (`/justice/chat-ai`)**). For **Update in chat** loaded signed-in **UUID** cases, **`handleApprovedNextActionOpen`** when **`approved_next_action.status === "approved"`** still sets **`status: "started"`** + **`started_at`**, session sync, and **`GET` + merge + `PATCH`** **`client_state`** — **does not** **`router.push`** to **`href`** after save; user stays on **`/justice/chat-ai`** for inline filing → confirmation → outcome → archive. Button **Mark step opened**; honest copy **Records this step as opened in Surrenderless. It does not submit, file, or contact anyone.** Optional manual **`Link`** **Open {label} (optional)** to prep surface — no auto-navigation. Non-loaded / unsigned / non-UUID paths still navigate after local session write (unchanged fallback). **`status === "completed"`** reopen still navigates. **Does not** change plan/packet/handling open-step behavior, APIs, schema, or handling ladder. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Chat AI intake — read-only handling UI when approved action is handled (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI parity** slice with §2 **Plan — Surrenderless handling requested** (read-only **`ApprovedNextActionHandlingRequestedReadOnly`** when **`status === "completed"`**). In **Recap → Current action tracking** for a **loaded** case: when **`handling_requested_at`** is set and **`approved_next_action.status === "completed"`**, renders **`ApprovedNextActionHandlingRequestedReadOnly`** (**`requestedAt`**, **`handling_request_note`**, **`handling_acknowledged_at`**) instead of **`ApprovedNextActionHandlingRequestBlock`** with **`allowEditNote`** — **no** **Save request note** textarea on handled actions. When **`handling_requested_at`** is set and **`status !== "completed"`**, keeps **`ApprovedNextActionHandlingRequestBlock`** with **Request Surrenderless handling** / **Save request note**. When **`handling_requested_at`** is absent and **`status !== "completed"`**, keeps existing **Request Surrenderless handling** UI. **Does not** change handled/open triage helper (§2 **Chat AI intake — handled-action open-handling triage note in Current action tracking**), queue status, workbench link, **Mark acknowledged**, hydrate/PATCH, disclaimers, fresh intake, **Needs attention** / workbench membership, or APIs. **No** schema, migration, new API route, queue worker, **`justice_case_filings`**, timeline **type**, external submission/contact/queue automation, or **`/api/submit-form`**; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **behavior** slice mirroring §2 **Approved next action started (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)** and §2 **Packet — Open approved step marks started tracking (`/justice/packet`)** (with §2 **Chat AI intake — approved next action display hydrate (`/justice/chat-ai`)**, §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`)**). In **Recap → Current action tracking** for **Update in chat** when **`approved_next_action.status === "approved"`** and **`label`** / **`href`** are present, shows short copy that opening is in-app tracking only, button **Open {label}** calling page-local **`handleApprovedNextActionOpen`**: preserves existing **`label`**, **`href`**, **`approved_at`**, and tracking fields via **`mergeApprovedNextActionTrackingFields`**, sets **`status: "started"`** and **`started_at`** (ISO), writes **`justice_approved_next_action_v1`** and **`justice_prepared_packet_approved_v1`** (page-local **`STORAGE_PREPARED_PACKET_APPROVED_V1`**), updates React state, **`await`**s **`GET` + merge + `PATCH`** existing **`/api/justice/cases/[id]`** **`client_state`** for signed-in **UUID** cases (same pattern as **Record action handled** / handling request), then **`router.push`** to **`approved_next_action.href`** (fallback **`/justice/packet`**). If already **`completed`**, navigates only — **no** status downgrade. **Does not** show **Open {label}** when **`status === "started"`** or **`completed`** — those branches unchanged (§2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`)**). Disclaimer: **Tracking only — not automatic filing or submission.** **Does not** change handling request logic, **Mark follow-up handled**, proof CRUD, **Continue** handoff, active-case banner checklist/focus/links (read-only approved label/status on banner — §2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**), outcome/follow-up form on plan, acknowledge, queue status, workbench link, Plan, Packet, Hub, Cases, APIs, schema, or prep routes. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **behavior** slice mirroring §2 **Approved next action completed / handled** on **`/justice/plan`** (with §2 **Chat AI intake — approved next action display hydrate (`/justice/chat-ai`)**, §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`)**, §2 **Approved next action started (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Packet — Open approved step marks started tracking (`/justice/packet`)**). In **Recap → Current action tracking** for **Update in chat** when **`approved_next_action.status === "started"`**, shows **Opened for next step.**, read-only **Opened {formatted time}** when **`started_at`** is set — §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**; button **Record action handled for now** calling page-local **`handleMarkApprovedNextActionHandled`**: builds **`approved_next_action`** with existing fields, sets **`status: "completed"`** and **`completed_at`** (ISO), merges tracking fields via **`mergeApprovedNextActionTrackingFields`**, writes **`justice_approved_next_action_v1`**, updates React state, **`await`**s **`GET` + **`mergeClientStateWithApprovedNextAction`** + **`PATCH`** existing **`/api/justice/cases/[id]`** **`client_state`** for signed-in **UUID** cases (same pattern as handling request / **Mark follow-up handled**). Disclaimer: **Tracking only — not automatic filing or submission.** **Does not** change handling request logic, **Mark follow-up handled**, proof CRUD, **Continue** handoff, active-case banner, **Request Surrenderless handling**, acknowledge, queue status, workbench link, Plan, Packet, Hub, Cases, APIs, schema, or prep routes. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **UI/copy** slice (with §2 **Chat AI intake — handled-for-now timestamp in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — Recap current-action label alignment (`/justice/chat-ai`)**, §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, §2 **Approved next action completed / handled**). When **`approved_next_action.status === "completed"`** in **Recap → Current action tracking** (**Update in chat**), after the **Approved next action:** line (**Handled** via **`approvedNextActionStatusLabel`**), shows **Next action recorded as handled for now** with **`: {label}`** in **bold** when **`label`** is present, plus **Tracking only — not automatic filing or submission.** Completed **handled-for-now** timestamp when **`completed_at`** is set — §2 **Chat AI intake — handled-for-now timestamp in Current action tracking (`/justice/chat-ai`)**. Clarifies that **Handled** means in-app tracking recorded for now — aligned with Plan/Packet completed narrative — **not** automatic filing or submission. **Does not** change **`handleMarkApprovedNextActionHandled`**, handling request logic, **Mark follow-up handled**, proof CRUD, **Continue** handoff, active-case banner, outcome/follow-up form (Plan-only **`ApprovedNextActionOutcomeTrackingForm`**), Plan, Packet, Hub, Cases, APIs, schema. **In-app tracking only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** schema, migration, new API route, queue worker, browser automation, filing automation, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically.
- **Chat AI intake — handled-for-now timestamp in Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **read-only continuity display** slice (with §2 **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**, §2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**, §2 **Justice hub — handled-for-now timestamp on Current case (`/justice`)**, §2 **Packet — handled-for-now timestamp on approved next action card (`/justice/packet`)**, §2 **Plan — handled-for-now timestamp on prepared-packet approved callouts (`/justice/plan`)**, §2 **Chat AI intake — approved next action display hydrate (`/justice/chat-ai`)**, §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In **Recap → Current action tracking** (**Update in chat** when hydrated **`approvedNextAction`** exists), inside the existing **`status === "completed"`** block, when **`completed_at`** is present on already-hydrated **`approvedNextAction`**, shows read-only **Handled for now {formatted time}** via existing **`formatApprovedNextActionHandlingTimestamp`** from **`approvedNextActionHandlingDisplay.tsx`** — complements **Next action recorded as handled for now** and **Approved next action: Handled** (§2 **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`)**). **Does not** add buttons, **`PATCH`**, timeline events, or show **completed** approved-packet actions in **Needs attention** or handling workbench **Approved packet actions** inbox lists. **Does not** change **Open {label}**, **Record action handled for now**, handling request/ack, follow-up UI, hydrate paths, active-case banner, Plan, Packet, Hub, Cases, or APIs. **Display correctness only** — **no** new API route, schema/migration, DB field, timeline **type**, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **display-only** slice (with §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — handled-for-now timestamp in Current action tracking (`/justice/chat-ai`)**, §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**, §2 **Approved next action started (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In **Recap → Current action tracking** (**Update in chat** when hydrated **`approvedNextAction`** exists), inside the existing **`status === "started"`** block, when **`started_at`** is set, shows read-only **Opened {formatted time}** via existing **`formatApprovedNextActionHandlingTimestamp`** alongside **Opened for next step.** — parity with read-only **Handled for now {formatted time}** when **`status === "completed"`** (§2 **Chat AI intake — handled-for-now timestamp in Current action tracking (`/justice/chat-ai`)**); uses existing **`approved_next_action.started_at`** from already-hydrated **`approvedNextAction`** / **`hydrateApprovedNextActionForDisplay`** (**no** new **`GET`** or **`PATCH`**). Complements §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)** and §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**. **Does not** change **Record action handled for now**, **Open {label}**, handling request/ack, follow-up UI, outcome form, hydrate paths, active-case banner, plan, packet, cases, hub, handling workbench, membership predicates, or any write paths. **Does not** add new API route, schema/migration, DB field, timeline **type**, timeline write, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **chat-first behavior** slice (with §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**, §2 **Chat AI intake — outcome / follow-up read-only dedupe in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`)**, §2 **Saved cases — Needs attention from approved next action follow-up (`/justice/cases`)**, §2 **Approved next action shared state helpers (`approvedNextActionState.ts`)**). In **Recap → Current action tracking** when hydrated **`approvedNextAction.status === "completed"`**, shows compact **Record outcome / follow-up** form (**outcome / note** textarea, **Follow-up needed** checkbox, optional **follow-up date (your pace)**, **Save tracking note**) calling page-local **`handleSaveApprovedNextActionTracking`**: writes existing **`outcome_note`**, **`follow_up_needed`**, and **`follow_up_at`** only on **`approved_next_action`** via **`mergeApprovedNextActionTrackingFields`**, **`writeSessionApprovedNextAction`**, and existing **`GET` + **`mergeClientStateWithApprovedNextAction`** + **`PATCH`** **`/api/justice/cases/[id]`** **`client_state`** for signed-in **UUID** cases — same field semantics as **`/justice/plan`**. When **`status === "completed"`**, the form is the **single** outcome/follow-up display in **Recap** — duplicate read-only lines below handling/workbench UI are hidden (§2 **Chat AI intake — outcome / follow-up read-only dedupe in Current action tracking (`/justice/chat-ai`)**). **Does not** change **`status`**, **`completed_at`**, **Mark follow-up handled** (clears **`follow_up_needed`** only), handling request/ack, **Open {label}**, **Record action handled for now**, proof CRUD, Continue handoff, or active-case banner. Copy: **Tracking only — not automatic filing or submission.** Setting **`follow_up_needed`** may surface the case in **Needs attention** on **`/justice/cases`** — intended in-app tracking only. **No** new API route, schema/migration, DB field, timeline **type**, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Chat AI intake — outcome / follow-up read-only dedupe in Current action tracking (`/justice/chat-ai`):** **`src/app/justice/chat-ai/page.tsx`** only — **display-only** slice (with §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — active case banner outcome / follow-up read-only hints (Update in chat) (`/justice/chat-ai`)**). In **Recap → Current action tracking** only, when **`approvedNextAction.status === "completed"`**, hides duplicate read-only **`outcome_note`**, **Follow-up needed**, and **`ApprovedNextActionFollowUpTimingLine`** below handling/workbench UI — the **Record outcome / follow-up** form (§2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**) is the single editable outcome/follow-up surface in **Recap** for completed actions. When **`status !== "completed"`**, existing read-only outcome/follow-up lines still render if data is present. **Does not** change **Active case** banner hints, disclaimer, **Mark follow-up handled**, work links, packet, plan, hub, cases, **Needs attention** builders, handling triage, **Clear follow-up**, inbox rules, routing, or **`handleSaveApprovedNextActionTracking`** / **`PATCH`** semantics. **Display correctness only** — **no** new API route, schema/migration, DB field, timeline **type**, worker, external queue, filing, submission, contact, upload, storage, or automation behavior; Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone.
- **Plan — recommended next presentation (unchanged logic):** The existing **`recommendationText`** string appears in the **Recommended next** panel (label + stronger typography + bordered / inset styling) above scrolling **Recommended next** destination cards — **not** inside the prepared-next-step card. **Main** destination list items that **already** show the **Recommended next** badge (**unchanged** gating vs. **`rules.ts`**) keep **accent** styling (light blue border, soft background wash, subtle ring — dark-mode equivalents).
- **Merchant / company contact — plan preview:** **`/justice/plan`** surfaces a compact **Suggested message** preview when **merchant or company contact** is **recommended** (same logic as the primary merchant card **“Recommended next”** badge). The preview uses the **same deterministic** contact letter as **`/justice/merchant`**, via **`src/lib/justice/buildMerchantContactMessage.ts`** (**`buildMerchantMessage`**). The preview is **collapsed by default** and **truncated** in the UI to reduce accidental PII exposure; **`/justice/merchant`** remains the **full** read-only composer, **Copy message**, and **save contact / proof** flow (existing **PATCH** case + timeline behavior). **No** email sending, **no** opening external websites from this slice, **no** task creation, **no** API route or handler changes, **no** changes to **`/justice/preview`** or **`submission_draft_reviewed`** tracking, **no** filing automation, **no** embedded browser, **no** schema changes, **no** new DB tables.
- **Final merchant follow-up — plan preview:** **`/justice/plan`** surfaces a **separate** **“Final follow-up before escalation”** block on the **merchant / company contact** card when **contact is already saved** (**`already_contacted === "yes"`**), **FTC is unlocked** (**`computeFtcUnlocked`**), the case is **not** merchant-resolved, and the **normal first-contact** **Suggested message** preview is **not** shown (separate gate from **`merchantBadge`** / **`merchantSuggestedMessageFull`** — those remain **unchanged**). The block is **collapsed by default** (`<details>`) and **distinct** from the original merchant suggested-message preview. It summarizes **saved contact metadata**: **contact method**, **contact date**, **merchant response / outcome**, and **truncated proof / notes** when present. It includes **deterministic**, **user-copyable** final-follow-up text (plan-only helpers in **`src/app/justice/plan/page.tsx`**; **not** from **`buildMerchantMessage`**). Copy states that **Surrenderless does not send** the message automatically. A link goes to **`/justice/merchant`** for the **full** contact record, **Copy message**, and **save** flow. **No** email sending, **no** task creation, **no** filings or evidence creation, **no** new timeline events from this preview alone, **no** filing automation, **no** embedded browser, **no** schema changes or new DB tables, **no** changes to **`submission_draft_reviewed`** / preview-review tracking. **Payment**, **evidence**, **filing**, and **FTC** plan previews remain **unchanged**; the **first-contact** merchant preview **badge logic and helper** remain **unchanged**.
- **Payment dispute — plan preview:** **`/justice/plan`** surfaces a compact **Suggested bank/card dispute letter** preview when **payment dispute** is **available or recommended** (same **`paymentDisputeAvailable`** / **`paymentOk`** gate as the plan’s **Payment dispute** **“Start checklist”** link; **“Recommended next”** on that card when FTC practice is done still implies availability). The preview uses the **same deterministic** issuer letter as **`/justice/payment-dispute`**, via **`src/lib/justice/buildPaymentDisputeBankLetter.ts`** (**`buildBankLetter`**, **`buildDefaultPaymentDisputeDraft`** for default checklist fields from **`JusticeIntake`**). The preview is **collapsed by default** and **truncated** in the UI to reduce accidental PII exposure; **`/justice/payment-dispute`** remains the **full** checklist, **Copy text**, and **save** flow (**`payment_dispute_draft`** + timeline via existing **PATCH** case behavior). **No** sending automation, **no** opening external bank or card issuer websites from this slice, **no** task creation, **no** automatic evidence rows, **no** automatic filing records, **no** API route or handler changes, **no** changes to **`/justice/preview`** or **`submission_draft_reviewed`** tracking, **no** change to the merchant-message plan preview behavior, **no** filing automation, **no** embedded browser, **no** schema changes, **no** new DB tables.
- **Saved evidence — plan preview:** **`/justice/plan`** surfaces **saved evidence metadata** under the **Evidence / proof** card when the user is **signed in** and **`GET /api/justice/evidence?case_id=...`** returns at least one row (same fetch already used for **case readiness** / evidence count). The block is **collapsed by default** (`<details>`) and shows **metadata only**: **title**, **evidence type**, optional **evidence date**, and a **short truncated description** (no `source_url` in the preview list; still **no file uploads** in the product). A **Manage evidence** link goes to **`/justice/evidence`** for full add/edit/delete. When **no** rows after load, shows **No saved proof notes on this case yet.** + **Add proof in chat →** **`/justice/chat-ai`**; **Add evidence** → **`/justice/evidence`** **unchanged** (§2 **Plan — proof readiness continuity after preview review (`/justice/plan`)**). **`/justice/evidence`** remains the **only** full evidence CRUD page; breadcrumb primary link chat-first — §2 **Evidence — breadcrumb primary link chat-first (`/justice/evidence`)**; **evidence save behavior** and **`POST /api/justice/evidence`** are **unchanged**; successful **create** still appends **`evidence_added`** to **`justice_cases.timeline`** via existing server logic. **No** automatic evidence creation, **no** task creation, **no** API changes, **no** changes to packet, merchant, payment, preview/review, or filing surfaces beyond this plan UI, **no** filing automation, **no** embedded browser, **no** schema changes, **no** new DB tables.
- **Evidence — breadcrumb primary link chat-first (`/justice/evidence`):** **`src/app/justice/evidence/page.tsx`** only — **UI/navigation** slice (with §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**, §2 **Chat AI intake — proof/evidence guidance (`/justice/chat-ai`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Packet — breadcrumb primary link chat-first (`/justice/packet`)**, §2 **Saved cases — breadcrumb primary link chat-first (`/justice/cases`)**). Top breadcrumb row: **Update in chat** → **`/justice/chat-ai`** (first link); **Justice workspace** → **`/justice`** (second); **Saved cases** → **`/justice/cases`** and **Home** → **`/`** **unchanged**. Replaces prior **Back to action plan** → **`/justice/plan`** first link + duplicate **Update in chat** second slot — completes chat-first proof loop exit **Chat AI → Organize evidence → Evidence → Update in chat** (§2 **Chat AI intake — proof/evidence guidance**). **Does not** change evidence CRUD, **`GET`/`POST`/`PATCH`/`DELETE`** APIs, form fields, empty/signed-out states, chat-ai proof panel, plan/preview proof cards, filing, submission, contact, queue, upload, storage, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Evidence — Update in chat breadcrumb return (`/justice/evidence`):** **`src/app/justice/evidence/page.tsx`** only — **UI/navigation** slice (with §2 **Chat AI intake — proof/evidence guidance (`/justice/chat-ai`)**, §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **Preview — proof on your case card (Chat AI handoff continuity)**, §2 **Plan — proof readiness continuity after preview review (`/justice/plan`)**). In the top breadcrumb row (**Back to action plan** · **Saved cases** · **Home**), adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan**, matching Preview/Plan breadcrumb link styling and **` · `** separators — **no** new API, **no** schema, **no** funnel-aware change to **Back to action plan**. Closes the chat-first proof loop **Chat AI → Organize evidence → Evidence → Update in chat** (§2 **Chat AI intake — proof/evidence guidance**). Superseded for primary breadcrumb order by §2 **Evidence — breadcrumb primary link chat-first (`/justice/evidence`)**. **Does not** change evidence CRUD, **`GET`/`POST`/`PATCH`/`DELETE`** APIs, form fields, empty/signed-out states, **Back to action plan**, **Saved cases**, **Home**, chat-ai proof panel, plan/preview proof cards, filing, submission, contact, queue, upload, storage, or automatic-action behavior. **Navigation/readiness only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Packet — Update in chat breadcrumb return (`/justice/packet`):** **`src/app/justice/packet/page.tsx`** only — **UI/navigation** slice (with §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **Packet — prepared action review framing (`/justice/packet`)**, §2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**, §2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**, §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**, §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**). In the top breadcrumb row (**Back to action plan** · **Evidence** · **Saved cases**), adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** in the **main** render and the **no-`caseId`** empty-state breadcrumb, matching Preview/Evidence link styling and **` · `** separators — **no** new API, **no** schema, **no** funnel-aware change to **Back to action plan** href. Supports **Packet → Update in chat → Chat AI** continuity (emerald-card **Update in chat** links in §2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)** remain). **Does not** change packet approval checkpoint, emerald explainer/readiness bridge, filing records, evidence list, approved-next handling UI, **`GET`/`POST`/`PATCH`/`DELETE`** APIs, session keys, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Saved cases — Update in chat breadcrumb return (`/justice/cases`):** **`src/app/justice/cases/page.tsx`** only — **UI/navigation** slice (PR **#376** / commit **`eede473`**; with §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**, §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**, §2 **Preview — pre-plan chat correction prompt (`/justice/preview`)**). In the top breadcrumb row (**Back to action plan** · **Update in chat** · **Home**), adds page-level **Update in chat** → **`/justice/chat-ai`** after **Back to action plan**, matching Preview/Evidence/Packet breadcrumb link styling and **` · `** separators — **no** new API, **no** schema, **no** funnel-aware change to **Back to action plan**. Supports **Saved cases → Update in chat → Chat AI** chat-first breadcrumb continuity alongside existing per-card **Update in chat** CTAs. **Does not** change **`openUpdateInChat`**, **`openCase`**, **`resolveOpenCaseHref`**, filters, Needs attention lists/builders, All cases readiness checklists, **`GET`/`PATCH`** case APIs, session keys, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **Handling workbench — Update in chat breadcrumb return (`/justice/handling`):** **`src/app/justice/handling/page.tsx`** only — **UI/navigation** slice (PR **#378** / commit **`115e295`**; with §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **Handling workbench — internal triage surface (`/justice/handling`)**, §2 **Handling workbench — active-case back-link from plan and chat-ai**, §2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**, §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**, §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**). In the top breadcrumb row (**Saved cases** · **Update in chat** · **Action plan**), adds page-level **Update in chat** → **`/justice/chat-ai`** after **Saved cases** and before **Action plan**, matching Preview/Evidence/Packet/Saved cases breadcrumb link styling and **` · `** separators — **no** new API, **no** schema. Supports **Handling workbench → Update in chat → Chat AI** chat-first breadcrumb continuity alongside existing per-card **Update in chat** buttons (§2 **Handling workbench — internal triage surface (`/justice/handling`)**). **Does not** change **`openChat`**, **`navigateWithCase`**, card buttons, **Mark acknowledged**, filters, **`GET`/`PATCH`** case APIs, session keys, schema, handling membership rules, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **Merchant — Update in chat breadcrumb return (`/justice/merchant`):** **`src/app/justice/merchant/page.tsx`** only — **UI/navigation** slice (PR **#380** / commit **`608a515`**; with §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **Approved next action target (`client_state` — packet + plan)**, §2 **Handling workbench — Update in chat breadcrumb return (`/justice/handling`)**, §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**, §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**, §2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**). In the top breadcrumb row (**Back to action plan** · **Update in chat**), adds page-level **Update in chat** → **`/justice/chat-ai`** after **Back to action plan**, matching Preview/Evidence/Packet/Saved cases/Handling breadcrumb link styling and **` · `** separators — **no** new API, **no** schema. Supports **Merchant → Update in chat → Chat AI** chat-first breadcrumb continuity on the common approved-step prep surface (e.g. **`approved_next_action.href`** **`/justice/merchant`** from §2 **Approved next action target**). **Does not** change contact message generation, copy/save contact flow, **`PATCH`** case behavior, timeline writes, approved-step logic, handling UI, session keys, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **Merchant — save return to chat routing (`/justice/merchant`):** **`src/app/justice/merchant/page.tsx`** only — **UI/navigation** slice (with §2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)**, §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — mark step opened stays in chat in Current action tracking (`/justice/chat-ai`)**, §2 **Approved next action target (`client_state` — packet + plan)**, §2 **Packet — signed-in approve returns to chat (`/justice/packet`)**). After successful merchant contact save only: **`router.push("/justice/chat-ai")`** replaces **`router.push("/justice/plan")`**; submit button idle label **Save and continue in chat** replaces **Save and return to action plan** — closes chat-first approved-step loop (**Open … (optional)** from chat Current action tracking → merchant prep → save → back to chat for filing/outcome tracking). **`STORAGE_CASE_ID`** / session intake **unchanged** — chat-ai hydrates active case on landing (§2 **Chat AI intake — hydrate & update commit**). **Does not** change breadcrumb links, form fields, save handler/**`PATCH`**/**`logEvent`** behavior, **`JusticeFilingRecords`**, intro copy, contact message generation, timeline writes, other prep pages, APIs, schema, or styling. **Navigation/copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Merchant — breadcrumb primary link chat-first reorder (`/justice/merchant`):** **`src/app/justice/merchant/page.tsx`** only — **UI/navigation** slice (with §2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)**, §2 **Merchant — save return to chat routing (`/justice/merchant`)**, §2 **FTC review — breadcrumb primary link chat-first reorder (`/justice/ftc-review`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Packet — breadcrumb primary link chat-first (`/justice/packet`)**). Top breadcrumb row: **Update in chat** → **`/justice/chat-ai`** (first link); **Back to action plan** → **`/justice/plan`** (second) — **order only**; hrefs, labels, and link styling **unchanged**. Replaces prior **Back to action plan** first · **Update in chat** second order from §2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)** — aligns merchant prep exit with preview/packet/FTC chat-first breadcrumb pattern and pairs with §2 **Merchant — save return to chat routing (`/justice/merchant`)**. **Does not** change post-save **`router.push("/justice/chat-ai")`**, **Save and continue in chat** button label, form fields, save handler/**`PATCH`**/**`logEvent`**, **`JusticeFilingRecords`**, intro copy, contact message generation, timeline writes, auth gating, APIs, schema, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`):** **`src/app/justice/payment-dispute/page.tsx`** only — **UI/navigation** slice (PR **#382** / commit **`151f93d`**; with §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **Payment dispute — plan preview**, §2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)**, §2 **Approved next action target (`client_state` — packet + plan)**, §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**, §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**, §2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**). In the top breadcrumb row (**Back to action plan** · **Update in chat**), adds page-level **Update in chat** → **`/justice/chat-ai`** after **Back to action plan**, matching Merchant breadcrumb link styling and **` · `** separators — **no** new API, **no** schema. Supports **Payment dispute → Update in chat → Chat AI** chat-first breadcrumb continuity on the bank/card checklist prep surface (e.g. **`approved_next_action.href`** **`/justice/payment-dispute`** or **`rules.ts`** **`internalRoute: "/justice/payment-dispute"`** when payment dispute is available). **Does not** change checklist logic, copy/save flow, **`payment_dispute_draft`**, **`PATCH`** case behavior, timeline writes, approved-step logic, plan preview, session keys, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **Payment dispute — save return to chat routing (`/justice/payment-dispute`):** **`src/app/justice/payment-dispute/page.tsx`** only — **UI/navigation** slice (with §2 **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`)**, §2 **Merchant — save return to chat routing (`/justice/merchant`)**, §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — mark step opened stays in chat in Current action tracking (`/justice/chat-ai`)**, §2 **Approved next action target (`client_state` — packet + plan)**). After successful payment-dispute checklist save only: **`router.push("/justice/chat-ai")`** replaces **`router.push("/justice/plan")`**; submit button idle label **Save and continue in chat** replaces **Save and return to action plan** — closes chat-first approved-step loop (**Open … (optional)** from chat Current action tracking → payment-dispute prep → save → back to chat for filing/outcome tracking). **`STORAGE_CASE_ID`** / session intake **unchanged** — chat-ai hydrates active case on landing (§2 **Chat AI intake — hydrate & update commit**). **Does not** change breadcrumb links, form fields, save handler/**`PATCH`**/analytics, checklist logic, **`payment_dispute_draft`**, intro copy, other prep pages, APIs, schema, or styling. **Navigation/copy only** — Surrenderless has **not** filed, submitted, sent, or contacted anyone. **No** migration, worker, **`justice_case_filings`**, or external submission/contact/queue automation.
- **Payment dispute — breadcrumb primary link chat-first reorder (`/justice/payment-dispute`):** **`src/app/justice/payment-dispute/page.tsx`** only — **UI/navigation** slice (with §2 **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`)**, §2 **Payment dispute — save return to chat routing (`/justice/payment-dispute`)**, §2 **Merchant — breadcrumb primary link chat-first reorder (`/justice/merchant`)**, §2 **FTC review — breadcrumb primary link chat-first reorder (`/justice/ftc-review`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Packet — breadcrumb primary link chat-first (`/justice/packet`)**). Top breadcrumb row: **Update in chat** → **`/justice/chat-ai`** (first link); **Back to action plan** → **`/justice/plan`** (second) — **order only**; hrefs, labels, and link styling **unchanged**. Replaces prior **Back to action plan** first · **Update in chat** second order from §2 **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`)** — aligns payment-dispute prep exit with merchant/FTC/preview/packet chat-first breadcrumb pattern and pairs with §2 **Payment dispute — save return to chat routing (`/justice/payment-dispute`)**. **Does not** change post-save **`router.push("/justice/chat-ai")`**, **Save and continue in chat** button label, checklist logic, form fields, save handler/**`PATCH`**/analytics, **`payment_dispute_draft`**, **`JusticeSavedEvidenceList`**, **`JusticeFilingRecords`**, intro copy, auth gating, APIs, schema, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **BBB — Update in chat breadcrumb return (`/justice/bbb`):** **`src/app/justice/bbb/page.tsx`** only — **UI/navigation** slice (PR **#384** / commit **`2fdf4f8`**; with §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **BBB / State AG / CFPB / FCC / DOT / demand letter (`small_claims`) — plan destination previews**, §2 **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`)**, §2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)**, §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**, §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**, §2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**). In the top breadcrumb row (**Back to action plan** · **Update in chat**), adds page-level **Update in chat** → **`/justice/chat-ai`** after **Back to action plan**, matching Merchant/Payment dispute breadcrumb link styling and **` · `** separators — **no** new API, **no** schema. Supports **BBB complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual BBB prep surface (e.g. **`rules.ts`** **`internalRoute: "/justice/bbb"`**). **Does not** change complaint draft generation, copy flow, **`bbb_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **BBB — breadcrumb primary link chat-first reorder (`/justice/bbb`):** **`src/app/justice/bbb/page.tsx`** only — **UI/navigation** slice (with §2 **BBB — Update in chat breadcrumb return (`/justice/bbb`)**, §2 **Payment dispute — breadcrumb primary link chat-first reorder (`/justice/payment-dispute`)**, §2 **Merchant — breadcrumb primary link chat-first reorder (`/justice/merchant`)**, §2 **FTC review — breadcrumb primary link chat-first reorder (`/justice/ftc-review`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Packet — breadcrumb primary link chat-first (`/justice/packet`)**). Top breadcrumb row: **Update in chat** → **`/justice/chat-ai`** (first link); **Back to action plan** → **`/justice/plan`** (second) — **order only**; hrefs, labels, and link styling **unchanged**. Replaces prior **Back to action plan** first · **Update in chat** second order from §2 **BBB — Update in chat breadcrumb return (`/justice/bbb`)** — aligns BBB complaint prep exit with merchant/payment-dispute/FTC/preview/packet chat-first breadcrumb pattern. **Does not** change complaint draft generation, copy-to-clipboard flow, **`bbb_prep_opened`** analytics, **`JusticeFilingRecords`**, **`JusticeSavedEvidenceList`**, intro/manual-filing copy, auth gating, APIs, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **CFPB — Update in chat breadcrumb return (`/justice/cfpb`):** **`src/app/justice/cfpb/page.tsx`** only — **UI/navigation** slice (PR **#386** / commit **`5de8335`**; with §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **BBB / State AG / CFPB / FCC / DOT / demand letter (`small_claims`) — plan destination previews**, §2 **BBB — Update in chat breadcrumb return (`/justice/bbb`)**, §2 **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`)**, §2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)**, §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**, §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**, §2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**). In the top breadcrumb row (**Back to action plan** · **Update in chat**), adds page-level **Update in chat** → **`/justice/chat-ai`** after **Back to action plan**, matching BBB/Merchant/Payment dispute breadcrumb link styling and **` · `** separators — **no** new API, **no** schema. Supports **CFPB complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual CFPB prep surface (e.g. **`rules.ts`** **`internalRoute: "/justice/cfpb"`** when CFPB prep is unlocked). **Does not** change complaint draft generation, copy flow, **`cfpb_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **CFPB — breadcrumb primary link chat-first reorder (`/justice/cfpb`):** **`src/app/justice/cfpb/page.tsx`** only — **UI/navigation** slice (with §2 **CFPB — Update in chat breadcrumb return (`/justice/cfpb`)**, §2 **BBB — breadcrumb primary link chat-first reorder (`/justice/bbb`)**, §2 **Payment dispute — breadcrumb primary link chat-first reorder (`/justice/payment-dispute`)**, §2 **Merchant — breadcrumb primary link chat-first reorder (`/justice/merchant`)**, §2 **FTC review — breadcrumb primary link chat-first reorder (`/justice/ftc-review`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Packet — breadcrumb primary link chat-first (`/justice/packet`)**). Top breadcrumb row: **Update in chat** → **`/justice/chat-ai`** (first link); **Back to action plan** → **`/justice/plan`** (second) — **order only**; hrefs, labels, and link styling **unchanged**. Replaces prior **Back to action plan** first · **Update in chat** second order from §2 **CFPB — Update in chat breadcrumb return (`/justice/cfpb`)** — aligns CFPB complaint prep exit (primary Step 3 for many cases) with BBB/merchant/payment-dispute/FTC/preview/packet chat-first breadcrumb pattern. **Does not** change complaint draft generation, copy-to-clipboard flow, **`cfpb_prep_opened`** analytics, **`JusticeFilingRecords`**, **`JusticeSavedEvidenceList`**, intro/manual-filing copy, auth gating, APIs, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **FCC — Update in chat breadcrumb return (`/justice/fcc`):** **`src/app/justice/fcc/page.tsx`** only — **UI/navigation** slice (PR **#388** / commit **`35e3e7c`**; with §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **BBB / State AG / CFPB / FCC / DOT / demand letter (`small_claims`) — plan destination previews**, §2 **CFPB — Update in chat breadcrumb return (`/justice/cfpb`)**, §2 **BBB — Update in chat breadcrumb return (`/justice/bbb`)**, §2 **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`)**, §2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)**, §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**, §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**, §2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**). In the top breadcrumb row (**Back to action plan** · **Update in chat**), adds page-level **Update in chat** → **`/justice/chat-ai`** after **Back to action plan**, matching CFPB/BBB/Merchant/Payment dispute breadcrumb link styling and **` · `** separators — **no** new API, **no** schema. Supports **FCC complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual FCC prep surface (e.g. **`rules.ts`** **`internalRoute: "/justice/fcc"`** when FCC prep is relevant). **Does not** change complaint draft generation, copy flow, **`fcc_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **FCC — breadcrumb primary link chat-first reorder (`/justice/fcc`):** **`src/app/justice/fcc/page.tsx`** only — **UI/navigation** slice (with §2 **FCC — Update in chat breadcrumb return (`/justice/fcc`)**, §2 **CFPB — breadcrumb primary link chat-first reorder (`/justice/cfpb`)**, §2 **BBB — breadcrumb primary link chat-first reorder (`/justice/bbb`)**, §2 **Payment dispute — breadcrumb primary link chat-first reorder (`/justice/payment-dispute`)**, §2 **Merchant — breadcrumb primary link chat-first reorder (`/justice/merchant`)**, §2 **FTC review — breadcrumb primary link chat-first reorder (`/justice/ftc-review`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Packet — breadcrumb primary link chat-first (`/justice/packet`)**). Top breadcrumb row: **Update in chat** → **`/justice/chat-ai`** (first link); **Back to action plan** → **`/justice/plan`** (second) — **order only**; hrefs, labels, and link styling **unchanged**. Replaces prior **Back to action plan** first · **Update in chat** second order from §2 **FCC — Update in chat breadcrumb return (`/justice/fcc`)** — aligns FCC complaint prep exit (Step 3 after CFPB for many cases) with CFPB/BBB/merchant/payment-dispute/FTC/preview/packet chat-first breadcrumb pattern. **Does not** change complaint draft generation, copy-to-clipboard flow, **`fcc_prep_opened`** analytics, **`JusticeFilingRecords`**, **`JusticeSavedEvidenceList`**, intro/manual-filing copy, auth gating, APIs, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **DOT — Update in chat breadcrumb return (`/justice/dot`):** **`src/app/justice/dot/page.tsx`** only — **UI/navigation** slice (PR **#390** / commit **`d56f7be`**; with §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **DOT / USDOT aviation — manual prep page and plan priority**, §2 **BBB / State AG / CFPB / FCC / DOT / demand letter (`small_claims`) — plan destination previews**, §2 **FCC — Update in chat breadcrumb return (`/justice/fcc`)**, §2 **CFPB — Update in chat breadcrumb return (`/justice/cfpb`)**, §2 **BBB — Update in chat breadcrumb return (`/justice/bbb`)**, §2 **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`)**, §2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)**, §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**, §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**, §2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**). In the top breadcrumb row (**Back to action plan** · **Update in chat**), adds page-level **Update in chat** → **`/justice/chat-ai`** after **Back to action plan**, matching FCC/CFPB/BBB/Merchant/Payment dispute breadcrumb link styling and **` · `** separators — **no** new API, **no** schema. Supports **USDOT aviation complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual DOT prep surface (e.g. **`rules.ts`** **`internalRoute: "/justice/dot"`** when aviation hints match). **Does not** change complaint draft generation, copy flow, **`dot_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **DOT — breadcrumb primary link chat-first reorder (`/justice/dot`):** **`src/app/justice/dot/page.tsx`** only — **UI/navigation** slice (with §2 **DOT — Update in chat breadcrumb return (`/justice/dot`)**, §2 **FCC — breadcrumb primary link chat-first reorder (`/justice/fcc`)**, §2 **CFPB — breadcrumb primary link chat-first reorder (`/justice/cfpb`)**, §2 **BBB — breadcrumb primary link chat-first reorder (`/justice/bbb`)**, §2 **Payment dispute — breadcrumb primary link chat-first reorder (`/justice/payment-dispute`)**, §2 **Merchant — breadcrumb primary link chat-first reorder (`/justice/merchant`)**, §2 **FTC review — breadcrumb primary link chat-first reorder (`/justice/ftc-review`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Packet — breadcrumb primary link chat-first (`/justice/packet`)**). Top breadcrumb row: **Update in chat** → **`/justice/chat-ai`** (first link); **Back to action plan** → **`/justice/plan`** (second) — **order only**; hrefs, labels, and link styling **unchanged**. Replaces prior **Back to action plan** first · **Update in chat** second order from §2 **DOT — Update in chat breadcrumb return (`/justice/dot`)** — aligns USDOT aviation complaint prep exit (primary Step 3 for aviation cases) with FCC/CFPB/BBB/merchant/payment-dispute/FTC/preview/packet chat-first breadcrumb pattern. **Does not** change complaint draft generation, copy-to-clipboard flow, **`dot_prep_opened`** analytics, **`JusticeFilingRecords`**, **`JusticeSavedEvidenceList`**, intro/manual-filing copy, auth gating, APIs, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **Demand letter — Update in chat breadcrumb return (`/justice/demand-letter`):** **`src/app/justice/demand-letter/page.tsx`** only — **UI/navigation** slice (PR **#392** / commit **`051c35a`**; with §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **Demand letter (`/justice/demand-letter`) — manual prep slice**, §2 **BBB / State AG / CFPB / FCC / DOT / demand letter (`small_claims`) — plan destination previews**, §2 **DOT — Update in chat breadcrumb return (`/justice/dot`)**, §2 **FCC — Update in chat breadcrumb return (`/justice/fcc`)**, §2 **CFPB — Update in chat breadcrumb return (`/justice/cfpb`)**, §2 **BBB — Update in chat breadcrumb return (`/justice/bbb`)**, §2 **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`)**, §2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)**, §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**, §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**, §2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**). In the top breadcrumb row (**Back to action plan** · **Update in chat**), adds page-level **Update in chat** → **`/justice/chat-ai`** after **Back to action plan**, matching DOT/FCC/CFPB/BBB/Merchant/Payment dispute breadcrumb link styling and **` · `** separators — **no** new API, **no** schema. Supports **Demand letter prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual **`small_claims`** demand-letter prep surface (e.g. **`rules.ts`** **`internalRoute: "/justice/demand-letter"`**). **Does not** change letter generation, copy flow, **`demand_letter_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **Demand letter — breadcrumb primary link chat-first reorder (`/justice/demand-letter`):** **`src/app/justice/demand-letter/page.tsx`** only — **UI/navigation** slice (with §2 **Demand letter — Update in chat breadcrumb return (`/justice/demand-letter`)**, §2 **State AG — breadcrumb primary link chat-first reorder (`/justice/state-ag`)**, §2 **DOT — breadcrumb primary link chat-first reorder (`/justice/dot`)**, §2 **FCC — breadcrumb primary link chat-first reorder (`/justice/fcc`)**, §2 **CFPB — breadcrumb primary link chat-first reorder (`/justice/cfpb`)**, §2 **BBB — breadcrumb primary link chat-first reorder (`/justice/bbb`)**, §2 **Payment dispute — breadcrumb primary link chat-first reorder (`/justice/payment-dispute`)**, §2 **Merchant — breadcrumb primary link chat-first reorder (`/justice/merchant`)**, §2 **FTC review — breadcrumb primary link chat-first reorder (`/justice/ftc-review`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Packet — breadcrumb primary link chat-first (`/justice/packet`)**). Top breadcrumb row: **Update in chat** → **`/justice/chat-ai`** (first link); **Back to action plan** → **`/justice/plan`** (second) — **order only**; hrefs, labels, and link styling **unchanged**. Replaces prior **Back to action plan** first · **Update in chat** second order from §2 **Demand letter — Update in chat breadcrumb return (`/justice/demand-letter`)** — completes manual complaint-prep breadcrumb chat-first alignment across BBB, State AG, CFPB, FCC, DOT, demand letter, merchant, payment-dispute, and FTC review (with §2 **FTC review — breadcrumb primary link chat-first reorder (`/justice/ftc-review`)**). **Does not** change letter generation, copy-to-clipboard flow, **`demand_letter_prep_opened`** analytics, **`JusticeFilingRecords`**, **`JusticeSavedEvidenceList`**, intro/manual-filing copy, auth gating, APIs, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **State AG — Update in chat breadcrumb return (`/justice/state-ag`):** **`src/app/justice/state-ag/page.tsx`** only — **UI/navigation** slice (PR **#394** / commit **`7154ef5`**; with §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **BBB / State AG / CFPB / FCC / DOT / demand letter (`small_claims`) — plan destination previews**, §2 **Demand letter — Update in chat breadcrumb return (`/justice/demand-letter`)**, §2 **DOT — Update in chat breadcrumb return (`/justice/dot`)**, §2 **FCC — Update in chat breadcrumb return (`/justice/fcc`)**, §2 **CFPB — Update in chat breadcrumb return (`/justice/cfpb`)**, §2 **BBB — Update in chat breadcrumb return (`/justice/bbb`)**, §2 **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`)**, §2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)**, §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**, §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**, §2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**). In the top breadcrumb row (**Back to action plan** · **Update in chat**), adds page-level **Update in chat** → **`/justice/chat-ai`** after **Back to action plan**, matching Demand letter/DOT/FCC/CFPB/BBB/Merchant/Payment dispute breadcrumb link styling and **` · `** separators — **no** new API, **no** schema. Supports **State AG complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual State AG prep surface (e.g. **`rules.ts`** **`internalRoute: "/justice/state-ag"`**). **Does not** change complaint draft generation, **`consumer_us_state`** selector, copy flow, **`state_ag_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **State AG — breadcrumb primary link chat-first reorder (`/justice/state-ag`):** **`src/app/justice/state-ag/page.tsx`** only — **UI/navigation** slice (with §2 **State AG — Update in chat breadcrumb return (`/justice/state-ag`)**, §2 **DOT — breadcrumb primary link chat-first reorder (`/justice/dot`)**, §2 **FCC — breadcrumb primary link chat-first reorder (`/justice/fcc`)**, §2 **CFPB — breadcrumb primary link chat-first reorder (`/justice/cfpb`)**, §2 **BBB — breadcrumb primary link chat-first reorder (`/justice/bbb`)**, §2 **Payment dispute — breadcrumb primary link chat-first reorder (`/justice/payment-dispute`)**, §2 **Merchant — breadcrumb primary link chat-first reorder (`/justice/merchant`)**, §2 **FTC review — breadcrumb primary link chat-first reorder (`/justice/ftc-review`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Packet — breadcrumb primary link chat-first (`/justice/packet`)**). Top breadcrumb row: **Update in chat** → **`/justice/chat-ai`** (first link); **Back to action plan** → **`/justice/plan`** (second) — **order only**; hrefs, labels, and link styling **unchanged**. Replaces prior **Back to action plan** first · **Update in chat** second order from §2 **State AG — Update in chat breadcrumb return (`/justice/state-ag`)** — aligns State AG complaint prep exit with DOT/FCC/CFPB/BBB/merchant/payment-dispute/FTC/preview/packet chat-first breadcrumb pattern. **Does not** change complaint draft generation, **`consumer_us_state`** selector, copy-to-clipboard flow, **`state_ag_prep_opened`** analytics, **`JusticeFilingRecords`**, **`JusticeSavedEvidenceList`**, intro/manual-filing copy, auth gating, APIs, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **FTC review — Update in chat breadcrumb return (`/justice/ftc-review`):** **`src/app/justice/ftc-review/page.tsx`** only — **UI/navigation** slice (PR **#396** / commit **`25144d7`**; with §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **FTC practice — plan preview**, §2 **State AG — Update in chat breadcrumb return (`/justice/state-ag`)**, §2 **Demand letter — Update in chat breadcrumb return (`/justice/demand-letter`)**, §2 **DOT — Update in chat breadcrumb return (`/justice/dot`)**, §2 **FCC — Update in chat breadcrumb return (`/justice/fcc`)**, §2 **CFPB — Update in chat breadcrumb return (`/justice/cfpb`)**, §2 **BBB — Update in chat breadcrumb return (`/justice/bbb`)**, §2 **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`)**, §2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)**, §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**, §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**, §2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**). In the top breadcrumb row (**Back to action plan** · **Update in chat**), adds page-level **Update in chat** → **`/justice/chat-ai`** after **Back to action plan**, matching State AG/Demand letter/DOT/FCC/CFPB/BBB/Merchant/Payment dispute breadcrumb link styling and **` · `** separators — **no** new API, **no** schema. Supports **FTC practice review → Update in chat → Chat AI** chat-first breadcrumb continuity on the internal practice FTC review surface (e.g. **`rules.ts`** **`/justice/ftc-review`** when FTC is unlocked). **Does not** change FTC unlock redirect gate, practice autofill, success-panel **Back to action plan** button, auth gating, mock form behavior, timeline writes, filing records UI, plan FTC preview, session keys, schema, routes, or automatic-action behavior. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **FTC review — practice success return to chat routing (`/justice/ftc-review`):** **`src/app/justice/ftc-review/page.tsx`** only — **UI/navigation** slice (with §2 **FTC review — Update in chat breadcrumb return (`/justice/ftc-review`)**, §2 **Merchant — save return to chat routing (`/justice/merchant`)**, §2 **Payment dispute — save return to chat routing (`/justice/payment-dispute`)**, §2 **FTC practice — plan preview**, §2 **Chat AI intake — hydrate & update commit**). In practice-success emerald panel only, after **Run practice autofill** completes: **`Link`** **Continue in chat** → **`/justice/chat-ai`** replaces **Back to action plan** → **`/justice/plan`** — closes chat-first post-practice exit (plan FTC preview → practice review → success → back to chat). **`STORAGE_CASE_ID`** / session intake **unchanged** — chat-ai hydrates active case on landing. **Does not** change FTC unlock gate **`router.replace("/justice/plan")`** when not unlocked, breadcrumb links, **`runPractice`**, timeline/mock form, **`JusticeFilingRecords`**, **`JusticeSavedEvidenceList`**, auth gating, APIs, schema, or styling/classes on the link. **Navigation only** — practice flow is **not** a real government submission. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **FTC review — breadcrumb primary link chat-first reorder (`/justice/ftc-review`):** **`src/app/justice/ftc-review/page.tsx`** only — **UI/navigation** slice (with §2 **FTC review — Update in chat breadcrumb return (`/justice/ftc-review`)**, §2 **FTC review — practice success return to chat routing (`/justice/ftc-review`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**, §2 **Packet — breadcrumb primary link chat-first (`/justice/packet`)**). Top breadcrumb row: **Update in chat** → **`/justice/chat-ai`** (first link); **Back to action plan** → **`/justice/plan`** (second) — **order only**; hrefs, labels, and link styling **unchanged**. Replaces prior **Back to action plan** first · **Update in chat** second order from §2 **FTC review — Update in chat breadcrumb return (`/justice/ftc-review`)** — aligns FTC practice review exit with preview/packet chat-first breadcrumb pattern. **Does not** change FTC unlock gate **`router.replace("/justice/plan")`** when not unlocked, practice-success **Continue in chat** panel (§2 **FTC review — practice success return to chat routing (`/justice/ftc-review`)**), **`runPractice`**, timeline/mock form, filing records UI, evidence list, auth gating, APIs, schema, or automatic-action behavior. **Navigation only** — practice flow is **not** a real government submission. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **FTC review — unlock gate redirect to chat (`/justice/ftc-review`):** **`src/app/justice/ftc-review/page.tsx`** only — **UI/navigation** slice (with §2 **FTC review — breadcrumb primary link chat-first reorder (`/justice/ftc-review`)**, §2 **FTC review — practice success return to chat routing (`/justice/ftc-review`)**, §2 **Chat AI intake — hydrate & update commit**). When **`computeFtcUnlocked`** is false (same gate conditions and **`STORAGE_FTC_MANUAL_UNLOCK`** check as before), **`router.replace("/justice/chat-ai")`** replaces **`router.replace("/justice/plan")`** — **completes** FTC review programmatic chat-first routing alongside breadcrumb (§2 **FTC review — breadcrumb primary link chat-first reorder (`/justice/ftc-review`)**) and practice-success **Continue in chat** (§2 **FTC review — practice success return to chat routing (`/justice/ftc-review`)**). **`STORAGE_CASE_ID`** / session intake **unchanged** — chat-ai hydrates active case on landing. **Does not** change **`computeFtcUnlocked`** logic, breadcrumb order, **`runPractice`**, practice-success panel, timeline/mock form, **`JusticeFilingRecords`**, **`JusticeSavedEvidenceList`**, auth gating, APIs, schema, link styling, or automatic-action behavior. **Navigation only** — practice flow is **not** a real government submission. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **Structured intake — chat-first breadcrumb links (`/justice/intake`):** **`src/app/justice/intake/page.tsx`** only — **UI/navigation** slice (PR **#398** / commit **`2c35e2d`**; with §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **Chat-first navigation promotion (UI only on hub/header)**, §2 **FTC review — Update in chat breadcrumb return (`/justice/ftc-review`)**). In the top breadcrumb row (**Home · Chat intake · Step-by-step chat**), **Home** → **`/`**, **Chat intake** → **`/justice/chat-ai`**, and **Step-by-step chat** → **`/justice/chat`**, matching existing justice breadcrumb link styling and **` · `** separators — **no** new API, **no** schema. Keeps **structured intake** connected back to the **chat-first** intake flow so users on the full structured form can reach AI conversational or scripted step-by-step chat without losing alternate-intake context. **Does not** change form submit, validation, saved intake loading, preview routing, **`commitIntakeToSessionAndServer`**, session keys, schema, routes, or APIs. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **Structured intake — subtitle action plan copy de-bias (`/justice/intake`):** **`src/app/justice/intake/page.tsx`** only — **UI/copy** slice (with §2 **Structured intake — chat-first breadcrumb links (`/justice/intake`)**, §2 **Step-by-step chat — review-step action plan copy de-bias (`/justice/chat`)**). Subtitle under **Your consumer case** only: **Answer once. Next you'll review a submission draft.** replaces trailing **, then your action plan** — removes plan skip-ahead wording; matches **`handleSubmit`** → **`/justice/preview`** routing. Breadcrumb, form submit/validation, commit logic, APIs, schema, auth, prep/plan pages **unchanged** — **no** filing/submission/contact/queue/upload/storage/automatic-action claims.
- **Step-by-step chat — chat-first breadcrumb return (`/justice/chat`):** **`src/app/justice/chat/page.tsx`** only — **UI/navigation** slice (PR **#400** / commit **`809412b`**; with §2 **Justice-wide navigation alignment toward AI chat intake**, §2 **Chat-first navigation promotion (UI only on hub/header)**, §2 **Structured intake — chat-first breadcrumb links (`/justice/intake`)**). In the top breadcrumb row (**Home · Chat intake · Action plan · Structured form**), **Chat intake** → **`/justice/chat-ai`** after **Home** → **`/`** and before **Action plan** → **`/justice/plan`** and **Structured form** → **`/justice/intake`**, matching existing justice breadcrumb link styling and **` · `** separators — **no** new API, **no** schema. Gives **scripted step-by-step chat** a visible return path to the **primary AI chat intake**. **Does not** change scripted chat flow, questions, answers, form routing, preview routing, **`commitIntakeToSessionAndServer`**, session keys, schema, routes, or APIs. **Navigation only** — Surrenderless has **not** filed, submitted, sent, queued externally, or contacted anyone. **No** migration, worker, filing/submission/contact/queue/upload/storage automation.
- **Step-by-step chat — breadcrumb Action plan de-bias (`/justice/chat`):** **`src/app/justice/chat/page.tsx`** only — **UI/navigation** slice (with §2 **Step-by-step chat — chat-first breadcrumb return (`/justice/chat`)**, §2 **Chat AI intake — signed-out fresh intake breadcrumb chat-first (`/justice/chat-ai`)**, §2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**). In the top breadcrumb row, third link only: **Justice workspace** → **`/justice`** replaces **Action plan** → **`/justice/plan`** — breadcrumb reads **Home · Chat intake · Justice workspace · Structured form**; aligns scripted step-by-step chat exit with preview/chat-ai hub breadcrumb pattern. **Chat intake** → **`/justice/chat-ai`** and **Structured form** → **`/justice/intake`** **unchanged**. Scripted Q&A flow, **`handleContinue`** → **`/justice/preview`**, review-step copy, commit logic, APIs, schema, auth, prep/plan pages **unchanged** — **no** filing/submission/contact/queue/upload/storage/automatic-action claims.
- **Step-by-step chat — review-step action plan copy de-bias (`/justice/chat`):** **`src/app/justice/chat/page.tsx`** only — **UI/copy** slice (with §2 **Step-by-step chat — breadcrumb Action plan de-bias (`/justice/chat`)**, §2 **Structured intake — chat-first breadcrumb links (`/justice/intake`)**). In page-local **`stepQuestion`**, **`review`** branch only: **Here's a quick recap. When you're ready, continue to review your submission draft.** replaces trailing **, then your action plan** — removes plan skip-ahead wording; matches **`handleContinue`** → **`/justice/preview`** routing. Breadcrumb (#544), scripted Q&A flow, other **`stepQuestion`** branches, commit logic, APIs, schema, auth, prep/plan pages **unchanged** — **no** filing/submission/contact/queue/upload/storage/automatic-action claims.
- **Saved filings — plan preview:** **`/justice/plan`** surfaces **saved filing record metadata** under the **Filing records** card when the user is **signed in** and **`GET /api/justice/filings?case_id=...`** returns at least one row (same **`filings`** state already loaded for **case readiness**). The block is **collapsed by default** (`<details>`) and **metadata-only**: **destination**, optional **filed date** (`filed_at` when present), **truncated confirmation** snippet, **truncated notes** snippet — it **intentionally omits `filing_url`** on the plan card to reduce nudging users toward external sites from the plan surface. Copy links to **`/justice/packet`** to **add or edit** filing records (**`JusticeFilingRecords`** on the packet page); **`/justice/packet`** remains the **full** packet bundle plus **filing record** management. **Filing** save/edit/delete behavior and **`POST` / `PATCH` / `DELETE`** filing APIs are **unchanged**; **`POST /api/justice/filings`** still appends **`filing_recorded`** to **`justice_cases.timeline`**. **No** automatic filing rows, **no** inline filing form on **`/justice/plan`**, **no** task creation, **no** API changes, **no** changes to packet/merchant/payment/evidence/preview/review behavior beyond this plan UI, **no** filing automation, **no** embedded browser, **no** schema changes, **no** new DB tables.
- **FTC practice — plan preview:** **`/justice/plan`** surfaces a compact **FTC practice preview** inside **Step 3 — Escalate to FTC** when that **FTC fallback** Step 3 path is active (i.e. not when **CFPB**, **FCC**, or **DOT** is the primary Step 3), **FTC is unlocked** (same gating as the existing FTC CTA in **`rules.ts` / `computeFtcUnlocked`**) and **FTC practice is not already completed** (session mock-completion flag not set). The block uses **existing `JusticeIntake`** on the plan page only — **no** new fetches. It is **collapsed by default** (`<details>`) and **metadata-only**: **company name**, **issue / problem category**, optional **money amount**, optional **date / order date**, optional **reply email**, and a **truncated complaint / story** summary. Copy states clearly that the flow is **practice-only**, **not** a real government filing, **nothing** is submitted to the FTC automatically, and there is **no** external FTC submission from this slice. A **Continue to practice FTC form** link goes to **`/justice/ftc-review`** for the full internal practice review and optional autofill. **`/justice/ftc-review`** remains the **full** practice review / autofill page; **`/mock/ftc-complaint`** remains **internal mock QA only**. **No** real FTC filing, **no** **`/api/submit-form`** behavior changes, **no** API changes, **no** timeline behavior changes from this preview alone, **no** automatic filings, evidence, or tasks, **no** filing automation, **no** embedded browser, **no** schema changes or new DB tables. **Merchant**, **payment**, **evidence**, and **filing** plan previews remain **unchanged**.
- **BBB / State AG / CFPB / FCC / DOT / demand letter (`small_claims`) — plan destination previews:** **`/justice/plan`** surfaces **compact**, **collapsed** (`<details>`) **metadata-only** previews on the **Better Business Bureau**, **State Attorney General**, **CFPB**, **FCC**, **USDOT / aviation**, and **Small claims / demand letter** (**`small_claims`**) rows in **“Other places this may go”** when each destination is present with **`internalRoute`** (same **`computeJusticeDestinations`** / row shape as before). Previews sit **in the row before/near** the existing **Open** controls; **destination ordering**, **badges**, **`Open`** **`href`s**, and **analytics** (`bbb_prep_opened`, `state_ag_prep_opened`, `cfpb_prep_opened`, `fcc_prep_opened`, **`dot_prep_opened`**, **`demand_letter_prep_opened`** on link click) follow the shipped pattern. **BBB, State AG, CFPB, FCC, FTC, payment dispute, and merchant** destination rows behave as before; **DOT** receives **`internalRoute: "/justice/dot"`** when aviation hints match (**`dotLikelyRelevant`**); **`small_claims`** receives **`internalRoute: "/justice/demand-letter"`** (row **label** remains **“Small claims / demand letter”**), keeps **`status: "later"`**, and is **not** promoted to **primary Step 3**. Metadata: **company**, **issue / category**, optional **money**, optional **order/payment date**, optional **`consumer_us_state`** on the **State AG** row only, **merchant contact** summary/outcome when documented, and **truncated story** — **no** full complaint draft text on the plan. Copy: **manual prep**; **Surrenderless does not file** automatically; one-line guidance to copy the full draft on **`/justice/bbb`**, **`/justice/state-ag`**, **`/justice/cfpb`**, **`/justice/fcc`**, the **DOT aviation prep** page, or the **demand letter prep** page as applicable. **`/justice/bbb`**, **`/justice/state-ag`**, **`/justice/cfpb`**, **`/justice/fcc`**, **`/justice/dot`**, and **`/justice/demand-letter`** are manual prep surfaces (no automatic filing from the app). **No** new backend APIs for this slice, **no** tasks, **no** automatic filings or evidence, **no** new timeline **types** from opening the prep page alone, **no** filing automation, **no** embedded browser, **no** schema or new DB tables, **no** changes to **`submission_draft_reviewed`** / preview-review tracking. **Merchant**, **payment**, **evidence**, **filing**, **FTC** (fallback Step 3), and **final follow-up** plan previews remain **unchanged** except where **DOT** primary Step 3 applies (see next bullet).
- **Demand letter (`/justice/demand-letter`) — manual prep slice:** **`/justice/demand-letter`** is shipped as a **manual** demand-letter prep page reached through the existing **`small_claims`** destination row labeled **“Small claims / demand letter.”** The page provides **copy-ready** neutral demand-letter text (from **`JusticeIntake`** only), **case summary**, **company / person** info, **what happened**, **requested resolution**, **evidence checklist**, **`JusticeSavedEvidenceList`**, and **user-driven** **`JusticeFilingRecords`**. It is **not legal advice**; it does **not** determine whether the user **qualifies for small claims**; it does **not** file, submit, embed court sites, or automate court or government actions. Users are told to **verify deadlines, dollar limits, venue / jurisdiction, service rules, and court rules** with **official or local court** resources. **No** schema changes, **no** new DB tables, **no** automatic tasks, **no** automatic filings, **no** automatic evidence, **no** court filing, **no** embedded browser, **no** filing automation. **Existing** merchant, payment, evidence, filing, FTC, final follow-up, BBB, State AG, CFPB, FCC, and DOT behavior **remains unchanged**.
- **DOT / USDOT aviation — manual prep page and plan priority:** **`/justice/dot`** is shipped as a **manual** USDOT / aviation consumer complaint prep page: **copy-ready** DOT aviation complaint text (from existing **`JusticeIntake`** fields only), **evidence checklist**, **`JusticeSavedEvidenceList`**, and **user-driven** **`JusticeFilingRecords`** — **no** automatic filing rows, **no** automatic evidence, **no** task creation. Surrenderless **does not file** automatically, **does not embed** the DOT website, and **does not submit** to any government system; there are **no** new APIs, **no** schema changes, **no** new DB tables, **no** embedded browser, and **no** filing automation for this surface. In **`src/lib/justice/rules.ts`**, when DOT/aviation text hints match (**`dotLikelyRelevant`** / **`matchesDotHints`**), the USDOT/aviation destination includes **`internalRoute: "/justice/dot"`** (unresolved action plans). **`/justice/plan`** Step 3 escalation priority is **CFPB → FCC → DOT → FTC fallback**; for aviation / flight / refund / cancellation / baggage-related cases **without** CFPB or FCC primacy, **DOT is the primary Step 3** (**“Step 3 — Escalate to DOT”**), with CTA **“Prepare DOT aviation complaint”** → **`/justice/dot`**, manual-prep-only copy, and **`dot_prep_opened`** analytics from the Step 3 link (**`from: "plan_step3"`**). **FTC** remains available as a **secondary** path (e.g. **“Other places this may go”** and **`/justice/ftc-review`**) rather than the **primary** Step 3 for those aviation-focused cases. **Existing** merchant contact, payment dispute, evidence, filing, final follow-up, BBB, State AG, CFPB, and FCC behavior is **unchanged** except **DOT** may take **primary Step 3** when relevant and appears in **destinations** with **`internalRoute`** and the **collapsed** plan preview as above. **Demand letter** (**`small_claims`**, **`/justice/demand-letter`**) is separate — see the preceding bullet — and is **not** promoted to primary Step 3.
- **Persistence:** When signed in, a **`justice_cases`** row holds intake, timeline JSON, optional payment-dispute draft and client state; **evidence**, **filing records**, and **follow-up tasks** live in child tables.
- **Parallel legacy surface:** Root home (`/`) and dashboard-style flows (e.g. generic ask, form-fill demos, task logs, USPS demo) are **not** the Consumer Justice product; they share the repo.

---

## 3. What existing systems should be reused

Do **not** throw away these; they are the right backbone for a chat-first evolution:

| System | Role |
|--------|------|
| **`justice_cases`** | Canonical case: `intake`, `timeline`, `payment_dispute_draft`, `client_state`, `case_label`, `archived_at`. |
| **Timeline** | Append-only-style events (client session + server JSONB); server idempotent append for resource-linked events. |
| **Evidence** | Metadata rows (no file blobs in MVP); types/labels in `src/lib/justice/evidence.ts`. **`/justice/evidence`** (`src/app/justice/evidence/page.tsx`): full evidence CRUD; breadcrumb includes **Update in chat** → **`/justice/chat-ai`** (§2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**) for chat-first **Organize evidence** return. **`/justice/plan`** (`src/app/justice/plan/page.tsx`): when signed in and saved rows exist, reuses the same **`GET /api/justice/evidence`** response as readiness to show a **collapsed** “Saved evidence” preview (title, type, optional date, truncated description) + link to **`/justice/evidence`** — **no** API or save-behavior changes. |
| **Filing records** | User-tracked external/manual filings; confirmation numbers, URLs, notes. **`/justice/plan`** (`src/app/justice/plan/page.tsx`): when signed in and saved rows exist, reuses the same **`GET /api/justice/filings`** response as readiness for a **collapsed** “Saved filings” preview (destination, optional filed date, truncated confirmation and notes; **no `filing_url`**) + link to **`/justice/packet`** for full **`JusticeFilingRecords`** CRUD — **no** API or save-behavior changes. |
| **Follow-up tasks** | Titles, text due dates, notes, completion; ties to timeline. |
| **Readiness** | `src/lib/justice/caseReadiness.ts` and plan UX (“ready to escalate” vs “needs more info”). |
| **Saved cases** | `src/app/justice/cases/page.tsx` + `GET/PATCH` case APIs (**`client_state`** on list **GET**); search/filter/sort (client-side). Page breadcrumb includes **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (§2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**; PR **#376** / commit **`eede473`**) — matches Preview/Evidence/Packet chat-first breadcrumb continuity. **Start new case** clears local Justice session (**`clearLocalJusticeSession`**) and routes to **`/justice`** workspace hub (§2 **Saved cases — Start new case routing**). **Needs attention** uses **`filteredSortedCases`** like **All cases** — open **tasks** (**Open case**) plus **`approved_next_action.follow_up_needed`** cards (**Open action**, **Clear follow-up**; §2 **Needs attention opens approved action surface**, **Saved cases — clear Needs attention follow-up**) with user-paced timing labels (§2 **Approved next action follow-up timing**). Per-card **readiness helper** only when **Needs more info**; **Ready to escalate** keeps status + stats (§2 **readiness status hints**). **All cases** cards: compact **Basic case info** / **Evidence** / **Submission draft reviewed** checklist when progress loaded (§2 **Saved cases — compact readiness checklist on All cases cards**). **`Open case`** delegates to **`openUpdateInChat`** → **`/justice/chat-ai`** regardless of draft-review or prepared-packet state (§2 **Saved cases — Open case chat-first routing (`/justice/cases`)**); checklist deep links **Review submission draft** / **Review prepared case packet** unchanged. |
| **Packet generation** | `src/app/justice/packet/page.tsx` — aggregate intake, timeline, evidence, filings, tasks for review/export/copy/print; **primary** post–draft-review destination from **`/justice/plan`** (`Review prepared next step`); breadcrumb includes **Update in chat** → **`/justice/chat-ai`** in main and no-**`caseId`** empty-state rows (§2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**) for chat-first **Packet → Update in chat → Chat AI** return. When **`showPreparedActionPacketFraming`** applies (§2), emerald **Your prepared action review** / **Your approved case packet** card + **pre-approval explainer** / **readiness bridge** (§2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**) + **post-approval approved-step explainer** (§2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**) + optional approval checkpoint; approved state persists via **`client_state.prepared_packet_approved`** and **`client_state.approved_next_action`** (**`PATCH`** existing case API) for signed-in **UUID** cases plus session fallbacks (§2) — **no** new routes/schema/timeline types; **no** auto-submit. Packet hydrates **`approved_next_action.status`** (**`approved`**, **`started`**, or **`completed`**) and shows **Next action started** / **Opened for next step**, **Next action recorded as handled**, or saved **outcome / follow-up** summary when applicable (§2 **Packet — approved next action started surfaced**, **Approved next action completed / handled**, **Approved next action outcome / follow-up tracking**). **`/justice/plan`** and **`/justice/packet`** mark **`started`** on approved **Open {label}** click before navigation (§2 **Approved next action started**, **Packet — Open approved step marks started tracking (`/justice/packet`)**); **`/justice/plan`** records **`completed`** when started, saves **outcome / follow-up** when **`completed`**, and surfaces all states after return/refresh (§2). |
| **Submission draft preview** | `src/app/justice/preview/page.tsx` — user reviews what would be submitted **before** any filing. **Deterministic** plain text from **`buildSubmissionDraftPreview.ts`** remains **source of truth / fallback**. **Optional AI-assisted** draft: **`POST /api/justice/preview-draft`** (authenticated, rate-limited, **`OPENAI_API_KEY`** server-side only) + **`buildSubmissionDraftAiPrompt.ts`**; bounded JSON body; optional **`case_id`** UUID gated with **`userOwnsJusticeCase`**; AI output **review-only**; UI discloses **OpenAI**. **Proof on your case** card (§2 **Preview — proof on your case card (Chat AI handoff continuity)**): reuses existing **`GET /api/justice/evidence`** via **`loadEvidence`** — loading/error/empty/saved states; count + up to **3** recent proof note titles (type/date when available); empty links to **`/justice/chat-ai`**; metadata-only copy; intro clarifies saved chat intake/case and proof titles in draft — **no** new API. **Review milestone:** **`submission_draft_reviewed`** timeline event when the user continues from preview — **`POST /api/justice/submission-draft-reviewed`** for owned UUID cases (returns **`timeline`** for **`applyServerTimelineFromResponse`**), else **`appendSubmissionDraftReviewedOnce`** (session); after success, navigation goes to **`/justice/packet`** when **`prepared_packet_approved`** is not set in session and **`/justice/plan`** when it is (§2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**). **`/justice/plan`** (`src/app/justice/plan/page.tsx`): timeline status summary can read **Submission draft reviewed.**; when the milestone is present and the case has **not** moved past that stage (not merchant-resolved; no prep-opened / complaint-filed / **FTC practice completed** yet), **post-review handoff** shows emerald **Surrenderless prepared your next step** (**`pickPreparedNextAction`**: primary **`Review prepared next step`** → **`/justice/packet`**; **next-step explainer** + **readiness bridge** when not ready to escalate — §2 **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`)**; optional secondary **Open {focus} preparation** to an internal prep route; full plan below)—**UI-only**; **no** new APIs, schema, timeline writes beyond the existing milestone, **`rules.ts`** changes, filing automation, queue workers, or real submission behavior; **no** task auto-creation. **`recommendationText`** remains in the **Recommended next** panel only; destination rows that **already** show **Recommended next** get **accent** styling. |
| **Action routing / rules** | `src/lib/justice/rules.ts` — destination ordering, locks, FTC/CFPB/FCC relevance, **DOT/aviation hints** (**`dotLikelyRelevant`**) and **`internalRoute: "/justice/dot"`** when matched (unresolved plans), **`small_claims`** with **`internalRoute: "/justice/demand-letter"`** (**`status: "later"`**; not primary **Step 3**), payment dispute availability. **`/justice/plan`** (**`Other places this may go`**): **BBB**, **State AG**, **CFPB**, **FCC**, **DOT**, and **`small_claims` / demand letter** rows can show a **collapsed** **metadata-only** preview when **`internalRoute`** is set (see §2). **Step 3** priority on the plan: **CFPB → FCC → DOT → FTC fallback** (see §2). |
| **Merchant / company contact message** | **`src/lib/justice/buildMerchantContactMessage.ts`** — deterministic letter from **`JusticeIntake`**; consumed by **`/justice/merchant`** (full copy-and-save; breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** — §2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)**; PR **#380** / commit **`608a515`**) and **`/justice/plan`** (compact **Suggested message** preview when contact is **recommended** / **`merchantBadge`**). **`/justice/plan`** (`src/app/justice/plan/page.tsx`) also surfaces a **separate** **Final follow-up before escalation** preview (saved contact metadata + plan-only deterministic copy; **not** **`buildMerchantMessage`**) when contact is saved, **FTC** is unlocked, the case is unresolved, and the first-contact preview is **not** shown — **no** change to **`merchantBadge`** / first-contact preview behavior. |
| **Payment dispute bank/card letter** | **`src/lib/justice/buildPaymentDisputeBankLetter.ts`** — deterministic issuer letter from checklist draft + **`JusticeIntake`**; consumed by **`/justice/payment-dispute`** (full checklist/copy/save; breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** — §2 **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`)**; PR **#382** / commit **`151f93d`**) and **`/justice/plan`** (compact preview when payment dispute is available/recommended). |

New conversational UI should **read and write** these same primitives (intake shape can evolve carefully; prefer additive fields and migrations if the schema changes). **`buildJusticeIntake.ts`** is the shared **forward** assembler (**`buildJusticeIntakeFromParts`**), **reverse** mapper (**`justiceIntakeToBuildJusticeIntakeParts`**), defaults (**`defaultBuildJusticeIntakeParts`**), and contact-proof gate (**`validateContactProofForIntake`**) for **AI** and **scripted** chat intake (structured form can stay aligned via the same parts shape); **`POST /api/justice/intake-chat`** + **`/justice/chat-ai`** are the **primary** conversational path, including **Update in chat** (§2) and **proof/evidence guidance** that links into **`/justice/evidence`** without auto-creating rows (§2); **`/justice/chat`** and **`/justice/intake`** remain **fallbacks** (§2). Long-term goal (§1): user **only chats**; Surrenderless gathers facts, structures the case, organizes proof, prepares submissions, gets approval, submits/queues where feasible, tracks follow-ups — **not** full filing automation yet.

---

## 4. What should not be expanded blindly

- **Do not** keep adding **disconnected form pages** as the primary UX; each new `/justice/*` wizard is debt unless it converges on chat-first or a single shell.
- **Do not** treat **external links + manual filing** as the **final** product story; they are acceptable **transition** paths, not the north star.
- **Do not** grow **mock/demo flows** (e.g. internal practice forms) as if they were **production** regulatory submission; label them, isolate them, and avoid coupling core case state to test-only pages.

---

## 5. Near-term direction

**Bend the Consumer Justice MVP toward chat-first intake and action** while **preserving** persistence and case management:

- **`/justice/chat-ai` is shipped** as the **primary AI conversational intake** and **Update in chat** for the active case (hub/header **Chat intake**; plan **Update in chat**; preview/plan/prep link alignment §2). **Hydration:** on mount, valid local **`JusticeIntake`** → **`justiceIntakeToBuildJusticeIntakeParts`** + loaded-case greeting/recap (§2). **Active case banner (UI):** on **Update in chat**, compact **Basic case info** / **Evidence** / **Submission draft reviewed** checklist in **Active case** banner (§2 **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`)**); when draft reviewed, **Prepared case packet reviewed: yes/not yet** with **Review prepared case packet** → **`/justice/packet`** when not approved (§2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**); when **`approvedNextAction`** exists, read-only **Next step: {label}** and **Approved next action: {Approved|Started|Handled}** (§2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**) — **Open** / **Record handled** stay in Recap **Current action tracking** only. **Current action tracking (UI):** when **`approved_next_action`** exists, **Recap** shows **Current action tracking** — next step, **Approved next action:** status via **`approvedNextActionStatusLabel`** (§2 **Chat AI intake — Recap current-action label alignment (`/justice/chat-ai`)**), outcome note, follow-up timing, **Request Surrenderless handling** when **`handling_requested_at`** is missing (§2 **Chat AI intake — handling requested in Current action tracking**), **Surrenderless handling requested** after request, **display-only derived queue status** when **`handling_requested_at`** is set on a loaded case (§2 **Chat AI intake — display-only derived handling queue status**), read-only handling when **`status === "completed"`** with handling requested (§2 **Chat AI intake — read-only handling UI when approved action is handled**), handled-action open-handling triage helper when **`status === "completed"`** with handling requested and unacknowledged (§2 **Chat AI intake — handled-action open-handling triage note in Current action tracking**), **View in handling workbench** when handling requested (§2 **Handling workbench — active-case back-link from plan and chat-ai**), **Mark acknowledged** when handling requested and not yet acknowledged (§2 **Chat AI intake — acknowledge handling request in Current action tracking**), **Open {label}** when **`status === "approved"`** (§2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**), **Opened for next step.** + **Record action handled for now** when **`status === "started"`** (§2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**), **Next action recorded as handled for now** (+ step **label** when present) and **Tracking only — not automatic filing or submission.** when **`status === "completed"`** (§2 **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`)**), links to **`/justice/plan`** and **`/justice/packet`**; **Mark follow-up handled** when **`follow_up_needed === true`** (§2 **Chat AI intake — approved next action display hydrate**). **Proof/evidence guidance (UI/copy):** Recap **Proof / evidence** panel + **Organize evidence** → **`/justice/evidence`**; optional before preview; metadata-only (notes/links, **no file uploads**); **no** auto-create from chat (§2 **Chat AI intake — proof/evidence guidance**). **Saved evidence count** on **Update in chat** when signed in + **UUID** case id (§2 **Chat AI intake — saved evidence count in Recap (Proof / evidence panel)**). **Add a proof note** + **Recent proof notes** (up to **3** rows when **N > 0**, inline **Edit** / **Delete** on displayed rows) via **`loadSavedEvidencePreview`** / existing **`GET`** + **`POST`** + **`PATCH`** + **`DELETE`** — add form optional **evidence date** / **description**; edit via **`PATCH /api/justice/evidence/[id]`**; delete via **`DELETE /api/justice/evidence/[id]`** (§2 **Chat AI intake — add proof note from Recap (Proof / evidence panel)**, §2 **Chat AI intake — recent saved proof preview in Recap (Proof / evidence panel)**, §2 **Chat AI intake — edit recent proof note in Recap (Proof / evidence panel)**, §2 **Chat AI intake — delete recent proof note in Recap (Proof / evidence panel)**); **proof keyword nudge** after successful chat send on **Update in chat** with local **Add a proof note** prefill (empty fields only — §2 **Chat AI intake — proof keyword nudge in Recap (Proof / evidence panel)**); **fresh-intake staged proof notes** in **`sessionStorage`** via **`stagedProofNotes.ts`**, flushed after confirmed **create** commit; **cross-mode** pending list + flush retry to existing session **UUID** (including **Update in chat** after refresh — §2 **Chat AI intake — staged proof notes on fresh intake (Proof / evidence panel)**). **Continue handoff (UI):** compact **What happens next** panel above **Continue** mirrors save/flush/preview/action-plan paths (§2 **Chat AI intake — Continue handoff panel (What happens next)**). **Commit:** **`commitIntakeToSessionAndServer`** returns **`{ caseId, serverPersisted }`**; **`mode: "update"`** preserves case id/timeline and **PATCH**es when signed in + UUID; **`mode: "create"`** for brand-new intake (unchanged semantics). **`/justice/chat` is shipped** as the **step-by-step scripted fallback** (unchanged scripted Q&A; always **create** commit). **`/justice/intake`** remains the **structured form fallback** (**Edit structured form**; **create** commit only — duplicate-case risk on edit pre-existing). All paths share post-commit **`/justice/preview`** → **`/justice/plan`** (§1 north star: chat-first action, **not** auto-filing yet). Company website: **`normalizeCompanyWebsite.ts`** on form + scripted chat.
- **Conversational intake** produces the same **`JusticeIntake`** / case row; **Update in chat** no longer resets the active case when continuing to preview. **Next:** fold proof organization further into chat (**LLM/system-prompt** proof nudges in **`buildJusticeIntakeAiPrompt.ts`** — separate from client keyword nudge §2; fresh-intake keyword nudge; staged proof edit/delete) without new intake-chat fields; fix structured-form **update** commit; filing queue and follow-up tracking (§1). **Shipped:** recap **saved evidence count** on **Update in chat** (§2 **Chat AI intake — saved evidence count in Recap (Proof / evidence panel)**); collapsed **Add a proof note** on **Update in chat** with optional **evidence date** / **description** (§2 **Chat AI intake — add proof note from Recap (Proof / evidence panel)**); **Recent proof notes** on **Update in chat** when **N > 0** with inline **Edit** and **Delete** on displayed rows (§2 **Chat AI intake — recent saved proof preview in Recap (Proof / evidence panel)**, §2 **Chat AI intake — edit recent proof note in Recap (Proof / evidence panel)**, §2 **Chat AI intake — delete recent proof note in Recap (Proof / evidence panel)**); **client-side proof keyword nudge** after successful chat send when the user message matches strong evidence-like terms, with local **Add a proof note** prefill (empty title/description only; opens **`<details>`** for review — §2 **Chat AI intake — proof keyword nudge in Recap (Proof / evidence panel)**); **fresh-intake staged proof notes** (**`stagedProofNotes.ts`**, flush via existing **`POST /api/justice/evidence`** after confirmed **create**; **cross-mode** pending visibility + flush retry to session **UUID** without duplicate **create** — §2 **Chat AI intake — staged proof notes on fresh intake (Proof / evidence panel)**); **Continue handoff** **What happens next** panel above **Continue** (§2 **Chat AI intake — Continue handoff panel (What happens next)**).
- **`/justice/preview` is shipped** as the **first in-app submission draft preview** surface: **deterministic** draft from **`buildSubmissionDraftPreview`** stays primary and **fallback**; optional **AI-assisted** draft (first **Consumer Justice** LLM integration) via **`POST /api/justice/preview-draft`** — **Clerk**-authenticated, **rate-limited**, **`OPENAI_API_KEY`** only on server, prompts from **`buildSubmissionDraftAiPrompt.ts`**; users see copy that **OpenAI** receives case summary data; still **not filed** / **not legal advice**; **“I reviewed this draft”** unlocks **Continue** — **`/justice/chat-ai`** when signed in with a **UUID** case (§2 **Preview — signed-in Continue returns to chat (`/justice/preview`)**); otherwise **`/justice/packet`** when prepared packet not approved, **`/justice/plan`** when approved (§2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**); linked from the plan’s **case packet** block. **Proof continuity (UI):** compact **Proof on your case** card after **Chat AI** handoff — existing evidence **`GET`**, loading/error/empty/saved states, count + up to **3** recent proof note titles (§2 **Preview — proof on your case card (Chat AI handoff continuity)**); intro copy clarifies saved chat intake/case and metadata-only proof titles. **Review tracking:** **`submission_draft_reviewed`** timeline milestone on continue (**`POST /api/justice/submission-draft-reviewed`** + session fallback **`appendSubmissionDraftReviewedOnce`**); **`/justice/plan`** can show **Submission draft reviewed.** **Post-review plan UX (shipped):** when the milestone applies and the user has not moved on, **`/justice/plan`** shows **Surrenderless prepared your next step** — primary in-app **`Review prepared next step`** → **`/justice/packet`** (**`pickPreparedNextAction`** in **`plan/page.tsx`**), optional secondary link to merchant/prep **`internalRoute`** when inferred, **proof readiness continuity** in the post-draft callout from existing **`evidenceCount`** (§2 **Plan — proof readiness continuity after preview review (`/justice/plan`)**), **post-draft next-step explainer** (**`getPreparedNextStepExplainer`**: prepared review / approved in-app step; **Surrenderless does not file or submit for you** / **Nothing is sent automatically** — §2 **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`)**), **readiness bridge** when **`!readyToEscalate`** with **Update in chat** / **Add proof in chat** → **`/justice/chat-ai`** and **Review submission draft** → **`/justice/preview`** (§2 **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`)**); **packet prepared review explainer and readiness bridge** on **`/justice/packet`** pre-approval card (§2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**); **packet post-approval approved-step explainer** after approval (§2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**), **post-draft chat continuity prompt** with **Update in chat** → **`/justice/chat-ai`** so users can fix details or add proof notes without leaving the chat-first path (§2 **Plan — post-draft chat continuity prompt after preview review (`/justice/plan`)**), **post-draft re-preview continuity prompt** with **Review submission draft again** → **`/justice/preview`** so users who updated in chat can confirm the draft matches the saved case before escalating (§2 **Plan — post-draft re-preview continuity prompt after preview review (`/justice/plan`)**), **Case readiness checklist** inline **Add proof in chat** on the **Evidence added** row when no saved proof (§2 **Plan — Case readiness checklist proof CTA (`/justice/plan`)**), **Update in chat** on the **Basic case info present** row when basics incomplete (§2 **Plan — Case readiness checklist basics CTA (`/justice/plan`)**), **Submission draft reviewed: yes/not yet** on the checklist with **Review submission draft** → **`/justice/preview`** when not yet reviewed (§2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**), legally cautious **not filed automatically** copy, and the **full** action plan unchanged below—**no** new APIs, schema, timeline writes beyond the milestone, filing automation, queue state, submission workers, or claim that Surrenderless actually files/submits; interim scaffolding toward §1 (user eventually **only chats**; Surrenderless prepares/submits-or-queues/tracks where feasible). **No** task auto-creation, preview/API changes, embedded browser, or new tables. Server path updates only existing **`justice_cases.timeline`** JSON. This advances **chat → structured case → generated submission preview → approval → in-app prepared review (packet) → manual prep / tracking**.
- **`/justice/plan` merchant-message preview** is **shipped**: when **merchant / company contact** is **recommended**, the plan shows a **Suggested message** block (collapsed + truncated preview) built from the **same** helper as **`/justice/merchant`** (**`src/lib/justice/buildMerchantContactMessage.ts`**), with a clear path to the **full** merchant page for copy and save. **No** email, **no** embedded browser or auto-opened external sites, **no** new tasks, **no** API or preview/review-tracking changes, **no** filing automation, **no** schema or new tables.
- **`/justice/plan` final merchant follow-up preview** is **shipped**: a **separate** **Final follow-up before escalation** block on the merchant/company card when contact is **already saved**, **FTC** is **unlocked**, the case is **unresolved**, and the **first-contact** suggested-message preview is **not** shown — **collapsed** saved-contact summary (method, date, outcome, truncated proof) plus **deterministic** copy-only nudge text in **`plan/page.tsx`**; **Surrenderless does not send** automatically; link to **`/justice/merchant`** for full save flow. **`merchantBadge`** / first-contact preview logic **unchanged**. **No** tasks, filings, evidence, timeline events, API, preview-review tracking, filing automation, embedded browser, schema, or new tables; **payment**, **evidence**, **filing**, and **FTC** plan previews **unchanged**.
- **`/justice/plan` payment-dispute letter preview** is **shipped**: when **payment dispute** is **available or recommended**, the plan shows a **Suggested bank/card dispute letter** block (collapsed + truncated preview) built from the **same** helper as **`/justice/payment-dispute`** (**`src/lib/justice/buildPaymentDisputeBankLetter.ts`**), with a clear path to the **full** checklist page for copy and save. **No** sending automation, **no** auto-opened external bank/card sites, **no** new tasks, **no** automatic evidence or filing records, **no** API or preview/review-tracking changes, **no** change to merchant-message preview behavior, **no** filing automation, **no** embedded browser, **no** schema or new tables.
- **`/justice/plan` saved-evidence preview** is **shipped**: under **Evidence / proof**, when signed in and **`GET /api/justice/evidence`** returns rows, a **collapsed** preview lists **metadata only** (title, type, optional date, truncated description) and links to **`/justice/evidence`** for full management. Empty state links **Add proof in chat →** **`/justice/chat-ai`**; **Add evidence** unchanged (§2 **Plan — proof readiness continuity after preview review (`/justice/plan`)**). Evidence **POST** still appends **`evidence_added`** to timeline; **no** automatic evidence rows, **no** file uploads, **no** tasks, **no** API or packet/merchant/payment/preview/filing behavior changes, **no** filing automation, **no** embedded browser, **no** schema or new tables.
- **`/justice/plan` saved-filings preview** is **shipped**: under **Filing records**, when signed in and **`GET /api/justice/filings`** returns rows, a **collapsed** preview lists **metadata only** (destination, optional filed date, truncated confirmation and notes; **no `filing_url`**) and points users to **`/justice/packet`** to add/edit records (**`JusticeFilingRecords`**). **`/justice/packet`** remains the **full** packet + filing management surface; **`POST /api/justice/filings`** still appends **`filing_recorded`**; **no** automatic filing rows, **no** inline filing form on the plan, **no** tasks, **no** API or packet/merchant/payment/evidence/preview behavior changes, **no** filing automation, **no** embedded browser, **no** schema or new tables.
- **`/justice/packet` prepared-action review framing** is **shipped**: after plan handoff, emerald **Your prepared action review** when post-review gates match — **Your approved case packet** after approval (§2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**); **pre-approval explainer** + **readiness bridge** when not ready to escalate (§2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**); existing packet sections and copy/download/print **unchanged**; **no** APIs, schema, automation, or real submission (§2 **Packet — prepared action review framing**).
- **`/justice/packet` prepared review explainer and readiness bridge** is **shipped**: **`packet/page.tsx`** only — in **`showPreparedActionPacketFraming && !packetApproved`** emerald card, **`getPreparedPacketReviewExplainer`** + bold **`stepLabel`** from **`pickPreparedNextAction`** explains approval marks packet ready for next in-app step / **does not file or submit**; when signed in and **`!readyToEscalate`**, **Before you approve, finish readiness:** with **Update in chat** / **Add proof in chat** → **`/justice/chat-ai`** and **Review submission draft** → **`/justice/preview`**; **Need to fix details or add proof notes?** **Update in chat** line; **`handleApprovePreparedPacket`**, persistence/**`client_state`**, framing gates, packet body, copy/download/print, filing/tasks, APIs, schema, rules, plan, preview, chat-ai, hub, cases unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**).
- **`/justice/packet` post-approval approved-step explainer and emerald card copy refresh** is **shipped**: **`packet/page.tsx`** only — when **`packetApproved && approvedNextAction?.label`**, top emerald card retitles **Your approved case packet**, replaces pre-approval copy with **`getApprovedPacketNextStepExplainer`** naming approved in-app step / **nothing sent automatically**; **Update in chat** correction line kept; inner **Packet approved for next action** card clarifies **Open {label}** opens in-app prep/tracking not automatic filing; **`handleApprovePreparedPacket`**, approval/**`started`**/**`completed`** persistence, framing gates, packet body, copy/download/print, filing/tasks, APIs, schema, rules, plan, preview, chat-ai, hub, cases unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**).
- **`/justice/packet` prepared packet approval checkpoint** is **shipped**: when prepared framing is active, user can check **I reviewed this prepared packet** and **Approve prepared packet for next action**; approved state **Packet approved for next action**; **survives refresh/reopen** for persisted signed-in **UUID** cases via existing **`justice_cases.client_state`** (**`prepared_packet_approved`** + **`approved_next_action`** — §2 **Approved next action target**); **session** fallback for local/non-UUID; **no** filing/submission/send/contact automation (§2 **Packet — prepared packet approval checkpoint**).
- **`/justice/plan` prepared packet approval surfaced** is **shipped**: **`plan/page.tsx`**; existing case **`GET`** hydrate paths read **`client_state.prepared_packet_approved`** and **`approved_next_action`** (+ session fallbacks); when true, plan shows **Prepared packet approved for next action** with stored **label** when available, **Open {label}** to approved **`href`**, and **View approved case packet** / **View case packet** — **no** new APIs, schema, migrations, timeline types, queue workers, or filing/browser automation; **nothing** filed/submitted/sent/contacted automatically; advances **prepare → approve → submit/queue/track** toward §1 (§2 **Plan — prepared packet approval surfaced**).
- **`/justice/plan` post-draft approved step label alignment** is **shipped**: **`plan/page.tsx`** only — **copy/label-only**; in **`showPostDraftReviewCallout`** when **`preparedPacketApproved`**, emerald main paragraph uses **Next step: {approvedStepLabel}** instead of **Approved next step:** — matches Hub/Cases/Chat AI/Preview/Plan checklist label taxonomy; **`approvedStepLabel`**, **`approvedNextAction`** logic, started/completed suffixes, **Approved next action:** status rows, CTAs, routing, handling tracking, disclaimers, APIs, schema, session keys unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Plan — post-draft approved step label alignment (`/justice/plan`)**).
- **Approved next action target in `client_state`** is **shipped**: **`types.ts`** (**`JusticeApprovedNextAction`**), **`packet/page.tsx`**, **`plan/page.tsx`**; approval stores packet-approved flag **and** next in-app step metadata (**`label`**, **`href`**, **`status`**, **`approved_at`**) on existing **`client_state`** only; packet hydrates after refresh; plan surfaces exact stored target on return — **no** schema/migration/new API/queue/browser/filing automation/timeline types; **not** auto-filed/submitted/sent/contacted (§2 **Approved next action target**).
- **Approved next action started on `/justice/plan`** is **shipped**: **`plan/page.tsx`**, **`types.ts`** (**`status: "approved" | "started"`**, **`started_at`**); green **`View approved case packet`** button persists **`started`** to session + **`client_state`** (**`PATCH`** existing case API) before routing; plan shows **Next action started** / **Opened for next step** after return/refresh; hydrate never downgrades **`started`** → **`approved`** — **no** schema/migration/new API/queue/browser/filing automation/timeline types; **not** auto-filed/submitted/sent/contacted; advances **prepare → approve → start/track** (§2 **Approved next action started**). Packet **Open {label}** write path — §2 **`/justice/packet` Open approved step marks started tracking**. Chat **Open {label}** write path — §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**.
- **`/justice/packet` approved next action started surfaced** is **shipped**: **`packet/page.tsx`** only; hydrates **`approved_next_action.status`** from **`client_state`** or session fallback; shows **Next action started** / **Opened for next step** in the existing approval area when **`started`**; **`approved`** packet behavior unchanged; case summary, filings, tasks, copy/export/print unchanged — **no** schema/migration/new API/queue/browser/filing automation/timeline types; **not** auto-filed/submitted/sent/contacted; keeps packet aligned with plan in **prepare → approve → start/track** (§2 **Packet — approved next action started surfaced**).
- **`/justice/packet` started-state record-handled copy alignment** is **shipped**: **`packet/page.tsx`** only — **copy-only**; **`getApprovedPacketNextStepExplainer`** **started** branch and duplicate inline started-state post-approval copy now say record handled **from your action plan or chat intake when ready** (not action plan alone); logic, hydrate, APIs, schema, Plan, Chat AI, Hub, Cases, routing, buttons, state handling unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — started-state record-handled copy alignment (`/justice/packet`)**).
- **`/justice/packet` Open approved step marks started tracking** is **shipped**: **`packet/page.tsx`** only — when packet is approved and **`status === "approved"`**, **Open {label}** **`button`** calls **`handleApprovedNextActionOpen`**: sets **`status: "started"`** + **`started_at`**, writes **`justice_approved_next_action_v1`** / **`justice_prepared_packet_approved_v1`** session maps, **`GET` + merge + `PATCH`** **`client_state`** for signed-in **UUID** cases, then navigates to approved **`href`** — mirrors **`/justice/plan`** started persistence (§2 **Approved next action started**); already **`started`** or **`completed`** keeps plain **`Link`** navigation without downgrade; approval checkpoint, **`handleApprovePreparedPacket`**, explainer copy, gates, packet body, prep routes, APIs, schema, plan, preview, chat-ai, hub, cases unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — Open approved step marks started tracking (`/justice/packet`)**).
- **Approved next action completed / handled** is **shipped**: **`types.ts`**, **`plan/page.tsx`**, **`packet/page.tsx`**, **`chat-ai/page.tsx`** (write on chat — §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**; completed clarity on chat — §2 **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`)**); **`approved_next_action.status`** **`"approved" | "started" | "completed"`** with **`completed_at`** on existing **`client_state`** + session fallback; plan **Record action handled for now** when **`started`**; chat **Current action tracking** **Open {label}** when **`approved`** (§2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**), **Record action handled for now** when **`started`**, and **Next action recorded as handled for now** when **`completed`** (§2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, §2 **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`)**); plan and packet show **Next action recorded as handled** with tracking-only copy — **no** schema/migration/new API/queue/browser/filing automation/timeline types; **not** auto-filed/submitted/sent/contacted; **tracking scaffold** toward §1 **prepare → approve → submit/queue/track**, not final manual workflow (§2 **Approved next action completed / handled**).
- **`/justice/chat-ai` Open approved step marks started in Current action tracking** is **shipped**: **`chat-ai/page.tsx`** only — when **`approved_next_action.status === "approved"`** with **`label`** and **`href`**, **Recap → Current action tracking** shows **Mark step opened**; **`handleApprovedNextActionOpen`** sets **`status: "started"`** + **`started_at`**, writes session maps, **`GET` + merge + `PATCH`** **`client_state`** for signed-in **UUID** loaded cases — **stays on chat** (§2 **Chat AI intake — mark step opened stays in chat in Current action tracking (`/justice/chat-ai`)**); optional **Open {label} (optional)** link — non-UUID/unsigned still navigates — **`started`** / **`completed`** branches unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**).
- **`/justice/chat-ai` mark step opened stays in chat in Current action tracking** is **shipped**: **`chat-ai/page.tsx`** only; loaded signed-in **UUID** **Mark step opened** no post-save **`router.push`** — continuous path to inline filing/confirmation/archive — honest opened copy — optional prep **`Link`** — **no** API/schema change — plan/packet/handling open-step navigate behavior unchanged (§2 **Chat AI intake — mark step opened stays in chat in Current action tracking (`/justice/chat-ai`)**).
- **`/justice/chat-ai` record action handled in Current action tracking** is **shipped**: **`chat-ai/page.tsx`** only — when **`approved_next_action.status === "started"`**, **Recap → Current action tracking** shows **Opened for next step.**, **Record action handled for now**, and **Tracking only — not automatic filing or submission.**; **`handleMarkApprovedNextActionHandled`** sets **`status: "completed"`** + **`completed_at`**, writes **`justice_approved_next_action_v1`**, **`GET` + merge + `PATCH`** **`client_state`** for signed-in **UUID** cases — mirrors plan; handling request, follow-up clear, proof CRUD, Continue handoff, active-case banner, Plan, Packet, Hub, Cases, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**).
- **`/justice/chat-ai` Recap current-action label alignment** is **shipped**: **`chat-ai/page.tsx`** only — **copy/label-only**; **Recap → Current action tracking** uses **Approved next action: {Approved|Started|Handled}** via **`approvedNextActionStatusLabel`** instead of **Status:** — matches Active case banner, Hub, and Cases; **Next step:**, **Open {label}**, **Record action handled**, hydrate, proof, handoff, routing, Plan, Packet, Hub, Cases, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — Recap current-action label alignment (`/justice/chat-ai`)**).
- **`/justice/chat-ai` completed-step clarity in Current action tracking** is **shipped**: **`chat-ai/page.tsx`** only — when **`approved_next_action.status === "completed"`**, **Recap → Current action tracking** shows **Next action recorded as handled for now** with **`: {label}`** when present and **Tracking only — not automatic filing or submission.** after **Approved next action: Handled** (§2 **Chat AI intake — Recap current-action label alignment (`/justice/chat-ai`)**); **`handleMarkApprovedNextActionHandled`**, handling request, follow-up clear, proof CRUD, Continue handoff, active-case banner, outcome/follow-up form, Plan, Packet, Hub, Cases, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`)**).
- **Approved next action outcome / follow-up tracking** is **shipped**: **`types.ts`**, **`plan/page.tsx`**, **`packet/page.tsx`**, **`chat-ai/page.tsx`**; **`approved_next_action`** adds **`outcome_note`**, **`follow_up_needed`**, **`follow_up_at`** on existing **`client_state`** + session fallback when **`status === "completed"`**; **`/justice/plan`**, **`/justice/chat-ai`**, and **`/justice/packet`** compact **Save tracking note** form persists after refresh for signed-in **UUID** cases; hub/cases/handling read-only continuity from hydrated **`client_state`** — **no** schema/migration/new API/queue/browser/filing automation/timeline types; **not** auto-filed/submitted/sent/contacted; **tracking scaffold** toward §1 **prepare → approve → submit/queue/track**, not final manual workflow (§2 **Approved next action outcome / follow-up tracking**, §2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)**).
- **`/justice/cases` Needs attention from approved next action follow-up** is **shipped**: **`cases/page.tsx`** only; **Needs attention** lists cases with **`client_state.approved_next_action.follow_up_needed === true`** (from existing list **GET** **`client_state`**) plus open **tasks**; compact cards with case title, **Handled**/**status**, follow-up timing, outcome note, **Open action**, **Clear follow-up**; **search**/**filter**/**sort**/labels/archive unchanged — **no** schema/migration/new API/queue/browser/filing automation/timeline types; **not** auto-filed/submitted/sent/contacted; **tracking scaffold** toward §1 **prepare → approve → submit/queue/track**, not final manual workflow (§2 **Saved cases — Needs attention from approved next action follow-up**, **Saved cases — clear Needs attention follow-up**).
- **`/justice/cases` Needs attention opens approved action surface** is **shipped**: **`cases/page.tsx`** only; follow-up-needed **Needs attention** cards use **`openApprovedNextActionFollowUp`** — **`approved_next_action.href`** when present (internal **`/justice/*`**), else **`/justice/chat-ai`** (§2 **Saved cases — Needs attention follow-up default chat-first routing (`/justice/cases`)**); **Open action** / **Open tracking** via **`approvedNextActionFollowUpOpenLabel`** (same session activate as **Open case**); **All cases** **Open case** unchanged — **no** schema/migration/new API/queue/browser/filing/automation/timeline types; **not** auto-filed/submitted/sent/contacted; **tracking/navigation scaffold** toward §1 **prepare → approve → submit/queue/track**, not final manual workflow (§2 **Saved cases — Needs attention opens approved action surface**).
- **`/justice/cases` clear Needs attention follow-up** is **shipped**: **`cases/page.tsx`** only; **Clear follow-up** on follow-up-needed cards sets **`follow_up_needed`** false while preserving **outcome note**, **follow-up date**, **status**, **href**, **label**, and other **`approved_next_action`** fields via existing **`client_state`** + **`PATCH /api/justice/cases/[id]`**; item leaves **Needs attention**, case stays in **All cases**; **Open action** / **Open case** unchanged — **no** schema/migration/new API/queue/browser/filing/automation/timeline types; **not** auto-filed/submitted/sent/contacted; **tracking/navigation scaffold** toward §1 **prepare → approve → submit/queue/track**, not final manual workflow (§2 **Saved cases — clear Needs attention follow-up**).
- **`/justice/cases` Needs attention for handling requested** is **shipped**: **`cases/page.tsx`** + **`approvedNextActionHandlingDisplay.tsx`**; **Needs attention** lists cases matching **`isHandlingAwaitingTriageApprovedNextAction`** from **`filteredSortedCases`** (§2 **Approved next action — shared handling awaiting-triage membership predicate**; inclusion rules **unchanged**); compact cards with **Surrenderless handling requested**, recorded time, optional note, **display-only** **Queue status: Awaiting internal triage.** (**`ApprovedNextActionHandlingQueueStatusReadOnly`**; **no** new persisted field), honest tracking copy, **Open case** → **`/justice/plan`**, **View in handling workbench** → **`/justice/handling`** (§2 **Saved cases — View in handling workbench on Needs attention handling cards**), **Mark acknowledged** **unchanged** (§2 **Saved cases — acknowledge handling request from Needs attention**); **follow-up** and **task** attention unchanged; **no** new filters/sort — **no** schema/migration/new API/queue/worker/filing/timeline/external submission/contact/queue automation; **nothing** filed/submitted/sent/queued externally/contacted (§2 **Saved cases — Needs attention for handling requested**).
- **`/justice/cases` acknowledge handling request from Needs attention** is **shipped**: **`handling_acknowledged_at`** on **`approved_next_action`**; **Mark acknowledged** on handling **Needs attention** cards; **`GET` + merge + `PATCH`** + session sync; card removed from inbox; **All cases** still shows handling requested + **Acknowledged {time}**; triage-only copy — **not** filed/submitted/sent/queued/contacted (§2 **Saved cases — acknowledge handling request from Needs attention**).
- **`/justice/cases` View in handling workbench on Needs attention handling cards** is **shipped**: **`cases/page.tsx`** only; handling-request **Needs attention** cards show **View in handling workbench** → **`/justice/handling`** between **Open case** and **Mark acknowledged** (outline secondary control); **card-level navigation only** — **no** session change on click, **no** workbench filtering or deep-link, **no** Needs attention inclusion/queue-status/ack persistence/filter/header/follow-up/task changes; control hidden after acknowledge; **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation/persistence (§2 **Saved cases — View in handling workbench on Needs attention handling cards**).
- **`/justice/cases` View in handling workbench on All cases handling-request cards** is **shipped**: **`cases/page.tsx`** only; **`CaseApprovedNextActionTracking`** shows **View in handling workbench** → **`/justice/handling`** when **`handling_requested_at`** is set (including handled/completed + unacknowledged cases not in **Needs attention**); **read-only navigation** — **no** session change on click, **no** query/hash/filter, **no** workbench membership/**Mark acknowledged**/queue-status/notes/API/pagination/filter/page-level workbench link/**Open case**/external-action copy changes; **no** link without **`handling_requested_at`** — **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation/persistence (§2 **Saved cases — View in handling workbench on All cases handling-request cards**).
- **`/justice/cases` approved next-step label on All cases cards** is **shipped**: **`cases/page.tsx`** only; **`CaseApprovedNextActionTracking`** on **All cases** cards shows read-only **Next step: {label}** when **`client_state.approved_next_action.label`** exists (via **`parseApprovedNextActionFromClientState`** from existing list **GET** **`client_state`**) **above** unchanged **Approved next action: {Approved|Started|Handled}**; handling blocks, disclaimers, **Needs attention** lists, filters, search, **Open case**, Plan, Packet, Chat AI, Hub, APIs, schema **unchanged** — **visibility only**; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — approved next-step label on All cases cards**).
- **`/justice/cases` Needs attention approved-next step label visibility** is **shipped**: **`cases/page.tsx`** only — **display-only**; **Needs attention** handling-requested and follow-up cards show read-only **Next step: {label}** when **`next.label?.trim()`** exists, **before** **Approved next action: {status}** — same styling as status line; matches **All cases** **`CaseApprovedNextActionTracking`** and Hub/Chat/Plan/Preview pair; builders, filters, hydrate, **Open case**, **acknowledge handling request**, approved-action follow-up, routes, APIs, schema, handlers, shared libs unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**).
- **`/justice/cases` Needs attention approved-next-action label alignment** is **shipped**: **`cases/page.tsx`** only — **display/copy-only**; **Needs attention** handling-requested and follow-up cards show **Approved next action: {status}** via **`approvedNextActionStatusLabel`** instead of **Status:** — matches **All cases**, Hub, and Chat AI wording; **Next step:** on those cards — §2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**; builders, handlers, hydrate, routing, filters, Plan, Packet, Hub, Chat AI, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — Needs attention approved-next-action label alignment (`/justice/cases`)**).
- **`/justice/handling` Handling workbench** is **shipped**: **`handling/page.tsx`**; signed-in **GET /api/justice/cases** (paginated) loads cases; read-only **Approved packet actions** section above triage buckets for packet-approved cases without **`handling_requested_at`** (§2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)**); handling-request triage still loads cases with **`handling_requested_at`** — **Awaiting internal triage** vs **Acknowledged** sections; triage cards show company/label, product, approved-action status/label, request time, note, queue status, ack time, honest in-app disclaimer; per-card navigation after session activate — **Open action plan** → **`/justice/plan`**, **Open case packet** → **`/justice/packet`**, **Update in chat** → **`/justice/chat-ai`** (loaded case, not fresh intake), **Open approved step** when internal **`href`** (not plan-only); **Mark acknowledged** on awaiting triage rows only (same **`client_state`** PATCH as **Saved cases**); entry link from **`/justice/cases`** — **no** schema/migration/new API/queue/worker/filing/timeline type/external submission/contact/queue automation; **nothing** filed/submitted/sent/queued externally/contacted (§2 **Handling workbench — internal triage surface**).
- **`/justice/handling` Awaiting internal triage aligned with Saved cases Needs attention** is **shipped**: **`handling/page.tsx`** only; **Awaiting internal triage** uses the same active-case rule as **`/justice/cases`** **Needs attention** — **`handling_requested_at`**, no **`handling_acknowledged_at`**, **`status !== "completed"`**; completed/handled + unacknowledged handling requests no longer in **Awaiting** but remain on **All cases** and can be ack’d from **plan**/**chat**; after ack they appear in workbench **Acknowledged**; helper copy under **Awaiting** — **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation; **Acknowledged** section, CTAs, session highlight, **Mark acknowledged**, queue status, notes, disclaimers, pagination, Needs attention, and chat/plan/cases ack unchanged (§2 **Handling workbench — align Awaiting internal triage with Saved cases Needs attention**).
- **Approved next action shared handling awaiting-triage predicate** is **shipped**: **`approvedNextActionState.ts`**, **`cases/page.tsx`**, **`handling/page.tsx`**; **`isHandlingAwaitingTriageApprovedNextAction`** centralizes active handling-awaiting membership (**`handling_requested_at`**, no **`handling_acknowledged_at`**, **`status !== "completed"`**) for **Saved cases** **Needs attention** and workbench **Awaiting** — **no-UI refactor**; behavior, copy, API, persistence, schema, and workflow **unchanged** (§2 **Approved next action — shared handling awaiting-triage membership predicate**).
- **Approved next action centralized handled/open handling triage note copy** is **shipped**: **`approvedNextActionHandlingDisplay.tsx`**, **`JusticeHubWorkspaceBody.tsx`**, **`packet/page.tsx`**, **`plan/page.tsx`**, **`chat-ai/page.tsx`**; **`ApprovedNextActionHandlingHandledOpenTriageNote`** with **`redirect`** (hub, packet) and **`inlineAck`** (plan, chat) centralizes handled/open triage note copy — **no-behavior-change** refactor; display conditions, **Mark acknowledged** behavior, queue status, workbench links, hydration/**PATCH**, APIs, schema, persistence, and external-action copy **unchanged** (§2 **Approved next action — centralized handled/open handling triage note copy**).
- **`/justice/handling` completed-unacknowledged handling count in Awaiting helper** is **shipped**: **`handling/page.tsx`** only; when **`completedUnacknowledgedCount`** **> 0** (handled **`status === "completed"`**, **`handling_requested_at`**, no **`handling_acknowledged_at`**), **Awaiting** helper points to nested **Handled — open handling request** — §2 **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`)**; discoverability only; those cases stay out of **Awaiting**; **no** third top-level section; per-case nested rows with workbench **Mark acknowledged** — §2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**; active **Awaiting** membership/CTAs/queue status/notes/APIs/Needs attention unchanged (§2 **Handling workbench — completed-unacknowledged handling count in Awaiting helper**).
- **`/justice/handling` read-only handled/open-request rows under Awaiting** is **shipped**: **`handling/page.tsx`** only; nested **Handled — open handling request** list under **Awaiting internal triage** identifies completed/handled approved actions with **`handling_requested_at`** and no **`handling_acknowledged_at`**; cases **excluded** from **Awaiting** and **Saved cases** **Needs attention**; **Open action plan** / **Update in chat** (session-first); workbench **Mark acknowledged** on nested rows — §2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**; **Awaiting**/**Acknowledged** membership, **Needs attention** rules, APIs, persistence, queue status, notes, disclaimers, and external-action copy on other surfaces **unchanged** — **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation (§2 **Handling workbench — read-only handled/open-request rows under Awaiting**).
- **`/justice/handling` Mark acknowledged on nested Handled — open handling request cards** is **shipped**: **`handling/page.tsx`** only; nested **`completedUnacknowledgedItems`** cards show **Mark acknowledged** using existing **`acknowledgeHandlingRequest`** + **`GET` + merge + `PATCH`** — same acknowledgement path as active **Awaiting internal triage** cards and §2 **Plan — acknowledge handling request in Surrenderless handling UI** / §2 **Chat AI intake — acknowledge handling request in Current action tracking** / §2 **Saved cases — acknowledge handling request from Needs attention**; nested helper updated; per-card **`inlineAck`** triage note; **internal tracking triage only** disclaimers unchanged — **does not** change **`isHandlingAwaitingTriageApprovedNextAction`**, **Needs attention**, approved-packet inbox, outcome/follow-up fields, plan/chat/cases/hub/packet, or routing — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**).
- **`/justice/handling` handled/open redirect note on nested Handled — open cards** is **shipped**: **`handling/page.tsx`** + **`approvedNextActionHandlingDisplay.tsx`**; **`HandlingWorkbenchCaseCard`** shows **`ApprovedNextActionHandlingHandledOpenTriageNote`** when **`handling_requested_at`**, no **`handling_acknowledged_at`**, and **`status === "completed"`** — active **Awaiting** cards use variant **`redirect`**; nested **Handled — open handling request** cards with workbench **Mark acknowledged** use variant **`inlineAck`** (§2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**); hub, packet, plan, chat, **All cases** still use **`redirect`** where applicable; **not** on **Acknowledged** cards; **Open action plan** / **Update in chat**, refetch on focus, session highlight, **Saved cases**, APIs, schema, persistence, and workbench membership **unchanged** — **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation (§2 **Handling workbench — handled/open redirect note on nested Handled — open cards**, **Approved next action — centralized handled/open handling triage note copy**).
- **`/justice/plan` and `/justice/chat-ai` View in handling workbench** is **shipped**: **`plan/page.tsx`**, **`chat-ai/page.tsx`**; when active case has **`handling_requested_at`**, read-only **View in handling workbench** → **`/justice/handling`** below handling UI on plan and in loaded-case **Current action tracking** on chat-ai; **not** on fresh intake — **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation (§2 **Handling workbench — active-case back-link from plan and chat-ai**).
- **`/justice/packet` View in handling workbench** is **shipped**: **`packet/page.tsx`** only; when hydrated **`approved_next_action.handling_requested_at`** is set, read-only **View in handling workbench** → **`/justice/handling`** below **`ApprovedNextActionHandlingRequestedReadOnly`**; **no** session change on click; **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation (§2 **Handling workbench — active-case back-link from packet**).
- **`/justice/packet` display-only derived handling queue status** is **shipped**: **`packet/page.tsx`** + **`approvedNextActionHandlingDisplay.tsx`**; when **`handling_requested_at`** is set, **`ApprovedNextActionHandlingQueueStatusReadOnly`** shows **Queue status: Awaiting internal triage.** or **Queue status: Acknowledged for internal tracking triage only** from existing **`handling_acknowledged_at`** only — **no** new persisted field; **no** packet approval/timeline/filings/tasks/workbench-link changes; **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation/persistence (§2 **Packet — display-only derived handling queue status**).
- **`/justice/packet` handled/open handling triage note in prepared-action area** is **shipped**: **`packet/page.tsx`** + **`approvedNextActionHandlingDisplay.tsx`**; when **`handling_requested_at`**, no **`handling_acknowledged_at`**, and **`status === "completed"`**, copy-only handled/open triage note after queue status — **`inlineAck`** when **`packetApproved`** (§2 **Packet — Surrenderless handling request and acknowledge on approved case packet surface (`/justice/packet`)**); legacy **`redirect`** when handling visible before packet approval — packet approval, timeline, filings, tasks, workbench link, queue status, hydration/**PATCH** **unchanged** (§2 **Packet — handled/open handling triage note in prepared-action area**, **Approved next action — centralized handled/open handling triage note copy**).
- **`/justice/packet` Surrenderless handling request and acknowledge on approved case packet surface** is **shipped**: **`packet/page.tsx`** only; in prepared-action handling area when **`packetApproved`** + **`approvedNextAction`**, **Request Surrenderless handling** + optional **`handling_request_note`** edit + **Mark acknowledged** via **`handleRequestSurrenderlessHandling`** / **`handleUpdateHandlingRequestNote`** / **`handleAcknowledgeHandlingRequest`** — existing **`handling_requested_at`**, **`handling_request_note`**, **`handling_acknowledged_at`** on **`client_state.approved_next_action`** only; persists via **`persistApprovedNextAction`** — mirrors plan — §2 **Plan — Surrenderless handling requested (`/justice/plan`)** and chat-ai — §2 **Chat AI intake — handling requested in Current action tracking (`/justice/chat-ai`)**; **View in handling workbench** retained; **internal triage tracking only** — **does not** auto-request on packet approval; **does not** change inner approved-next-action card, plan, chat-ai, cases, hub, handling workbench, membership predicates, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Packet — Surrenderless handling request and acknowledge on approved case packet surface (`/justice/packet`)**).
- **`/justice/chat-ai` display-only derived handling queue status** is **shipped**: **`chat-ai/page.tsx`** + **`approvedNextActionHandlingDisplay.tsx`**; in loaded-case **Current action tracking**, when **`handling_requested_at`** is set, **`ApprovedNextActionHandlingQueueStatusReadOnly`** shows **Queue status: Awaiting internal triage.** or **Queue status: Acknowledged for internal tracking triage only** from existing **`handling_acknowledged_at`** only — **no** queue line on fresh intake; **no** request/note/ack/hydration/PATCH/workbench-link changes; **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation/persistence (§2 **Chat AI intake — display-only derived handling queue status**).
- **`/justice/plan` display-only derived handling queue status** is **shipped**: **`plan/page.tsx`** + **`approvedNextActionHandlingDisplay.tsx`**; wherever plan handling UI appears with **`handling_requested_at`**, **`ApprovedNextActionHandlingQueueStatusReadOnly`** shows **Queue status: Awaiting internal triage.** or **Queue status: Acknowledged for internal tracking triage only** from existing **`handling_acknowledged_at`** only — **no** new persisted field; handled-action open-handling triage helper when completed + handling + unacknowledged — §2 **Plan — handled-action open-handling triage note in Surrenderless handling UI**; **no** request/note/hydration/PATCH/workbench-link/packet/preview/plan-CTA changes; **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation/persistence (§2 **Plan — display-only derived handling queue status**).
- **`/justice/plan` Mark acknowledged in Surrenderless handling UI** is **shipped**: **`plan/page.tsx`** only; both prepared-packet handling callouts show **Mark acknowledged** when **`handling_requested_at`** is set and **`handling_acknowledged_at`** is absent (below **View in handling workbench**); **`acknowledgeHandlingRequestInApprovedNextAction`** + **`persistApprovedNextAction`** (**existing** **`client_state`** **`GET` + merge + `PATCH`** + session sync); after success shows **Acknowledged {time}**, queue status **Acknowledged for internal tracking triage only**, button hidden, workbench link remains — **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation/persistence; request handling, Save request note, workbench link, prepared-packet approval, started/handled tracking, plan CTAs, chat/cases/workbench/Needs attention unchanged (§2 **Plan — acknowledge handling request in Surrenderless handling UI**).
- **`/justice/plan` handled-action open-handling triage note in Surrenderless handling UI** is **shipped**: **`plan/page.tsx`** + **`approvedNextActionHandlingDisplay.tsx`**; in **both** prepared-packet handling callouts, when **`handling_requested_at`**, no **`handling_acknowledged_at`**, and **`status === "completed"`**, shows copy-only **`inlineAck`** helper — **no** persistence/API/workbench-membership/queue-status/request/note/ack/hydrate/PATCH/workbench-link/disclaimer/external-action changes (§2 **Plan — handled-action open-handling triage note in Surrenderless handling UI**, **Approved next action — centralized handled/open handling triage note copy**).
- **`/justice` hub View in handling workbench** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only; when **Current case** snapshot has **`handling_requested_at`**, read-only **View in handling workbench** → **`/justice/handling`** below the **Current case** card — completes workbench access from hub, plan, chat-ai, and packet; **no** session change on click; **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation (§2 **Handling workbench — active-case back-link from hub**).
- **`/justice` hub display-only derived handling queue status** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** + **`approvedNextActionHandlingDisplay.tsx`**; when **Current case** snapshot has **`handling_requested_at`**, **`ApprovedNextActionHandlingQueueStatusReadOnly`** shows **Queue status: Awaiting internal triage.** or **Queue status: Acknowledged for internal tracking triage only** from session **`handling_acknowledged_at`** only — completes queue-status parity on hub, cases, workbench, packet, chat-ai, and plan; **no** new persisted field; **no** session/GET/request/ack/note/plan-preview-CTA/intake-card/hub-refresh/workbench-link changes; **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation/persistence (§2 **Justice hub — display-only derived handling queue status**).
- **`/justice` hub handled/open handling triage note on Current case** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** + **`approvedNextActionHandlingDisplay.tsx`**; when **`packetApproved`** + **`approvedNextAction`** and **`handling_requested_at`**, no **`handling_acknowledged_at`**, and **`status === "completed"`**, copy-only **`inlineAck`** note below queue status alongside hub **Mark acknowledged** — §2 **Justice hub — Surrenderless handling request and acknowledge on Current case (`/justice`)**; legacy **`redirect`** when handling visible before packet approval — queue status, workbench link, focus/visibility refresh **unchanged** (§2 **Justice hub — handled/open handling triage note on Current case**, **Approved next action — centralized handled/open handling triage note copy**).
- **`/justice` hub Surrenderless handling request and acknowledge on Current case** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only; on **Current case** approved-action area when **`packetApproved`** + **`approvedNextAction`**, **Request Surrenderless handling** + optional **`handling_request_note`** edit + **Mark acknowledged** via **`handleRequestSurrenderlessHandling`** / **`handleUpdateHandlingRequestNote`** / **`handleAcknowledgeHandlingRequest`** — existing **`handling_requested_at`**, **`handling_request_note`**, **`handling_acknowledged_at`** on **`client_state.approved_next_action`** only; persists via signed-in **`GET` + merge + `PATCH`** + session sync — mirrors plan — §2 **Plan — Surrenderless handling requested (`/justice/plan`)**, packet — §2 **Packet — Surrenderless handling request and acknowledge on approved case packet surface (`/justice/packet`)**, and chat-ai — §2 **Chat AI intake — handling requested in Current action tracking (`/justice/chat-ai`)**; makes **`/justice`** a handling entry point; **View in handling workbench** retained; **internal triage tracking only** — **does not** auto-request on packet approval; **does not** change plan, chat-ai, cases, packet inner card, handling workbench, membership predicates, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Justice hub — Surrenderless handling request and acknowledge on Current case (`/justice`)**).
- **`/justice/handling` current browser-session case highlight** is **shipped**: **`handling/page.tsx`** only; read-only **Current case in this browser** on the card whose id matches **`STORAGE_CASE_ID`** in **Awaiting internal triage** and **Acknowledged**; refreshes on load, cases loaded, window focus, and **`activateCaseInSession`** — **no** change to section membership, routing, filtering, persistence, acknowledge, notes, queue status, disclaimers, schema, API, worker, filing, timeline type, external queue, submission, contact, or automation (§2 **Handling workbench — current browser-session case highlight**).
- **`/justice/handling` refetch active cases on window focus** is **shipped**: **`handling/page.tsx`** only; shared **`loadCases`** wraps existing paginated **`GET /api/justice/cases`**; initial mount load **unchanged**; on window **focus** and tab **visibility** → **visible**, refetches active cases and replaces in-memory **`cases`** (same error handling as mount); **Awaiting** / **Handled — open handling request** / **Acknowledged** stay fresh after ack from plan or chat; workbench-local **Mark acknowledged** still immediate; session highlight, bucketing, UI copy, APIs, schema, persistence, and external-action behavior **unchanged** — **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation (§2 **Handling workbench — refetch active cases on window focus**).
- **`/justice/cases` handling requested visibility on All cases cards** is **shipped**: **`cases/page.tsx`** + **`approvedNextActionHandlingDisplay.tsx`**; **`CaseApprovedNextActionTracking`** on each **All cases** card shows **Next step: {label}** when set (§2 **Saved cases — approved next-step label on All cases cards**), **Approved next action: {status}**, **Surrenderless handling requested** and recorded time when **`client_state.approved_next_action.handling_requested_at`** exists (from existing list **GET** **`client_state`** via **`parseApprovedNextActionFromClientState`**); **display-only derived queue status** when requested — **Queue status: Awaiting internal triage.** without **`handling_acknowledged_at`**, **Queue status: Acknowledged for internal tracking triage only** when acknowledged (**no** new persisted field); cards without **`handling_requested_at`** show **no** queue status line; per-card **View in handling workbench** when handling requested — §2 **Saved cases — View in handling workbench on All cases handling-request cards**; **in-app tracking only** copy — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone yet; **Needs attention** handling-request cards also show **Queue status: Awaiting internal triage.** (§2 **Saved cases — Needs attention for handling requested**); **Open case**, **Archive**, **Save label**, **search**/**filter**/**sort** unchanged — **visibility only**; **no** schema, migration, new API route, queue worker, filing record, timeline **type**, browser automation, filing automation, external submission/contact/queue automation, or **`/api/submit-form`**; **nothing** filed, submitted, sent, queued externally, or contacted automatically (§2 **Saved cases — handling requested visibility on All cases cards**).
- **`/justice/cases` handled/open handling triage note on All cases cards** is **shipped**: **`cases/page.tsx`** + **`approvedNextActionHandlingDisplay.tsx`**; in **`CaseApprovedNextActionTracking`**, when **`handling_requested_at`**, no **`handling_acknowledged_at`**, and **`status === "completed"`**, **`ApprovedNextActionHandlingHandledOpenTriageNote`** variant **`inlineAck`** when **All cases** acknowledge is enabled — §2 **Saved cases — Mark acknowledged on All cases handling-request cards (`/justice/cases`)**; after queue status and before **View in handling workbench** — **Needs attention** rules, active handling **Mark acknowledged**, **Open case**, search/filter/sort, APIs, persistence, and workbench membership **unchanged** — **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation (§2 **Saved cases — handled/open handling triage note on All cases cards**, **Approved next action — centralized handled/open handling triage note copy**).
- **`/justice/cases` Mark acknowledged on All cases handling-request cards** is **shipped**: **`cases/page.tsx`** only; in **`CaseApprovedNextActionTracking`** on **All cases** cards when **`handling_requested_at`**, no **`handling_acknowledged_at`**, and **`status === "completed"`**, **Mark acknowledged** via reused **`acknowledgeHandlingRequest`** + **`GET` + merge + `PATCH`** + session sync — closes Saved cases post-handled acknowledgment loop after **Needs attention** record-handled — §2 **Saved cases — record action handled on Needs attention handling-request cards (`/justice/cases`)**; mirrors **Needs attention** ack — §2 **Saved cases — acknowledge handling request from Needs attention** and active-surface ack on plan/packet/hub/chat; **`inlineAck`** triage note + **`APPROVED_NEXT_ACTION_HANDLING_ACKNOWLEDGE_HELPER`**; **View in handling workbench** retained — **does not** change **Needs attention** membership, request/note/record-handled behavior, plan, packet, chat-ai, hub, handling workbench, or routing — **internal triage tracking only** — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Saved cases — Mark acknowledged on All cases handling-request cards (`/justice/cases`)**).
- **`/justice/cases` refetch active cases on window focus** is **shipped**: **`cases/page.tsx`** only; shared **`loadCases`** wraps existing **`GET /api/justice/cases`**; initial mount load **unchanged**; on window **focus** and tab **visibility** → **visible**, refetches with **`offset=0`** and **`limit = Math.max(visible case count, CASES_PAGE_LIMIT)`** so **Load more** length is preserved, replaces in-memory **`cases`** and **`has_more`** (same error handling as mount); **Needs attention**, **All cases** handling blocks, queue status, and handled/open notes stay fresh after **Mark acknowledged** from plan or chat; **Cases-local Mark acknowledged** still immediate; search/filter/sort, **Load more**, **Open case**, archive/labels, **Needs attention** rules, APIs, schema, persistence, and workbench membership **unchanged** — **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation (§2 **Saved cases — refetch active cases on window focus**).
- **Approved next action follow-up timing (user-paced labels)** is **shipped**: **`src/lib/justice/approvedNextActionFollowUp.tsx`**, **`cases/page.tsx`**, **`plan/page.tsx`**, **`packet/page.tsx`**; gentle labels from existing **`follow_up_at`** (**Follow-up date passed** / **Follow-up today** / **Upcoming follow-up**) on cases, plan, and packet tracking summaries — **not** overdue/deadline enforcement; dates are user-chosen tracking aids at the user’s pace — **no** schema/migration/new API/queue/browser/filing automation/timeline types; **not** auto-filed/submitted/sent/contacted; **tracking scaffold** toward §1 **prepare → approve → submit/queue/track**, not final manual workflow (§2 **Approved next action follow-up timing**).
- **`/justice/chat-ai` current action tracking** is **shipped**: **`chat-ai/page.tsx`**; when **Update in chat** loads a case, **Recap** can show **Current action tracking** from **`justice_approved_next_action_v1`** session fallback and, when signed in with a **UUID** case id, **`GET /api/justice/cases/[id]`** **`client_state`** hydrated via **`hydrateApprovedNextActionForDisplay`** (§2 **Chat AI intake — approved next action display hydrate**); surfaces next step, status, outcome note, follow-up timing, **Request Surrenderless handling** / **Surrenderless handling requested** (§2 **Chat AI intake — handling requested in Current action tracking**), **display-only derived queue status** when **`handling_requested_at`** is set (§2 **Chat AI intake — display-only derived handling queue status**), read-only handling when completed + handling requested (§2 **Chat AI intake — read-only handling UI when approved action is handled**), handled-action open-handling triage helper when completed + handling + unacknowledged (§2 **Chat AI intake — handled-action open-handling triage note in Current action tracking**), **Mark acknowledged** when handling requested and unacknowledged (§2 **Chat AI intake — acknowledge handling request in Current action tracking**), and funnel-aware footer work links in **Recap → Current action tracking** — §2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)** (replaces prior fixed **Action plan** / **Case packet** pair); when **`approved_next_action.follow_up_needed === true`**, shows **Mark follow-up handled** — clicking sets **`follow_up_needed`** false while **preserving** **`outcome_note`**, **`follow_up_at`**, **`handling_requested_at`**, **`status`**, **`href`**, **`label`**, and other **`approved_next_action`** fields via existing **`justice_cases.client_state`** and existing **`GET` + merge + `PATCH /api/justice/cases/[id]`** (shared helpers §2 **Approved next action shared state helpers**); local chat UI updates after clearing or handling request and **keeps** **Current action tracking** visible — **no** schema, migration, new API route, queue worker, automation, filing behavior, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically; fresh intake unchanged; **Continue to submission preview** unchanged.
- **Approved next action shared state helpers** is **shipped**: **`src/lib/justice/approvedNextActionState.ts`** centralizes parsing, status labels, follow-up clearing, cleared-follow-up **`client_state`** merging, **`resolveApprovedNextAction`**, **`hydrateApprovedNextActionForDisplay`**, **`mergeApprovedNextActionTrackingFields`**, **`mergeClientStateWithApprovedNextAction`**, **`parseJusticeCaseClientState`**, handling awaiting-triage membership (**`isHandlingAwaitingTriageApprovedNextAction`**), and approved-packet action without handling request (**`parseApprovedPacketActionWithoutHandlingRequest`**, **`isApprovedPacketActionWithoutHandlingRequest`** — §2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)**, §2 **Approval-to-action engine — approved packet action visibility on Saved cases Needs attention (`/justice/cases`)**, §2 **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**, §2 **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`)**, §2 **Approval-to-action engine — approved packet action workbench link on action plan prepared-packet callouts (`/justice/plan`)**, §2 **Approval-to-action engine — approved packet action workbench link on justice hub Current case (`/justice`)**, §2 **Approval-to-action engine — approved packet action workbench link on All cases cards (`/justice/cases`)**); **`/justice/chat-ai`**, **`/justice/cases`**, **`/justice/plan`**, **`/justice/packet`**, **`/justice`** hub, and **`/justice/handling`** wired to shared helpers; chat-ai **Current action tracking**, plan **both** prepared-packet approved emerald inner callouts, packet **Packet approved for next action** card, hub **Current case** area, and **All cases** **`CaseApprovedNextActionTracking`** use **`isApprovedPacketActionWithoutHandlingRequest`** / **`parseApprovedPacketActionWithoutHandlingRequest`** for approved-packet workbench nav (§2 **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**, §2 **Approval-to-action engine — approved packet action workbench link on action plan prepared-packet callouts (`/justice/plan`)**, §2 **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`)**, §2 **Approval-to-action engine — approved packet action workbench link on justice hub Current case (`/justice`)**, §2 **Approval-to-action engine — approved packet action workbench link on All cases cards (`/justice/cases`)**); plan/chat/packet use display hydrate + tracking-field preserve on persist so **`handling_requested_at`**, **`handling_request_note`**, and **`handling_acknowledged_at`** stay visible when session and server disagree; cases **Needs attention** approved-packet-action cards (§2 **Approval-to-action engine — approved packet action visibility on Saved cases Needs attention (`/justice/cases`)**) + handling-request attention + **Clear follow-up** + **All cases** **`CaseApprovedNextActionTracking`** — **Next step: {label}** (§2 **Saved cases — approved next-step label on All cases cards**), handling visibility (§2 **Saved cases — handling requested visibility on All cases cards**); **no** schema, migration, new API route, queue worker, automation, filing behavior, or timeline **type**; **nothing** filed, submitted, sent, queued, or contacted automatically (§2 **Approved next action shared state helpers**).
- **Prepared next action shared helpers (`preparedNextAction.ts`)** is **shipped** (PR **#402** / commit **`bc61aef`**): **`preparedNextAction.ts`** + **`packet/page.tsx`** import — **`pickPreparedNextAction`** and **`buildApprovedNextActionTarget`** extracted; **`/justice/packet`** uses shared helpers for prepared-next-action selection and **`approved_next_action`** target building — approval-to-action engine groundwork; preserves existing packet approval behavior and **`approved_next_action`** shape — **no** handling queue, timeline events, API, schema, DB, filing/submission/contact/upload/storage/automation (§2 **Approval-to-action engine — prepared next action shared helpers (`preparedNextAction.ts`)**).
- **`/justice/plan` prepared next action shared helper usage** is **shipped** (PR **#404** / commit **`037f892`**): **`plan/page.tsx`** only — removes page-local duplicate **`pickPreparedNextAction`**; imports shared **`pickPreparedNextAction`** from **`preparedNextAction.ts`** with plan-local **`href`** / **`buttonLabel`** wrapper — packet and plan share prepared next-action selection logic; preserves existing plan behavior — **no** packet approval behavior change, handling queue, timeline events, API, schema, DB, filing/submission/contact/upload/storage/automation (§2 **Approval-to-action engine — plan prepared next action shared helper usage (`/justice/plan`)**).
- **`/justice/handling` approved packet action visibility** is **shipped** (PR **#406** / commit **`b83acdd`**): **`handling/page.tsx`** + **`approvedNextActionState.ts`**; **`parseApprovedPacketActionWithoutHandlingRequest`** / **`isApprovedPacketActionWithoutHandlingRequest`** from existing **`client_state`** (**`prepared_packet_approved`**, **`approved_next_action`**, no **`handling_requested_at`**, **`status !== "completed"`**); **Approved packet actions** section above triage buckets with honest not-a-handling-request copy and session-first nav — **first real approval-to-action behavior slice**; user-controlled **started** / **completed** status writes on **`ApprovedPacketActionCaseCard`** only — §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**; **does not** auto-set **`handling_requested_at`**, change triage membership, packet approval persistence, or **Request Surrenderless handling** — **no** new DB field, timeline **type**, API route, worker, filing/submission/contact/queue/upload/storage/automation/schema (§2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)**).
- **`/justice/handling` approved packet action status writes** is **shipped**: **`handling/page.tsx`** only; on **`ApprovedPacketActionCaseCard`**, **Open approved step** when **`status === "approved"`** persists **`started`** + **`started_at`** then navigates; when **`status === "started"`**, **Record action handled for now** persists **`completed`** + **`completed_at`** with **Tracking only — not automatic filing or submission.** copy; optimistic **`setCases`** + **`writeSessionApprovedNextAction`** + signed-in **UUID** **`GET` + merge + `PATCH`** **`client_state`** (same pattern as **Mark acknowledged**); completed cards drop from **Approved packet actions** via existing predicate — **does not** set **`handling_requested_at`**, **`handling_acknowledged_at`**, outcome/follow-up fields, or change handling-request triage cards; outcome/follow-up read-only on handling-request triage cards — §2 **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`)** — **no** filing, submission, external queue, worker, timeline **type**, new API route, schema/migration, DB field, or persistence-model change beyond existing **`approved_next_action`** updates (§2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**).
- **`/justice/handling` outcome / follow-up read-only on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; on **`HandlingWorkbenchCaseCard`**, read-only truncated **`outcome_note`** (max **200**), **Follow-up needed**, and **`ApprovedNextActionFollowUpTimingLine`** when present on **`item.next`** from existing workbench list **`client_state`** when **`status !== "completed"`** — manual-handling context for triage; completed cards use outcome/follow-up write — §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**; complements §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)** / §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)** / §2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)** writes and §2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)** / §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)** browse display — **does not** change **`buildHandlingWorkbenchItems`**, **`isHandlingAwaitingTriageApprovedNextAction`**, **Needs attention**, acknowledge **`PATCH`**, or approved-packet inbox — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` outcome / follow-up write on completed handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), **Record outcome / follow-up** form when **`status === "completed"`** — persists **`outcome_note`**, **`follow_up_needed`**, **`follow_up_at`** via **`mergeApprovedNextActionTrackingFields`** + **`mergeClientStateWithApprovedNextAction`** + optimistic **`setCases`** + **`writeSessionApprovedNextAction`** + signed-in **UUID** **`GET` + merge + `PATCH`** (same pattern as **Mark acknowledged**); clear follow-up when **`follow_up_needed`** — §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**; complements §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)** / §2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)** / §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)** and read-only context — §2 **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`)** — **Tracking only — not automatic filing or submission.** — **does not** edit **`handling_request_note`**, change membership predicates, acknowledge **`PATCH`**, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` clear follow-up on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), **Mark follow-up handled** when **`follow_up_needed === true`** — **`clearFollowUpFromApprovedNextAction`** + **`mergeClientStateWithClearedFollowUp`** + optimistic **`setCases`** + **`writeSessionApprovedNextAction`** + signed-in **UUID** **`GET` + merge + `PATCH`**; preserves **`outcome_note`** and **`follow_up_at`**; clears from Saved cases **Needs attention** follow-up inbox — same semantics as §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)** / §2 **Saved cases — Needs attention from approved next action follow-up (`/justice/cases`)**; complements outcome write — §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)** and record handled — §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**; read-only context — §2 **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`)** — **does not** edit **`handling_request_note`**, change **`isHandlingAwaitingTriageApprovedNextAction`**, handling-request **Needs attention** builders, acknowledge **`PATCH`**, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` record action handled on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), **Record action handled for now** when **`status === "started"`** — persists **`completed`** + **`completed_at`** via **`mergeApprovedNextActionTrackingFields`** (preserves **`handling_requested_at`** and **`handling_acknowledged_at`**) + **`mergeClientStateWithApprovedNextAction`** + optimistic **`setCases`** + **`writeSessionApprovedNextAction`** + signed-in **UUID** **`GET` + merge + `PATCH`** — same pattern as **`ApprovedPacketActionCaseCard`** (§2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**) and §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**; **Tracking only — not automatic filing or submission.** — requires **Open approved step** started persist when **`status === "approved"`** — §2 **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`)**; unlocks outcome/follow-up write — §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**; clear follow-up when **`follow_up_needed`** — §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)** — **does not** change **`ApprovedPacketActionCaseCard`** membership rules, **`isHandlingAwaitingTriageApprovedNextAction`**, handling-request **Needs attention** builders, acknowledge **`PATCH`**, **`handling_request_note`** edit rules, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` Open approved step marks started on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), **Open approved step** when **`status === "approved"`** and internal **`href`** exists — persists **`started`** + **`started_at`** via **`mergeApprovedNextActionTrackingFields`** (preserves **`handling_requested_at`** and **`handling_acknowledged_at`**) + existing **`openApprovedPacketStep`** / **`persistApprovedPacketNextAction`** + optimistic **`setCases`** + **`writeSessionApprovedNextAction`** + signed-in **UUID** **`GET` + merge + `PATCH`**, then navigates — same started persist path as **`ApprovedPacketActionCaseCard`** (§2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**) and §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**; **Saving…** while persisting; helper when **`status === "approved"`**: **Tracking only — not automatic filing or submission.** — hidden once **`status`** is **`"started"`** or **`"completed"`** — §2 **Handling workbench — Open approved step visibility on handling-request triage cards (`/justice/handling`)**; unlocks record handled — §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**; outcome/follow-up write — §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**; clear follow-up when **`follow_up_needed`** — §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)** — **does not** change **`ApprovedPacketActionCaseCard`** membership rules, **`isHandlingAwaitingTriageApprovedNextAction`**, handling-request **Needs attention** builders, acknowledge **`PATCH`**, **`handling_request_note`** edit rules, or other nav semantics — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` Open approved step visibility on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), **Open approved step** and tracking helper shown only while **`status === "approved"`** and internal **`href`** exists — hidden after started/completed so operators use **Opened for next step.** + **Record action handled for now** — §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)** → outcome/follow-up write — §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)** → clear follow-up when **`follow_up_needed`** — §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**; started persist when approved unchanged — §2 **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`)**; other nav unchanged — **does not** change **`ApprovedPacketActionCaseCard`**, membership predicates, acknowledge **`PATCH`**, outcome/clear-follow-up writes — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — Open approved step visibility on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` manual-handling handoff context on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), read-only **Handled for now {time}** when **`status === "completed"`** and **`completed_at`** is set; packet-approved and draft-reviewed signals via approval-funnel checklist — §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)** (**single source** after dedupe — §2 **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`)**); **Awaiting** helpers direct handled-open cases to nested section / workbench **Mark acknowledged** — §2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)** — **does not** change **`isHandlingAwaitingTriageApprovedNextAction`**, **Needs attention**, approved-packet inbox, acknowledge **`PATCH`**, **`handling_request_note`** edit rules, outcome/follow-up fields, or plan/chat/cases/hub/packet routing — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` submission draft reviewed read-only on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; draft-reviewed signal via **`caseDraftReviewed`** / **`SUBMISSION_DRAFT_REVIEWED_TIMELINE_ID`** — now shown only inside approval-funnel checklist — §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)** (**single source** after dedupe — §2 **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`)**); standalone lines removed — read-only review context only, **not** filed or submitted — **does not** change membership, acknowledge **`PATCH`**, routing, or timeline writes — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — submission draft reviewed read-only on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` approval-funnel readiness summary on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), read-only **Manual review readiness (approval funnel)** checklist + rollup from already-loaded list **`GET /api/justice/cases`** — **`isBasicCaseInfoReadyForEscalation`**, **`caseDraftReviewed`**, **`prepared_packet_approved`** — evidence **intentionally omitted**; **single source** for packet-approved and draft-reviewed signals after dedupe — §2 **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`)** — read-only only, **not** filed or submitted — **does not** change membership, acknowledge **`PATCH`**, routing, or timeline writes — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` saved manual filing records read-only on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; after workbench load, **`GET /api/justice/filings?case_id={id}`** per handling-request case id only — **not** approved-packet-action cards; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), collapsed read-only **Saved manual filings** preview (**destination**, filed date, truncated confirmation/notes; **no `filing_url`**) or honest **not recorded yet** line when **Ready for manual review**; subcopy under **Open case packet** — writes remain on **`/justice/packet`** — **no** inline form, **no** filing **`POST` / `PATCH`** from workbench — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — saved manual filing records read-only on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` saved proof/evidence readiness on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; extends handling-request per-case fetch with **`GET /api/justice/evidence?case_id={id}`** (count only) alongside filings — **not** approved-packet-action cards; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), read-only **Evidence on case** line in approval-funnel checklist + honest **no saved proof** line when **Ready for manual review** — rollup unchanged; evidence writes on packet/evidence/chat — **no** inline form, **no** evidence **`POST` / `PATCH`** from workbench — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — saved proof/evidence readiness on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` case handoff summary on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), collapsed read-only **Case handoff summary** from already-loaded list data — company, category, story snippet, next step — after readiness/proof/filing blocks, before handling-request UI — full bundle on **`/justice/packet`** — **no** new API, copy/export, or external links — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — case handoff summary on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` internal operator note on handling-request triage cards** is **shipped**: **`handling/page.tsx`** + **`types.ts`** + **`approvedNextActionState.ts`**; optional **`handling_operator_note`** on **`client_state.approved_next_action`**; workbench-only **Internal operator note** read/edit on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**) — distinct from user **`handling_request_note`** — **`GET` + merge + `PATCH`** + session sync — **not** on plan/chat/packet/hub/cases — **no** new API route, DB migration, timeline write, worker, queue, filing/submission automation, or external action (§2 **Handling workbench — internal operator note on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` external manual action readiness on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), read-only **Ready for external manual action** line + collapsed **Suggested manual channels** when ready — **`computeJusticeDestinations`** label strings only — after case handoff summary, before handling-request UI — prep on packet/approved step — **no** new API, **`client_state`**, links, or automation — **does not** change APIs, schema, timeline writes, workers, queues, or external action (§2 **Handling workbench — external manual action readiness on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` filing confirmation tracking on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), read-only **Manual filing recorded** + **Confirmation on file** rollup from existing **`savedFilings`** — post-action nudges when **started**/**completed** — writes on **`/justice/packet`** — **no** new API, **`client_state`**, or inline form — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — filing confirmation tracking on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` manual action progress on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), collapsed read-only **Manual action progress** + plain-text **Next step** from existing lifecycle signals — after confirmation rollup, before handling-request UI — existing blocks unchanged — **no** new API, **`client_state`**, buttons, or links — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` follow-up carry-forward on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; page-level **Follow-up tracking** section from **`followUpHandlingItems`** — extends **Manual action progress** + **`deriveManualActionNextStep`** for open follow-up — existing **Mark follow-up handled** unchanged — **no** new API, **`client_state`**, or write paths — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — follow-up carry-forward on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` operator handoff queue tiers on Awaiting internal triage** is **shipped**: **`handling/page.tsx`** only; nested readiness-tier sublists under **Awaiting internal triage** from **`awaitingHandoffTiers`** — flat list when **`!filingsReady`** — **no** new API, **`client_state`**, badges, or card actions — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — operator handoff queue tiers on Awaiting internal triage (`/justice/handling`)**).
- **`/justice/handling` confirmation follow-up queue on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; page-level **Confirmation follow-up** section from **`postExternalConfirmationTiers`** before **Follow-up tracking** — **no** new API, **`client_state`**, badges, or card actions — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — confirmation follow-up queue on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` outcome closure queue on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; page-level **Outcome closure** section from **`outcomeClosureTiers`** after **Confirmation follow-up**, before **Follow-up tracking** — **no** new API, **`client_state`**, badges, or card actions — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — outcome closure queue on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` tracking complete queue on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; page-level **Tracking complete** section from **`trackingCompleteHandlingItems`** after **Follow-up tracking** — **`deriveManualActionNextStep`** parity — **no** new API, **`client_state`**, badges, or card actions — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**).
- **`/justice/cases` handling tracking status on All cases cards** is **shipped**: **`cases/page.tsx`** only; read-only **Handling tracking:** line on **All cases** **`CaseApprovedNextActionTracking`** when **`handling_requested_at`** is set — **`deriveCasesManualActionNextStep`** gate parity with workbench **`deriveManualActionNextStep`** — **`hasFilingRecord`** / **`hasConfirmationOnFile`** from existing filings progress fetch — subcopy **In-app tracking only — not filed or submitted.** — **View in handling workbench** unchanged — **no** new API, **`client_state`**, timeline write, worker, or queue — **does not** change **Needs attention**, acknowledge writes, workbench pages, or automation (§2 **Saved cases — handling tracking status on All cases cards (`/justice/cases`)**).
- **`/justice/plan` handling tracking status on prepared-packet handling callouts** is **shipped**: **`plan/page.tsx`** only; read-only **Handling tracking:** in **both** prepared-packet emerald handling callouts when **`handling_requested_at`** is set — **`derivePlanManualActionNextStep`** gate parity with workbench **`deriveManualActionNextStep`** and Saved cases **`deriveCasesManualActionNextStep`** — **`hasFilingRecord`** / **`hasConfirmationOnFile`** from existing **`filings`** fetch — subcopy **In-app tracking only — not filed or submitted.** — queue status, ack, workbench link, and write buttons unchanged — **no** new API, **`client_state`**, timeline write, worker, or queue — **does not** change **Needs attention**, acknowledge writes, hub, packet, chat-ai, cases, workbench, or automation (§2 **Plan — handling tracking status on prepared-packet handling callouts (`/justice/plan`)**).
- **`/justice` hub handling tracking status on Current case** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only; read-only **Handling tracking:** on **both** Current case handling paths when **`handling_requested_at`** is set — **`deriveHubManualActionNextStep`** gate parity with workbench **`deriveManualActionNextStep`**, Saved cases **`deriveCasesManualActionNextStep`**, and plan **`derivePlanManualActionNextStep`** — parallel evidence + filings fetch in **`refreshHubState`** — subcopy **In-app tracking only — not filed or submitted.** — queue status, ack, workbench link unchanged — **no** new API route, **`client_state`**, timeline write, worker, or queue — **does not** change request/note/ack writes, **Needs attention**, plan, packet, chat-ai, cases, workbench, or automation (§2 **Justice hub — handling tracking status on Current case (`/justice`)**).
- **`/justice/chat-ai` handling tracking status in Current action tracking** is **shipped**: **`chat-ai/page.tsx`** only; read-only **Handling tracking:** in **loaded-case** **Recap → Current action tracking** when **`handling_requested_at`** is set — **`deriveChatManualActionNextStep`** gate parity with workbench **`deriveManualActionNextStep`**, Saved cases (#470) **`deriveCasesManualActionNextStep`**, plan (#471) **`derivePlanManualActionNextStep`**, hub (#472) **`deriveHubManualActionNextStep`** — parallel evidence + filings fetch in **`loadSavedEvidencePreview`** — subcopy **In-app tracking only — not filed or submitted.** — queue status, ack, workbench link, and write buttons unchanged — **no** new API, **`client_state`**, timeline write, worker, or queue — **does not** change **Needs attention**, acknowledge writes, hub, plan, packet, cases, workbench, or automation (§2 **Chat AI intake — handling tracking status in Current action tracking (`/justice/chat-ai`)**).
- **`/justice/chat-ai` inline manual filing and confirmation capture in Current action tracking** is **shipped**: **`chat-ai/page.tsx`** only; compact **`ChatManualFilingCaptureForm`** in **Recap → Current action tracking** when signed in + **UUID** case and derived step is add-filing or add-confirmation — **`POST /api/justice/filings`** / **`PATCH /api/justice/filings/[id]`** — reuses **`loadSavedEvidencePreview`** refresh — honest **Record a manual action** copy — suppresses packet filing contextual link while form shown — tracking advances on-page without leaving chat — **no** schema, new API route, or **`client_state`** change — **does not** change packet **`JusticeFilingRecords`**, plan, hub, cases, workbench, or automation (§2 **Chat AI intake — inline manual filing and confirmation capture in Current action tracking (`/justice/chat-ai`)**).
- **`/justice/chat-ai` archive case on tracking complete in Current action tracking** is **shipped**: **`chat-ai/page.tsx`** only; **Archive case** when derived step is **Tracking complete for now.** — **`PATCH`** **`archived_at`** — **`clearLocalJusticeSession`** + **`/justice`** on success — honest archive copy — **no** schema, new API route, or handling-ladder change — **does not** change **`/justice/cases`** archive UI, workbench, or automation (§2 **Chat AI intake — archive case on tracking complete in Current action tracking (`/justice/chat-ai`)**).
- **`/justice/chat-ai` inline submission draft review in Active case checklist** is **shipped**: **`chat-ai/page.tsx`** only; **Review submission draft** block when signed in + **UUID** + draft not reviewed — **`buildSubmissionDraftPreview`** + preview-parity destination logic — checkbox **I reviewed the submission draft shown above.** — **`POST /api/justice/submission-draft-reviewed`** / **`appendSubmissionDraftReviewedOnce`** — unlocks inline prepared packet approval — optional **Open full submission preview** — **Continue** stays on chat for loaded signed-in **UUID** **Update in chat** saves — **no** schema or new API route — **does not** change **`/justice/preview`** UI (§2 **Chat AI intake — inline submission draft review in Active case checklist (`/justice/chat-ai`)**).
- **`/justice/chat-ai` inline prepared packet approval in Active case checklist** is **shipped**: **`chat-ai/page.tsx`** only; **Approve prepared packet** when signed in + **UUID** + draft reviewed + packet not approved — mirrors packet **`handleApprovePreparedPacket`** — **`PATCH`** **`client_state.prepared_packet_approved`** + **`approved_next_action`** — user stays on chat; **Recap → Current action tracking** can appear — **no** schema or new API route — **does not** change **`/justice/packet`** approve UI or workbench (§2 **Chat AI intake — inline prepared packet approval in Active case checklist (`/justice/chat-ai`)**).
- **`/justice/packet` handling tracking status in prepared-action handling UI** is **shipped**: **`packet/page.tsx`** only; read-only **Handling tracking:** in **both** prepared-action handling blocks when **`handling_requested_at`** is set — **`derivePacketManualActionNextStep`** gate parity with workbench **`deriveManualActionNextStep`**, Saved cases (#470) **`deriveCasesManualActionNextStep`**, plan (#471) **`derivePlanManualActionNextStep`**, hub (#472) **`deriveHubManualActionNextStep`**, chat-ai (#473) **`deriveChatManualActionNextStep`** — reuses existing evidence + filings fetches — subcopy **In-app tracking only — not filed or submitted.** — queue status, ack, workbench link, **`JusticeFilingRecords`** CRUD, and write buttons unchanged — **no** new API, **`client_state`**, timeline write, worker, or queue — **does not** change **Needs attention**, acknowledge writes, hub, plan, chat-ai, cases, workbench, or automation (§2 **Packet — handling tracking status in prepared-action handling UI (`/justice/packet`)**).
- **Handling tracking contextual action links on justice surfaces** is **shipped**: **`approvedNextActionHandlingDisplay.tsx`** + **`packet/page.tsx`**, **`plan/page.tsx`**, **`cases/page.tsx`**, **`chat-ai/page.tsx`**, **`JusticeHubWorkspaceBody.tsx`**; read-only navigation link below **Handling tracking:** when derived step maps to a known user action — **`resolveHandlingTrackingContextualLink`** / **`ApprovedNextActionHandlingTrackingContextualLink`** — filing/confirmation → **`/justice/packet#packet-filings`** (same-page on packet); outcome/ack/follow-up → **`/justice/plan`** when not redundant; suppresses link when **Mark acknowledged** already on-screen — cross-ref workbench **Confirmation follow-up**, **Outcome closure**, **Manual-action progress** — parity with handling tracking sync #470–#474 — **no** API, **`client_state`**, timeline write, worker, queue, gate derivation, or write-path changes — **does not** change workbench, filing CRUD, or automation (§2 **Approved next action — handling tracking contextual action links (justice surfaces)**).
- **Saved cases All-card session-aware handling tracking contextual links** is **shipped**: **`cases/page.tsx`** + **`approvedNextActionHandlingDisplay.tsx`**; **All cases** **Handling tracking** contextual links call **`activateCaseInSession(caseRow)`** before **`router.push(resolvedHref)`** via **`onNavigate`** — completes #475 multi-case carry-forward — hub/plan/packet/chat plain **`<Link>`** unchanged — **no** API, **`client_state`**, timeline write, worker, queue, resolver mapping, or write-path changes — **does not** change **Needs attention**, **Open case**, or automation (§2 **Saved cases — session-aware handling tracking contextual links on All cards (`/justice/cases`)**).
- **Saved cases Needs attention handling-request handling tracking** is **shipped**: **`cases/page.tsx`** only; **Needs attention → handling-request** cards show read-only **Handling tracking:** + session-aware contextual link after queue status — **`deriveCasesHandlingTrackingLine`** + **`openHandlingTrackingContextualLink`** (#476 pattern) — **`markAcknowledgedOnScreen={true}`** — workbench **Manual-action progress** / **Confirmation follow-up** / **Outcome closure** parity — **no** API, **`client_state`**, timeline write, worker, queue, membership, or write-path changes — **does not** change **All cases**, hub, plan, packet, chat, or automation (§2 **Saved cases — handling tracking on Needs attention handling-request cards (`/justice/cases`)**).
- **Saved cases approved-packet-action handling tracking** is **shipped**: **`cases/page.tsx`** only; **All cases** + **Needs attention → Approved packet action** cards without **`handling_requested_at`** show read-only **Handling tracking:** + session-aware contextual links — **`parseApprovedPacketActionWithoutHandlingRequest`** + **`deriveCasesHandlingTrackingLine`** + **`openHandlingTrackingContextualLink`** (#476) — cross-ref #477 handling-request parity — workbench **Manual-action progress** / **Confirmation follow-up** / **Outcome closure** parity — **no** queue status — **not** a Surrenderless handling request — **no** API, **`client_state`**, timeline write, worker, queue, membership, or write-path changes — **does not** change handling-request cards, hub, plan, packet, chat, or automation (§2 **Saved cases — handling tracking on approved-packet-action cards (`/justice/cases`)**).
- **`/justice/packet` approved-packet-action handling tracking** is **shipped**: **`packet/page.tsx`** only; **`PacketHandlingTrackingStatusReadOnly`** + contextual links on approved-packet-action path without **`handling_requested_at`** — **`derivePacketHandlingTrackingLine`** — cross-ref #478 #477 — workbench **Manual-action progress** / **Confirmation follow-up** / **Outcome closure** parity — **no** queue status — **not** a Surrenderless handling request — **no** API, **`client_state`**, timeline write, worker, queue, or write-path changes — **does not** change handling-request path, hub, plan, chat, cases, or automation (§2 **Packet — handling tracking on approved-packet-action prepared-action UI (`/justice/packet`)**).
- **`/justice/plan` approved-packet-action handling tracking** is **shipped**: **`plan/page.tsx`** only; **`PlanHandlingTrackingStatusReadOnly`** + contextual links on approved-packet-action path without **`handling_requested_at`** in **both** prepared-packet approved emerald inner callouts — **`derivePlanHandlingTrackingLine`** — cross-ref #479 #478 — workbench **Manual-action progress** / **Confirmation follow-up** / **Outcome closure** parity — **no** queue status — **not** a Surrenderless handling request — **no** API, **`client_state`**, timeline write, worker, queue, or write-path changes — **does not** change handling-request path, hub, packet, chat, cases, or automation (§2 **Plan — handling tracking on approved-packet-action prepared-packet callouts (`/justice/plan`)**).
- **`/justice` hub approved-packet-action handling tracking** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only; **`HubHandlingTrackingStatusReadOnly`** + contextual links on approved-packet-action path without **`handling_requested_at`** on **Current case** when **`packetApproved && approvedNextAction`** — **`deriveHubHandlingTrackingLine`** — cross-ref #479 #478 — workbench **Manual-action progress** / **Confirmation follow-up** / **Outcome closure** parity — **no** queue status — **not** a Surrenderless handling request — **no** API, **`client_state`**, timeline write, worker, queue, or write-path changes — **does not** change handling-request path, plan, packet, chat, cases, or automation (§2 **Justice hub — handling tracking on approved-packet-action Current case (`/justice`)**).
- **`/justice/chat-ai` approved-packet-action handling tracking** is **shipped**: **`chat-ai/page.tsx`** only; **`ChatHandlingTrackingStatusReadOnly`** + contextual links on approved-packet-action path without **`handling_requested_at`** in loaded-case **Recap → Current action tracking** — **`deriveChatHandlingTrackingLine`** — cross-ref #479 #478 — workbench **Manual-action progress** / **Confirmation follow-up** / **Outcome closure** parity — **no** queue status — **not** a Surrenderless handling request — **no** API, **`client_state`**, timeline write, worker, queue, or write-path changes — **does not** change handling-request path, hub, plan, packet, cases, or automation (§2 **Chat AI intake — handling tracking on approved-packet-action Current action tracking (`/justice/chat-ai`)**).
- **`/justice/handling` approval-funnel context dedupe on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), removes redundant standalone **Packet approved for next action** and **Submission draft reviewed** / **not reviewed yet** lines — **Manual review readiness (approval funnel)** checklist is **single source** for basics, draft reviewed, packet approved, and rollup — **does not** change behavior, persistence, membership, acknowledge **`PATCH`**, open-step/record-handled/outcome/clear-follow-up writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` started-at read-only on handling-request triage cards** is **shipped**: **`handling/page.tsx`** only; on **`HandlingWorkbenchCaseCard`** (**Awaiting**, nested **Handled — open handling request**, **Acknowledged**), read-only **Opened {formatted time}** when **`status === "started"`** and **`started_at`** is set — existing **`formatApprovedNextActionHandlingTimestamp`** + list **`GET`** **`client_state.approved_next_action.started_at`** only; alongside **Opened for next step.** — parity with **Handled for now {time}** when completed — §2 **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`)**; complements approved-packet action cards — §2 **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`)** and open-step started persist — §2 **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`)** — **does not** change behavior, persistence, membership, open-step/record-handled/outcome/clear-follow-up/acknowledge writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**).
- **`/justice/handling` started-at read-only on approved-packet action cards** is **shipped**: **`handling/page.tsx`** only; on **`ApprovedPacketActionCaseCard`** (**Approved packet actions** section), read-only **Opened {formatted time}** when **`status === "started"`** and **`started_at`** is set — existing **`formatApprovedNextActionHandlingTimestamp`** + list **`GET`** **`client_state.approved_next_action.started_at`** only; below **Opened for next step.** and before **Record action handled for now** — mirrors triage-card started-at — §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**; complements plan — §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, packet — §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, and chat-ai — §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)** — **does not** change behavior, persistence, membership, open-step/record-handled/outcome/clear-follow-up/acknowledge writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`)**).
- **`/justice/cases` approved packet action Needs attention visibility** is **shipped** (PR **#408** / commit **`cef391f`**): **`cases/page.tsx`** only; **`buildApprovedPacketActionAttentionItems`** from **`filteredSortedCases`** via **`parseApprovedPacketActionWithoutHandlingRequest`**; read-only **Approved packet action** **Needs attention** cards (blue border — distinct from handling-request triage cards) with **Next step**, **Approved next action** status, **`approved_at`**, honest not-a-handling-request copy, **Open case**, navigation-only **View on handling workbench**; **`hasNeedsAttentionContent`** includes approved-packet-action list; rendered before handling-request cards — **does not** auto-set **`handling_requested_at`**, change **`buildHandlingRequestedAttentionItems`** / handling-request triage cards, queue status, or **Mark acknowledged** — **no** new DB field, timeline **type**, API route, worker, filing/submission/contact/queue/upload/storage/automation/schema (§2 **Approval-to-action engine — approved packet action visibility on Saved cases Needs attention (`/justice/cases`)**).
- **`/justice/cases` record action handled on Needs attention approved-packet-action cards** is **shipped**: **`cases/page.tsx`** only; on **Needs attention → Approved packet action** cards when **`status === "started"`**, **Record action handled for now** via **`markApprovedPacketActionHandled`** — sets **`status: "completed"`** + **`completed_at`** (ISO), preserves other **`approved_next_action`** fields, **`mergeApprovedNextActionTrackingFields`** + **`mergeClientStateWithApprovedNextAction`**, optimistic **`cases`** update, signed-in **UUID** **`GET` + merge + `PATCH`** **`client_state`**, session sync when active browser case — mirrors plan — §2 **Plan — prepared packet approval surfaced (`/justice/plan`)**, packet — §2 **Packet — record action handled on approved next action card (`/justice/packet`)**, chat-ai — §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, and workbench — §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**; after status / optional **Opened {formatted time}** with **Tracking only — not automatic filing or submission.** — card leaves **Needs attention** on **`completed`** (existing predicate) — **does not** change **All cases**, handling-request record-handled — §2 **Saved cases — record action handled on Needs attention handling-request cards (`/justice/cases`)**, membership predicates, plan, packet, chat-ai, hub, handling workbench, or routing — **in-app tracking only** — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Saved cases — record action handled on Needs attention approved-packet-action cards (`/justice/cases`)**).
- **`/justice/cases` record action handled on Needs attention handling-request cards** is **shipped**: **`cases/page.tsx`** only; on **Needs attention → handling-request** cards when **`status === "started"`**, **Record action handled for now** via reused **`markApprovedPacketActionHandled`** — sets **`status: "completed"`** + **`completed_at`** (ISO), preserves **`handling_requested_at`**, **`handling_request_note`**, **`handling_acknowledged_at`**, and other **`approved_next_action`** fields, **`GET` + merge + `PATCH`** **`client_state`**, session sync when active browser case — mirrors approved-packet **Needs attention** record-handled — §2 **Saved cases — record action handled on Needs attention approved-packet-action cards (`/justice/cases`)** and workbench handling-request triage cards — §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**; optional **Opened {formatted time}** + **Tracking only — not automatic filing or submission.** — card leaves **Needs attention** on **`completed`** via existing **`isHandlingAwaitingTriageApprovedNextAction`** predicate — **does not** change **Mark acknowledged**, request handling, **All cases**, approved-packet cards, membership predicates, plan, packet, chat-ai, hub, handling workbench, or routing — **in-app tracking only** — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Saved cases — record action handled on Needs attention handling-request cards (`/justice/cases`)**).
- **`/justice/chat-ai` approved packet action workbench link in Current action tracking** is **shipped** (PR **#410** / commit **`2090248`**): **`chat-ai/page.tsx`** only; **`isApprovedPacketActionWithoutHandlingRequest`** on session **`preparedPacketApproved`** + hydrated **`approvedNextAction`**; read-only honest copy (**approved case packet and next in-app step — not a Surrenderless handling request**) + navigation-only **View on handling workbench** → **`/justice/handling`** in **Recap → Current action tracking** (**Update in chat** only) — **does not** activate case on link click, auto-set **`handling_requested_at`**, change **Request Surrenderless handling**, queue status, or **Mark acknowledged** — **no** new DB field, timeline **type**, API route, worker, filing/submission/contact/queue/upload/storage/automation/schema (§2 **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**).
- **`/justice/packet` approved packet action workbench link on approved card** is **shipped** (PR **#412** / commit **`40e56f9`**): **`packet/page.tsx`** only; **`isApprovedPacketActionWithoutHandlingRequest`** on **`packetApproved`** + hydrated **`approvedNextAction`**; read-only honest copy (**approved case packet and next in-app step — not a Surrenderless handling request**; request handling from action plan) + navigation-only **View on handling workbench** → **`/justice/handling`** inside **`packetApproved`** emerald card — **does not** activate case on link click, auto-set **`handling_requested_at`**, change handling-request **View in handling workbench**, queue status, or **Mark acknowledged** — **no** new DB field, timeline **type**, API route, worker, filing/submission/contact/queue/upload/storage/automation/schema (§2 **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`)**).
- **`/justice/plan` approved packet action workbench link on prepared-packet callouts** is **shipped** (PR **#414** / commit **`33e8fad`**): **`plan/page.tsx`** only; **`isApprovedPacketActionWithoutHandlingRequest`** on **`preparedPacketApproved`** + hydrated **`approvedNextAction`** in **both** prepared-packet approved emerald inner cards (**`showPostDraftReviewCallout`** inner block and standalone **`preparedPacketApproved`** block); read-only honest copy (**approved case packet and next in-app step — not a Surrenderless handling request**; request Surrenderless handling when you want internal triage tracking) + navigation-only **View on handling workbench** → **`/justice/handling`** — **does not** activate case on link click, auto-set **`handling_requested_at`**, change handling-request **View in handling workbench**, queue status, or **Mark acknowledged** — **no** new DB field, timeline **type**, API route, worker, filing/submission/contact/queue/upload/storage/automation/schema (§2 **Approval-to-action engine — approved packet action workbench link on action plan prepared-packet callouts (`/justice/plan`)**).
- **`/justice` hub approved packet action workbench link on Current case** is **shipped** (PR **#416** / commit **`e046e4f`**): **`JusticeHubWorkspaceBody.tsx`** only; **`isApprovedPacketActionWithoutHandlingRequest`** on session **`packetApproved`** + **`approvedNextAction`** in **Current case** snapshot; read-only honest copy (**approved case packet and next in-app step — not a Surrenderless handling request**; request Surrenderless handling from your action plan when you want internal triage tracking) + navigation-only **View on handling workbench** → **`/justice/handling`** below **Current case** card (**outside** primary card link) — **does not** activate case on link click, auto-set **`handling_requested_at`**, change handling-request **View in handling workbench**, queue status, or **Mark acknowledged** — **no** new DB field, timeline **type**, API route, worker, filing/submission/contact/queue/upload/storage/automation/schema (§2 **Approval-to-action engine — approved packet action workbench link on justice hub Current case (`/justice`)**).
- **`/justice` hub approved next action display hydrate on Current case** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only; on **`refreshHubState`**, signed-in **UUID** **`STORAGE_CASE_ID`** **`GET /api/justice/cases/[id]`** + **`hydrateApprovedNextActionForDisplay`** merges server **`client_state.approved_next_action`** with session; updates **Current case** **Next step** / **Approved next action** status / handling read-only fields; **`writeSessionApprovedNextAction`** sync — lets **Started** / **Handled** from handling/plan/chat/packet appear on hub without plan/chat visit first; session fallback when **`GET`** fails — **does not** add buttons, **`PATCH`**, inbox completed cards, or new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Justice hub — approved next action display hydrate on Current case (`/justice`)**).
- **`/justice` hub handled-for-now timestamp on Current case** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only; on **Current case** card, when hydrated **`snapshot.approvedNextAction.status === "completed"`** and **`completed_at`** is set, read-only **Handled for now {formatted time}** via **`formatApprovedNextActionHandlingTimestamp`** — continuity display from already-hydrated **`approvedNextAction`** only; **does not** add buttons, **`PATCH`**, new **`GET`**, timeline events, or inbox changes — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Justice hub — handled-for-now timestamp on Current case (`/justice`)**).
- **`/justice` hub started-at read-only on Current case** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only; on **Current case** card (inside primary **`Link`**), when hydrated **`snapshot.approvedNextAction.status === "started"`** and **`started_at`** is set, read-only **Opened {formatted time}** via **`formatApprovedNextActionHandlingTimestamp`** — continuity display from already-hydrated **`approvedNextAction`** only; after **Approved next action: {status}** and before **Handled for now** / outcome / handling blocks — parity with **Handled for now {time}** when completed — §2 **Justice hub — handled-for-now timestamp on Current case (`/justice`)**; complements plan — §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, packet — §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, chat-ai — §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, handling triage — §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**, handling approved-packet cards — §2 **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`)**, and cases All cases — §2 **Saved cases — started-at read-only on All cases cards (`/justice/cases`)** — **does not** change primary CTA, checklist, handling blocks, workbench links, **Needs attention** templates, membership, write paths, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Justice hub — started-at read-only on Current case (`/justice`)**).
- **`/justice` hub outcome / follow-up read-only on Current case** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only; on **Current case** card (inside primary **`Link`**), read-only truncated **`outcome_note`** (max **200**), **Follow-up needed**, and **`ApprovedNextActionFollowUpTimingLine`** when present on already-hydrated **`snapshot.approvedNextAction`** from existing **`refreshHubState`** / server **`client_state`** — continuity display only; complements §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)** and §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**; **does not** add forms, buttons, **`PATCH`**, new **`GET`**, or change **Needs attention** builders, handling triage, **Clear follow-up**, or inbox rules — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)**).
- **`/justice/cases` approved packet action workbench link on All cases cards** is **shipped**: **`cases/page.tsx`** **`CaseApprovedNextActionTracking`** only; **`parseApprovedPacketActionWithoutHandlingRequest(clientState)`** from existing list **`GET`** **`client_state`**; read-only honest copy (**approved case packet and next in-app step — not a Surrenderless handling request**; request handling from your action plan when you want internal triage tracking) + navigation-only **View on handling workbench** → **`/justice/handling`** after handling-request UI and before follow-up/disclaimer — **does not** activate case on link click, auto-set **`handling_requested_at`**, change handling-request **View in handling workbench**, queue status, or **Mark acknowledged** — **no** new API route, DB field, timeline **type**, worker, filing/submission/contact/queue/upload/storage/automation/schema (§2 **Approval-to-action engine — approved packet action workbench link on All cases cards (`/justice/cases`)**).
- **`/justice/cases` handled-for-now timestamp on All cases cards** is **shipped**: **`cases/page.tsx`** **`CaseApprovedNextActionTracking`** only; when **`approved_next_action.status === "completed"`** and **`completed_at`** is set on list **`GET`** **`client_state`**, read-only **Handled for now {formatted time}** via **`formatApprovedNextActionHandlingTimestamp`** — browse/history display only; **does not** add buttons, **`PATCH`**, timeline events, or show completed rows in **Needs attention** / workbench approved-packet inbox — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**).
- **`/justice/cases` started-at read-only on All cases cards** is **shipped**: **`cases/page.tsx`** **`CaseApprovedNextActionTracking`** only; when **`approved_next_action.status === "started"`** and **`started_at`** is set on list **`GET`** **`client_state`**, read-only **Opened {formatted time}** via **`formatApprovedNextActionHandlingTimestamp`** — browse/history display only; after **Approved next action: {status}** and before outcome/handling blocks — parity with **Handled for now {time}** when completed — §2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**; complements plan — §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, packet — §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, chat-ai — §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, handling triage — §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**, and handling approved-packet cards — §2 **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`)** — **does not** change **Needs attention** card templates, builders, membership, write paths, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Saved cases — started-at read-only on All cases cards (`/justice/cases`)**).
- **`/justice/cases` outcome note on All cases cards** is **shipped**: **`cases/page.tsx`** **`CaseApprovedNextActionTracking`** only; when list **`GET`** **`client_state.approved_next_action.outcome_note`** is set, read-only truncated outcome text on **All cases** browse cards — continuity display from existing list **`client_state`** only; **does not** change **Needs attention** builders, **Clear follow-up**, or inbox rules — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**).
- **`/justice/chat-ai` outcome / follow-up tracking in Current action tracking** is **shipped**: **`chat-ai/page.tsx`** only; when **`approvedNextAction.status === "completed"`**, **Recap → Current action tracking** shows **Record outcome / follow-up** form with **Save tracking note** — chat-first write on already-hydrated **`approvedNextAction`**; persists existing **`outcome_note`**, **`follow_up_needed`**, **`follow_up_at`** via **`GET` + merge + `PATCH`** + session sync; duplicate read-only outcome/follow-up lines below handling UI hidden when completed — §2 **Chat AI intake — outcome / follow-up read-only dedupe in Current action tracking (`/justice/chat-ai`)**; **Mark follow-up handled** unchanged — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**).
- **`/justice/chat-ai` outcome / follow-up read-only dedupe in Current action tracking** is **shipped**: **`chat-ai/page.tsx`** only; in **Recap → Current action tracking**, when **`approvedNextAction.status === "completed"`**, hides duplicate read-only **`outcome_note`**, **Follow-up needed**, and follow-up timing below handling/workbench UI — **Record outcome / follow-up** form is the single outcome/follow-up display in **Recap** for completed actions; non-completed statuses keep read-only lines when data exists — **does not** change **Active case** banner, disclaimer, **Mark follow-up handled**, work links, or **`PATCH`** semantics — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Chat AI intake — outcome / follow-up read-only dedupe in Current action tracking (`/justice/chat-ai`)**).
- **`/justice/chat-ai` Active case banner outcome / follow-up read-only hints** is **shipped**: **`chat-ai/page.tsx`** only; when **`isUpdatingExistingCase`** and hydrated **`approvedNextAction`** has outcome/follow-up fields, **Active case** banner shows compact read-only truncated **`outcome_note`** (max **200**), **Follow-up needed**, and **`ApprovedNextActionFollowUpTimingLine`** — continuity display from already-hydrated **`approvedNextAction`** only; complements §2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)**, §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**, and §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**; **does not** add forms, buttons, **`PATCH`**, new **`GET`**, or change **Recap → Current action tracking**, **Needs attention**, handling triage, **Clear follow-up**, or inbox rules — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Chat AI intake — active case banner outcome / follow-up read-only hints (Update in chat) (`/justice/chat-ai`)**).
- **`/justice/chat-ai` handled-for-now timestamp in Current action tracking** is **shipped**: **`chat-ai/page.tsx`** only; in **Recap → Current action tracking** completed block, when hydrated **`approvedNextAction.completed_at`** is set, read-only **Handled for now {formatted time}** via **`formatApprovedNextActionHandlingTimestamp`** — continuity display from already-hydrated **`approvedNextAction`** only; **does not** add buttons, **`PATCH`**, timeline events, or inbox changes — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Chat AI intake — handled-for-now timestamp in Current action tracking (`/justice/chat-ai`)**).
- **`/justice/chat-ai` started-at read-only in Current action tracking** is **shipped**: **`chat-ai/page.tsx`** only; in **Recap → Current action tracking** started block, read-only **Opened {formatted time}** when **`status === "started"`** and **`started_at`** is set — existing **`formatApprovedNextActionHandlingTimestamp`** + hydrated **`approvedNextAction`** only; alongside **Opened for next step.** — parity with **Handled for now {time}** when completed — §2 **Chat AI intake — handled-for-now timestamp in Current action tracking (`/justice/chat-ai`)**; complements handling workbench started-at — §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)** — **does not** change behavior, persistence, membership, open-step/record-handled/outcome/clear-follow-up/acknowledge writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**).
- **`/justice/packet` handled-for-now timestamp on approved next action card** is **shipped**: **`packet/page.tsx`** only; in **`packetApproved`** inner emerald card, when hydrated **`approvedNextAction.status === "completed"`** and **`completed_at`** is set, read-only **Handled for now {formatted time}** via **`formatApprovedNextActionHandlingTimestamp`** — continuity display from already-hydrated **`approvedNextAction`** only; **does not** add buttons, **`PATCH`**, new **`GET`**, timeline events, or inbox changes — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Packet — handled-for-now timestamp on approved next action card (`/justice/packet`)**).
- **`/justice/packet` started-at read-only on approved next action card** is **shipped**: **`packet/page.tsx`** only; in **`packetApproved`** inner emerald approved-next-action card, read-only **Opened {formatted time}** when **`status === "started"`** and **`started_at`** is set — existing **`formatApprovedNextActionHandlingTimestamp`** + hydrated **`approvedNextAction`** only; below **Opened for next step.** and before **Record action handled for now** / **Open {label}** / completed tracking blocks — parity with **Handled for now {time}** when completed — §2 **Packet — handled-for-now timestamp on approved next action card (`/justice/packet`)**; complements plan — §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, chat-ai — §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, and handling workbench — §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)** — **does not** change behavior, persistence, membership, open-step/record-handled/outcome/clear-follow-up/acknowledge writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**).
- **`/justice/packet` record action handled on approved next action card** is **shipped**: **`packet/page.tsx`** only; in **`packetApproved`** inner emerald approved-next-action card **`approvedNextActionStarted`** block when **`status === "started"`**, **Record action handled for now** via **`handleMarkApprovedNextActionHandled`** — sets **`status: "completed"`** + **`completed_at`** (ISO), preserves other **`approved_next_action`** fields, persists via existing **`persistApprovedNextAction`** (**`mergeApprovedNextActionTrackingFields`**, session sync, signed-in **UUID** **`GET` + merge + `PATCH`** **`client_state`**) — mirrors plan — §2 **Plan — prepared packet approval surfaced (`/justice/plan`)**, chat-ai — §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, and handling workbench — §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)** / §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**; placed after **Opened {formatted time}** with **Tracking only — not automatic filing or submission.** subcopy — **does not** change **`handleApprovedNextActionOpen`**, outcome/follow-up form, clear follow-up, handling blocks, workbench links, approval checkpoint, plan, chat-ai, cases, hub, handling workbench, membership predicates, or routing — **in-app tracking only** — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Packet — record action handled on approved next action card (`/justice/packet`)**).
- **`/justice/packet` outcome / follow-up tracking on approved next action card** is **shipped**: **`packet/page.tsx`** only; in **`packetApproved`** inner emerald **Next action recorded as handled** card when **`approvedNextAction.status === "completed"`**, **Record outcome / follow-up** form with **Save tracking note** after **`ApprovedNextActionTrackingSummary`** — same **`outcome_note`**, **`follow_up_needed`**, **`follow_up_at`** semantics as §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)** and §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)**; persists via existing **`mergeApprovedNextActionTrackingFields`** + **`GET` + merge + `PATCH`** + **`writeSessionApprovedNextAction`**; clear follow-up when **`follow_up_needed`** — §2 **Packet — clear follow-up on approved next action card (`/justice/packet`)**; hub/cases/handling read-only continuity unchanged — **does not** change **`status`**, **`completed_at`**, handling fields, inbox rules, workbench, chat, plan, routing, or shared components — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)**).
- **`/justice/packet` clear follow-up on approved next action card** is **shipped**: **`packet/page.tsx`** only; in **`packetApproved`** inner emerald **Next action recorded as handled** card, **Mark follow-up handled** when **`status === "completed"`** and **`follow_up_needed === true`** — **`clearFollowUpFromApprovedNextAction`** + **`persistApprovedNextAction`** (**`GET` + merge + `PATCH`** **`client_state`** + session sync); preserves **`outcome_note`** and **`follow_up_at`**; clears **`follow_up_needed`** only — removes case from **Saved cases** **Needs attention** follow-up list; mirrors plan / chat-ai / cases / handling workbench clear-follow-up — **does not** dedupe summary/form; **does not** change plan, chat-ai, cases, handling workbench, membership predicates, acknowledge **`PATCH`**, open-step/record-handled writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Packet — clear follow-up on approved next action card (`/justice/packet`)**).
- **`/justice/plan` handled-for-now timestamp on prepared-packet approved callouts** is **shipped**: **`plan/page.tsx`** only; in **both** prepared-packet approved inner emerald callouts, when hydrated **`approvedNextAction.status === "completed"`** and **`completed_at`** is set, read-only **Handled for now {formatted time}** via **`formatApprovedNextActionHandlingTimestamp`** — continuity display from already-hydrated **`approvedNextAction`** only; **does not** add buttons, **`PATCH`**, new **`GET`**, timeline events, or inbox changes — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Plan — handled-for-now timestamp on prepared-packet approved callouts (`/justice/plan`)**).
- **`/justice/plan` started-at read-only on prepared-packet callouts** is **shipped**: **`plan/page.tsx`** only; in **both** prepared-packet approved inner emerald callouts, read-only **Opened {formatted time}** when **`status === "started"`** and **`started_at`** is set — existing **`formatApprovedNextActionHandlingTimestamp`** + hydrated **`approvedNextAction`** only; below **Opened for next step.** and before **Record action handled for now** — parity with **Handled for now {time}** when completed — §2 **Plan — handled-for-now timestamp on prepared-packet approved callouts (`/justice/plan`)**; complements chat-ai — §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)** and handling workbench — §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)** — **does not** change behavior, persistence, membership, open-step/record-handled/outcome/clear-follow-up/acknowledge writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**).
- **`/justice/plan` clear follow-up on prepared-packet callouts** is **shipped**: **`plan/page.tsx`** only; in **both** prepared-packet approved inner emerald callouts, **Mark follow-up handled** when **`status === "completed"`** and **`follow_up_needed === true`** — **`clearFollowUpFromApprovedNextAction`** + **`persistApprovedNextAction`** (**`GET` + merge + `PATCH`** **`client_state`** + session sync); preserves **`outcome_note`** and **`follow_up_at`**; clears **`follow_up_needed`** only — removes case from **Saved cases** **Needs attention** follow-up list; mirrors chat-ai / cases / handling workbench clear-follow-up — **does not** change handling workbench, membership predicates, acknowledge **`PATCH`**, open-step/record-handled writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Plan — clear follow-up on prepared-packet callouts (`/justice/plan`)**).
- **`/justice/chat-ai` approved next action display hydrate** is **shipped**: **`chat-ai/page.tsx`** only; **`hydrateApprovedNextActionForDisplay`** on session seed and after server **`GET`** aligns with **`/justice/plan`** and **`/justice/packet`** (**Handled** / **Started** / **Approved**; **`handling_requested_at`**, **`handling_request_note`**, **`handling_acknowledged_at`**, and **`follow_up_needed`** not lost when session and server disagree); **`mergeApprovedNextActionTrackingFields`** on handling request, note update, and clear-follow-up persists; **`writeSessionApprovedNextAction`** after hydrate keeps **`justice_approved_next_action_v1`** aligned; **Current action tracking** still appears; **Mark follow-up handled** still works and **keeps** the tracking box visible — **no** schema, migration, new API route, queue worker, automation, filing behavior, or timeline **type**; **nothing** filed, submitted, sent, queued externally, or contacted automatically (§2 **Chat AI intake — approved next action display hydrate**).
- **`/justice/chat-ai` Request Surrenderless handling in Current action tracking** is **shipped**: **`chat-ai/page.tsx`** only; when **`approved_next_action`** exists and **`handling_requested_at`** is missing, **Recap** **Current action tracking** shows **Request Surrenderless handling**; click records **`handling_requested_at`** on existing **`approved_next_action`** in **`client_state`** (**`GET` + merge + `PATCH`** for signed-in **UUID** cases + **`justice_approved_next_action_v1`** session sync — same honest tracking semantics as **`/justice/plan`** §2 **Plan — Surrenderless handling requested**); state **survives refresh**; after request, shows **Surrenderless handling requested** with **in-app tracking only** copy — Surrenderless has **not** filed, submitted, sent, **queued externally**, or contacted anyone yet; **Current action tracking** and **Mark follow-up handled** otherwise **unchanged** — **no** schema, migration, new API route, queue worker, filing record, timeline **type**, browser automation, filing automation, or **`/api/submit-form`**; **nothing** filed, submitted, sent, queued externally, or contacted automatically (§2 **Chat AI intake — handling requested in Current action tracking**).
- **`/justice/chat-ai` Mark acknowledged in Current action tracking** is **shipped**: **`chat-ai/page.tsx`** only; loaded-case **Recap → Current action tracking** shows **Mark acknowledged** when **`handling_requested_at`** is set and **`handling_acknowledged_at`** is absent; existing **`acknowledgeHandlingRequestInApprovedNextAction`** + **`GET` + merge + `PATCH`** **`client_state`** + session sync; after success shows **Acknowledged {time} — internal tracking triage only**, queue status **Acknowledged for internal tracking triage only**, button hidden, **View in handling workbench** remains; **not** on fresh intake — **no** schema/API/worker/filing/timeline/external queue/submission/contact/automation/persistence; request handling, note save/clear, follow-up handled, commit/hydrate, plan/packet links, Needs attention/workbench membership/filtering unchanged (§2 **Chat AI intake — acknowledge handling request in Current action tracking**).
- **`/justice/chat-ai` handled-action open-handling triage note in Current action tracking** is **shipped**: **`chat-ai/page.tsx`** + **`approvedNextActionHandlingDisplay.tsx`**; when **`handling_requested_at`**, no **`handling_acknowledged_at`**, and **`status === "completed"`**, shows copy-only **`inlineAck`** helper — **no** persistence/API/workbench-membership/queue-status/request/note/ack/hydrate/PATCH/workbench-link/disclaimer/external-action changes; **not** on fresh intake (§2 **Chat AI intake — handled-action open-handling triage note in Current action tracking**, **Approved next action — centralized handled/open handling triage note copy**).
- **`/justice/chat-ai` read-only handling UI when approved action is handled** is **shipped**: **`chat-ai/page.tsx`** only; when **`handling_requested_at`** and **`status === "completed"`**, **Current action tracking** uses **`ApprovedNextActionHandlingRequestedReadOnly`** (no **Save request note** on handled actions); active non-completed handling keeps editable **`ApprovedNextActionHandlingRequestBlock`**; no handling + not completed keeps **Request Surrenderless handling** — **no** change to triage helper, queue status, workbench link, **Mark acknowledged**, hydrate/PATCH, disclaimers, fresh intake, workbench/Needs attention, or APIs (§2 **Chat AI intake — read-only handling UI when approved action is handled**).
- **`/justice/packet` Update in chat breadcrumb return** is **shipped**: **`packet/page.tsx`** only — breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** in main and no-**`caseId`** empty-state rows (Preview/Evidence styling); supports **Packet → Update in chat → Chat AI** continuity — **no** new API; packet approval checkpoint, emerald explainer/readiness bridge, filing records, evidence list, approved-next handling UI, APIs, session keys, schema, routes, **Back to action plan** href, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**).
- **`/justice/evidence` Update in chat breadcrumb return** is **shipped**: **`evidence/page.tsx`** only — breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (Preview/Plan styling); supports **Chat AI → Organize evidence → Evidence → Update in chat** loop — **no** new API; evidence CRUD, **`GET`/`POST`/`PATCH`/`DELETE`**, form fields, empty/signed-out states, **Back to action plan**, **Saved cases**, **Home**, chat-ai proof panel, plan/preview proof cards, schema, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**).
- **`/justice/cases` Update in chat breadcrumb return** is **shipped** (PR **#376** / commit **`eede473`**): **`cases/page.tsx`** only — page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (Preview/Evidence/Packet styling); supports **Saved cases → Update in chat → Chat AI** chat-first breadcrumb continuity — **no** new API; **`openUpdateInChat`**, **`openCase`**, **`resolveOpenCaseHref`**, filters, Needs attention, All cases checklists, case APIs, session keys, schema, routes, **Back to action plan**, **Home**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**).
- **`/justice/handling` Update in chat breadcrumb return** is **shipped** (PR **#378** / commit **`115e295`**): **`handling/page.tsx`** only — page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Saved cases** and before **Action plan** (**Saved cases** · **Update in chat** · **Action plan**; Preview/Evidence/Packet/Saved cases styling); supports **Handling workbench → Update in chat → Chat AI** chat-first breadcrumb continuity — **no** new API; **`openChat`**, **`navigateWithCase`**, card buttons, **Mark acknowledged**, filters, case APIs, session keys, schema, handling membership rules, routes, **Saved cases**, **Action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Handling workbench — Update in chat breadcrumb return (`/justice/handling`)**).
- **`/justice/merchant` Update in chat breadcrumb return** is **shipped** (PR **#380** / commit **`608a515`**): **`merchant/page.tsx`** only — page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; Preview/Evidence/Packet/Saved cases/Handling styling); supports **Merchant → Update in chat → Chat AI** chat-first breadcrumb continuity on the common approved-step prep surface — **no** new API; contact message generation, copy/save contact flow, **`PATCH`** case behavior, timeline writes, approved-step logic, handling UI, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)**).
- **`/justice/merchant` save return to chat routing** is **shipped**: **`merchant/page.tsx`** only — post-save **`router.push("/justice/chat-ai")`** replaces **`router.push("/justice/plan")`**; submit button **Save and continue in chat** replaces **Save and return to action plan**; breadcrumb links, form fields, save handler/**`PATCH`**/**`logEvent`**, **`JusticeFilingRecords`**, intro copy, other prep pages, session intake/**`STORAGE_CASE_ID`**, APIs, schema, styling unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Merchant — save return to chat routing (`/justice/merchant`)**).
- **`/justice/merchant` breadcrumb primary link chat-first reorder** is **shipped**: **`merchant/page.tsx`** only — top breadcrumb **Update in chat** → **`/justice/chat-ai`** first, **Back to action plan** → **`/justice/plan`** second; hrefs, labels, and styling unchanged — order-only alignment with preview/packet/FTC chat-first breadcrumbs; post-save **`router.push("/justice/chat-ai")`**, **Save and continue in chat** button label, form fields, save handler/**`PATCH`**/**`logEvent`**, **`JusticeFilingRecords`**, intro copy, auth gating, session intake/**`STORAGE_CASE_ID`**, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Merchant — breadcrumb primary link chat-first reorder (`/justice/merchant`)**).
- **`/justice/payment-dispute` Update in chat breadcrumb return** is **shipped** (PR **#382** / commit **`151f93d`**): **`payment-dispute/page.tsx`** only — page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; Merchant styling); supports **Payment dispute → Update in chat → Chat AI** chat-first breadcrumb continuity on the bank/card checklist prep surface — **no** new API; checklist logic, copy/save flow, **`payment_dispute_draft`**, **`PATCH`** case behavior, timeline writes, approved-step logic, plan preview, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`)**).
- **`/justice/payment-dispute` save return to chat routing** is **shipped**: **`payment-dispute/page.tsx`** only — post-save **`router.push("/justice/chat-ai")`** replaces **`router.push("/justice/plan")`**; submit button **Save and continue in chat** replaces **Save and return to action plan**; breadcrumb links, form fields, save handler/**`PATCH`**/analytics, checklist logic, **`payment_dispute_draft`**, intro copy, other prep pages, session intake/**`STORAGE_CASE_ID`**, APIs, schema, styling unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Payment dispute — save return to chat routing (`/justice/payment-dispute`)**).
- **`/justice/payment-dispute` breadcrumb primary link chat-first reorder** is **shipped**: **`payment-dispute/page.tsx`** only — top breadcrumb **Update in chat** → **`/justice/chat-ai`** first, **Back to action plan** → **`/justice/plan`** second; hrefs, labels, and styling unchanged — order-only alignment with merchant/FTC/preview/packet chat-first breadcrumbs; post-save **`router.push("/justice/chat-ai")`**, **Save and continue in chat** button label, checklist logic, form fields, save handler/**`PATCH`**/analytics, **`payment_dispute_draft`**, **`JusticeSavedEvidenceList`**, **`JusticeFilingRecords`**, intro copy, auth gating, session intake/**`STORAGE_CASE_ID`**, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Payment dispute — breadcrumb primary link chat-first reorder (`/justice/payment-dispute`)**).
- **`/justice/bbb` Update in chat breadcrumb return** is **shipped** (PR **#384** / commit **`2fdf4f8`**): **`bbb/page.tsx`** only — page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; Merchant/Payment dispute styling); supports **BBB complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual BBB prep surface — **no** new API; complaint draft generation, copy flow, **`bbb_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **BBB — Update in chat breadcrumb return (`/justice/bbb`)**).
- **`/justice/bbb` breadcrumb primary link chat-first reorder** is **shipped**: **`bbb/page.tsx`** only — top breadcrumb **Update in chat** → **`/justice/chat-ai`** first, **Back to action plan** → **`/justice/plan`** second; hrefs, labels, and styling unchanged — order-only alignment with merchant/payment-dispute/FTC/preview/packet chat-first breadcrumbs; complaint draft generation, copy-to-clipboard flow, **`bbb_prep_opened`** analytics, **`JusticeFilingRecords`**, **`JusticeSavedEvidenceList`**, intro/manual-filing copy, auth gating, session intake/**`STORAGE_CASE_ID`**, APIs, schema, routes unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **BBB — breadcrumb primary link chat-first reorder (`/justice/bbb`)**).
- **`/justice/cfpb` Update in chat breadcrumb return** is **shipped** (PR **#386** / commit **`5de8335`**): **`cfpb/page.tsx`** only — page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; BBB/Merchant/Payment dispute styling); supports **CFPB complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual CFPB prep surface — **no** new API; complaint draft generation, copy flow, **`cfpb_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **CFPB — Update in chat breadcrumb return (`/justice/cfpb`)**).
- **`/justice/cfpb` breadcrumb primary link chat-first reorder** is **shipped**: **`cfpb/page.tsx`** only — top breadcrumb **Update in chat** → **`/justice/chat-ai`** first, **Back to action plan** → **`/justice/plan`** second; hrefs, labels, and styling unchanged — order-only alignment with BBB/merchant/payment-dispute/FTC/preview/packet chat-first breadcrumbs; complaint draft generation, copy-to-clipboard flow, **`cfpb_prep_opened`** analytics, **`JusticeFilingRecords`**, **`JusticeSavedEvidenceList`**, intro/manual-filing copy, auth gating, session intake/**`STORAGE_CASE_ID`**, APIs, schema, routes unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **CFPB — breadcrumb primary link chat-first reorder (`/justice/cfpb`)**).
- **`/justice/fcc` Update in chat breadcrumb return** is **shipped** (PR **#388** / commit **`35e3e7c`**): **`fcc/page.tsx`** only — page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; CFPB/BBB/Merchant/Payment dispute styling); supports **FCC complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual FCC prep surface — **no** new API; complaint draft generation, copy flow, **`fcc_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **FCC — Update in chat breadcrumb return (`/justice/fcc`)**).
- **`/justice/fcc` breadcrumb primary link chat-first reorder** is **shipped**: **`fcc/page.tsx`** only — top breadcrumb **Update in chat** → **`/justice/chat-ai`** first, **Back to action plan** → **`/justice/plan`** second; hrefs, labels, and styling unchanged — order-only alignment with CFPB/BBB/merchant/payment-dispute/FTC/preview/packet chat-first breadcrumbs; complaint draft generation, copy-to-clipboard flow, **`fcc_prep_opened`** analytics, **`JusticeFilingRecords`**, **`JusticeSavedEvidenceList`**, intro/manual-filing copy, auth gating, session intake/**`STORAGE_CASE_ID`**, APIs, schema, routes unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **FCC — breadcrumb primary link chat-first reorder (`/justice/fcc`)**).
- **`/justice/dot` Update in chat breadcrumb return** is **shipped** (PR **#390** / commit **`d56f7be`**): **`dot/page.tsx`** only — page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; FCC/CFPB/BBB/Merchant/Payment dispute styling); supports **USDOT aviation complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual DOT prep surface — **no** new API; complaint draft generation, copy flow, **`dot_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **DOT — Update in chat breadcrumb return (`/justice/dot`)**).
- **`/justice/dot` breadcrumb primary link chat-first reorder** is **shipped**: **`dot/page.tsx`** only — top breadcrumb **Update in chat** → **`/justice/chat-ai`** first, **Back to action plan** → **`/justice/plan`** second; hrefs, labels, and styling unchanged — order-only alignment with FCC/CFPB/BBB/merchant/payment-dispute/FTC/preview/packet chat-first breadcrumbs; complaint draft generation, copy-to-clipboard flow, **`dot_prep_opened`** analytics, **`JusticeFilingRecords`**, **`JusticeSavedEvidenceList`**, intro/manual-filing copy, auth gating, session intake/**`STORAGE_CASE_ID`**, APIs, schema, routes unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **DOT — breadcrumb primary link chat-first reorder (`/justice/dot`)**).
- **`/justice/demand-letter` Update in chat breadcrumb return** is **shipped** (PR **#392** / commit **`051c35a`**): **`demand-letter/page.tsx`** only — page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; DOT/FCC/CFPB/BBB/Merchant/Payment dispute styling); supports **Demand letter prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual **`small_claims`** demand-letter prep surface — **no** new API; letter generation, copy flow, **`demand_letter_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Demand letter — Update in chat breadcrumb return (`/justice/demand-letter`)**).
- **`/justice/demand-letter` breadcrumb primary link chat-first reorder** is **shipped**: **`demand-letter/page.tsx`** only — top breadcrumb **Update in chat** → **`/justice/chat-ai`** first, **Back to action plan** → **`/justice/plan`** second; hrefs, labels, and styling unchanged — order-only alignment with State AG/DOT/FCC/CFPB/BBB/merchant/payment-dispute/FTC/preview/packet chat-first breadcrumbs; **completes** manual complaint-prep breadcrumb chat-first alignment across all prep surfaces; letter generation, copy-to-clipboard flow, **`demand_letter_prep_opened`** analytics, **`JusticeFilingRecords`**, **`JusticeSavedEvidenceList`**, intro/manual-filing copy, auth gating, session intake/**`STORAGE_CASE_ID`**, APIs, schema, routes unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Demand letter — breadcrumb primary link chat-first reorder (`/justice/demand-letter`)**).
- **`/justice/state-ag` Update in chat breadcrumb return** is **shipped** (PR **#394** / commit **`7154ef5`**): **`state-ag/page.tsx`** only — page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; Demand letter/DOT/FCC/CFPB/BBB/Merchant/Payment dispute styling); supports **State AG complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual State AG prep surface — **no** new API; complaint draft generation, **`consumer_us_state`** selector, copy flow, **`state_ag_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **State AG — Update in chat breadcrumb return (`/justice/state-ag`)**).
- **`/justice/state-ag` breadcrumb primary link chat-first reorder** is **shipped**: **`state-ag/page.tsx`** only — top breadcrumb **Update in chat** → **`/justice/chat-ai`** first, **Back to action plan** → **`/justice/plan`** second; hrefs, labels, and styling unchanged — order-only alignment with DOT/FCC/CFPB/BBB/merchant/payment-dispute/FTC/preview/packet chat-first breadcrumbs; complaint draft generation, **`consumer_us_state`** selector, copy-to-clipboard flow, **`state_ag_prep_opened`** analytics, **`JusticeFilingRecords`**, **`JusticeSavedEvidenceList`**, intro/manual-filing copy, auth gating, session intake/**`STORAGE_CASE_ID`**, APIs, schema, routes unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **State AG — breadcrumb primary link chat-first reorder (`/justice/state-ag`)**).
- **`/justice/ftc-review` Update in chat breadcrumb return** is **shipped** (PR **#396** / commit **`25144d7`**): **`ftc-review/page.tsx`** only — page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; State AG/Demand letter/DOT/FCC/CFPB/BBB/Merchant/Payment dispute styling); supports **FTC practice review → Update in chat → Chat AI** chat-first breadcrumb continuity on the internal practice FTC review surface — **no** new API; FTC unlock redirect gate, practice autofill, success-panel **Back to action plan** button, auth gating, mock form behavior, timeline writes, filing records UI, plan FTC preview, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **FTC review — Update in chat breadcrumb return (`/justice/ftc-review`)**).
- **`/justice/ftc-review` practice success return to chat routing** is **shipped**: **`ftc-review/page.tsx`** only — practice-success panel **`Link`** **Continue in chat** → **`/justice/chat-ai`** replaces **Back to action plan** → **`/justice/plan`**; FTC unlock gate redirect, breadcrumb links, **`runPractice`**, timeline/mock form, filing records, evidence list, auth gating, session intake/**`STORAGE_CASE_ID`**, APIs, schema, link styling unchanged — **no** real government submission/filing/contact/queue/upload/storage/automatic-action claims (§2 **FTC review — practice success return to chat routing (`/justice/ftc-review`)**).
- **`/justice/ftc-review` breadcrumb primary link chat-first reorder** is **shipped**: **`ftc-review/page.tsx`** only — top breadcrumb **Update in chat** → **`/justice/chat-ai`** first, **Back to action plan** → **`/justice/plan`** second; hrefs, labels, and styling unchanged — order-only alignment with preview/packet chat-first breadcrumbs; FTC unlock gate redirect, practice-success **Continue in chat** panel, **`runPractice`**, timeline/mock form, filing records, evidence list, auth gating, session intake/**`STORAGE_CASE_ID`**, APIs, schema unchanged — **no** real government submission/filing/contact/queue/upload/storage/automatic-action claims (§2 **FTC review — breadcrumb primary link chat-first reorder (`/justice/ftc-review`)**).
- **`/justice/ftc-review` unlock gate redirect to chat** is **shipped**: **`ftc-review/page.tsx`** only — when FTC not unlocked, **`router.replace("/justice/chat-ai")`** replaces **`router.replace("/justice/plan")`**; **`computeFtcUnlocked`** gate conditions unchanged — **completes** FTC review programmatic chat-first routing alongside breadcrumb (#527) and practice-success **Continue in chat** (#526); breadcrumb order, **`runPractice`**, success panel, timeline/mock form, filing records, evidence list, auth gating, session intake/**`STORAGE_CASE_ID`**, APIs, schema, link styling unchanged — **no** real government submission/filing/contact/queue/upload/storage/automatic-action claims (§2 **FTC review — unlock gate redirect to chat (`/justice/ftc-review`)**).
- **`/justice/intake` chat-first breadcrumb links** is **shipped** (PR **#398** / commit **`2c35e2d`**): **`intake/page.tsx`** only — page-level breadcrumb **Home · Chat intake · Step-by-step chat** with **Chat intake** → **`/justice/chat-ai`** and **Step-by-step chat** → **`/justice/chat`**; keeps structured intake connected back to the chat-first flow — **no** new API; form submit, validation, saved intake loading, preview routing, commit helper, session keys, schema, routes, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Structured intake — chat-first breadcrumb links (`/justice/intake`)**).
- **`/justice/intake` subtitle action plan copy de-bias** is **shipped**: **`intake/page.tsx`** only — subtitle **Answer once. Next you'll review a submission draft.** removes **, then your action plan**; matches **`handleSubmit`** → **`/justice/preview`**; breadcrumb, form submit/validation, commit logic, APIs, schema, auth, prep/plan pages unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Structured intake — subtitle action plan copy de-bias (`/justice/intake`)**).
- **`/justice/chat` chat-first breadcrumb return** is **shipped** (PR **#400** / commit **`809412b`**): **`chat/page.tsx`** only — page-level breadcrumb **Home · Chat intake · Action plan · Structured form** with **Chat intake** → **`/justice/chat-ai`**; gives scripted step-by-step chat a visible return path to primary AI chat intake — **no** new API; scripted chat flow, questions, answers, form routing, preview routing, commit helper, session keys, schema, routes, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Step-by-step chat — chat-first breadcrumb return (`/justice/chat`)**).
- **`/justice/chat` breadcrumb Action plan de-bias** is **shipped**: **`chat/page.tsx`** only — third breadcrumb link **Justice workspace** → **`/justice`** replaces **Action plan** → **`/justice/plan`** — **Home · Chat intake · Justice workspace · Structured form**; aligns scripted step-by-step chat exit with preview/chat-ai hub breadcrumb pattern; **Chat intake** and **Structured form** links unchanged; scripted Q&A, **`handleContinue`** → **`/justice/preview`**, review-step copy, commit logic, APIs, schema, auth, prep/plan pages unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Step-by-step chat — breadcrumb Action plan de-bias (`/justice/chat`)**).
- **`/justice/chat` review-step action plan copy de-bias** is **shipped**: **`chat/page.tsx`** only — **`stepQuestion`** **`review`** branch removes **, then your action plan** — **Here's a quick recap. When you're ready, continue to review your submission draft.**; matches **`handleContinue`** → **`/justice/preview`**; breadcrumb (#544), scripted Q&A, other **`stepQuestion`** branches, commit logic, APIs, schema, auth, prep/plan pages unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Step-by-step chat — review-step action plan copy de-bias (`/justice/chat`)**).
- **`/justice/chat-ai` saved evidence count in Recap (Proof / evidence panel)** is **shipped**: **`chat-ai/page.tsx`** only; on **Update in chat** when signed in with a **UUID** case id, **`loadSavedEvidencePreview`** → **`GET /api/justice/evidence?case_id={id}`** (existing route) sets **`savedEvidenceCount`** and **`recentEvidenceRows`** (up to **3** for §2 **Chat AI intake — recent saved proof preview in Recap (Proof / evidence panel)**); **Recap → Proof / evidence** shows **Saved evidence: {N} item(s).** or **No saved evidence yet.**; refetch on window **focus** / tab **visibility** → **visible** after **`/justice/evidence`** or after in-chat save (§2 **Chat AI intake — add proof note from Recap (Proof / evidence panel)**); **fresh intake** and signed-out show generic panel only (no misleading count); **Organize evidence** return via evidence breadcrumb — §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**; commit → preview, **Current action tracking**, handling UI, APIs, schema, and persistence **unchanged** on this slice (§2 **Chat AI intake — saved evidence count in Recap (Proof / evidence panel)**, **Chat AI intake — proof/evidence guidance**).
- **`/justice/chat-ai` add proof note from Recap (Proof / evidence panel)** is **shipped**: **`chat-ai/page.tsx`** only; on **Update in chat** when signed in with a **UUID** server-backed case id, collapsed **Add a proof note** form — required **title** + **evidence_type**; optional **evidence date** (max 200) and **description** (max 8000) — **`POST`s** **`/api/justice/evidence`** with **`evidence_date`** / **`description`** only when trimmed non-empty; **`applyServerTimelineFromResponse`** on success; clears title, date, and description (type unchanged); refreshes **`loadSavedEvidencePreview`** (count + recent list; date/description in preview after refetch); **no** **`source_url`** / **`storage_note`** in chat form; **metadata only** — **no** file upload/storage, filing, submission, external queue, or contact; **`/justice/evidence`** for list/edit/delete; **fresh intake** before first server commit **out of scope** (§2 **Chat AI intake — add proof note from Recap (Proof / evidence panel)**).
- **`/justice/chat-ai` recent saved proof preview in Recap (Proof / evidence panel)** is **shipped**: **`chat-ai/page.tsx`** only; on **Update in chat** when signed in with a **UUID** case id and **savedEvidenceCount > 0**, reuses **`GET /api/justice/evidence?case_id={id}`** via **`loadSavedEvidencePreview`** (same fetch as count — **no** second route); collapsed **Recent proof notes** (up to **3** rows: title, type label, optional date, truncated description; per-row **Edit** — §2 **Chat AI intake — edit recent proof note in Recap (Proof / evidence panel)**; per-row **Delete** — §2 **Chat AI intake — delete recent proof note in Recap (Proof / evidence panel)**; **no** **`source_url`** / **`storage_note`** in chat); refetch on focus/visibility and after in-chat proof note save/edit/delete; **`/justice/evidence`** for full list and rows outside recent preview; **fresh intake** **out of scope** (§2 **Chat AI intake — recent saved proof preview in Recap (Proof / evidence panel)**).
- **`/justice/chat-ai` edit recent proof note in Recap (Proof / evidence panel)** is **shipped**: **`chat-ai/page.tsx`** only; inline **Edit** on each displayed recent row (max **3**); compact form (**title**, **evidence_type**, optional date/description); **`PATCH /api/justice/evidence/[id]`** with those four fields only (**`evidence_date`** / **`description`** **`null`** when cleared); **no** **`applyServerTimelineFromResponse`**; success → **Proof note updated.** + **`loadSavedEvidencePreview`**; **`/justice/evidence`** for full list and **`source_url`**/**`storage_note`**; **fresh intake** **out of scope** (§2 **Chat AI intake — edit recent proof note in Recap (Proof / evidence panel)**).
- **`/justice/chat-ai` delete recent proof note in Recap (Proof / evidence panel)** is **shipped**: **`chat-ai/page.tsx`** only; per-row **Delete** on displayed recent rows (max **3**); **`window.confirm`**; **`DELETE /api/justice/evidence/[id]`**; clears edit state if deleted row was open; success → **Proof note deleted.** + **`loadSavedEvidencePreview`**; visible error on failure; **no** timeline append; **no** **`source_url`**/**`storage_note`**; **`/justice/evidence`** for all rows / bulk delete; **fresh intake** **out of scope** (§2 **Chat AI intake — delete recent proof note in Recap (Proof / evidence panel)**).
- **`/justice/chat-ai` proof keyword nudge in Recap (Proof / evidence panel)** is **shipped**: **`chat-ai/page.tsx`** only — **client-side** after **successful** **`handleSend`** (intake-chat round trip unchanged); local strong-term detection on the latest user message (**no** auto-created evidence rows; **no** API/schema/**`buildJusticeIntakeAiPrompt.ts`**/**`POST /api/justice/intake-chat`** changes). When **`canAddProofNoteInChat`**, dismissible Recap → **Proof / evidence** banner above **Add a proof note** (**Got it**; metadata-only copy — **not** file upload); **prefills** **Add a proof note** from the triggering message (**empty fields only** — title ~**120** chars from trimmed/collapsed text; description only when message exceeds title cap; **no** **`evidence_type`**/**`evidence_date`** inference; opens **`<details>`**; user must **Save proof note**); **at most once** per page session; banner hides on dismiss or after proof note save; **fresh intake** before first server commit **out of scope**; **LLM/system-prompt** proof nudges remain **future** work (§2 **Chat AI intake — proof keyword nudge in Recap (Proof / evidence panel)**).
- **`/justice/chat-ai` staged proof notes on fresh intake (Proof / evidence panel)** is **shipped**: **`chat-ai/page.tsx`** + **`src/lib/justice/stagedProofNotes.ts`** — on **fresh intake** (**`canStageProofNoteInChat`**), **Add a proof note** stages **title** / **`evidence_type`** / optional **evidence date** / **description** in **`sessionStorage`** (**`justice_staged_proof_notes_v1`**); mount hydrates staged state even on **Update in chat** load; **pending proof notes** list shown whenever signed in and staged rows exist (not only fresh staging mode); **Stage proof note** (not **`POST`**) until **Continue**; first flush gated by **`commitIntakeToSessionAndServer`** **`CommitIntakeResult`** on **create**; **cross-mode retry flush** when staged notes + local intake + **UUID** **`STORAGE_CASE_ID`** exist — regardless of **`isUpdatingExistingCase`** — flushes to existing case **without** another **`create`**; flushed rows cleared only after successful evidence **`POST`s**; failures preserved; normal **create → flush → stay in chat** when signed-in **UUID** server persist succeeds; **`/justice/preview`** fallback when continuity guards fail; **update-mode** immediate saved-evidence **`POST`** **unchanged**; pending/local until **Continue** saves to case; **`clearLocalJusticeSession`** clears staging; **no** schema/new API/prompt/upload/filing/queue/AI-inferred rows (§2 **Chat AI intake — staged proof notes on fresh intake (Proof / evidence panel)**).
- **`/justice/chat-ai` Continue handoff panel (What happens next)** is **shipped**: **`chat-ai/page.tsx`** only — compact **What happens next** panel above **Save and continue in chat** when preview basics complete and contact proof valid; **`getContinueHandoffSteps`** mirrors **`handleContinueToPreview`** (**fresh create** with/without staged proof, **staged flush retry** to session **UUID**, **update mode** with optional saved-proof note or folded **`sessionChangeLines`** — §2 **Chat AI intake — Continue handoff session-change integration (Update in chat)**); explains save case/updates, pending proof where applicable, in-chat draft review / packet approval / current action tracking when **`chatFirstContinuity`** — §2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)** — **no** filing/submission/contact/queue/upload/storage claims; replaces staged-proof one-liner above **Continue**; **Save and continue in chat** disabled when submitting/loading or basics missing; **Still needed before preview** and contact-proof warnings unchanged (§2 **Chat AI intake — Continue handoff panel (What happens next)**).
- **`/justice/chat-ai` Continue handoff funnel copy alignment** is **shipped**: **`chat-ai/page.tsx`** only — when **`chatFirstContinuity`** (signed-in), **`getContinueHandoffSteps`** uses in-chat funnel bullets (draft review in Active case checklist, approve packet in chat, continue in Current action tracking) instead of preview → packet → plan **`previewStep`** / **`postPreviewFunnelStep`**; **Save and continue in chat** button; focus/subtitle/session-change copy say **save in chat**; packet checklist link hidden while inline approval block shown — preview-oriented fallback when **`chatFirstContinuity`** false — **no** **`handleContinueToPreview`**, stay-in-chat guards, handoff gates, proof/staged-proof logic, optional work links, routes, APIs, schema changes — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)**).
- **`/justice/chat-ai` Continue handoff session-change integration (Update in chat)** is **shipped**: **`chat-ai/page.tsx`** only — when handoff visible and **`sessionChangeLines`** exist, folds changes into **What happens next** (**Save your updates from this chat to your case:** + per-change steps); hides standalone **Updated in this chat** panel while handoff shown; standalone panel still when changes exist but handoff blocked; **`handleSend`**, **Continue**, commit, proof CRUD, staged proof, active-case banner, session-change helper, APIs, schema, hub, cases, plan, preview unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — Continue handoff session-change integration (Update in chat)**).
- **`/justice/preview` proof on your case card (Chat AI handoff continuity)** is **shipped**: **`preview/page.tsx`** only — **Proof on your case** card after **Related action** using existing **`loadEvidence`** / **`GET /api/justice/evidence`** (**no** new API); loading/error/empty/saved states; count + up to **3** recent proof note titles with type/date when available; empty state links **Add proof in chat →** **`/justice/chat-ai`**; intro copy clarifies saved chat intake/case, draft review, metadata-only proof titles, nothing filed automatically — **no** upload/storage/submission/contact/queue claims; draft generation/review milestone/AI draft/continue-to-plan unchanged (§2 **Preview — proof on your case card (Chat AI handoff continuity)**).
- **`/justice/preview` chat-update continuity card (Chat AI handoff)** is **shipped**: **`preview/page.tsx`** + **`previewChatUpdateHandoff.ts`** — chat-ai writes **`sessionChangeLines`** to **`justice_preview_chat_update_summary_v1`** on successful **Update in chat** Continue; preview **`readAndClearPreviewChatUpdateSummary`** after intake ready; one-time **From your chat update** card (company line, change bullets, draft reflects saved case / nothing filed automatically, **Update in chat** link); hidden on fresh intake, plan → preview, no-change updates; draft generation, review milestone, evidence GET, AI draft, continue-to-plan, commit, APIs, schema, hub/cases/plan unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — chat-update continuity card (Chat AI handoff)**).
- **`/justice/preview` pre-plan chat correction prompt** is **shipped**: **`preview/page.tsx`** only — bottom review card includes **Need to fix something in the draft?** **Update in chat** → **`/justice/chat-ai`**; subline describe changes in chat then return to review updated draft / nothing filed automatically; always on loaded preview before primary **Continue**; button label and routing — §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**; proof card, chat-update card, approved-next panel, review checkbox, **`submission-draft-reviewed`** API, draft generation, evidence GET, AI draft, APIs, schema, hub/cases/plan/chat-ai unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — pre-plan chat correction prompt (`/justice/preview`)**).
- **`/justice/preview` Continue routes to packet when prepared packet not approved** is **shipped**: **`preview/page.tsx`** only — after successful **`submission_draft_reviewed`** save, **`router.push`** to **`/justice/packet`** when **`!preparedPacketApproved`**, **`/justice/plan`** when approved; bottom button **Review prepared case packet** / **Continue to action plan**; uses existing session **`preparedPacketApproved`** hydrate — **no** new API; proof card, chat-update card, approved-next panel, checkbox, **`submission-draft-reviewed`**, draft generation, evidence GET, AI draft, handlers, Plan, Packet, Chat AI, Cases, Hub, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**).
- **`/justice/preview` approved-next session hydrate wiring** is **shipped**: **`preview/page.tsx`** only — **`useEffect`** when **`hydrationStatus === "ready"`** and **`caseId`** sets **`preparedPacketApproved`** via **`readSessionPreparedPacketApproved`** and **`approvedNextAction`** via **`hydrateApprovedNextActionForDisplay(caseId)`**; clears both when not ready or no **`caseId`**; enables read-only panel (§2 **Preview — approved-next read-only continuity (`/justice/preview`)**) and Continue routing (§2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**) — **no** signed-in **`GET`**; panel JSX, proof card, chat-update card, draft generation, review checkbox, **`submission-draft-reviewed`**, evidence loading, handlers, Plan, Packet, Chat AI, Cases, Hub, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — approved-next session hydrate wiring (`/justice/preview`)**).
- **`/justice/preview` approved-next read-only continuity** is **shipped**: **`preview/page.tsx`** only — **visibility-only** read-only panel (§2 **Preview — approved-next session hydrate wiring (`/justice/preview`)**); when **`preparedPacketApproved && approvedNextAction`**, shows **Next step: {label}** and **Approved next action: {status}** via **`approvedNextActionStatusLabel`** after optional chat-update card and before **Related action**; **no** buttons/links/**Open**/**Record handled**; draft generation, review checkbox, proof card, Continue routing, evidence loading, handlers, Plan, Packet, Chat AI, Cases, Hub, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — approved-next read-only continuity (`/justice/preview`)**).
- **`/justice/plan` proof readiness continuity after preview review** is **shipped**: **`plan/page.tsx`** only — post-draft **`showPostDraftReviewCallout`** proof line from existing **`evidenceCount`** / **`readinessLoading`** (**no** new API); saved count + metadata-only reviewed-draft wording, or empty + **Add proof in chat** → **`/justice/chat-ai`**; **Evidence / proof** empty state also links **Add proof in chat**; **Add evidence** → **`/justice/evidence`** unchanged; **`evidenceReady`** / **`readyToEscalate`**, packet/handling/approval unchanged — **no** upload/storage/submission/contact/queue claims; **no** chat-ai/preview/lib/schema/prompt changes (§2 **Plan — proof readiness continuity after preview review (`/justice/plan`)**).
- **`/justice/plan` Case readiness checklist proof CTA** is **shipped**: **`plan/page.tsx`** only — **Case readiness** **Evidence added** row appends inline **Add proof in chat** → **`/justice/chat-ai`** when signed in, **`!readinessLoading`**, and **`evidenceCount < 1`**; mirrors §2 **Plan — Case readiness checklist basics CTA (`/justice/plan`)** inline link styling; **`evidenceReady`** / **`readyToEscalate`** / **`basicsReady`** / readiness fetch / packet CTAs / post-draft callout / evidence-row / breadcrumb unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims; **no** API/schema/helper/preview/chat-ai/hub/cases changes (§2 **Plan — Case readiness checklist proof CTA (`/justice/plan`)**).
- **`/justice/plan` Case readiness checklist basics CTA** is **shipped**: **`plan/page.tsx`** only — **Case readiness** **Basic case info present** row appends inline **Update in chat** → **`/justice/chat-ai`** when signed in, **`!readinessLoading`**, and **`!basicsReady`**; mirrors proof CTA inline link pattern; evidence row / draft-review row / breadcrumb / **`basicsReady`** / **`readyToEscalate`** / readiness fetch / packet CTAs / post-draft callout unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims; **no** API/schema/helper/preview/chat-ai/hub/cases changes (§2 **Plan — Case readiness checklist basics CTA (`/justice/plan`)**).
- **`/justice/plan` Case readiness checklist draft-review CTA** is **shipped**: **`plan/page.tsx`** only — **Case readiness** **Submission draft reviewed: yes/not yet** row from existing **`submission_draft_reviewed`** timeline event; inline **Review submission draft** → **`/justice/preview`** when signed in, **`!readinessLoading`**, and not yet reviewed; **`basicsReady`** / **`evidenceReady`** / **`readyToEscalate`** / readiness fetch / packet CTAs / post-draft callout / basics/evidence rows / breadcrumb unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims; **no** API/schema/helper/preview/chat-ai/hub/cases changes (§2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**).
- **`/justice/plan` Case readiness approved-next visibility** is **shipped**: **`plan/page.tsx`** only — **visibility-only**; **Case readiness** shows read-only **Next step: {label}** and **Approved next action: {status}** via **`approvedNextActionStatusLabel`** when **`preparedPacketApproved && approvedNextAction`** (after draft-review row); checklist-only — **no** links/buttons; emerald callout, **Open** / **Record handled**, **`showApprovedNextActionCta`**, handlers, CTAs, hydrate, routing, Chat AI, Cases, Hub, Packet, Preview, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Plan — Case readiness approved-next visibility (`/justice/plan`)**).
- **`/justice/plan` post-draft chat continuity prompt after preview review** is **shipped**: **`plan/page.tsx`** only — in **`showPostDraftReviewCallout`**, after next-step explainer / readiness bridge and proof-readiness line and before primary packet/prep CTA: **Need to fix details or add more proof notes?** **Update in chat** → **`/justice/chat-ai`** — **Surrenderless keeps your case aligned with your draft and plan.**; keeps chat-first workflow after action plan shown; **`evidenceReady`** / **`readyToEscalate`** / **`recommendationText`** / **`pickPreparedNextAction`** / packet approval / handling / evidence logic unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims; **no** chat-ai/preview/packet/lib/schema/API/prompt changes (§2 **Plan — post-draft chat continuity prompt after preview review (`/justice/plan`)**).
- **`/justice/plan` post-draft next-step explainer and readiness bridge** is **shipped**: **`plan/page.tsx`** only — in **`showPostDraftReviewCallout`**, after main paragraph and before proof/chat continuity lines: **`getPreparedNextStepExplainer`** from **`preparedPacketApproved`** + **`preparedNextAction.stepLabel`** explains prepared review / approved in-app step and states **Surrenderless does not file or submit for you** / **Nothing is sent automatically**; when signed in, **`!readinessLoading`**, and **`!readyToEscalate`**, **Before you escalate, finish readiness:** bridge with **Update in chat** / **Add proof in chat** → **`/justice/chat-ai`** and **Review submission draft** → **`/justice/preview`** for incomplete items; **Review prepared next step** / **View approved case packet** primary CTA and secondary prep link **unchanged**; **`showPostDraftReviewCallout`** gating, **`pickPreparedNextAction`**, **`recommendationText`**, full plan, packet handling, APIs, schema, helpers, hub, cases, chat-ai, preview unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`)**).
- **`/justice/plan` post-draft re-preview continuity prompt after preview review** is **shipped**: **`plan/page.tsx`** only — in **`showPostDraftReviewCallout`**, after **Update in chat** and before primary packet/prep CTA: **Updated your case in chat?** **Review submission draft again** → **`/justice/preview`**; subline confirm draft matches saved case before escalate / nothing filed automatically; helps users who updated in chat after draft review re-check submission draft before escalating; **`showPostDraftReviewCallout`** gating, **`evidenceReady`** / **`readyToEscalate`**, packet CTAs, preview Continue, timeline writes, APIs, schema, helpers, hub, cases, chat-ai unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims; **no** preview/packet/lib/schema/API/prompt implementation changes beyond link (§2 **Plan — post-draft re-preview continuity prompt after preview review (`/justice/plan`)**).
- **`/justice/preview` signed-in Continue returns to chat** is **shipped**: **`preview/page.tsx`** only — in **`handleContinueToPlan`**, after successful **`submission_draft_reviewed`** persistence, when **`isSignedIn`** and **`STORAGE_CASE_ID`** is a **UUID**, **`router.push("/justice/chat-ai")`**; unsigned / non-UUID keeps **`preparedPacketApproved ? "/justice/plan" : "/justice/packet"`** fallback (§2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**); closes checklist **Review submission draft** → preview → **Continue** → chat loop; preview UI, draft generation, review checkbox, proof/chat-update/approved-next panels, checklist deep links, packet/plan pages, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — signed-in Continue returns to chat (`/justice/preview`)**).
- **`/justice/preview` signed-in Continue button label chat-first** is **shipped**: **`preview/page.tsx`** only — **`signedInContinueToChat`** (**`isSignedIn && Boolean(caseId) && isUuid(caseId)`**) sets bottom **Continue** label **Continue in chat**; unsigned / non-UUID keeps **Review prepared case packet** / **Continue to action plan**; **`handleContinueToPlan`** routing, breadcrumb, review checkbox, draft body, proof/chat-update/approved-next panels, checklist deep links, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — signed-in Continue button label chat-first (`/justice/preview`)**).
- **`/justice/preview` signed-in non-UUID Continue returns to chat** is **shipped**: **`preview/page.tsx`** only — **`handleContinueToPlan`** routes **`isSignedIn && cid`** (any case id) to **`/justice/chat-ai`**; **`signedInContinueToChat`** (**`isSignedIn && Boolean(caseId)`**) — **Continue in chat** for any signed-in case; **UUID** server **`POST`** and **non-UUID** session append unchanged; unsigned / no **`caseId`** keeps packet/plan fallback — **completes** signed-in preview Continue chat-first routing (removes last programmatic plan redirect for signed-in users); breadcrumb, review checkbox, proof/chat-update/approved-next panels, pre-plan correction prompt, APIs, schema, auth unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — signed-in non-UUID Continue returns to chat (`/justice/preview`)**).
- **`/justice/preview` session caseId Continue returns to chat** is **shipped**: **`preview/page.tsx`** only — **`handleContinueToPlan`** routes **`cid`** (any session **`STORAGE_CASE_ID`**, signed-in or unsigned) to **`/justice/chat-ai`**; **`continueToChat`** (**`Boolean(caseId)`**) — **Continue in chat** for any session-backed case; **UUID** server **`POST`** and **non-UUID** session append unchanged; no **`caseId`** keeps packet/plan fallback — **completes** preview Continue chat-first routing for all session-backed cases (extends §2 **Preview — signed-in non-UUID Continue returns to chat (`/justice/preview`)**); removes last programmatic plan redirect when **`STORAGE_CASE_ID`** is present; breadcrumb, review checkbox, proof/chat-update/approved-next panels, pre-plan correction prompt, APIs, schema, auth unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — session caseId Continue returns to chat (`/justice/preview`)**).
- **`/justice/preview` no-caseId Continue plan branch removal** is **shipped**: **`preview/page.tsx`** only — **`!cid`** **`router.push("/justice/packet")`** only; removes dead plan ternary and **Continue to action plan** button label — **completes** removal of last consumer **`router.push("/justice/plan")`**; **`cid`** → **`/justice/chat-ai`** and **Continue in chat** unchanged; **`submission_draft_reviewed`** persistence, breadcrumb, proof/chat-update panels, pre-plan correction prompt, **`preparedPacketApproved`** hydrate, packet/plan pages, APIs, schema, auth unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — no-caseId Continue plan branch removal (`/justice/preview`)**).
- **`/justice/preview` breadcrumb primary link chat-first** is **shipped**: **`preview/page.tsx`** only — breadcrumb **Update in chat** → **`/justice/chat-ai`** first, **Justice workspace** → **`/justice`** second; **Step-by-step chat** / **Structured form** unchanged; replaces **Back to action plan** → **`/justice/plan`** first link; **Continue** routing, draft generation, review checkbox, pre-plan correction prompt, proof/chat-update panels, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — breadcrumb primary link chat-first (`/justice/preview`)**).
- **`/justice/cases` Needs more info chat routing CTA** is **shipped**: **`cases/page.tsx`** only — on **All cases** **Needs more info** cards, under **`caseReadinessHint`**: **Update in chat** when basics incomplete, **Add proof in chat** when basics OK and **`evidenceCount < 1`**; **`openUpdateInChat`** activates case in session then **`/justice/chat-ai`**; evidence-only hint mentions chat + evidence page; **`openCase`** routing — §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**; compact readiness checklist / readiness predicates/filters/attention/APIs/schema/hub/plan/chat-ai/**`caseReadiness.ts`** unchanged on this slice — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — Needs more info chat routing CTA (`/justice/cases`)**).
- **`/justice/cases` compact readiness checklist on All cases cards** is **shipped**: **`cases/page.tsx`** only — each card when progress loaded shows **Basic case info** / **Evidence** / **Submission draft reviewed: yes/not yet** from existing intake, **`progressById`**, **`row.timeline`**; inline **Update in chat** / **Add proof in chat** / **Review submission draft** via **`activateCaseInSession`** + **`openUpdateInChat`** / **`openReviewSubmissionDraft`**; **`isReadyToEscalate`**, filters, hints, fetch, Needs more info buttons unchanged — **`openCase`** routing — §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**; **no** filing/submission/contact/queue/upload/storage/automatic-action claims; **no** API/schema/helper/hub/plan/preview/chat-ai changes (§2 **Saved cases — compact readiness checklist on All cases cards (`/justice/cases`)**).
- **`/justice/cases` Open case chat-first routing** is **shipped**: **`cases/page.tsx`** only — **`openCase(row)`** delegates to **`openUpdateInChat(row)`** (**`activateCaseInSession`** then **`/justice/chat-ai`**) regardless of draft-review or prepared-packet state; checklist deep links **`openReviewSubmissionDraft`** → **`/justice/preview`** and **`openReviewPreparedPacket`** → **`/justice/packet`** unchanged; preview **Continue** unchanged on this slice — **no** new API; **Open case** label, card layout, **Needs attention** builders, **Open action**, approved-next labels, filters/search, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — Open case chat-first routing (`/justice/cases`)**).
- **`/justice/cases` Needs attention follow-up default chat-first routing** is **shipped**: **`cases/page.tsx`** only — **`resolveApprovedNextActionFollowUpHref`** fallback **`"/justice/plan"`** → **`"/justice/chat-ai"`**; explicit internal **`next.href`** unchanged; **Open tracking** on follow-up cards → **Update in chat** with **Current action tracking**; **Open case**, checklist links, filters, card layout, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — Needs attention follow-up default chat-first routing (`/justice/cases`)**).
- **`/justice/packet` signed-in approve returns to chat** is **shipped**: **`packet/page.tsx`** only — in **`handleApprovePreparedPacket`**, after successful signed-in **UUID** **`client_state`** **`PATCH`**, **`router.push("/justice/chat-ai")`**; unsigned / non-UUID stays on packet; inline chat approve unchanged; approve checkbox/button, packet content, handling links, checklist deep links, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — signed-in approve returns to chat (`/justice/packet`)**).
- **`/justice/packet` signed-in non-UUID approve returns to chat** is **shipped**: **`packet/page.tsx`** only — in **`handleApprovePreparedPacket`**, after session/local approval writes, **`isLoaded && isSignedIn && caseId && !isUuid(caseId)`** → **`router.push("/justice/chat-ai")`**; **UUID** **`GET` + `PATCH`** path unchanged; unsigned / no **`caseId`** stays on packet — **completes** signed-in preview→packet→chat checklist routing for session-only case ids (pairs with §2 **Preview — signed-in non-UUID Continue returns to chat (`/justice/preview`)**); inline chat approve, emerald **Continue in chat** links, breadcrumb, handling links, approve checkbox/button, packet content, APIs, schema, auth unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — signed-in non-UUID approve returns to chat (`/justice/packet`)**).
- **`/justice/packet` session caseId approve returns to chat** is **shipped**: **`packet/page.tsx`** only — in **`handleApprovePreparedPacket`**, after session/local approval writes, **`caseId && (!isSignedIn || !isUuid(caseId))`** → **`router.push("/justice/chat-ai")`**; **signed-in UUID** **`GET` + `PATCH`** path unchanged; no **`caseId`** unchanged — **completes** session-backed preview→packet→chat checklist routing for unsigned/local cases (pairs with §2 **Preview — session caseId Continue returns to chat (`/justice/preview`)** (#539)); inline chat approve, emerald **Continue in chat** links, breadcrumb, handling links, approve checkbox/button, packet content, APIs, schema, auth unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — session caseId approve returns to chat (`/justice/packet`)**).
- **`/justice/packet` breadcrumb primary link chat-first** is **shipped**: **`packet/page.tsx`** only — top breadcrumb **Update in chat** → **`/justice/chat-ai`** first, **Justice workspace** → **`/justice`** second; **Evidence** / **Saved cases** unchanged; applies to main and no-**`caseId`** empty-state rows; in-card plan links, **Approve** → chat routing, approve checkbox/button, packet body, handling links, checklist deep links, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — breadcrumb primary link chat-first (`/justice/packet`)**).
- **`/justice/cases` breadcrumb primary link chat-first** is **shipped**: **`cases/page.tsx`** only — top breadcrumb **Update in chat** → **`/justice/chat-ai`** first, **Justice workspace** → **`/justice`** second; **Home** unchanged; intro copy, **Open case**, **Open tracking**, checklist links, filters, card layout, Needs attention builders, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — breadcrumb primary link chat-first (`/justice/cases`)**).
- **`/justice/cases` intro copy chat-first** is **shipped**: **`cases/page.tsx`** only — intro **Open a case you saved while signed in to continue in chat.** replaces **… continue your action plan.**; **`openCase`**, breadcrumb, **Open tracking**, checklist links, filters, card layout, Needs attention builders, handling copy, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — intro copy chat-first (`/justice/cases`)**).
- **`/justice/cases` approved-packet handling request copy chat-first** is **shipped**: **`cases/page.tsx`** only — **`CaseApprovedNextActionTracking`** helper second sentence **Request Surrenderless handling from chat intake when you want internal triage tracking.** replaces **Request handling from your action plan…** on **All cases** and **Needs attention** approved-packet blocks; **View on handling workbench**, handling tracking UI, **`openCase`**, breadcrumb, Needs attention builders, filters, card layout, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — approved-packet handling request copy chat-first (`/justice/cases`)**).
- **`/justice/evidence` breadcrumb primary link chat-first** is **shipped**: **`evidence/page.tsx`** only — top breadcrumb **Update in chat** → **`/justice/chat-ai`** first, **Justice workspace** → **`/justice`** second; **Saved cases** / **Home** unchanged; evidence CRUD, APIs, form fields, empty/signed-out states, chat-ai proof panel, plan/preview proof cards unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Evidence — breadcrumb primary link chat-first (`/justice/evidence`)**).
- **`/justice/chat-ai` proof panel continuation copy chat-first** is **shipped**: **`chat-ai/page.tsx`** only — **Recap → Proof / evidence** advisory paragraph says **continue in chat** and **Review your submission draft in the Active case checklist when ready** instead of **continue to your submission preview**; proof CRUD, **Organize evidence**, **Add a proof note**, handoff panel, Active case checklist links, preview/packet/plan, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — proof panel continuation copy chat-first (`/justice/chat-ai`)**).
- **`/justice/chat-ai` signed-in fresh intake breadcrumb chat-first** is **shipped**: **`chat-ai/page.tsx`** only — page-local **`chatFirstBreadcrumbContinuity`** (**`Boolean(isSignedIn)`**); signed-in fresh intake and **Update in chat** breadcrumb middle **Justice workspace** → **`/justice`**; signed-out fresh intake keeps **Action plan** → **`/justice/plan`**; signed-out update funnel breadcrumb unchanged; **`chatFirstWorkLinkContinuity`**, banner/Recap work links, handoff, proof CRUD, **Continue**, checklist, preview/packet/plan, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — signed-in fresh intake breadcrumb chat-first (`/justice/chat-ai`)**).
- **`/justice/chat-ai` signed-out fresh intake breadcrumb chat-first** is **shipped**: **`chat-ai/page.tsx`** only — breadcrumb **Justice workspace** → **`/justice`** when **`chatFirstBreadcrumbContinuity || !isUpdatingExistingCase`**; signed-out **Update in chat** keeps funnel **`activeCaseWorkHref`** / **`activeCaseWorkLabel`** — extends §2 **Chat AI intake — signed-in fresh intake breadcrumb chat-first (`/justice/chat-ai`)** to signed-out fresh-intake visitors; **`chatFirstWorkLinkContinuity`**, banner/Recap work links, **`handleSend`**, **`handleContinueToPreview`**, handoff, proof CRUD, checklist, preview/packet/plan, APIs, schema, auth unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — signed-out fresh intake breadcrumb chat-first (`/justice/chat-ai`)**).
- **`/justice/chat-ai` resolveActiveCaseWorkHref post-packet-approved chat-first** is **shipped**: **`chat-ai/page.tsx`** only — **`resolveActiveCaseWorkHref`** / **`resolveActiveCaseWorkLabel`** post-packet branch **`/justice/chat-ai`** / **Continue in chat** replaces **`/justice/plan`** / **Action plan**; preview/packet branches unchanged — **completes** signed-out **Update in chat** post-packet-approved funnel alignment with §2 **Preview — session caseId Continue returns to chat (`/justice/preview`)** (#539) and §2 **Packet — session caseId approve returns to chat (`/justice/packet`)** (#540); **`activeCaseSecondaryWorkLink`**, **`chatFirstWorkLinkContinuity`**, breadcrumb hub logic, **`handleSend`**, **`handleContinueToPreview`**, handoff, proof CRUD, checklist, preview/packet/plan, APIs, schema, auth unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — resolveActiveCaseWorkHref post-packet-approved chat-first (`/justice/chat-ai`)**).
- **`/justice/chat-ai` activeCaseSecondaryWorkLink mid-funnel plan de-bias** is **shipped**: **`chat-ai/page.tsx`** only — **`activeCaseSecondaryWorkLink`** mid-funnel branch (**draft reviewed, packet not approved**) **`null`** replaces secondary **Action plan** → **`/justice/plan`** — **removes** signed-out **Update in chat** plan skip-ahead before packet approval; primary **Review prepared case packet** → **`/justice/packet`** unchanged; post-packet secondary **Submission preview** unchanged; **`resolveActiveCaseWorkHref`** / **`resolveActiveCaseWorkLabel`**, **`chatFirstWorkLinkContinuity`**, breadcrumb hub logic, handoff, checklist, preview/packet/plan, APIs, schema, auth unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — activeCaseSecondaryWorkLink mid-funnel plan de-bias (`/justice/chat-ai`)**).
- **`/justice` hub Needs more info chat routing CTA on Current case** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only — when **Current case** exists: **Update in chat** when basics incomplete, **Add proof in chat** when basics OK and loaded **`evidenceCount < 1`**; both → **`/justice/chat-ai`**; existing **`GET /api/justice/evidence`** for active **UUID** session case when signed in; primary nested card target — §2 **Justice hub — Current case primary CTA routing (`/justice`)**; compact readiness checklist / handling blocks / hub link list unchanged on this slice — **no** filing/submission/contact/queue/upload/storage/automatic-action claims; **no** plan/preview/chat-ai/cases/API/schema changes beyond prior slice (§2 **Justice hub — Needs more info chat routing CTA on Current case (`/justice`)**).
- **`/justice` hub Current case primary CTA routing** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only — **`primaryHref`** **`/justice/chat-ai`** and **`primaryLabel`** **Continue in chat** on nested **Current case** card regardless of draft-review or prepared-packet state; checklist **Review submission draft** → **`/justice/preview`** and **Review prepared case packet** → **`/justice/packet`** unchanged; prepared-packet row, approved-next lines, handling, evidence fetch, chat routing CTAs, hub link list, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Justice hub — Current case primary CTA routing (`/justice`)**).
- **`/justice` hub remove plan-first resume card when no Current case** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only — removes **Continue current action plan** → **`/justice/plan`** link-list card when **`!snapshot`**; **Start with chat intake**, **Use step-by-step chat**, **Start with form intake**, **Saved cases**, **Current case** card, checklist, handling blocks, **`primaryHref`**, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Justice hub — remove plan-first resume card when no Current case (`/justice`)**).
- **`/justice` hub approved-packet handling request copy chat-first** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only — both **Current case** approved-packet helper blocks second sentence **Request Surrenderless handling from chat intake, the case packet, or here on the hub…** replaces **…from your action plan, case packet…**; first sentence, **View on handling workbench**, **`primaryHref`**, checklist, handling tracking UI, hub link list, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Justice hub — approved-packet handling request copy chat-first (`/justice`)**).
- **`/justice/packet` approved-packet handling request copy chat-first** is **shipped**: **`packet/page.tsx`** only — approved-packet emerald helper card second sentence **Request Surrenderless handling from chat intake when you want internal triage tracking.** replaces **Request handling from your action plan…**; first sentence, **View on handling workbench**, **Continue from action plan**, breadcrumbs, **Update in chat**, approve → chat routing, handling-request UI, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — approved-packet handling request copy chat-first (`/justice/packet`)**).
- **`/justice/packet` approved-step explainer chat-first** is **shipped**: **`packet/page.tsx`** only — **`getApprovedPacketNextStepExplainer`** **approved** branch and matching top emerald explainer JSX **open it below or continue in chat. Nothing is sent automatically.** replaces **…continue from your action plan…**; started/completed branches, **Continue from action plan**, **Back to action plan**, approve → chat routing, handling-request UI, breadcrumbs, **Update in chat**, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — approved-step explainer chat-first (`/justice/packet`)**).
- **`/justice/packet` started-state record-handled copy chat-first ordering** is **shipped**: **`packet/page.tsx`** only — **`getApprovedPacketNextStepExplainer`** **started** branch and matching top emerald explainer JSX record-handled clause **from chat intake, or from your action plan when ready** replaces **from your action plan, or from chat intake when ready**; approved/completed branches, **Continue from action plan**, **Back to action plan**, approve → chat routing, handling-request UI, breadcrumbs, **Update in chat**, **Open {label}**, record-handled behavior, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — started-state record-handled copy chat-first ordering (`/justice/packet`)**).
- **`/justice/packet` approved-step started explainer plan de-bias** is **shipped**: **`packet/page.tsx`** only — **`getApprovedPacketNextStepExplainer`** **started** branch and matching top emerald explainer JSX record-handled clause **from chat intake when ready** replaces **from chat intake, or from your action plan when ready**; approved/completed branches, **Back to action plan**, approve → chat routing, handling-request UI, breadcrumbs, **Update in chat**, inner approved card started copy, **Open {label}**, record-handled behavior, APIs, schema, auth, plan page unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — approved-step started explainer plan de-bias (`/justice/packet`)**).
- **`/justice/packet` inner approved card Continue in chat link** is **shipped**: **`packet/page.tsx`** only — inner **Packet approved for next action** card **`Link`** **Continue in chat** → **`/justice/chat-ai`** replaces **Continue from action plan** → **`/justice/plan`**; **Back to action plan**, top explainers, **View on handling workbench**, handling-request UI, **Open {label}**, record-handled, approve → chat routing, breadcrumbs, **Update in chat**, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — inner approved card Continue in chat link (`/justice/packet`)**).
- **`/justice/packet` inner approved card approved-state body copy chat-first** is **shipped**: **`packet/page.tsx`** only — inner **Packet approved for next action** card **approved** branch body **Continue in chat when you are ready for the next in-app step.** replaces **Continue from your action plan when you are ready for the next in-app step.**; **Continue in chat** link, started/completed branches, **Back to action plan**, top explainers, approve → chat routing, handling-request UI, **Open {label}**, record-handled, breadcrumbs, **Update in chat**, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — inner approved card approved-state body copy chat-first (`/justice/packet`)**).
- **`/justice/packet` top emerald card Continue in chat link** is **shipped**: **`packet/page.tsx`** only — top emerald **Your prepared action review** / **Your approved case packet** card footer **`Link`** **Continue in chat** → **`/justice/chat-ai`** replaces **Back to action plan** → **`/justice/plan`**; breadcrumb row, **Approve for next action** checkpoint, inner approved card, handling UI, filing records, approve → chat routing, explainer copy, **Update in chat** breadcrumb, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — top emerald card Continue in chat link (`/justice/packet`)**).
- **Handled/open triage redirect note chat-first acknowledge ordering** is **shipped**: **`approvedNextActionHandlingDisplay.tsx`** only — **`HANDLING_HANDLED_OPEN_TRIAGE_NOTE_REDIRECT`** **Acknowledge it from chat intake or the action plan…** replaces **…from the action plan or chat intake…**; **`inlineAck`**, **`resolveHandlingTrackingContextualLink`**, **Mark acknowledged** UI/behavior, handling **Awaiting** intro, empty-state copy, hub/packet/cases/handling **`redirect`** display conditions, APIs, schema, routing unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Approved next action — handled/open triage redirect note chat-first acknowledge ordering (`approvedNextActionHandlingDisplay.tsx`)**).
- **`/justice/handling` approved-packet handling request copy chat-first** is **shipped**: **`handling/page.tsx`** only — **`ApprovedPacketActionCaseCard`** helper and **Approved packet actions** section intro plan-first clause **Request/request Surrenderless handling from chat intake when you want internal triage tracking.** replaces **…from your action plan…**; first sentence, section lead-in, **Open action plan**, **Open case packet**, **Update in chat**, handling-request triage cards, record-handled / acknowledge UI, navigation handlers, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Handling workbench — approved-packet handling request copy chat-first (`/justice/handling`)**).
- **`/justice/handling` approved-packet card Update in chat primary navigation** is **shipped**: **`handling/page.tsx`** only — on **`ApprovedPacketActionCaseCard`**, **Update in chat** first + **`navButtonPrimaryCls`**, **Open action plan** second + **`navButtonSecondaryCls`**, **Open case packet** / **Open approved step** unchanged; same **`onOpenChat`** / **`onOpenActionPlan`** / **`navigateWithCase`** handlers and destinations; **`HandlingWorkbenchCaseCard`** triage cards, approved-packet copy, handling-request triage, **Mark acknowledged**, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Handling workbench — approved-packet card Update in chat primary navigation (`/justice/handling`)**).
- **`/justice/handling` triage card Update in chat primary navigation** is **shipped**: **`handling/page.tsx`** only — on **`HandlingWorkbenchCaseCard`** (**Awaiting internal triage**, nested **Handled — open handling request**, **Acknowledged**), **Update in chat** first + **`navButtonPrimaryCls`**, **Open action plan** second + **`navButtonSecondaryCls`**, **Open case packet** unchanged (secondary, same **`compactNavigation`** gate); manual filing records note, **Open approved step**, **Mark acknowledged** unchanged; same **`onOpenChat`** / **`onOpenActionPlan`** / **`navigateWithCase`** handlers and destinations; **`ApprovedPacketActionCaseCard`**, approved-packet copy, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Handling workbench — triage card Update in chat primary navigation (`/justice/handling`)**).
- **`/justice/handling` Awaiting intro acknowledge copy chat-first ordering** is **shipped**: **`handling/page.tsx`** only — **Awaiting internal triage** section intro **acknowledge from chat intake or your action plan** replaces **acknowledge from your action plan or chat intake**; **`HANDLING_HANDLED_OPEN_TRIAGE_NOTE_REDIRECT`** / **`inlineAck`**, **`resolveHandlingTrackingContextualLink`**, **Mark acknowledged** UI/behavior, triage card button order, empty-state copy, approved-packet sections, APIs, schema, routing unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Handling workbench — Awaiting intro acknowledge copy chat-first ordering (`/justice/handling`)**).
- **`/justice/handling` empty-state packet-approval copy chat-first** is **shipped**: **`handling/page.tsx`** only — **`!hasAnyWorkbenchContent`** empty state **Approve your prepared case packet from chat intake or on the case packet…** replaces **…on the action plan…**; **Awaiting** intro, triage card nav, **`redirect`** / **`inlineAck`** notes, approved-packet sections, **Mark acknowledged**, APIs, schema, routing unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Handling workbench — empty-state packet-approval copy chat-first (`/justice/handling`)**).
- **Handling tracking Mark acknowledged contextual link chat-first routing** is **shipped**: **`approvedNextActionHandlingDisplay.tsx`** only — **`resolveHandlingTrackingContextualLink`** **`HANDLING_TRACKING_STEP_MARK_ACKNOWLEDGED`** branch **Mark acknowledged in chat** → **`/justice/chat-ai`** replaces **Mark acknowledged on action plan** → **`/justice/plan`**; **`surface === "chat-ai"`** returns **`null`**; **`markAcknowledgedOnScreen`** suppression unchanged; **`RECORD_OUTCOME`** / **`REVIEW_FOLLOW_UP`** / filing / packet / open-step branches, triage notes, handling workbench copy, on-screen **Mark acknowledged** UI/behavior, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Approved next action — handling tracking Mark acknowledged contextual link chat-first routing (`approvedNextActionHandlingDisplay.tsx`)**).
- **Handling tracking Record outcome contextual link chat-first routing** is **shipped**: **`approvedNextActionHandlingDisplay.tsx`** only — **`resolveHandlingTrackingContextualLink`** **`HANDLING_TRACKING_STEP_RECORD_OUTCOME`** branch **Record outcome in chat** → **`/justice/chat-ai`** replaces **Record outcome on action plan** → **`/justice/plan`**; **`surface === "plan"`** and **`surface === "chat-ai"`** return **`null`**; Mark acknowledged branch, **`REVIEW_FOLLOW_UP`**, filing / packet / open-step branches, triage notes, handling workbench copy, outcome form UI/behavior, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Approved next action — handling tracking Record outcome contextual link chat-first routing (`approvedNextActionHandlingDisplay.tsx`)**).
- **Handling tracking Review follow-up contextual link chat-first routing** is **shipped**: **`approvedNextActionHandlingDisplay.tsx`** only — **`resolveHandlingTrackingContextualLink`** **`HANDLING_TRACKING_STEP_REVIEW_FOLLOW_UP`** branch **Review follow-up in chat** → **`/justice/chat-ai`** replaces **Review follow-up on action plan** → **`/justice/plan`**; **`surface === "plan"`** and **`surface === "chat-ai"`** return **`null`**; Mark acknowledged / Record outcome branches, filing / packet / open-step branches, triage notes, handling workbench copy, follow-up form/button UI/behavior, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Approved next action — handling tracking Review follow-up contextual link chat-first routing (`approvedNextActionHandlingDisplay.tsx`)**).
- **`/justice` hub Current case prepared packet reviewed checklist row** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only — **navigation/readiness**; **`packetApproved`** from **`justice_prepared_packet_approved_v1`** via **`readSessionPreparedPacketApproved`** in **`readSnapshotFromLocalSession`**; when **`snapshot.reviewed`**, checklist shows **Prepared case packet reviewed: yes/not yet** with **Review prepared case packet** → **`/justice/packet`** when not approved; existing **`refreshHubState`** triggers only — **no** new listeners; evidence fetch, handling blocks, approved-next labels, **Update in chat** / **Add proof in chat**, primary card routing — §2 **Justice hub — Current case primary CTA routing (`/justice`)** — routes, APIs, schema, handlers unchanged on this slice — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Justice hub — Current case prepared packet reviewed checklist row (`/justice`)**).
- **`/justice` hub Current case compact readiness checklist** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only — **Current case** shows checklist (**Basic case info**, **Evidence**, **Submission draft reviewed: yes/not yet**) from existing **`basicsReady`**, **`evidenceCount`**, **`snapshot.reviewed`**; inline **Update in chat** / **Add proof in chat** / **Review submission draft** when needed; **Prepared case packet reviewed** when draft reviewed — §2 **Justice hub — Current case prepared packet reviewed checklist row (`/justice`)**; **`showUpdateInChat`** / **`showAddProofInChat`** / primary card routing — §2 **Justice hub — Current case primary CTA routing (`/justice`)** — evidence fetch / handling unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims; **no** API/schema/helper/plan/preview/chat-ai/cases changes (§2 **Justice hub — Current case compact readiness checklist (`/justice`)**).
- **`/justice` hub approved next-step label and status on Current case** is **shipped**: **`JusticeHubWorkspaceBody.tsx`** only — **Current case** card shows read-only **Next step: {label}** and **Approved next action: {status}** from session **`approvedNext`** via **`readSessionApprovedNextAction`** + **`approvedNextActionStatusLabel`**; placed after submission-draft-reviewed line and before **`{primaryLabel}`** / in-card handling; readiness checklist, chat routing CTAs, queue status, workbench link, link list, Plan, Packet, Chat AI, Cases, APIs, schema **unchanged** — **visibility only**; **no** Open/Record handled/GET/PATCH — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Justice hub — approved next-step label and status on Current case (`/justice`)**).
- **`/justice/chat-ai` active case prepared packet reviewed checklist row (Update in chat)** is **shipped**: **`chat-ai/page.tsx`** only — **navigation/readiness**; **`preparedPacketApproved`** from **`justice_prepared_packet_approved_v1`** + existing case **GET** **`client_state.prepared_packet_approved`**; when **`activeCaseDraftReviewed`**, checklist shows **Prepared case packet reviewed: yes/not yet** with **Review prepared case packet** → **`/justice/packet`** when not approved; proof CRUD, **Continue** handoff, **`handleSend`**, Recap tracking, **Open**/**Record handled**, routes, APIs, schema, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged — **no** new API (§2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**).
- **`/justice/chat-ai` active case compact readiness checklist (Update in chat)** is **shipped**: **`chat-ai/page.tsx`** only — **Update in chat** **Active case** banner shows **Basic case info** / **Evidence** / **Submission draft reviewed: yes/not yet** from existing **`parts`**, **`savedEvidenceCount`**, session timeline; in-page **Continue in chat below** / **Add proof below** / **Review submission draft** → **`/justice/preview`**; **Prepared case packet reviewed** row when draft reviewed — §2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**; standalone draft-reviewed line replaced; trimmed focus line; **`handleSend`**, **Continue**, proof CRUD, staged flush, handoff, session-change summary unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims; **no** API/schema/helper/hub/plan/cases/preview changes (§2 **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`)**).
- **`/justice/chat-ai` page breadcrumb work-link funnel routing (Update in chat)** is **shipped**: **`chat-ai/page.tsx`** only — page breadcrumb work link uses **`activeCaseWorkLabel`** → **`activeCaseWorkHref`** when **`isUpdatingExistingCase`** (reuses **`resolveActiveCaseWorkHref`** / **`resolveActiveCaseWorkLabel`** from **`activeCaseDraftReviewed`** + **`preparedPacketApproved`** — no new helpers): **Submission preview** → **`/justice/preview`** when draft not reviewed; **Review prepared case packet** → **`/justice/packet`** when draft reviewed and packet not approved; **Action plan** → **`/justice/plan`** when packet approved; fresh intake keeps **Action plan** → **`/justice/plan`** — no secondary breadcrumb link; aligned with Active case banner and Recap work links (§2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**); **`handleSend`**, **`handleContinueToPreview`**, Recap, banner footers, proof CRUD, handling UI, APIs, session keys, schema, routes, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged — **no** new API (§2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**).
- **`/justice/chat-ai` active case banner work-link funnel routing (Update in chat)** is **shipped**: **`chat-ai/page.tsx`** only — **Active case** banner footer primary work link from **`resolveActiveCaseWorkHref`** / **`resolveActiveCaseWorkLabel`** using **`activeCaseDraftReviewed`** + **`preparedPacketApproved`**: **Submission preview** → **`/justice/preview`** when draft not reviewed; **Review prepared case packet** → **`/justice/packet`** when draft reviewed and packet not approved; **Action plan** → **`/justice/plan`** when packet approved; optional secondary link when draft reviewed (plan when primary is packet, preview when primary is plan); no duplicate **Submission preview** when primary is preview — aligned with hub primary CTA, preview **Continue**, saved cases **Open case** (§2 **Justice hub — Current case primary CTA routing (`/justice`)**, §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**, §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**); **Recap → Current action tracking** footer uses same resolved targets — §2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**; page breadcrumb work link — §2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**; checklist inline CTAs, **`handleSend`**, **`handleContinueToPreview`**, handoff panel, Recap label/status/Open/Record handled, proof CRUD, routes, APIs, schema, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged — **no** new API (§2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**).
- **`/justice/chat-ai` Recap current-action work-link funnel routing (Update in chat)** is **shipped**: **`chat-ai/page.tsx`** only — **Recap → Current action tracking** footer reuses **`activeCaseWorkHref`** / **`activeCaseWorkLabel`** / **`activeCaseSecondaryWorkLink`** from **`resolveActiveCaseWorkHref`** / **`resolveActiveCaseWorkLabel`** (**`activeCaseDraftReviewed`** + **`preparedPacketApproved`** — no new helpers): **Submission preview** → **`/justice/preview`** when draft not reviewed; **Review prepared case packet** → **`/justice/packet`** when draft reviewed and packet not approved; **Action plan** → **`/justice/plan`** when packet approved; optional secondary when draft reviewed only; no duplicate **Submission preview** when primary is preview — same funnel as **Active case** banner (§2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**); page breadcrumb work link — §2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**; signed-in de-bias — §2 **Chat AI intake — signed-in work-link funnel de-bias (Update in chat) (`/justice/chat-ai`)**; emerald footer styling unchanged; **`handleSend`**, **`handleContinueToPreview`**, handoff panel, proof CRUD, Active case checklist CTAs, Open/Record handled, handling UI, APIs, schema, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged — **no** new API (§2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**).
- **`/justice/chat-ai` signed-in work-link funnel de-bias (Update in chat)** is **shipped**: **`chat-ai/page.tsx`** only — when **`chatFirstWorkLinkContinuity`** (**signed-in** + **`isUpdatingExistingCase`**): hides **Active case** banner footer and **Recap → Current action tracking** footer funnel work links to preview/packet/plan; breadcrumb middle link **Justice workspace** → **`/justice`**; checklist **Review submission draft** → **`/justice/preview`** and **Review prepared case packet** → **`/justice/packet`** unchanged; optional approved-action **Open {label} (optional)** deep links unchanged; **`handleContinueToPreview`**, stay-in-chat guards, inline draft/packet approval, **Mark step opened** / **Record action handled**, proof CRUD, preview/packet/plan pages, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — signed-in work-link funnel de-bias (Update in chat) (`/justice/chat-ai`)**).
- **`/justice/chat-ai` active case context banner (Update in chat)** is **shipped**: **`chat-ai/page.tsx`** only — when **`isUpdatingExistingCase`**, compact **Active case** banner above main chat card confirms continuing same case (company/category/product, compact readiness checklist, trimmed focus line); funnel-aware footer work links — §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**; **`handleSend`**, **Continue**, proof CRUD, staged proof, handoff panel, session-change summary, prompts, APIs, schema, hub, cases, plan unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — active case context banner (Update in chat)**).
- **`/justice/chat-ai` approved next-step label/status on Active case banner (Update in chat)** is **shipped**: **`chat-ai/page.tsx`** only — when **`isUpdatingExistingCase`** and **`approvedNextAction`** exists, **Active case** banner shows read-only **Next step: {label}** when label set and **Approved next action: {Approved|Started|Handled}** via **`approvedNextActionStatusLabel`** (Hub/Cases wording); placed after readiness checklist, before focus line — **no** buttons; **`handleApprovedNextActionOpen`**, **`handleMarkApprovedNextActionHandled`**, hydrate, proof, Continue handoff, Recap label/status/Open/Record handled (footer work links — §2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**), Plan, Packet, Hub, Cases, APIs, schema unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**).
- **`/justice/chat-ai` session change summary (Update in chat)** is **shipped**: **`chat-ai/page.tsx`** + **`summarizeBuildJusticeIntakePartsSessionChanges.ts`** — baseline **`parts`** on hydrate; compares to current **`parts`** in same update session; **Updated in this chat** standalone panel when changes detected and Continue handoff not yet visible; when handoff visible, changes fold into handoff instead (§2 **Chat AI intake — Continue handoff session-change integration (Update in chat)**); company/website/what happened/requested outcome/contact details/consumer state; **Added proof note(s) this visit** when saved evidence count safely increases; hidden on fresh intake and no-change sessions; **`handleSend`**, **Continue**, commit, proof CRUD, staged proof, active-case banner, prompts, APIs, schema, hub, cases, plan unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — session change summary (Update in chat)**).
- **`/justice/plan` FTC practice preview** is **shipped**: inside **Step 3 — Escalate to FTC** when that **FTC fallback** Step 3 path is active (not when **CFPB**, **FCC**, or **DOT** is primary Step 3), **FTC is unlocked** and **mock practice is not yet completed**, a **collapsed** preview shows **metadata only** from **`JusticeIntake`** (company, category, optional money/date/email, truncated story) plus **practice-only** disclaimers (**not** a real FTC filing; **no** automatic government submission; **no** external FTC site). Users continue via **`/justice/ftc-review`** for the **full** internal practice form and autofill; **`/mock/ftc-complaint`** stays **internal QA** only. **No** **`submit-form`** / API / timeline changes from the preview, **no** automatic filings/evidence/tasks, **no** filing automation or embedded browser, **no** schema or new tables; **merchant**, **payment**, **evidence**, and **filing** plan previews unchanged.
- **`/justice/plan` BBB / State AG / CFPB / FCC / DOT / demand letter (`small_claims`) destination previews** are **shipped**: in **Other places this may go**, **BBB**, **State AG**, **CFPB**, **FCC**, **USDOT / aviation**, and **Small claims / demand letter** (**`small_claims`**) rows with **`internalRoute`** show a **collapsed** **metadata-only** block (company, category, optional money/date, optional state for AG only, merchant contact summary, truncated story) **before/near** the existing **Open** control — **no** full draft on the plan; guidance points users to copy the full draft on **`/justice/bbb`**, **`/justice/state-ag`**, **`/justice/cfpb`**, **`/justice/fcc`**, **`/justice/dot`**, or **`/justice/demand-letter`**; **manual prep**, **Surrenderless does not file** automatically. **`small_claims`** stays **`status: "later"`** and is **not** primary **Step 3**. **Ordering**, badges, **`Open`** links, and **`bbb_prep_opened` / `state_ag_prep_opened` / `cfpb_prep_opened` / `fcc_prep_opened` / `dot_prep_opened` / `demand_letter_prep_opened`** analytics follow the shipped pattern; **DOT** adds **`internalRoute`** when aviation hints match; **`small_claims`** uses **`internalRoute: "/justice/demand-letter"`**. **No** new backend APIs for this slice, **no** tasks, **no** automatic filings/evidence, **no** new timeline **types** from prep-open alone, **no** filing automation, **no** embedded browser, **no** schema or new tables, **no** preview-review tracking changes beyond this routing; **merchant**, **payment**, **evidence**, **filing**, **FTC** (fallback Step 3), and **final follow-up** previews **unchanged** except **DOT** may be **primary Step 3** when relevant (see §2).
- **`/justice/demand-letter` manual demand-letter prep** is **shipped**: neutral copy-ready letter text, case summary, company/person, story, requested resolution, evidence checklist, saved evidence list, user-driven filing records; **not** legal advice; **no** small-claims eligibility determination; **no** filing, court submission, embedded court browser, or automation; users directed to verify rules with **official / local court** resources; **no** schema/DB/task/auto-filing/auto-evidence changes (see §2).
- **`/justice/dot` USDOT / aviation manual prep** is **shipped**: copy-ready aviation complaint text, evidence checklist, saved evidence list, user-driven filing records; **no** automatic filing, **no** embedded DOT site, **no** government submission from the app, **no** new APIs, **no** schema/DB/task/auto-evidence changes. **`/justice/plan`** Step 3 priority: **CFPB → FCC → DOT → FTC fallback** (see §2).
- **`/justice` Consumer Justice workspace hub** is **shipped**: thin landing at **`src/app/justice/page.tsx`** with intro copy (organize issues into an action plan; drafts/checklists/records; **no** automatic filing); **recommended: start with chat intake** (AI conversational **`/justice/chat-ai`**); form intake available for users who prefer the structured form; link cards **Start with chat intake** (**`/justice/chat-ai`**) first, **Use step-by-step chat** (**`/justice/chat`**; §2), then plan, form intake, saved cases; optional **session-only Current case** block (**`JusticeHubWorkspaceBody`**) when local intake exists (see §2); **Current case** client refresh on **visible tab**, **window focus**, and **`storage`** (see §2); **Needs more info** **Update in chat** / **Add proof in chat** on **Current case** when basics or saved proof are missing (§2 **Justice hub — Needs more info chat routing CTA on Current case (`/justice`)**); static **Continue current action plan** card hidden then; also the **recovery hub** when resume/hydration fails — **`router.replace("/justice")`** / **`router.push("/justice")`** instead of **`/justice/intake`** (see §2 **Justice workspace recovery routing**). **Header** **Justice** → **Chat intake** (**`/justice/chat-ai`**) → **Consumer case** (see §2). Hub/header ordering is **UI only** — **no** case-list **`GET`** from the hub, **no** plan logic duplicate, **no** new APIs/schema/auth/persistence/LLM/prompt/parser/builder/commit/preview/plan/**`rules.ts`**/readiness changes beyond **read-only** session reads plus active-case evidence count for chat routing (§2). **Intake finish:** **`/justice/intake`** and **`/justice/chat`** route to **`/justice/preview`** after commit; **`/justice/chat-ai`** stays on chat after signed-in **UUID** **create** or **update** when continuity guards pass (§2 **Chat AI intake — inline submission draft review in Active case checklist (`/justice/chat-ai`)**), with **`/justice/preview`** fallback otherwise. **`/justice/plan`** remains the full action plan.
- **Next (workspace):** evolve beyond the thin **`/justice`** hub toward a richer shell with **case status**, **next actions**, and **approval previews** without scattering state across many routes — the hub is a **start**, not the final chat-first workspace.
- Reuse **timeline + tasks + filings** for “what happened / what’s next” instead of duplicating status in ad-hoc UI only.
- Keep **server timeline append** and **PATCH case** patterns for anything that must stay consistent under automation later.
- **Next:** extend **LLM-assisted** follow-ups and in-app action (fewer manual forms/tabs/external links; more guided steps inside the app) while keeping the same persistence primitives; avoid parallel ad-hoc state outside `JusticeIntake` + session keys. **Shipped:** hydrate **`/justice/chat-ai`** + **update** commit (§2); chat-ai **proof/evidence guidance** (§2); chat-ai **saved evidence count** in Recap via **`GET /api/justice/evidence`** (§2); chat-ai **Add a proof note** on **Update in chat** via existing **`POST /api/justice/evidence`** with optional **evidence date** / **description** (§2 **Chat AI intake — add proof note from Recap (Proof / evidence panel)**); chat-ai **Recent proof notes** when **N > 0** with inline **Edit** via **`PATCH /api/justice/evidence/[id]`** and **Delete** via **`DELETE /api/justice/evidence/[id]`** (§2 **Chat AI intake — recent saved proof preview in Recap (Proof / evidence panel)**, §2 **Chat AI intake — edit recent proof note in Recap (Proof / evidence panel)**, §2 **Chat AI intake — delete recent proof note in Recap (Proof / evidence panel)**); chat-ai **client-side proof keyword nudge** after successful send with local **Add a proof note** prefill (empty fields only — §2 **Chat AI intake — proof keyword nudge in Recap (Proof / evidence panel)**); chat-ai **fresh-intake staged proof notes** via **`stagedProofNotes.ts`**, flush after confirmed **create** with **cross-mode** pending visibility + retry flush to session **UUID** (§2 **Chat AI intake — staged proof notes on fresh intake (Proof / evidence panel)**); chat-ai **Continue handoff** **What happens next** panel (§2 **Chat AI intake — Continue handoff panel (What happens next)**); **preview proof on your case** card continuing Chat AI handoff (§2 **Preview — proof on your case card (Chat AI handoff continuity)**); **plan proof readiness continuity** after preview review (§2 **Plan — proof readiness continuity after preview review (`/justice/plan`)**); **plan Case readiness checklist proof CTA** on **Evidence added** when no proof (§2 **Plan — Case readiness checklist proof CTA (`/justice/plan`)**); **plan Case readiness checklist basics CTA** on **Basic case info present** when basics incomplete (§2 **Plan — Case readiness checklist basics CTA (`/justice/plan`)**); **plan Case readiness checklist draft-review CTA** (**Submission draft reviewed: yes/not yet**, **Review submission draft** → **`/justice/preview`** when not reviewed — §2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**); **plan Case readiness prepared packet reviewed checklist row** when draft reviewed (**Prepared case packet reviewed: yes/not yet**, **Review prepared case packet** → **`/justice/packet`** — §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**); **plan post-draft chat continuity prompt** with **Update in chat** after action plan shown (§2 **Plan — post-draft chat continuity prompt after preview review (`/justice/plan`)**); **plan post-draft re-preview continuity prompt** with **Review submission draft again** → **`/justice/preview`** after chat updates (§2 **Plan — post-draft re-preview continuity prompt after preview review (`/justice/plan`)**); **plan post-draft next-step explainer and readiness bridge** (**`getPreparedNextStepExplainer`**, no auto-file/submit copy, **Before you escalate, finish readiness:** chat/preview links when **`!readyToEscalate`**; **Review prepared next step** CTA unchanged — §2 **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`)**); **packet prepared review explainer and readiness bridge** on **`/justice/packet`** pre-approval card (**`getPreparedPacketReviewExplainer`**, **`stepLabel`**, **Before you approve, finish readiness:**, **Update in chat** correction — §2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**); **packet post-approval approved-step explainer** on **`/justice/packet`** after approval (**`getApprovedPacketNextStepExplainer`**, **Your approved case packet**, **Open {label}** in-app prep/tracking clarity — §2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**); **saved cases Needs more info chat routing** (**Update in chat** / **Add proof in chat** on **All cases** cards — §2 **Saved cases — Needs more info chat routing CTA (`/justice/cases`)**); **saved cases compact readiness checklist** on each **All cases** card when progress loaded (§2 **Saved cases — compact readiness checklist on All cases cards (`/justice/cases`)**); **saved cases All cases prepared packet reviewed checklist row** when draft reviewed — **`client_state.prepared_packet_approved`**, **Review prepared case packet** → **`/justice/packet`** (§2 **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`)**); **hub Current case Needs more info chat routing** (§2 **Justice hub — Needs more info chat routing CTA on Current case (`/justice`)**); **hub Current case compact readiness checklist** (basics/evidence/draft yes-not-yet + inline chat/preview links — §2 **Justice hub — Current case compact readiness checklist (`/justice`)**); **hub Current case primary CTA routing** (preview when draft not reviewed, packet when draft reviewed and packet not approved, plan when packet approved — §2 **Justice hub — Current case primary CTA routing (`/justice`)**); **chat-ai active case compact readiness checklist** on **Update in chat** **Active case** banner (§2 **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`)**); **chat-ai active case context banner** when **Update in chat** (§2 **Chat AI intake — active case context banner (Update in chat)**); **chat-ai active case banner work-link funnel routing** on **Update in chat** (preview → packet → plan primary footer link; no duplicate preview — §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**); **chat-ai Recap current-action work-link funnel routing** on **Update in chat** (same resolved targets as Active case banner in **Current action tracking** footer — §2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**); **chat-ai session change summary** on **Update in chat** (§2 **Chat AI intake — session change summary (Update in chat)**); **chat-ai Continue handoff session-change integration** (§2 **Chat AI intake — Continue handoff session-change integration (Update in chat)**); **preview chat-update continuity card** after **Update in chat** Continue (§2 **Preview — chat-update continuity card (Chat AI handoff)**); **preview pre-plan chat correction prompt** (§2 **Preview — pre-plan chat correction prompt (`/justice/preview`)**); **preview Continue routes to packet when prepared packet not approved** (§2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**); **saved cases Open case funnel routing** (§2 **Saved cases — Open case chat-first routing (`/justice/cases`)**); **chat-ai Continue handoff funnel copy alignment** (§2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)**); **evidence Update in chat breadcrumb return** (§2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**); **packet Update in chat breadcrumb return** (§2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**); **chat-ai page breadcrumb work-link funnel routing** (§2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**). **Still to do:** richer in-chat proof (**LLM/system-prompt** proof nudges in **`buildJusticeIntakeAiPrompt.ts`** — separate from client keyword nudge §2; fresh-intake keyword nudge; staged proof edit/delete); structured-form safe update on submit; timeline “intake updated” milestone if needed; filing queue and follow-up tracking (§1) — **without** expanding auto-filing until product-ready.

---

## 6. Current technical map

### Major app routes (`src/app/**/page.tsx`)

| Area | Paths (representative) |
|------|-------------------------|
| Legacy / misc | `/` (home), `/dashboard`, `/admin`, `/sign-in`, `/debug/me` |
| Justice | **`/justice`** (thin Consumer Justice **workspace hub** — **recommends AI chat intake**; **Current case** re-read from local session/timeline helpers on **tab visible**, **window focus**, **`storage`** (§2); **recovery hub** when resume/hydration fails (**`router.replace("/justice")`** / **`router.push("/justice")`** fallback; §2 **Justice workspace recovery routing**); form intake for structured form; optional **session-only** **Current case** card + **conditional** hub links via **`JusticeHubWorkspaceBody`** when valid local intake: static **Continue current action plan** card **hidden**; link cards **chat first** (**Start with chat intake** → **`/justice/chat-ai`**, **Use step-by-step chat** → **`/justice/chat`**; §2), then plan or intake/cases; explains organize → action plan, drafts/checklists/records, **no** automatic filing; **no** server case **`GET`** on hub, **no** plan duplicate; hub/header ordering **UI-only**), **`/justice/intake`** (form intake → **`/justice/preview`** → plan; **create** commit only), **`/justice/chat-ai`** (primary **AI conversational** intake + **Update in chat**; mount hydrate via **`readValidLocalJusticeIntake`** + **`justiceIntakeToBuildJusticeIntakeParts`**; Recap **Proof / evidence** guidance + **Organize evidence** → **`/justice/evidence`**; **`POST /api/justice/intake-chat`**; **`commitIntakeToSessionAndServer`** **`create`** or **`update`**; evidence **not** required before preview; §2), **`/justice/chat`** (scripted step-by-step chat intake; **`buildJusticeIntakeFromParts`** + **`validateContactProofForIntake`** → **`/justice/preview`** → plan; **unchanged** scripted Q&A; **create** commit; §2), **`/justice/plan`** (readiness + recommendations; breadcrumb **Update in chat** → **`/justice/chat-ai`** (hydrate/update), **Edit structured form** → **`/justice/intake`**; signed-out **Start a new case** → **`/justice`**; §2; existing **`GET /api/justice/cases/[id]`** / list hydrate reads **`client_state.prepared_packet_approved`** + session fallback; when approved after packet, **Prepared packet approved for next action** callout + **View approved case packet** / **View case packet** — §2 **Plan — prepared packet approval surfaced**; after **`submission_draft_reviewed`**, when user has not moved on: **Submission draft reviewed** + optional **Surrenderless prepared your next step** (**`pickPreparedNextAction`**: primary **`Review prepared next step`** → **`/justice/packet`**, **not filed automatically** copy; full plan below — §2 **Plan — prepared next action**; **UI-only**, **no** new APIs/schema/timeline writes beyond existing milestone); **`recommendationText`** in **Recommended next** panel; **accent** on rows **already** **Recommended next**; **compact Suggested message** preview for merchant/company contact when **recommended**, same helper as **`/justice/merchant`**; **separate Final follow-up before escalation** preview on the same card when contact is saved, **FTC** unlocked, unresolved, and first-contact preview not shown — plan-only deterministic copy + saved-contact summary, link **`/justice/merchant`**; **compact Suggested bank/card dispute letter** preview when payment dispute is **available/recommended**, same helper as **`/justice/payment-dispute`**; **saved evidence metadata** preview under Evidence / proof when signed in and rows exist, same **`GET /api/justice/evidence`** as readiness; **saved filing metadata** preview under Filing records when signed in and rows exist, same **`GET /api/justice/filings`** as readiness; **Step 3** escalation priority **CFPB → FCC → DOT → FTC fallback**; **FTC practice preview** inside **Step 3 — Escalate to FTC** when that **fallback** path is active, **FTC** is unlocked and mock practice is not yet completed — **metadata-only** from **`JusticeIntake`**, collapsed/truncated UI, practice-only copy, link to **`/justice/ftc-review`** for full internal practice; **primary Step 3 — Escalate to DOT** with CTA to **`/justice/dot`** when aviation hints match and CFPB/FCC are not primary; **Other places this may go**: **collapsed metadata-only** previews on **BBB**, **State AG**, **CFPB**, **FCC**, **DOT**, and **Small claims / demand letter** (**`small_claims`**) rows when **`internalRoute`** is set — manual prep copy, truncated story, **no** full draft on plan; **`small_claims`** stays **later** (not primary Step 3); **Open** links include **`/justice/bbb`**, **`/justice/state-ag`**, **`/justice/cfpb`**, **`/justice/fcc`**, **`/justice/dot`**, **`/justice/demand-letter`**), **`/justice/preview`** (deterministic + optional AI-assisted submission draft preview; no-case **Chat intake** → **`/justice/chat-ai`**, **Step-by-step chat** → **`/justice/chat`**, **Structured form** → **`/justice/intake`**; §2 **Justice-wide navigation alignment**), **`/justice/merchant`** (full deterministic contact message + copy + save contact/proof), **`/justice/payment-dispute`** (full payment dispute checklist + copy + save **`payment_dispute_draft`** / timeline), **`/justice/ftc-review`** (full internal FTC practice review + optional autofill; not a real government filing), **`/justice/bbb`** (BBB complaint prep + copy-ready draft; manual filing), **`/justice/state-ag`** (State AG complaint prep + copy-ready draft; manual filing), **`/justice/cfpb`** (CFPB complaint prep + copy-ready draft; manual filing), **`/justice/fcc`** (FCC complaint prep + copy-ready draft; manual filing), **`/justice/dot`** (USDOT / aviation consumer complaint prep + copy-ready draft + checklist + saved evidence + user-driven filing records; manual filing only; **no** embed, **no** auto-submit), **`/justice/demand-letter`** (manual demand-letter prep from **`JusticeIntake`**; not legal advice; **no** small-claims eligibility determination; **no** court filing or embed; copy-ready letter + checklist + saved evidence + user-driven filing records), **`/justice/evidence`** (full evidence add/edit/delete; breadcrumb **Update in chat** → **`/justice/chat-ai`** — §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**), `/justice/cases` (**Start new case** → **`/justice`** after **`clearLocalJusticeSession`**; breadcrumb **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** — §2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**; **Needs attention** + conditional **readiness helper** when not ready; §2), `/justice/cases/archived`, **`/justice/packet`** (packet export + **`JusticeFilingRecords`** filing CRUD; breadcrumb **Update in chat** → **`/justice/chat-ai`** in main and no-**`caseId`** empty-state rows — §2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**) |
| Internal QA | `/mock/ftc-complaint` |

### Major Justice components (`src/app/components/`)

- **`Header.tsx`** — Global nav links (order): **Justice** (`/justice` workspace hub), **Chat intake** (`/justice/chat-ai`), **Consumer case** (`/justice/intake` structured form), plus Clerk controls; used across Justice pages. Promotes **AI chat-first** entry (§2 hub/header + §2 **Justice-wide navigation alignment** on preview/plan/prep); scripted **`/justice/chat`** **Q&A** unchanged; all intake paths route to **`/justice/preview`** after commit (**`/justice/intake`** and **`/justice/chat`**; **`/justice/chat-ai`** stays in chat when signed-in **UUID** continuity guards pass — §2);
- **`JusticeActionResumeSignInPrompt.tsx`** — Hydration gate when session/user is insufficient; **Start new case** routing targets **`/justice`** workspace (chat-first hub) where aligned with §2 recovery routing.
- **`JusticeSavedEvidenceList.tsx`** — Evidence list for current session case (prep pages).
- **`JusticeFilingRecords.tsx`** — Filing CRUD for current case.
- **`JusticeCaseTasks.tsx`** — Task CRUD + due badges; used on **plan** and **packet** (not every prep page).

### Justice API routes (`src/app/api/justice/`)

- **`cases`** — `GET` list (non-archived default; `?archived=1` for archived); optional **`limit`** (default **10**, max **50**) and **`offset`** pagination; JSON **`{ cases, has_more, offset, limit }`**; `POST` create.
- **`cases/[id]`** — `GET` one case; `PATCH` intake, timeline, drafts, `archived_at`, `case_label`.
- **`evidence`**, **`evidence/[id]`** — List/create; update/delete; POST may append server timeline (`evidence_added`).
- **`filings`**, **`filings/[id]`** — Same pattern; `filing_recorded` on create.
- **`tasks`**, **`tasks/[id]`** — Same pattern; `task_added` / completion-related updates.
- **`events`** — Authenticated analytics append (expects a `history`-style table in Supabase; **not defined in repo migrations** — see limitations).
- **`preview-draft`** — `POST` only: optional **AI-assisted** plain-text submission draft for **`/justice/preview`**. **Clerk** (`getUserOr401`), **`rateLimit`**, **`OPENAI_API_KEY`** server-side; validates intake + destination + bounded evidence/timeline; optional **`case_id`** with **`userOwnsJusticeCase`**; returns `{ draft }` JSON; **no** persistence or filing.
- **`intake-chat`** — `POST` only: **endpoint-only** AI conversational intake turn (§2). **Clerk** (`getUserOr401`), **`rateLimit`**, **`OPENAI_API_KEY`** server-side; body **`{ user_message, parts, conversation_history? }`**; returns **`{ assistantMessage, parts }`** (`BuildJusticeIntakeParts`); **`buildJusticeIntakeAiPrompt.ts`** + **`parseIntakeChatAiResponse.ts`**; **no** DB, timeline, case creation, or commit.
- **`submission-draft-reviewed`** — `POST` only: records **`submission_draft_reviewed`** on **`justice_cases.timeline`** (idempotent **`appendCaseTimelineEntry`**). **Clerk** (`getUserOr401`), UUID **`case_id`**, **`userOwnsJusticeCase`**; optional **`destination_label`** / **`used_ai`** for entry **detail**; returns **`{ timeline }`** for client merge (**`applyServerTimelineFromResponse`**). **No** filing records, tasks, or schema changes — **tracking only**.

### Supabase tables (from `supabase/migrations/`)

| Table | Purpose |
|-------|---------|
| **`justice_cases`** | `id`, `user_id` (text), `intake` (jsonb), `timeline` (jsonb), `payment_dispute_draft`, `client_state`, timestamps, `archived_at`, `case_label`. RLS enabled; **no policies on cases** — access enforced in API with Clerk `user_id` + admin client. |
| **`justice_case_evidence`** | Metadata evidence rows; optional `source_url`, `storage_note`. RLS policies use **`authenticated` / `auth.uid()`** — **not** how server routes access data (service role bypasses RLS). |
| **`justice_case_filings`** | Manual/external filing records. |
| **`justice_case_tasks`** | Follow-up tasks. |

**Referenced in code but not in repo migrations:** `users` (profile init upsert), `history` (justice events). Deployments must define these separately or migrations must be added.

### Key libraries (`src/lib/justice/`)

- **`types.ts`** — `JusticeIntake`, timeline types (including **`submission_draft_reviewed`**), **`JusticeApprovedNextAction`** (`label`, `href`, **`status: "approved" | "started"`**, `approved_at`, `started_at`) and **`JusticeCaseClientState`** (`prepared_packet_approved`, **`approved_next_action`** on **`justice_cases.client_state`**), **storage key constants**.
- **`commitIntakeToSessionAndServer.ts`** — Shared client helper used by **`/justice/intake`**, **`/justice/chat`**, and **`/justice/chat-ai`**. **`mode: "create"`** (default): new **`STORAGE_CASE_ID`**, **`clearTimelineForNewCase`**, **`case_started`**, clears FTC/mock keys, **`POST /api/justice/cases`** when signed in (session fallback on failure), server id/timeline merge, **`intake_completed`** analytics. **`mode: "update"`** (**`/justice/chat-ai`** when case hydrated): preserves **`STORAGE_CASE_ID`** and timeline, writes **`STORAGE_INTAKE`**, **`PATCH /api/justice/cases/[id]`** when signed in + UUID (no **`POST`**, no **`case_started`**, no FTC clears, no **`intake_completed`**). Callers **`/justice/intake`** and **`/justice/chat`** **`router.push("/justice/preview")`** then **`/justice/plan`** after preview continue; **`/justice/chat-ai`** uses **`shouldStayInChatAfterSignedInUuidCaseSave`** to stay in chat when signed-in **UUID** continuity guards pass.
- **`normalizeCompanyWebsite.ts`** — Shared normalizer for **`company_website`** on **`/justice/intake`** and **`/justice/chat`**: empty / `none` / `n/a` / `-` / `no` → `""`; values already starting with **`http://`** or **`https://`** unchanged; bare hosts (e.g. **`amazon.com`**) → **`https://amazon.com`**. Form intake uses **`type="text"`** (not **`type="url"`**) so bare domains are not blocked by the browser.
- **`buildJusticeIntake.ts`** — Shared **`BuildJusticeIntakeParts`** module: **`defaultBuildJusticeIntakeParts`**, forward **`buildJusticeIntakeFromParts`** (trims, **`normalizeCompanyWebsite`**, merges money + desired outcome into **`money_involved`**, conditional contact fields, **`consumer_us_state`**), reverse **`justiceIntakeToBuildJusticeIntakeParts`** (hydrate **`JusticeIntake`** for chat-ai/plan **Update in chat**), and **`validateContactProofForIntake`**. Used by **`/justice/chat`** and **`/justice/chat-ai`** before **`commitIntakeToSessionAndServer`** (**`create`** or **`update`**) → **`/justice/preview`** → **`/justice/plan`** (§2).
- **`timeline.ts`** — Session timeline store, sync helpers, many `append*Once` helpers, **`appendSubmissionDraftReviewedOnce`** / **`buildSubmissionDraftReviewedDetail`** / stable id for preview→plan review milestone.
- **`rules.ts`** — Destination computation and gating; **CFPB**, **FCC**, and **DOT/aviation** relevance (**`dotLikelyRelevant`**); **`internalRoute: "/justice/dot"`** when DOT hints match (unresolved plans); **`small_claims`** with **`internalRoute: "/justice/demand-letter"`** (**`status: "later"`**; not primary **Step 3**); FTC unlock and payment dispute availability.
- **`buildSubmissionDraftPreview.ts`** — Pure, deterministic plain-text draft for **`/justice/preview`** from **`JusticeIntake`**, selected destination label/id, and optional evidence titles (**source of truth**; no external AI).
- **`buildSubmissionDraftAiPrompt.ts`** — Pure system/user message builder for **`POST /api/justice/preview-draft`** (constraints: not legal advice, not filed, faithful to facts, plain text, no fake URLs); consumed only on the server.
- **`buildJusticeIntakeAiPrompt.ts`** — Pure message builder for **`POST /api/justice/intake-chat`**; model returns JSON **`{ assistantMessage, parts }`** aligned with **`BuildJusticeIntakeParts`**; explicit rules to fill **`purchase_or_signup`** when the complained-about object is clearly stated (§2 **AI intake — `purchase_or_signup` extraction**).
- **`parseIntakeChatAiResponse.ts`** — Request **`parts`** clamp/coerce, optional **`conversation_history`** cap, merge/parse model JSON before API response; imports **`defaultBuildJusticeIntakeParts`** from **`buildJusticeIntake.ts`** (re-exported for **`/justice/chat-ai`**); consumed only by **`intake-chat`** route. **Contract unchanged.**
- **`buildMerchantContactMessage.ts`** — Pure deterministic merchant/company contact letter from **`JusticeIntake`** (**`buildMerchantMessage`**, **`cfpbFinancialProductSummary`**); used by **`/justice/merchant`** and the **`/justice/plan`** compact preview (no sending, no new persistence from the preview alone).
- **`buildPaymentDisputeBankLetter.ts`** — Pure deterministic bank/card issuer dispute letter from checklist draft + **`JusticeIntake`** (**`buildBankLetter`**, **`buildDefaultPaymentDisputeDraft`**); used by **`/justice/payment-dispute`** and the **`/justice/plan`** compact preview (no sending automation, no new persistence from the preview alone).
- **`caseReadiness.ts`**, **`caseApiValidation.ts`**, **`hydrateActiveCaseFromServer.ts`**, **`clearLocalJusticeSession.ts`**, **`taskDueStatus.ts`**, **`useJusticeActionPageHydration.ts`** (**signed-in** resume-from-list failure → **`/justice`**; see §2 **Justice workspace recovery routing**), etc.

### Session / local storage keys (from `src/lib/justice/types.ts` and `clearLocalJusticeSession.ts`)

| Key constant | Storage key string | Purpose |
|--------------|-------------------|---------|
| `STORAGE_INTAKE` | `justice_intake_v1` | Active case intake JSON |
| `STORAGE_CASE_ID` | `justice_case_id` | Active case UUID |
| `STORAGE_TIMELINE_V1` | `justice_timeline_v1` | JSON map of case id → timeline entries |
| `STORAGE_PAYMENT_DISPUTE_CHECKLIST_DRAFT_V1` | `justice_payment_dispute_checklist_draft_v1` | Payment dispute draft |
| `STORAGE_FTC_MANUAL_UNLOCK` | `justice_ftc_manual_unlock` | Manual FTC unlock flag |
| (session) | `justice_ftc_mock_completed` | Mock FTC lane completion (cleared with full justice session clear) |

### Auth / server

- **Clerk:** `ClerkProvider` in `src/app/layout.tsx` and `src/app/providers.tsx` (duplicate provider wrap — known quirk).
- **Middleware:** `src/middleware.ts` — `clerkMiddleware`; optional `DEPLOY_PASSWORD` Basic Auth gate.
- **API auth:** `src/server/requireUser.ts` — `getAuth(request).userId` for Justice routes.

### Known limitations (non-exhaustive)

- **Large saved-case lists:** `GET /api/justice/cases` paginates (**`limit`** / **`offset`**, **`has_more`**); **`/justice/cases`** offers **Load more cases**. Very large offsets or huge histories can still be slow — prefer reasonable page sizes.
- **Dual timeline:** Session + DB can drift if sync fails or multiple tabs are used.
- **Pre-server case id:** Intake can generate a non-UUID id in edge environments; evidence/task APIs require UUID `case_id`. **`/justice/chat-ai` update commit** only **PATCH**es the server when the active id is already a UUID; local-only ids still get **`STORAGE_INTAKE`** updated.
- **Structured form edit:** **`/justice/intake`** submit still always uses **create** commit — editing a loaded case via the form can still duplicate cases (chat-ai **update** path does not apply there yet).
- **Chat-ai evidence:** On **Update in chat**, **`/justice/chat-ai`** reads **`GET /api/justice/evidence?case_id={id}`** via **`loadSavedEvidencePreview`** for a **saved evidence count** in Recap (§2 **Chat AI intake — saved evidence count in Recap (Proof / evidence panel)**), a collapsed **Recent proof notes** block when **N > 0** (up to **3** rows — optional date/truncated description; per-row **Edit** / **Delete** — §2 **Chat AI intake — recent saved proof preview in Recap (Proof / evidence panel)**, §2 **Chat AI intake — edit recent proof note in Recap (Proof / evidence panel)**, §2 **Chat AI intake — delete recent proof note in Recap (Proof / evidence panel)**), can **`POST`** via **Add a proof note** (required **title** + **evidence_type**; optional **evidence date** / **description** when trimmed non-empty — §2 **Chat AI intake — add proof note from Recap (Proof / evidence panel)**), can **`PATCH`** displayed recent rows (**title**, **evidence_type**, **`evidence_date`**/**`description`** including **`null`** clear — **no** **`source_url`**/**`storage_note`**; **no** **`applyServerTimelineFromResponse`** on edit), and can **`DELETE`** displayed recent rows (**confirm** dialog — **no** timeline append; **no** **`source_url`**/**`storage_note`**); **fresh intake** before first server commit can **stage** proof-note metadata locally (**`stagedProofNotes.ts`**, **`canStageProofNoteInChat`**) — **pending** count/list whenever staged rows exist (including after **Update in chat** hydrate); first pass flushes via existing **`POST /api/justice/evidence`** after **`commitIntakeToSessionAndServer`** confirms **`serverPersisted`**, or **cross-mode retry-flushes** to session **UUID** when intake + **UUID** case already exist (**no** duplicate **create** — §2 **Chat AI intake — staged proof notes on fresh intake (Proof / evidence panel)**); pending/local until **Continue** saves to case; does not show saved count, recent preview, add-to-server, edit, delete, or keyword nudge until **Update in chat** with server-backed case. After successful chat send on **Update in chat**, **client-side proof keyword nudge** may show a dismissible banner toward **Add a proof note** and **prefill empty** title/description from the triggering message (opens **`<details>`** for review; user must still **Save proof note** — §2 **Chat AI intake — proof keyword nudge in Recap (Proof / evidence panel)**). **Organize evidence** → **`/justice/evidence`**; evidence breadcrumb **Update in chat** return — §2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**. **`/justice/evidence`** remains the full list/edit/delete surface (and **`source_url`** / **`storage_note`**). Proof in chat has **metadata capture** on **Update in chat**, **fresh staging** before first server case, **keyword nudge**, and **local prefill** on **Update in chat**; **no** LLM/system-prompt proof capture, **no** auto-created evidence rows, **no** file uploads; staged/saved proof requires user action — not AI-inferred.
- **Evidence Update in chat breadcrumb return:** **`/justice/evidence`** breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (Preview/Plan link styling); completes **Chat AI → Organize evidence → Evidence → Update in chat** loop — **no** new API; evidence CRUD, APIs, form fields, empty/signed-out states, **Back to action plan**, **Saved cases**, **Home**, chat-ai proof panel, plan/preview proof cards, schema, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Evidence — Update in chat breadcrumb return (`/justice/evidence`)**).
- **Packet Update in chat breadcrumb return:** **`/justice/packet`** breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** in main and no-**`caseId`** empty-state rows (Preview/Evidence link styling); supports **Packet → Update in chat → Chat AI** continuity — **no** new API; packet approval checkpoint, emerald explainer/readiness bridge, filing records, evidence list, approved-next handling UI, APIs, session keys, schema, routes, **Back to action plan** href, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Packet — Update in chat breadcrumb return (`/justice/packet`)**).
- **Saved cases Update in chat breadcrumb return:** **`/justice/cases`** page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (Preview/Evidence/Packet link styling; PR **#376** / commit **`eede473`**); supports **Saved cases → Update in chat → Chat AI** chat-first breadcrumb continuity — **no** new API; **`openUpdateInChat`**, **`openCase`**, **`resolveOpenCaseHref`**, filters, Needs attention, All cases checklists, case APIs, session keys, schema, routes, **Back to action plan**, **Home**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Saved cases — Update in chat breadcrumb return (`/justice/cases`)**).
- **Handling workbench Update in chat breadcrumb return:** **`/justice/handling`** page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Saved cases** and before **Action plan** (**Saved cases** · **Update in chat** · **Action plan**; Preview/Evidence/Packet/Saved cases link styling; PR **#378** / commit **`115e295`**); supports **Handling workbench → Update in chat → Chat AI** chat-first breadcrumb continuity — **no** new API; **`openChat`**, **`navigateWithCase`**, card buttons, **Mark acknowledged**, filters, case APIs, session keys, schema, handling membership rules, routes, **Saved cases**, **Action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Handling workbench — Update in chat breadcrumb return (`/justice/handling`)**).
- **Merchant Update in chat breadcrumb return:** **`/justice/merchant`** page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; Preview/Evidence/Packet/Saved cases/Handling link styling; PR **#380** / commit **`608a515`**); supports **Merchant → Update in chat → Chat AI** chat-first breadcrumb continuity on the common approved-step prep surface — **no** new API; contact message generation, copy/save contact flow, **`PATCH`** case behavior, timeline writes, approved-step logic, handling UI, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Merchant — Update in chat breadcrumb return (`/justice/merchant`)**).
- **Payment dispute Update in chat breadcrumb return:** **`/justice/payment-dispute`** page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; Merchant link styling; PR **#382** / commit **`151f93d`**); supports **Payment dispute → Update in chat → Chat AI** chat-first breadcrumb continuity on the bank/card checklist prep surface — **no** new API; checklist logic, copy/save flow, **`payment_dispute_draft`**, **`PATCH`** case behavior, timeline writes, approved-step logic, plan preview, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Payment dispute — Update in chat breadcrumb return (`/justice/payment-dispute`)**).
- **BBB Update in chat breadcrumb return:** **`/justice/bbb`** page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; Merchant/Payment dispute link styling; PR **#384** / commit **`2fdf4f8`**); supports **BBB complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual BBB prep surface — **no** new API; complaint draft generation, copy flow, **`bbb_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **BBB — Update in chat breadcrumb return (`/justice/bbb`)**).
- **CFPB Update in chat breadcrumb return:** **`/justice/cfpb`** page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; BBB/Merchant/Payment dispute link styling; PR **#386** / commit **`5de8335`**); supports **CFPB complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual CFPB prep surface — **no** new API; complaint draft generation, copy flow, **`cfpb_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **CFPB — Update in chat breadcrumb return (`/justice/cfpb`)**).
- **FCC Update in chat breadcrumb return:** **`/justice/fcc`** page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; CFPB/BBB/Merchant/Payment dispute link styling; PR **#388** / commit **`35e3e7c`**); supports **FCC complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual FCC prep surface — **no** new API; complaint draft generation, copy flow, **`fcc_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **FCC — Update in chat breadcrumb return (`/justice/fcc`)**).
- **DOT Update in chat breadcrumb return:** **`/justice/dot`** page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; FCC/CFPB/BBB/Merchant/Payment dispute link styling; PR **#390** / commit **`d56f7be`**); supports **USDOT aviation complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual DOT prep surface — **no** new API; complaint draft generation, copy flow, **`dot_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **DOT — Update in chat breadcrumb return (`/justice/dot`)**).
- **Demand letter Update in chat breadcrumb return:** **`/justice/demand-letter`** page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; DOT/FCC/CFPB/BBB/Merchant/Payment dispute link styling; PR **#392** / commit **`051c35a`**); supports **Demand letter prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual **`small_claims`** demand-letter prep surface — **no** new API; letter generation, copy flow, **`demand_letter_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Demand letter — Update in chat breadcrumb return (`/justice/demand-letter`)**).
- **State AG Update in chat breadcrumb return:** **`/justice/state-ag`** page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; Demand letter/DOT/FCC/CFPB/BBB/Merchant/Payment dispute link styling; PR **#394** / commit **`7154ef5`**); supports **State AG complaint prep → Update in chat → Chat AI** chat-first breadcrumb continuity on the manual State AG prep surface — **no** new API; complaint draft generation, **`consumer_us_state`** selector, copy flow, **`state_ag_prep_opened`** / timeline writes, filing records UI, evidence list, plan destination previews, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **State AG — Update in chat breadcrumb return (`/justice/state-ag`)**).
- **FTC review Update in chat breadcrumb return:** **`/justice/ftc-review`** page-level breadcrumb adds **Update in chat** → **`/justice/chat-ai`** after **Back to action plan** (**Back to action plan** · **Update in chat**; State AG/Demand letter/DOT/FCC/CFPB/BBB/Merchant/Payment dispute link styling; PR **#396** / commit **`25144d7`**); supports **FTC practice review → Update in chat → Chat AI** chat-first breadcrumb continuity on the internal practice FTC review surface — **no** new API; FTC unlock redirect gate, practice autofill, success-panel **Back to action plan** button, auth gating, mock form behavior, timeline writes, filing records UI, plan FTC preview, session keys, schema, routes, **Back to action plan**, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **FTC review — Update in chat breadcrumb return (`/justice/ftc-review`)**).
- **Structured intake chat-first breadcrumb links:** **`/justice/intake`** page-level breadcrumb **Home · Chat intake · Step-by-step chat** with **Chat intake** → **`/justice/chat-ai`** and **Step-by-step chat** → **`/justice/chat`** (PR **#398** / commit **`2c35e2d`**); keeps structured intake connected back to the chat-first flow — **no** new API; form submit, validation, saved intake loading, preview routing, commit helper, session keys, schema, routes, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Structured intake — chat-first breadcrumb links (`/justice/intake`)**).
- **Step-by-step chat chat-first breadcrumb return:** **`/justice/chat`** page-level breadcrumb **Home · Chat intake · Action plan · Structured form** with **Chat intake** → **`/justice/chat-ai`** (PR **#400** / commit **`809412b`**); gives scripted step-by-step chat a visible return path to primary AI chat intake — **no** new API; scripted chat flow, questions, answers, form routing, preview routing, commit helper, session keys, schema, routes, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Step-by-step chat — chat-first breadcrumb return (`/justice/chat`)**).
- **Prepared next action shared helpers (`preparedNextAction.ts`):** **`preparedNextAction.ts`** exports **`pickPreparedNextAction`** and **`buildApprovedNextActionTarget`**; **`/justice/packet`** uses shared helpers for packet approval attach point (PR **#402** / commit **`bc61aef`**) — approval-to-action engine groundwork; preserves existing packet approval behavior and **`approved_next_action`** shape — **no** handling queue, timeline events, filing/submission/contact/upload/storage/automation (§2 **Approval-to-action engine — prepared next action shared helpers (`preparedNextAction.ts`)**).
- **Plan prepared next action shared helper usage:** **`/justice/plan`** imports shared **`pickPreparedNextAction`** from **`preparedNextAction.ts`**; page-local duplicate removed where covered (PR **#404** / commit **`037f892`**) — packet and plan share prepared next-action selection logic; preserves existing plan behavior — **`buildApprovedNextActionTarget`** remains packet-only; **no** packet approval behavior change, handling queue, timeline events, API, schema, DB, filing/submission/contact/upload/storage/automation (§2 **Approval-to-action engine — plan prepared next action shared helper usage (`/justice/plan`)**).
- **Handling workbench approved packet action visibility:** **`/justice/handling`** surfaces packet-approved next actions in **Approved packet actions** above triage buckets via **`parseApprovedPacketActionWithoutHandlingRequest`** (PR **#406** / commit **`b83acdd`**) — **first real approval-to-action behavior slice**; uses existing **`client_state`** only (**`prepared_packet_approved`**, **`approved_next_action`**, no **`handling_requested_at`**, not **completed**); honest copy distinguishes from Surrenderless handling requests; user-controlled **started** / **completed** writes on **`ApprovedPacketActionCaseCard`** only — §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**; **does not** auto-set **`handling_requested_at`**, change **`buildHandlingWorkbenchItems`** / **`isHandlingAwaitingTriageApprovedNextAction`**, packet approval persistence, **Request Surrenderless handling**, or triage bucket membership; outcome/follow-up read-only on handling-request triage cards — §2 **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`)**; signed-in server cases only (same as workbench today); **no** filing, submission, external queue, worker, timeline **type**, new API route, schema/migration, DB field, or persistence-model change beyond existing **`approved_next_action`** updates (§2 **Approval-to-action engine — approved packet action visibility on handling workbench (`/justice/handling`)**, §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**).
- **Handling workbench handling-request triage card outcome / follow-up read-only:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** shows read-only truncated **`outcome_note`**, **Follow-up needed**, and follow-up timing when present on **`item.next`** from existing workbench list **`GET`** **`client_state`** when **`status !== "completed"`** — manual-handling context for triage; completed cards use outcome/follow-up write — §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**; complements §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)** / §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)** / §2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)** writes and §2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)** / §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)** browse display — **does not** change **`buildHandlingWorkbenchItems`**, **`isHandlingAwaitingTriageApprovedNextAction`**, **Needs attention**, acknowledge **`PATCH`**, or approved-packet inbox — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench outcome / follow-up write on completed triage cards:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** shows **Record outcome / follow-up** form when **`status === "completed"`** — workbench write path on **`outcome_note`**, **`follow_up_needed`**, **`follow_up_at`** via **`GET` + merge + `PATCH`** + session sync (same pattern as **Mark acknowledged**); requires record handled when **`status === "started"`** — §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**; clear follow-up when **`follow_up_needed`** — §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**; complements §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)** / §2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)** / §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)** and read-only context — §2 **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`)** — **Tracking only — not automatic filing or submission.** — **does not** edit **`handling_request_note`**, change membership predicates, acknowledge **`PATCH`**, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**).
- **Handling workbench clear follow-up on triage cards:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** shows **Mark follow-up handled** when **`follow_up_needed === true`** — **`clearFollowUpFromApprovedNextAction`** + **`mergeClientStateWithClearedFollowUp`** + **`GET` + merge + `PATCH`** + session sync; preserves **`outcome_note`** and **`follow_up_at`**; clears Saved cases **Needs attention** follow-up inbox — same semantics as chat/cases — §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**; complements outcome write — §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)** and record handled — §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)** — **does not** edit **`handling_request_note`**, change **`isHandlingAwaitingTriageApprovedNextAction`**, handling-request **Needs attention** builders, acknowledge **`PATCH`**, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench record action handled on triage cards:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** shows **Record action handled for now** when **`status === "started"`** — persists **`completed`** + **`completed_at`** via **`mergeApprovedNextActionTrackingFields`** + **`mergeClientStateWithApprovedNextAction`** + **`GET` + merge + `PATCH`** + session sync; preserves **`handling_requested_at`** and **`handling_acknowledged_at`** — same pattern as **`ApprovedPacketActionCaseCard`** (§2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**) and chat/plan record-handled — §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**; requires **Open approved step** started persist when **`status === "approved"`** — §2 **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`)**; unlocks outcome/follow-up write — §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**; clear follow-up when **`follow_up_needed`** — §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)** — **Tracking only — not automatic filing or submission.** — **does not** change **`ApprovedPacketActionCaseCard`** membership rules, **`isHandlingAwaitingTriageApprovedNextAction`**, handling-request **Needs attention** builders, acknowledge **`PATCH`**, **`handling_request_note`** edit rules, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench Open approved step marks started on triage cards:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** **Open approved step** when **`status === "approved"`** and internal **`href`** exists — persists **`started`** + **`started_at`** via **`openApprovedPacketStep`** / **`mergeApprovedNextActionTrackingFields`** + **`GET` + merge + `PATCH`** + session sync; preserves **`handling_requested_at`** and **`handling_acknowledged_at`** — same started persist path as **`ApprovedPacketActionCaseCard`** (§2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**) and chat/packet/plan — §2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**; **Saving…** while persisting; helper when **`status === "approved"`**: **Tracking only — not automatic filing or submission.** — hidden once **`status`** is **`"started"`** or **`"completed"`** — §2 **Handling workbench — Open approved step visibility on handling-request triage cards (`/justice/handling`)**; unlocks record handled — §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**; outcome/follow-up write — §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)**; clear follow-up when **`follow_up_needed`** — §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)** — **does not** change **`ApprovedPacketActionCaseCard`** membership rules, **`isHandlingAwaitingTriageApprovedNextAction`**, handling-request **Needs attention** builders, acknowledge **`PATCH`**, **`handling_request_note`** edit rules, or other nav semantics — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench Open approved step visibility on triage cards:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** shows **Open approved step** and tracking helper only while **`status === "approved"`** — hidden after started/completed; started cards use **Opened for next step.** + **Record action handled for now** — §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)** → outcome/follow-up write — §2 **Handling workbench — outcome / follow-up write on completed handling-request triage cards (`/justice/handling`)** → clear follow-up when **`follow_up_needed`** — §2 **Handling workbench — clear follow-up on handling-request triage cards (`/justice/handling`)**; started persist when approved unchanged — §2 **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`)** — **does not** change **`ApprovedPacketActionCaseCard`**, membership predicates, acknowledge **`PATCH`**, outcome/clear-follow-up writes — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — Open approved step visibility on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench nested Handled — open handling request Mark acknowledged:** **`/justice/handling`** nested **`completedUnacknowledgedItems`** cards under **Awaiting internal triage** show **Mark acknowledged** via existing **`acknowledgeHandlingRequest`** + **`GET` + merge + `PATCH`** — same acknowledgement path as active **Awaiting** triage cards and §2 **Plan — acknowledge handling request in Surrenderless handling UI** / §2 **Chat AI intake — acknowledge handling request in Current action tracking** / §2 **Saved cases — acknowledge handling request from Needs attention**; nested helper and per-card **`inlineAck`** triage note updated; manual-handling handoff context on triage cards — §2 **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`)**; **internal tracking triage only** disclaimers unchanged — **does not** change **`isHandlingAwaitingTriageApprovedNextAction`**, **Needs attention**, approved-packet inbox, outcome/follow-up fields, plan/chat/cases/hub/packet, or routing — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)**).
- **Handling workbench manual-handling handoff context on triage cards:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** on **Awaiting**, nested **Handled — open handling request**, and **Acknowledged** shows read-only **Handled for now {time}** from **`approved_next_action.completed_at`**; packet-approved and draft-reviewed via approval-funnel checklist — §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)** (**single source** after dedupe — §2 **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`)**); **Awaiting** helpers point handled-open cases to nested section / workbench **Mark acknowledged** — §2 **Handling workbench — Mark acknowledged on nested Handled — open handling request cards (`/justice/handling`)** — **does not** change membership, acknowledge **`PATCH`**, or routing — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench submission draft reviewed on triage cards:** **`/justice/handling`** draft-reviewed signal via **`caseDraftReviewed`** — shown only inside approval-funnel checklist — §2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)** (**single source** after dedupe — §2 **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`)**); standalone lines removed — read-only review context only — **does not** change membership, acknowledge **`PATCH`**, routing, or timeline writes — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — submission draft reviewed read-only on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench approval-funnel readiness summary on triage cards:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** shows read-only **Manual review readiness (approval funnel)** checklist + **Ready for manual review** / **Needs more before manual review** rollup from already-loaded list **`GET /api/justice/cases`** — basics + draft reviewed + packet approved; evidence **intentionally omitted** — **single source** after dedupe — §2 **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`)** — read-only only — **does not** change membership, acknowledge **`PATCH`**, routing, or timeline writes — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — approval-funnel readiness summary on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench saved manual filing records on triage cards:** **`/justice/handling`** fetches **`GET /api/justice/filings?case_id={id}`** for handling-request case ids only after list load; **`HandlingWorkbenchCaseCard`** shows read-only **Saved manual filings** collapsed preview or **not recorded yet** when **Ready for manual review** — filing record writes on **`/justice/packet`** via **Open case packet** — **no** workbench inline form or filing **`POST` / `PATCH`** — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — saved manual filing records read-only on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench saved proof/evidence readiness on triage cards:** **`/justice/handling`** extends handling-request per-case fetch with **`GET /api/justice/evidence?case_id={id}`** (count only); **`HandlingWorkbenchCaseCard`** shows read-only **Evidence on case** in approval-funnel checklist — separate from **Ready for manual review** rollup — honest **no saved proof** line when ready and count **0** — evidence writes on packet/evidence/chat — **no** workbench inline form or evidence **`POST` / `PATCH`** — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — saved proof/evidence readiness on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench case handoff summary on triage cards:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** shows collapsed read-only **Case handoff summary** (company, category, truncated story, next step) from already-loaded list **`GET /api/justice/cases`** — full bundle on **`/justice/packet`** — **no** new API, copy/export, or external links — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — case handoff summary on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench internal operator note on triage cards:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** read/edit **`handling_operator_note`** on **`client_state.approved_next_action`** — workbench-only; distinct from user **`handling_request_note`** — **`GET` + merge + `PATCH`** — **not** on plan/chat/packet/hub/cases — **no** new API route, DB migration, timeline write, worker, queue, filing/submission automation, or external action (§2 **Handling workbench — internal operator note on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench external manual action readiness on triage cards:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** shows read-only **Ready for external manual action** + collapsed **Suggested manual channels** (recommended destination labels only) from **`computeJusticeDestinations`** — separate from **`readyForManualReview`** rollup — after case handoff summary — prep on packet/approved step — **no** new API, **`client_state`**, links, or automation — **does not** change APIs, schema, timeline writes, workers, queues, or external action (§2 **Handling workbench — external manual action readiness on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench filing confirmation tracking on triage cards:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** shows read-only **Manual filing recorded** + **Confirmation on file** rollup from existing **`savedFilings`** — post-action nudges when **started**/**completed** — collapsed filing preview remains detail — writes on **`/justice/packet`** — **no** new API, **`client_state`**, or inline form — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — filing confirmation tracking on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench manual action progress on triage cards:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** shows collapsed read-only **Manual action progress** lifecycle yes/not yet lines + plain-text **Next step** from **`deriveManualActionNextStep`** — after confirmation rollup — existing blocks unchanged — **no** new API, **`client_state`**, buttons, or links — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench follow-up carry-forward on triage cards:** **`/justice/handling`** page-level **Follow-up tracking** section + **Manual action progress** follow-up lines + **`deriveManualActionNextStep`** follow-up nudge — existing **Mark follow-up handled** unchanged — **no** new API, **`client_state`**, or write paths — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — follow-up carry-forward on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench operator handoff queue tiers on Awaiting:** **`/justice/handling`** nested readiness-tier sublists under **Awaiting internal triage** from **`awaitingHandoffTiers`** — flat list when **`!filingsReady`** — **no** new API, **`client_state`**, badges, or card actions — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — operator handoff queue tiers on Awaiting internal triage (`/justice/handling`)**).
- **Handling workbench confirmation follow-up queue:** **`/justice/handling`** page-level **Confirmation follow-up** section from **`postExternalConfirmationTiers`** — nested **Manual filing not recorded yet** / **Confirmation not on file yet** sublists — before **Follow-up tracking** — **no** new API, **`client_state`**, badges, or card actions — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — confirmation follow-up queue on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench outcome closure queue:** **`/justice/handling`** page-level **Outcome closure** section from **`outcomeClosureTiers`** — nested **Outcome not recorded yet** / **Handling request not acknowledged yet** sublists — after **Confirmation follow-up**, before **Follow-up tracking** — **no** new API, **`client_state`**, badges, or card actions — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — outcome closure queue on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench tracking complete queue:** **`/justice/handling`** page-level **Tracking complete** section from **`trackingCompleteHandlingItems`** — **`deriveManualActionNextStep`** → **Tracking complete for now.** — after **Follow-up tracking** — in-app caught up only, not real-world closed — **no** new API, **`client_state`**, badges, or card actions — **does not** change APIs, schema, timeline writes, workers, queues, or automation (§2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench approval-funnel context dedupe on triage cards:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** removes redundant standalone packet-approved and draft-reviewed lines — approval-funnel checklist is **single source** for basics, draft reviewed, packet approved, and rollup — **does not** change behavior, persistence, membership, acknowledge **`PATCH`**, open-step/record-handled/outcome/clear-follow-up writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — approval-funnel context dedupe on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench started-at on triage cards:** **`/justice/handling`** **`HandlingWorkbenchCaseCard`** shows read-only **Opened {formatted time}** when **`status === "started"`** and **`started_at`** is set — existing **`formatApprovedNextActionHandlingTimestamp`** + list **`GET`** **`client_state`** only; alongside **Opened for next step.** — parity with **Handled for now {time}** — §2 **Handling workbench — manual-handling handoff context on handling-request triage cards (`/justice/handling`)**; complements approved-packet action cards — §2 **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`)** and open-step started persist — §2 **Handling workbench — Open approved step marks started on handling-request triage cards (`/justice/handling`)** — **does not** change behavior, persistence, membership, open-step/record-handled/outcome/clear-follow-up/acknowledge writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**).
- **Handling workbench started-at on approved-packet action cards:** **`/justice/handling`** **`ApprovedPacketActionCaseCard`** shows read-only **Opened {formatted time}** when **`status === "started"`** and **`started_at`** is set — existing **`formatApprovedNextActionHandlingTimestamp`** + list **`GET`** **`client_state`** only; below **Opened for next step.** and before **Record action handled for now** — mirrors triage-card started-at — §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**; complements plan — §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, packet — §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, and chat-ai — §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)** — **does not** change behavior, persistence, membership, open-step/record-handled/outcome/clear-follow-up/acknowledge writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`)**).
- **Saved cases Needs attention approved packet action visibility:** **`/justice/cases`** **Needs attention** surfaces the same approved-packet-action membership via **`buildApprovedPacketActionAttentionItems`** + **`parseApprovedPacketActionWithoutHandlingRequest`** (PR **#408** / commit **`cef391f`**) — read-only **Approved packet action** cards from list **`GET`** **`client_state`** (same **search** / **filter** / **sort** as **All cases**); honest copy describes next in-app step, **not** a Surrenderless handling request; **Open case** + navigation-only **View on handling workbench**; **`hasNeedsAttentionContent`** includes approved-packet-action cards; handling-request triage cards (**`buildHandlingRequestedAttentionItems`**) remain separate — **no** auto **`handling_requested_at`**, queue status, **Mark acknowledged**, plan/chat handling-request behavior change, timeline **type**, API route, worker, filing/submission/contact/queue/upload/storage/automation/schema (§2 **Approval-to-action engine — approved packet action visibility on Saved cases Needs attention (`/justice/cases`)**).
- **Saved cases Needs attention approved-packet record handled:** **`/justice/cases`** **Needs attention → Approved packet action** cards show **Record action handled for now** when **`status === "started"`** — **`markApprovedPacketActionHandled`** sets **`status: "completed"`** + **`completed_at`** (ISO), preserves other fields, **`GET` + merge + `PATCH`** **`client_state`**, session sync when active browser case — mirrors plan — §2 **Plan — prepared packet approval surfaced (`/justice/plan`)**, packet — §2 **Packet — record action handled on approved next action card (`/justice/packet`)**, chat-ai — §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, and workbench — §2 **Approval-to-action engine — approved packet action status writes on handling workbench (`/justice/handling`)**; **Tracking only — not automatic filing or submission.** — card leaves **Needs attention** on **`completed`** — **does not** change **All cases**, handling-request record-handled — §2 **Saved cases — record action handled on Needs attention handling-request cards (`/justice/cases`)**, membership predicates, or routing — **in-app tracking only** — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Saved cases — record action handled on Needs attention approved-packet-action cards (`/justice/cases`)**).
- **Saved cases Needs attention handling-request record handled:** **`/justice/cases`** **Needs attention → handling-request** cards show **Record action handled for now** when **`status === "started"`** — reuses **`markApprovedPacketActionHandled`** to set **`status: "completed"`** + **`completed_at`** (ISO), preserves **`handling_requested_at`**, **`handling_request_note`**, **`handling_acknowledged_at`**, and other fields, **`GET` + merge + `PATCH`** **`client_state`**, session sync when active browser case — mirrors approved-packet **Needs attention** record-handled — §2 **Saved cases — record action handled on Needs attention approved-packet-action cards (`/justice/cases`)** and workbench handling-request triage — §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**; **Tracking only — not automatic filing or submission.** — card leaves **Needs attention** on **`completed`** — post-handled ack on **All cases** — §2 **Saved cases — Mark acknowledged on All cases handling-request cards (`/justice/cases`)** — **does not** change **Mark acknowledged** on active **Needs attention** cards, request handling, approved-packet cards, membership predicates, plan, packet, chat-ai, hub, handling workbench, or routing — **in-app tracking only** — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Saved cases — record action handled on Needs attention handling-request cards (`/justice/cases`)**).
- **Saved cases All cases handling-request Mark acknowledged:** **`/justice/cases`** **`CaseApprovedNextActionTracking`** on **All cases** cards shows **Mark acknowledged** when **`handling_requested_at`**, no **`handling_acknowledged_at`**, and **`status === "completed"`** — reuses **`acknowledgeHandlingRequest`** + **`GET` + merge + `PATCH`** + session sync — closes Saved cases post-handled acknowledgment loop after **Needs attention** record-handled — §2 **Saved cases — record action handled on Needs attention handling-request cards (`/justice/cases`)**; mirrors **Needs attention** ack — §2 **Saved cases — acknowledge handling request from Needs attention** and plan/packet/hub/chat inline ack; **`inlineAck`** triage note + **`APPROVED_NEXT_ACTION_HANDLING_ACKNOWLEDGE_HELPER`**; **View in handling workbench** retained — **does not** change **Needs attention** membership, request/note/record-handled behavior, plan, packet, chat-ai, hub, handling workbench, or routing — **internal triage tracking only** — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Saved cases — Mark acknowledged on All cases handling-request cards (`/justice/cases`)**).
- **Saved cases All cases handling tracking status:** **`/justice/cases`** **All cases** cards with **`handling_requested_at`** show read-only **Handling tracking:** — loading line while progress fetches run; derived next step from **`deriveCasesManualActionNextStep`** (workbench **`deriveManualActionNextStep`** parity — §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**); **Tracking complete for now.** when gates satisfied — workbench **Tracking complete** queue parity — §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**; subcopy **In-app tracking only — not filed or submitted.** — reuses existing evidence/filings/tasks fetches — **no** new API, **`client_state`**, timeline write, worker, or queue — **does not** change **Needs attention**, acknowledge writes, workbench, or automation (§2 **Saved cases — handling tracking status on All cases cards (`/justice/cases`)**).
- **Action plan prepared-packet handling tracking status:** **`/justice/plan`** **both** prepared-packet emerald handling callouts with **`handling_requested_at`** show read-only **Handling tracking:** — loading line while **`readinessLoading`**; derived next step from **`derivePlanManualActionNextStep`** (workbench **`deriveManualActionNextStep`** parity — §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**; Saved cases parity — §2 **Saved cases — handling tracking status on All cases cards (`/justice/cases`)**); **Tracking complete for now.** when gates satisfied — workbench **Tracking complete** queue parity — §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**; subcopy **In-app tracking only — not filed or submitted.** — reuses existing evidence/filings readiness fetch — **no** new API, **`client_state`**, timeline write, worker, or queue — **does not** change request/note/ack/outcome/follow-up writes, **Needs attention**, hub, packet, chat-ai, cases, workbench, or automation (§2 **Plan — handling tracking status on prepared-packet handling callouts (`/justice/plan`)**).
- **Justice hub Current case handling tracking status:** **`/justice`** **Current case** handling blocks (prepared-packet + legacy/session paths) with **`handling_requested_at`** show read-only **Handling tracking:** — loading line while **`hubReadinessLoading`**; derived next step from **`deriveHubManualActionNextStep`** (workbench **`deriveManualActionNextStep`** parity — §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**; Saved cases parity — §2 **Saved cases — handling tracking status on All cases cards (`/justice/cases`)**; plan parity — §2 **Plan — handling tracking status on prepared-packet handling callouts (`/justice/plan`)**); **Tracking complete for now.** when gates satisfied — workbench **Tracking complete** queue parity — §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**; subcopy **In-app tracking only — not filed or submitted.** — parallel evidence + filings fetch in **`refreshHubState`** — **no** new API route, **`client_state`**, timeline write, worker, or queue — **does not** change request/note/ack writes, **Needs attention**, plan, packet, chat-ai, cases, workbench, or automation (§2 **Justice hub — handling tracking status on Current case (`/justice`)**).
- **Chat Current action handling tracking status:** **`/justice/chat-ai`** **loaded-case** **Recap → Current action tracking** with **`handling_requested_at`** shows read-only **Handling tracking:** — loading line while **`chatHandlingReadinessLoading`**; derived next step from **`deriveChatManualActionNextStep`** (workbench **`deriveManualActionNextStep`** parity — §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**; Saved cases parity — §2 **Saved cases — handling tracking status on All cases cards (`/justice/cases`)**; plan parity — §2 **Plan — handling tracking status on prepared-packet handling callouts (`/justice/plan`)**; hub parity — §2 **Justice hub — handling tracking status on Current case (`/justice`)**); **Tracking complete for now.** when gates satisfied — workbench **Tracking complete** queue parity — §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**; subcopy **In-app tracking only — not filed or submitted.** — parallel evidence + filings fetch in **`loadSavedEvidencePreview`** — **no** new API route, **`client_state`**, timeline write, worker, or queue — **does not** change request/note/ack/outcome/follow-up writes, **Open approved step**, workbench link, **Needs attention**, hub, plan, packet, cases, workbench, or automation (§2 **Chat AI intake — handling tracking status in Current action tracking (`/justice/chat-ai`)**).
- **Chat inline manual filing and confirmation capture:** **`/justice/chat-ai`** **Recap → Current action tracking** when signed in + **UUID** case and derived step is add-filing or add-confirmation shows **`ChatManualFilingCaptureForm`** — **`POST /api/justice/filings`** (destination required; optional filed_at, confirmation_number, notes; label prefill) or **`PATCH /api/justice/filings/[id]`** (confirmation on first row missing confirmation) — **`refreshChatFilings`** after save — honest **Record a manual action** / **does not submit or file anything for you** copy — packet filing contextual link suppressed while form shown — user stays on chat; tracking advances on-page — **manual record only** — **no** schema, new API route, or **`client_state`** change — **does not** change packet **`JusticeFilingRecords`**, plan, hub, cases, workbench, or automation (§2 **Chat AI intake — inline manual filing and confirmation capture in Current action tracking (`/justice/chat-ai`)**).
- **Chat archive case on tracking complete:** **`/justice/chat-ai`** **Recap → Current action tracking** when signed in + **UUID** case and **Handling tracking: Tracking complete for now.** shows **Archive case** — **`PATCH /api/justice/cases/[id]`** **`archived_at`** — **`clearLocalJusticeSession`** + **`/justice`** on success — honest **archives in Surrenderless; does not submit, file, or contact anyone** copy — **archive record only** — **no** schema or new API route — **does not** change **`/justice/cases`** archive UI, workbench, or automation (§2 **Chat AI intake — archive case on tracking complete in Current action tracking (`/justice/chat-ai`)**).
- **Chat inline submission draft review:** **`/justice/chat-ai`** **Active case** checklist when signed in + **UUID** + draft not reviewed shows compact draft review — **`buildSubmissionDraftPreview`** (deterministic; preview destination parity) — **Show more** truncation — checkbox **I reviewed the submission draft shown above.** — **`POST /api/justice/submission-draft-reviewed`** / timeline hydrate — unlocks inline prepared packet approval — optional **Open full submission preview** — **Continue** stays on chat for loaded signed-in **UUID** **Update in chat** saves — **in-app review record only** — **no** schema or new API route — **does not** change **`/justice/preview`** (§2 **Chat AI intake — inline submission draft review in Active case checklist (`/justice/chat-ai`)**).
- **Chat inline prepared packet approval:** **`/justice/chat-ai`** **Active case** checklist when signed in + **UUID** + draft reviewed + packet not approved shows **Approve prepared packet** — **`PATCH`** **`client_state.prepared_packet_approved`** + **`approved_next_action`** (packet **`handleApprovePreparedPacket`** parity) — honest **approves inside Surrenderless; does not submit, file, or contact anyone** — user stays on chat; **Current action tracking** can appear — **in-app approval only** — **no** schema or new API route — **does not** change **`/justice/packet`** or workbench (§2 **Chat AI intake — inline prepared packet approval in Active case checklist (`/justice/chat-ai`)**).
- **Chat mark step opened stays in chat:** **`/justice/chat-ai`** **Mark step opened** on loaded signed-in **UUID** cases persists **`started`** via existing **`PATCH`** but **does not** auto-navigate to prep **`href`** — optional **Open {label} (optional)** link — keeps continuous inline filing → confirmation → archive path — **in-app tracking only** — **no** API/schema change — plan/packet/handling unchanged (§2 **Chat AI intake — mark step opened stays in chat in Current action tracking (`/justice/chat-ai`)**).
- **Packet prepared-action handling tracking status:** **`/justice/packet`** **both** prepared-action handling blocks with **`handling_requested_at`** show read-only **Handling tracking:** — loading line while **`packetHandlingReadinessLoading`**; derived next step from **`derivePacketManualActionNextStep`** (workbench **`deriveManualActionNextStep`** parity — §2 **Handling workbench — manual action progress on handling-request triage cards (`/justice/handling`)**; Saved cases parity — §2 **Saved cases — handling tracking status on All cases cards (`/justice/cases`)**; plan parity — §2 **Plan — handling tracking status on prepared-packet handling callouts (`/justice/plan`)**; hub parity — §2 **Justice hub — handling tracking status on Current case (`/justice`)**; chat-ai parity — §2 **Chat AI intake — handling tracking status in Current action tracking (`/justice/chat-ai`)**); **Tracking complete for now.** when gates satisfied — workbench **Tracking complete** queue parity — §2 **Handling workbench — tracking complete queue on handling-request triage cards (`/justice/handling`)**; subcopy **In-app tracking only — not filed or submitted.** — reuses existing evidence + filings fetches — **no** new API route, **`client_state`**, timeline write, worker, or queue — **does not** change request/note/ack/outcome/follow-up writes, **Record action handled**, **Open** step, workbench link, **`JusticeFilingRecords`** CRUD, **Needs attention**, hub, plan, chat-ai, cases, workbench, or automation (§2 **Packet — handling tracking status in prepared-action handling UI (`/justice/packet`)**).
- **Handling tracking contextual action links:** All five justice surfaces with **Handling tracking:** show optional read-only navigation link below the tracking line when the derived step maps to a known action — **`resolveHandlingTrackingContextualLink`** (§2 **Approved next action — handling tracking contextual action links (justice surfaces)**) — **Open filing records** → **`/justice/packet#packet-filings`**; outcome/follow-up/ack → **`/justice/plan`** when not redundant; workbench **Confirmation follow-up** / **Outcome closure** / **Manual-action progress** parity — handling tracking sync #470–#474 — **read-only navigation only** — **no** API, **`client_state`**, timeline write, worker, queue, gate derivation, write-path, filing CRUD, or automation changes.
- **Saved cases All-card session-aware handling tracking links:** **`/justice/cases`** **All cases** **Handling tracking** contextual links activate the selected case in browser session (**`activateCaseInSession`**) before **`router.push`** — parity with **Open case** / **Needs attention** follow-up routing — completes contextual-link carry-forward from #475 — hub/plan/packet/chat **`<Link>`** unchanged — **read-only navigation only** — **no** API, **`client_state`**, timeline write, worker, queue, resolver mapping, **Needs attention**, or write-path changes (§2 **Saved cases — session-aware handling tracking contextual links on All cards (`/justice/cases`)**).
- **Saved cases Needs attention handling-request handling tracking:** **`/justice/cases`** **Needs attention → handling-request** cards show **Handling tracking:** + session-aware contextual links after queue status — reuses **`progressById`** + **`deriveCasesHandlingTrackingLine`** — **`openHandlingTrackingContextualLink`** (#476) — workbench **Manual-action progress** / **Confirmation follow-up** / **Outcome closure** parity — contextual links #475 — **read-only navigation only** — **no** API, **`client_state`**, timeline write, worker, queue, membership, or write-path changes (§2 **Saved cases — handling tracking on Needs attention handling-request cards (`/justice/cases`)**).
- **Saved cases approved-packet-action handling tracking:** **`/justice/cases`** **All cases** + **Needs attention → Approved packet action** cards without **`handling_requested_at`** show **Handling tracking:** + session-aware contextual links — **`parseApprovedPacketActionWithoutHandlingRequest`** — **`deriveCasesHandlingTrackingLine`** + **`openHandlingTrackingContextualLink`** (#476) — cross-ref #477 — workbench **Manual-action progress** / **Confirmation follow-up** / **Outcome closure** parity — **no** queue status — does **not** imply Surrenderless handling request — **read-only navigation only** — **no** API, **`client_state`**, timeline write, worker, queue, membership, or write-path changes (§2 **Saved cases — handling tracking on approved-packet-action cards (`/justice/cases`)**).
- **Packet approved-packet-action handling tracking:** **`/justice/packet`** shows **Handling tracking:** + contextual links on approved-packet-action path without **`handling_requested_at`** — **`derivePacketHandlingTrackingLine`** — cross-ref #478 #477 — workbench **Manual-action progress** / **Confirmation follow-up** / **Outcome closure** parity — **no** queue status — does **not** imply Surrenderless handling request — **read-only navigation only** — **no** API, **`client_state`**, timeline write, worker, queue, or write-path changes (§2 **Packet — handling tracking on approved-packet-action prepared-action UI (`/justice/packet`)**).
- **Plan approved-packet-action handling tracking:** **`/justice/plan`** **both** prepared-packet approved emerald inner callouts show **Handling tracking:** + contextual links on approved-packet-action path without **`handling_requested_at`** — **`derivePlanHandlingTrackingLine`** — cross-ref #479 #478 — workbench **Manual-action progress** / **Confirmation follow-up** / **Outcome closure** parity — **no** queue status — does **not** imply Surrenderless handling request — **read-only navigation only** — **no** API, **`client_state`**, timeline write, worker, queue, or write-path changes (§2 **Plan — handling tracking on approved-packet-action prepared-packet callouts (`/justice/plan`)**).
- **Justice hub approved-packet-action handling tracking:** **`/justice`** **Current case** shows **Handling tracking:** + contextual links on approved-packet-action path without **`handling_requested_at`** when **`packetApproved && approvedNextAction`** — **`deriveHubHandlingTrackingLine`** — cross-ref #479 #478 — workbench **Manual-action progress** / **Confirmation follow-up** / **Outcome closure** parity — **no** queue status — does **not** imply Surrenderless handling request — **read-only navigation only** — **no** API, **`client_state`**, timeline write, worker, queue, or write-path changes (§2 **Justice hub — handling tracking on approved-packet-action Current case (`/justice`)**).
- **Chat AI approved-packet-action handling tracking:** **`/justice/chat-ai`** loaded-case **Recap → Current action tracking** shows **Handling tracking:** + contextual links on approved-packet-action path without **`handling_requested_at`** — **`deriveChatHandlingTrackingLine`** — cross-ref #479 #478 — workbench **Manual-action progress** / **Confirmation follow-up** / **Outcome closure** parity — **no** queue status — does **not** imply Surrenderless handling request — **read-only navigation only** — **no** API, **`client_state`**, timeline write, worker, queue, or write-path changes (§2 **Chat AI intake — handling tracking on approved-packet-action Current action tracking (`/justice/chat-ai`)**).
- **Chat Current action approved packet action workbench link:** **`/justice/chat-ai`** **Recap → Current action tracking** shows navigation-only **View on handling workbench** → **`/justice/handling`** when **`isApprovedPacketActionWithoutHandlingRequest`** matches on session **`preparedPacketApproved`** + hydrated **`approvedNextAction`** (PR **#410** / commit **`2090248`**) — honest copy describes approved next in-app step, **not** a Surrenderless handling request — **no** case activation through the link, auto **`handling_requested_at`**, queue status, **Mark acknowledged**, timeline **type**, API route, worker, filing/submission/contact/queue/upload/storage/automation/schema (§2 **Approval-to-action engine — approved packet action workbench link in chat Current action tracking (`/justice/chat-ai`)**).
- **Packet Surrenderless handling request and acknowledge on approved case surface:** **`/justice/packet`** prepared-action handling area when **`packetApproved`** + **`approvedNextAction`** supports **Request Surrenderless handling**, optional **`handling_request_note`** edit, and **Mark acknowledged** — **`handleRequestSurrenderlessHandling`** / **`handleUpdateHandlingRequestNote`** / **`handleAcknowledgeHandlingRequest`** persist via **`persistApprovedNextAction`** on existing **`client_state.approved_next_action`** fields only — mirrors plan — §2 **Plan — Surrenderless handling requested (`/justice/plan`)** and chat-ai — §2 **Chat AI intake — handling requested in Current action tracking (`/justice/chat-ai`)**; closes packet-side internal manual-handling handoff — **View in handling workbench** retained — **internal triage tracking only** — **does not** auto-request on packet approval; **does not** change inner approved-next-action card, plan, chat-ai, cases, hub, handling workbench, membership predicates, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Packet — Surrenderless handling request and acknowledge on approved case packet surface (`/justice/packet`)**).
- **Packet approved card approved packet action workbench link:** **`/justice/packet`** **`packetApproved`** emerald card shows navigation-only **View on handling workbench** → **`/justice/handling`** when **`isApprovedPacketActionWithoutHandlingRequest`** matches on **`packetApproved`** + hydrated **`approvedNextAction`** (PR **#412** / commit **`40e56f9`**) — honest copy describes approved next in-app step, **not** a Surrenderless handling request; handling-request **View in handling workbench** in prepared-action area — §2 **Packet — Surrenderless handling request and acknowledge on approved case packet surface (`/justice/packet`)** — **no** case activation through the link, auto **`handling_requested_at`**, timeline **type**, API route, worker, filing/submission/contact/queue/upload/storage/automation/schema (§2 **Approval-to-action engine — approved packet action workbench link on packet approved card (`/justice/packet`)**).
- **Action plan prepared-packet approved packet action workbench link:** **`/justice/plan`** **both** prepared-packet approved emerald inner callouts show navigation-only **View on handling workbench** → **`/justice/handling`** when **`isApprovedPacketActionWithoutHandlingRequest`** matches on **`preparedPacketApproved`** + hydrated **`approvedNextAction`** (PR **#414** / commit **`33e8fad`**) — honest copy describes approved next in-app step, **not** a Surrenderless handling request; handling-request **View in handling workbench** unchanged (§2 **Handling workbench — active-case back-link from plan and chat-ai**) — **no** case activation through the link, auto **`handling_requested_at`**, queue status, **Mark acknowledged**, timeline **type**, API route, worker, filing/submission/contact/queue/upload/storage/automation/schema (§2 **Approval-to-action engine — approved packet action workbench link on action plan prepared-packet callouts (`/justice/plan`)**).
- **Action plan clear follow-up on prepared-packet callouts:** **`/justice/plan`** **both** prepared-packet approved emerald inner callouts show **Mark follow-up handled** when **`status === "completed"`** and **`follow_up_needed === true`** — **`clearFollowUpFromApprovedNextAction`** + **`persistApprovedNextAction`**; preserves **`outcome_note`** and **`follow_up_at`**; clears **`follow_up_needed`** only — removes case from **Saved cases** **Needs attention** follow-up list; mirrors chat-ai / cases / handling workbench — **does not** change handling workbench, membership predicates, acknowledge **`PATCH`**, open-step/record-handled writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Plan — clear follow-up on prepared-packet callouts (`/justice/plan`)**).
- **Justice hub Current case approved packet action workbench link:** **`/justice`** **Current case** area shows navigation-only **View on handling workbench** → **`/justice/handling`** when **`isApprovedPacketActionWithoutHandlingRequest`** matches on session **`packetApproved`** + **`approvedNextAction`** in the hub snapshot (PR **#416** / commit **`e046e4f`**) — honest copy describes approved next in-app step, **not** a Surrenderless handling request; copy now says request handling from **action plan, case packet, or here on the hub** when you want internal triage tracking; handling-request **View in handling workbench** + interactive request/note/ack — §2 **Justice hub — Surrenderless handling request and acknowledge on Current case (`/justice`)** — **no** case activation through the link, auto **`handling_requested_at`**, timeline **type**, API route, worker, filing/submission/contact/queue/upload/storage/automation/schema (§2 **Approval-to-action engine — approved packet action workbench link on justice hub Current case (`/justice`)**).
- **Justice hub Current case approved next action display hydrate:** **`/justice`** **Current case** hydrates **`approved_next_action`** from server **`client_state`** on signed-in **UUID** cases via existing **`GET /api/justice/cases/[id]`** + **`hydrateApprovedNextActionForDisplay`** in **`refreshHubState`** — **Started** / **Handled** (and handling-request fields) from handling/plan/chat/packet appear on hub without plan/chat visit first; session fallback when **`GET`** fails; **`writeSessionApprovedNextAction`** display sync only — **does not** add buttons, **`PATCH`**, show completed rows in inbox-style approved-packet lists, or new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Justice hub — approved next action display hydrate on Current case (`/justice`)**).
- **Justice hub Current case handled-for-now timestamp:** **`/justice`** **Current case** card shows read-only **Handled for now {formatted time}** when hydrated **`snapshot.approvedNextAction.status === "completed"`** and **`completed_at`** is set — continuity display from already-hydrated **`approvedNextAction`** only; complements **Approved next action: Handled** — **no** buttons, **`PATCH`**, new **`GET`**, inbox changes, or new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Justice hub — handled-for-now timestamp on Current case (`/justice`)**).
- **Justice hub Current case started-at read-only:** **`/justice`** **Current case** card (inside primary **`Link`**) shows read-only **Opened {formatted time}** when hydrated **`snapshot.approvedNextAction.status === "started"`** and **`started_at`** is set — continuity display from already-hydrated **`approvedNextAction`** only; after **Approved next action: {status}** and before **Handled for now** / outcome / handling blocks — parity with **Handled for now {time}** — §2 **Justice hub — handled-for-now timestamp on Current case (`/justice`)**; complements plan — §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, packet — §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, chat-ai — §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, handling triage — §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**, handling approved-packet cards — §2 **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`)**, and cases All cases — §2 **Saved cases — started-at read-only on All cases cards (`/justice/cases`)** — **does not** change primary CTA, checklist, handling blocks, workbench links, **Needs attention** templates, membership, write paths, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Justice hub — started-at read-only on Current case (`/justice`)**).
- **Justice hub Current case outcome / follow-up read-only:** **`/justice`** **Current case** card (inside primary **`Link`**) shows read-only truncated **`outcome_note`**, **Follow-up needed**, and follow-up timing when present on hydrated **`snapshot.approvedNextAction`** from existing **`refreshHubState`** / server **`client_state`** — continuity display only; complements §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)** browse display and §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)** writes — **does not** change **Needs attention** builders, handling triage, **Clear follow-up**, or inbox rules — **no** forms, buttons, **`PATCH`**, new **`GET`**, or new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)**).
- **Packet approved next action handled-for-now timestamp:** **`/justice/packet`** **`packetApproved`** inner emerald card shows read-only **Handled for now {formatted time}** when hydrated **`approvedNextAction.status === "completed"`** and **`completed_at`** is set — continuity display from already-hydrated **`approvedNextAction`** only; complements **Next action recorded as handled** prose — **no** buttons, **`PATCH`**, new **`GET`**, inbox changes, or new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Packet — handled-for-now timestamp on approved next action card (`/justice/packet`)**).
- **Packet approved next action started-at read-only:** **`/justice/packet`** **`packetApproved`** inner emerald approved-next-action card shows read-only **Opened {formatted time}** when **`status === "started"`** and **`started_at`** is set — existing **`formatApprovedNextActionHandlingTimestamp`** + hydrated **`approvedNextAction`** only; below **Opened for next step.** and before **Record action handled for now** / **Open {label}** / completed tracking blocks — parity with **Handled for now {time}** — §2 **Packet — handled-for-now timestamp on approved next action card (`/justice/packet`)**; complements plan — §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, chat-ai — §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, and handling workbench — §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)** — **does not** change behavior, persistence, membership, open-step/record-handled/outcome/clear-follow-up/acknowledge writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**).
- **Packet approved next action record handled:** **`/justice/packet`** **`packetApproved`** inner emerald approved-next-action card shows **Record action handled for now** when **`status === "started"`** — **`handleMarkApprovedNextActionHandled`** sets **`status: "completed"`** + **`completed_at`** (ISO), preserves other fields, persists via existing **`persistApprovedNextAction`** — mirrors plan — §2 **Plan — prepared packet approval surfaced (`/justice/plan`)**, chat-ai — §2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**, and handling workbench — §2 **Handling workbench — record action handled on handling-request triage cards (`/justice/handling`)**; after **Opened {formatted time}** with **Tracking only — not automatic filing or submission.** — **does not** change open-step, outcome/follow-up, clear follow-up, handling blocks, membership predicates, or routing — **in-app tracking only** — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Packet — record action handled on approved next action card (`/justice/packet`)**).
- **Packet approved next action outcome / follow-up tracking:** **`/justice/packet`** **`packetApproved`** inner emerald **Next action recorded as handled** card shows **Record outcome / follow-up** form when **`approvedNextAction.status === "completed"`** — packet write path on already-hydrated **`approvedNextAction`**; persists **`outcome_note`**, **`follow_up_needed`**, **`follow_up_at`** via existing **`GET` + merge + `PATCH`** + session sync; complements **`ApprovedNextActionTrackingSummary`** and §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)** / §2 **Approved next action outcome / follow-up tracking (`/justice/plan` + `/justice/packet` + `/justice/chat-ai`)** writes; clear follow-up when **`follow_up_needed`** — §2 **Packet — clear follow-up on approved next action card (`/justice/packet`)**; hub/cases/handling read-only continuity — §2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)**, §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**, §2 **Handling workbench — outcome / follow-up read-only on handling-request triage cards (`/justice/handling`)** — **does not** change **`status`**, **`completed_at`**, handling fields, inbox rules, workbench, chat, plan, routing, or shared components — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Packet — outcome / follow-up tracking on approved next action card (`/justice/packet`)**).
- **Packet approved next action clear follow-up:** **`/justice/packet`** **`packetApproved`** inner emerald **Next action recorded as handled** card shows **Mark follow-up handled** when **`status === "completed"`** and **`follow_up_needed === true`** — **`clearFollowUpFromApprovedNextAction`** + **`persistApprovedNextAction`**; preserves **`outcome_note`** and **`follow_up_at`**; clears **`follow_up_needed`** only — removes case from **Saved cases** **Needs attention** follow-up list; mirrors plan / chat-ai / cases / handling workbench — **does not** dedupe summary/form; **does not** change plan, chat-ai, cases, handling workbench, membership predicates, acknowledge **`PATCH`**, open-step/record-handled writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Packet — clear follow-up on approved next action card (`/justice/packet`)**).
- **Plan prepared-packet approved callouts handled-for-now timestamp:** **`/justice/plan`** **both** prepared-packet approved inner emerald callouts show read-only **Handled for now {formatted time}** when hydrated **`approvedNextAction.status === "completed"`** and **`completed_at`** is set — continuity display from already-hydrated **`approvedNextAction`** only; complements **Next action recorded as handled** prose — **no** buttons, **`PATCH`**, new **`GET`**, inbox changes, or new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Plan — handled-for-now timestamp on prepared-packet approved callouts (`/justice/plan`)**).
- **Plan prepared-packet callouts started-at read-only:** **`/justice/plan`** **both** prepared-packet approved inner emerald callouts show read-only **Opened {formatted time}** when **`status === "started"`** and **`started_at`** is set — existing **`formatApprovedNextActionHandlingTimestamp`** + hydrated **`approvedNextAction`** only; below **Opened for next step.** and before **Record action handled for now** — parity with **Handled for now {time}** — §2 **Plan — handled-for-now timestamp on prepared-packet approved callouts (`/justice/plan`)**; complements packet — §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, chat-ai — §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, and handling workbench — §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)** — **does not** change behavior, persistence, membership, open-step/record-handled/outcome/clear-follow-up/acknowledge writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**).
- **Saved cases All cases approved packet action workbench link:** **`/justice/cases`** **`CaseApprovedNextActionTracking`** on **All cases** cards shows navigation-only **View on handling workbench** → **`/justice/handling`** when **`parseApprovedPacketActionWithoutHandlingRequest(clientState)`** matches on list **`GET`** **`client_state`** — honest copy describes approved next in-app step, **not** a Surrenderless handling request; handling-request **View in handling workbench** unchanged (§2 **Saved cases — View in handling workbench on All cases handling-request cards**); approved-packet-action workbench nav parity complete across handling, **Needs attention**, chat, packet, plan, hub, and **All cases** browse cards — **no** case activation through the link, auto **`handling_requested_at`**, queue status, **Mark acknowledged**, timeline **type**, API route, worker, filing/submission/contact/queue/upload/storage/automation/schema (§2 **Approval-to-action engine — approved packet action workbench link on All cases cards (`/justice/cases`)**).
- **Saved cases All cases handled-for-now timestamp:** **`/justice/cases`** **`CaseApprovedNextActionTracking`** on **All cases** cards shows read-only **Handled for now {formatted time}** when **`approved_next_action.status === "completed"`** and **`completed_at`** is set on list **`GET`** **`client_state`** — browse/history display from existing **`client_state`** only; **completed** rows remain excluded from **Needs attention** approved-packet cards and handling workbench **Approved packet actions** inbox — **no** buttons, **`PATCH`**, timeline **type**, API route, worker, filing/submission/queue/automation/schema (§2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**).
- **Saved cases All cases started-at read-only:** **`/justice/cases`** **`CaseApprovedNextActionTracking`** on **All cases** cards shows read-only **Opened {formatted time}** when **`approved_next_action.status === "started"`** and **`started_at`** is set on list **`GET`** **`client_state`** — browse/history display from existing **`client_state`** only; after **Approved next action: {status}** and before outcome/handling blocks — parity with **Handled for now {time}** — §2 **Saved cases — handled-for-now timestamp on All cases cards (`/justice/cases`)**; complements plan — §2 **Plan — started-at read-only on prepared-packet callouts (`/justice/plan`)**, packet — §2 **Packet — started-at read-only on approved next action card (`/justice/packet`)**, chat-ai — §2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**, handling triage — §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)**, and handling approved-packet cards — §2 **Handling workbench — started-at read-only on approved-packet action cards (`/justice/handling`)** — **does not** change **Needs attention** card templates, builders, membership, write paths, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Saved cases — started-at read-only on All cases cards (`/justice/cases`)**).
- **Saved cases All cases outcome note:** **`/justice/cases`** **`CaseApprovedNextActionTracking`** on **All cases** cards shows read-only truncated **`outcome_note`** when present on list **`GET`** **`client_state`** — browse/history display from existing **`client_state`** only; complements **Follow-up needed** / follow-up timing lines — **does not** change **Needs attention** builders, **Clear follow-up**, or inbox rules — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**).
- **Chat Current action handled-for-now timestamp:** **`/justice/chat-ai`** **Recap → Current action tracking** shows read-only **Handled for now {formatted time}** when hydrated **`approvedNextAction.status === "completed"`** and **`completed_at`** is set — continuity display from already-hydrated **`approvedNextAction`** only; complements existing **Next action recorded as handled for now** copy — **no** buttons, **`PATCH`**, inbox changes, or new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Chat AI intake — handled-for-now timestamp in Current action tracking (`/justice/chat-ai`)**).
- **Chat Current action started-at read-only:** **`/justice/chat-ai`** **Recap → Current action tracking** shows read-only **Opened {formatted time}** when **`status === "started"`** and **`started_at`** is set — existing **`formatApprovedNextActionHandlingTimestamp`** + hydrated **`approvedNextAction`** only; alongside **Opened for next step.** — parity with **Handled for now {time}** — §2 **Chat AI intake — handled-for-now timestamp in Current action tracking (`/justice/chat-ai`)**; complements handling workbench started-at — §2 **Handling workbench — started-at read-only on handling-request triage cards (`/justice/handling`)** — **does not** change behavior, persistence, membership, open-step/record-handled/outcome/clear-follow-up/acknowledge writes, or routing — **no** new API/schema/timeline-write/worker/queue/submission/filing/automation (§2 **Chat AI intake — started-at read-only in Current action tracking (`/justice/chat-ai`)**).
- **Chat Current action outcome / follow-up tracking:** **`/justice/chat-ai`** **Recap → Current action tracking** shows **Record outcome / follow-up** form when **`approvedNextAction.status === "completed"`** — chat-first write on already-hydrated **`approvedNextAction`**; persists existing **`outcome_note`**, **`follow_up_needed`**, **`follow_up_at`** only — duplicate read-only outcome/follow-up lines hidden when completed (§2 **Chat AI intake — outcome / follow-up read-only dedupe in Current action tracking (`/justice/chat-ai`)**) — **Mark follow-up handled** unchanged — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**).
- **Chat Current action outcome / follow-up read-only dedupe:** **`/justice/chat-ai`** **Recap → Current action tracking** hides duplicate read-only **`outcome_note`**, **Follow-up needed**, and follow-up timing below handling/workbench UI when **`approvedNextAction.status === "completed"`** — **Record outcome / follow-up** form is the single outcome/follow-up display in **Recap** for completed actions; non-completed statuses keep read-only lines when data exists — **does not** change **Active case** banner, disclaimer, **Mark follow-up handled**, or work links — **no** new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Chat AI intake — outcome / follow-up read-only dedupe in Current action tracking (`/justice/chat-ai`)**).
- **Chat Active case banner outcome / follow-up read-only hints:** **`/justice/chat-ai`** **Active case** banner (Update in chat only) shows compact read-only truncated **`outcome_note`**, **Follow-up needed**, and follow-up timing when present on hydrated **`approvedNextAction`** — continuity display from existing hydrate only; complements §2 **Justice hub — outcome / follow-up read-only on Current case (`/justice`)**, §2 **Saved cases — outcome note on All cases cards (`/justice/cases`)**, and §2 **Chat AI intake — outcome / follow-up tracking in Current action tracking (`/justice/chat-ai`)**; **does not** change **Recap → Current action tracking**, **Needs attention**, handling triage, **Clear follow-up**, or inbox rules — **no** forms, buttons, **`PATCH`**, new **`GET`**, or new API/schema/timeline/worker/queue/submission/filing/automation (§2 **Chat AI intake — active case banner outcome / follow-up read-only hints (Update in chat) (`/justice/chat-ai`)**).
- **Chat-ai page breadcrumb work-link funnel routing (Update in chat):** **`/justice/chat-ai`** page breadcrumb uses **`activeCaseWorkLabel`** → **`activeCaseWorkHref`** when **`isUpdatingExistingCase`** (reuses **`resolveActiveCaseWorkHref`** / **`resolveActiveCaseWorkLabel`** from **`activeCaseDraftReviewed`** + **`preparedPacketApproved`**): **Submission preview** → **`/justice/preview`** when draft not reviewed; **Review prepared case packet** → **`/justice/packet`** when draft reviewed and packet not approved; **Action plan** → **`/justice/plan`** when packet approved; fresh intake keeps **Action plan** → **`/justice/plan`** — **no** secondary breadcrumb link; **`handleSend`**, **`handleContinueToPreview`**, Recap, banner footers, proof CRUD, handling UI, APIs, session keys, schema, routes, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Chat AI intake — page breadcrumb work-link funnel routing (Update in chat) (`/justice/chat-ai`)**).
- **Chat-ai Continue handoff:** **`/justice/chat-ai`** **Recap** shows **What happens next** panel above **Save and continue in chat** when basics complete (**`getPreviewBasicsMissing`**) and **`validateContactProofForIntake`** passes — **`getContinueHandoffSteps`** lists steps matching **`handleContinueToPreview`** (save case/updates, pending proof flush where applicable, in-chat draft review / packet approval / current action tracking when signed in — §2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)**); on **Update in chat** with **`sessionChangeLines`**, handoff includes **Save your updates from this chat to your case:** plus each detected change step (§2 **Chat AI intake — Continue handoff session-change integration (Update in chat)**); **Save and continue in chat** disabled while **`submitting`**, **`loading`**, or basics missing; replaces prior staged-proof one-liner above **Continue**; amber **Still needed before preview** and contact-proof warnings unchanged; **`handleContinueToPreview`**, handoff gates, proof/staged-proof logic, optional work links, routes, APIs, schema unchanged (§2 **Chat AI intake — Continue handoff panel (What happens next)**).
- **Chat-ai Continue handoff funnel copy:** **`/justice/chat-ai`** signed-in **`getContinueHandoffSteps`** uses in-chat funnel bullets (draft review in Active case checklist, approve packet in chat, continue in Current action tracking) via **`chatFirstContinuity`** instead of preview → packet → plan wording; **Save and continue in chat** label; focus/subtitle/session-change copy say **save in chat**; packet checklist link hidden while inline approval block shown — preview **`previewStep`** / **`postPreviewFunnelStep`** fallback when **`chatFirstContinuity`** false; **`handleContinueToPreview`**, stay-in-chat guards, handoff visibility gates, proof/staged-proof logic, optional work links, routes, APIs, schema, handlers unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — Continue handoff funnel copy alignment (`/justice/chat-ai`)**).
- **Preview proof continuity:** **`/justice/preview`** shows **Proof on your case** card after **Related action** using existing **`loadEvidence`** / **`GET /api/justice/evidence`** — loading (**Loading saved proof notes...**), error (inside card; draft still shown), empty (**Add proof in chat →** **`/justice/chat-ai`**), or saved count + up to **3** recent titles with type/date; intro copy clarifies saved chat intake/case and metadata-only proof titles in draft; replaces standalone evidence loading/error lines; **no** new API, draft-builder, plan, schema, or prompt changes (§2 **Preview — proof on your case card (Chat AI handoff continuity)**).
- **Preview chat-update continuity:** **`/justice/preview`** after intake hydration reads **`justice_preview_chat_update_summary_v1`** via **`readAndClearPreviewChatUpdateSummary`** (one-time); **From your chat update** card when valid **`sessionChangeLines`** from **Update in chat** Continue — company line, change bullets, draft reflects saved case / nothing filed automatically, **Update in chat** → **`/justice/chat-ai`**; not on fresh intake, plan → preview, or no-change updates; **`previewChatUpdateHandoff.ts`** + chat-ai **`writePreviewChatUpdateSummary`** / **`clearPreviewChatUpdateSummary`** — **no** draft generation, review milestone, evidence GET, AI draft, continue-to-plan, commit, hub/cases/plan changes; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — chat-update continuity card (Chat AI handoff)**).
- **Preview pre-plan chat correction:** **`/justice/preview`** bottom review card shows **Need to fix something in the draft?** **Update in chat** → **`/justice/chat-ai`** and subline to describe changes in chat then return to review updated draft (nothing filed automatically); always when intake loaded, before primary **Continue**; button label and post-save routing — §2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**; review checkbox, **`submission-draft-reviewed`** API, draft generation, evidence GET, AI draft, proof card, chat-update card, approved-next panel unchanged — **no** API, schema, helper, hub, cases, plan, chat-ai changes; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — pre-plan chat correction prompt (`/justice/preview`)**).
- **Preview Continue routes to packet when prepared packet not approved:** **`/justice/preview`** after successful draft-review save, when **signed-in** and **`STORAGE_CASE_ID`** is a **UUID**, **`router.push("/justice/chat-ai")`** (§2 **Preview — signed-in Continue returns to chat (`/justice/preview`)**); otherwise **`router.push`** to **`/justice/packet`** when **`!preparedPacketApproved`**, **`/justice/plan`** when approved; bottom button **Review prepared case packet** / **Continue to action plan** unchanged; uses session **`preparedPacketApproved`** from §2 **Preview — approved-next session hydrate wiring (`/justice/preview`)** — **no** new API; proof card, chat-update card, approved-next panel, checkbox, **`submission-draft-reviewed`**, draft generation, evidence loading, handlers, Plan, Packet, Chat AI, Cases, Hub, APIs, schema, checklist deep links unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**).
- **Preview approved-next session hydrate wiring:** **`/justice/preview`** **`useEffect`** hydrates **`preparedPacketApproved`** when preview is ready and **`caseId`** exists (**`readSessionPreparedPacketApproved`**); hydrates **`approvedNextAction`** via **`hydrateApprovedNextActionForDisplay(caseId)`**; clears both when not ready or no **`caseId`** — enables read-only panel (§2 **Preview — approved-next read-only continuity (`/justice/preview`)**) and Continue routing (§2 **Preview — Continue routes to packet when prepared packet not approved (`/justice/preview`)**); **no** signed-in **`GET`**; panel JSX, proof card, chat-update card, draft generation, review checkbox, evidence loading, handlers, Plan, Packet, Chat AI, Cases, Hub, APIs, schema, routes unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — approved-next session hydrate wiring (`/justice/preview`)**).
- **Preview approved-next read-only continuity:** **`/justice/preview`** when **`preparedPacketApproved && approvedNextAction`** (§2 **Preview — approved-next session hydrate wiring (`/justice/preview`)**), read-only panel shows **Next step: {label}** and **Approved next action: {status}** via **`approvedNextActionStatusLabel`**; panel only — **no** buttons, links, **Open**, or **Record handled**; draft generation, review checkbox, proof card, Continue routing, evidence loading, handlers, Plan, Packet, Chat AI, Cases, Hub, APIs, schema, routes unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Preview — approved-next read-only continuity (`/justice/preview`)**).
- **Plan proof readiness continuity:** **`/justice/plan`** post-draft **`showPostDraftReviewCallout`** includes proof line from existing **`evidenceCount`** (**Loading saved proof notes...**, saved count + reviewed-draft metadata wording, or empty + **Add proof in chat**); **Evidence / proof** empty state links **`/justice/chat-ai`**; **Add evidence** unchanged; **`evidenceReady`** / escalation logic unchanged; **no** new API, chat-ai, preview, lib, schema, or prompt changes (§2 **Plan — proof readiness continuity after preview review (`/justice/plan`)**).
- **Plan Case readiness checklist proof CTA:** **`/justice/plan`** **Case readiness** **Evidence added** row includes inline **Add proof in chat** → **`/justice/chat-ai`** when signed in, readiness loaded, and no evidence; mirrors basics CTA link styling; **`evidenceReady`** / **`readyToEscalate`** / **`basicsReady`** / readiness fetch / packet CTAs / post-draft callout / breadcrumb unchanged; **no** API, schema, helper, preview, chat-ai, hub, or cases changes; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Plan — Case readiness checklist proof CTA (`/justice/plan`)**).
- **Plan Case readiness checklist basics CTA:** **`/justice/plan`** **Case readiness** **Basic case info present** row includes inline **Update in chat** → **`/justice/chat-ai`** when signed in, readiness loaded, and basics incomplete; mirrors proof CTA pattern; evidence row / draft-review row / breadcrumb / **`basicsReady`** / **`readyToEscalate`** / readiness fetch / packet CTAs / post-draft callout unchanged; **no** API, schema, helper, preview, chat-ai, hub, or cases changes; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Plan — Case readiness checklist basics CTA (`/justice/plan`)**).
- **Plan Case readiness checklist draft-review CTA:** **`/justice/plan`** **Case readiness** **Submission draft reviewed: yes/not yet** from **`submission_draft_reviewed`** timeline; **Review submission draft** → **`/justice/preview`** when signed in, readiness loaded, not reviewed; prepared packet row when draft reviewed — §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**; **`basicsReady`** / **`evidenceReady`** / **`readyToEscalate`** / readiness fetch / emerald post-draft callout / packet CTAs / basics/evidence rows / breadcrumb unchanged; **no** API, schema, helper, preview, chat-ai, hub, or cases changes; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Plan — Case readiness checklist draft-review CTA (`/justice/plan`)**).
- **Plan Case readiness checklist prepared packet reviewed row:** **`/justice/plan`** **Case readiness** shows **Prepared case packet reviewed: yes/not yet** after **Submission draft reviewed** when **`draftReviewed`**; **`preparedPacketApproved`** from existing session + case hydrate; **Review prepared case packet** → **`/justice/packet`** when signed in, readiness loaded, not approved (same link classes as draft-review CTA); **`{preparedPacketApproved && approvedNextAction ? (...)}`** block unchanged; **not** tied to **`readyToEscalate`**; emerald **`showPostDraftReviewCallout`** copy unchanged; handlers, routes, APIs, schema, hub, cases, preview, chat-ai unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**).
- **Plan Case readiness approved-next visibility:** **`/justice/plan`** **Case readiness** shows read-only **Next step: {label}** and **Approved next action: {status}** via **`approvedNextActionStatusLabel`** when **`preparedPacketApproved && approvedNextAction`** — checklist-only, no links/buttons; follows prepared packet row when **`draftReviewed`** — §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**; emerald callout, **Open** / **Record handled**, **`showApprovedNextActionCta`**, handlers, CTAs, hydrate, routing, Chat AI, Cases, Hub, Packet, APIs, schema unchanged; same label pair on **Preview** when packet approved — §2 **Preview — approved-next read-only continuity (`/justice/preview`)**, §2 **Preview — approved-next session hydrate wiring (`/justice/preview`)**; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Plan — Case readiness approved-next visibility (`/justice/plan`)**).
- **Plan post-draft chat continuity:** **`/justice/plan`** **`showPostDraftReviewCallout`** includes chat-first line after next-step explainer / readiness bridge and proof-readiness and before packet CTA — **Update in chat** → **`/justice/chat-ai`** for fixing details or adding proof notes; **`recommendationText`** / **`pickPreparedNextAction`** / packet approval / handling / evidence logic unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims; **no** new API, chat-ai, preview, packet, lib, schema, or prompt changes (§2 **Plan — post-draft chat continuity prompt after preview review (`/justice/plan`)**).
- **Plan post-draft next-step explainer and readiness bridge:** **`/justice/plan`** **`showPostDraftReviewCallout`** includes **`getPreparedNextStepExplainer`** after main paragraph (prepared review / approved step; **does not file or submit** / **nothing sent automatically**); when signed in and **`!readyToEscalate`**, **Before you escalate, finish readiness:** with **Update in chat** / **Add proof in chat** → **`/justice/chat-ai`** and **Review submission draft** → **`/justice/preview`** for incomplete basics/evidence/draft; **Review prepared next step** primary CTA unchanged; **`showPostDraftReviewCallout`** gating, **`pickPreparedNextAction`**, **`recommendationText`**, full plan, packet, APIs, schema, helpers, hub, cases, chat-ai, preview unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Plan — post-draft next-step explainer and readiness bridge (`/justice/plan`)**).
- **Plan post-draft approved step label alignment:** **`/justice/plan`** **`showPostDraftReviewCallout`** when **`preparedPacketApproved`** uses **Next step: {approvedStepLabel}** in emerald main paragraph instead of **Approved next step:** — aligns with Hub/Cases/Chat AI/Preview/Plan checklist; **`approvedStepLabel`**, **`approvedNextAction`** logic, started/completed suffixes, **Approved next action:** status rows, CTAs, routing, handling tracking, disclaimers, APIs, schema, session keys unchanged — **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Plan — post-draft approved step label alignment (`/justice/plan`)**).
- **Plan post-draft re-preview continuity:** **`/justice/plan`** **`showPostDraftReviewCallout`** includes **Updated your case in chat?** **Review submission draft again** → **`/justice/preview`** after **Update in chat** and before packet CTA; subline confirm draft matches saved case / nothing filed automatically; same callout gate as other post-draft lines; **`evidenceReady`** / escalation / packet CTAs / preview Continue / timeline / APIs / schema / helpers / hub / cases / chat-ai unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims; **no** preview implementation changes beyond link target (§2 **Plan — post-draft re-preview continuity prompt after preview review (`/justice/plan`)**).
- **Saved cases Needs more info chat routing:** **`/justice/cases`** **All cases** cards in **Needs more info** show **Update in chat** (missing basics) or **Add proof in chat** (basics OK, no evidence) under **`caseReadinessHint`**; **`openUpdateInChat`** runs **`activateCaseInSession`** then **`/justice/chat-ai`**; evidence-only hint mentions chat + evidence page; **`openCase`** routing — §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**; compact checklist / readiness predicates/filters/attention unchanged on this slice; **no** filing/submission/contact/queue/upload/storage/automatic-action claims; **no** new API, hub, plan, chat-ai, **`caseReadiness.ts`**, schema, or prompt changes (§2 **Saved cases — Needs more info chat routing CTA (`/justice/cases`)**).
- **Saved cases compact readiness checklist:** **`/justice/cases`** **All cases** cards when progress loaded show compact **Basic case info** / **Evidence** / **Submission draft reviewed** rows from existing intake, progress, timeline; inline actions activate case then route to **`/justice/chat-ai`** or **`/justice/preview`**; **Prepared case packet reviewed** when draft reviewed — §2 **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`)**; **`isReadyToEscalate`**, filters, hints, progress fetch, Needs more info buttons unchanged; **`openCase`** — §2 **Saved cases — Open case chat-first routing (`/justice/cases`)**; **no** API, schema, helper, hub, plan, preview, or chat-ai changes; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — compact readiness checklist on All cases cards (`/justice/cases`)**).
- **Saved cases Open case chat-first routing:** **`/justice/cases`** **`openCase`** delegates to **`openUpdateInChat`** — **`activateCaseInSession`** then **`/justice/chat-ai`** regardless of draft-review or prepared-packet state; checklist **Review submission draft** / **Review prepared case packet** deep links unchanged — **no** new API; **Open case** label, card layout, **Needs attention** builders, **Open action**, approved-next labels, filters/search, APIs, schema, handlers unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — Open case chat-first routing (`/justice/cases`)**).
- **Saved cases All cases prepared packet reviewed checklist row:** **`/justice/cases`** **All cases** compact checklist shows **Prepared case packet reviewed: yes/not yet** after **Submission draft reviewed** when **`caseDraftReviewed(row)`**; **`casePreparedPacketApproved(row)`** from list **`client_state.prepared_packet_approved`** via **`parseJusticeCaseClientState`**; **Review prepared case packet** button calls **`openReviewPreparedPacket`** — **`activateCaseInSession`** then **`/justice/packet`**; reuses **`casesChecklistLinkCls`**; same funnel row on **Plan** **Case readiness** — §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**; **Needs attention** cards, approved-next labels, filters, builders, **`openCase`**, progress fetch, readiness predicates unchanged; **no** session-only fallback on this slice (list **`client_state`** is source of truth per card); **no** API, schema, helper file, hub, preview, or chat-ai changes on this slice; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`)**).
- **Saved cases approved next-step label on All cases:** **`/justice/cases`** **`CaseApprovedNextActionTracking`** shows read-only **Next step: {label}** above **Approved next action: {status}** when **`approved_next_action.label`** exists in list **`client_state`** (existing **`parseApprovedNextActionFromClientState`**); same pair on **Needs attention** handling-request and follow-up cards — §2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**; status labels, handling blocks, disclaimers, filters, search, APIs, schema, Plan, Packet, Chat AI, Hub unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — approved next-step label on All cases cards (`/justice/cases`)**).
- **Saved cases Needs attention approved-next step label visibility:** **`/justice/cases`** **Needs attention** handling-requested and follow-up cards show read-only **Next step: {label}** when **`next.label?.trim()`** exists, before **Approved next action: {status}** — display-only; matches **All cases**, Hub, Chat AI, Plan, and Preview approved-next pair; builders, filters, hydrate, **Open case**, **acknowledge handling request**, approved-action follow-up, routes, APIs, schema, handlers, shared libs unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**).
- **Saved cases Needs attention approved-next-action label alignment:** **`/justice/cases`** **Needs attention** handling-requested and follow-up cards show **Approved next action: {status}** via **`approvedNextActionStatusLabel`** instead of **Status:** — display/copy-only; matches **All cases**, Hub, and Chat AI; **Next step:** — §2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**; builders, handlers, hydrate, routing, filters, Plan, Packet, Hub, Chat AI, APIs, schema unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Saved cases — Needs attention approved-next-action label alignment (`/justice/cases`)**).
- **Hub Current case Needs more info chat routing:** **`/justice`** **`JusticeHubWorkspaceBody`** when **Current case** exists shows **Update in chat** (missing basics) or **Add proof in chat** (basics OK, loaded **`evidenceCount < 1`**) below primary nested card link — both → **`/justice/chat-ai`**; **`GET /api/justice/evidence`** for active **UUID** session case when signed in; primary nested card routing — §2 **Justice hub — Current case primary CTA routing (`/justice`)**; compact checklist / handling blocks / hub link list unchanged on this slice; session-only (no **`activateCaseInSession`**); saved cases/plan/chat-ai/APIs/schema/**`caseReadiness.ts`** unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Justice hub — Needs more info chat routing CTA on Current case (`/justice`)**).
- **Hub Current case primary CTA routing:** **`/justice`** nested **Current case** primary **`Link`** uses **`primaryHref`** / **`primaryLabel`**: **`/justice/preview`** + **Continue to submission preview** when **`!snapshot.reviewed`**; **`/justice/packet`** + **Review prepared case packet** when **`snapshot.reviewed && !snapshot.packetApproved`**; **`/justice/plan`** + **Continue to action plan** when **`snapshot.reviewed && snapshot.packetApproved`**; checklist **`<ul>`**, prepared-packet row, approved-next lines, handling blocks, evidence fetch, **`showUpdateInChat`**, **`showAddProofInChat`**, hub link list, APIs, schema, handlers, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged; **no** case **`GET`** (§2 **Justice hub — Current case primary CTA routing (`/justice`)**).
- **Hub Current case prepared packet reviewed checklist row:** **`/justice`** **Current case** checklist shows **Prepared case packet reviewed: yes/not yet** after submission draft reviewed via **`justice_prepared_packet_approved_v1`** (**`readSessionPreparedPacketApproved`** in snapshot); **Review prepared case packet** → **`/justice/packet`** when not approved — navigation/readiness only; existing **`refreshHubState`** triggers; evidence fetch, handling blocks, approved-next labels, **Update in chat** / **Add proof in chat**, primary card routing — §2 **Justice hub — Current case primary CTA routing (`/justice`)** — routes, APIs, schema, handlers, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged on this slice (§2 **Justice hub — Current case prepared packet reviewed checklist row (`/justice`)**).
- **Hub Current case compact readiness checklist:** **`/justice`** **Current case** compact **Basic case info** / **Evidence** / **Submission draft reviewed** yes-not-yet rows from existing **`basicsReady`**, **`evidenceCount`**, **`snapshot.reviewed`**; inline **Update in chat** / **Add proof in chat** / **Review submission draft** when incomplete; **Prepared case packet reviewed** when draft reviewed — §2 **Justice hub — Current case prepared packet reviewed checklist row (`/justice`)**; same funnel row on **Plan** **Case readiness** — §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**; **`showUpdateInChat`** / **`showAddProofInChat`** / primary card routing — §2 **Justice hub — Current case primary CTA routing (`/justice`)** — evidence fetch / handling unchanged; **no** API, schema, helper, plan, preview, chat-ai, or cases changes; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Justice hub — Current case compact readiness checklist (`/justice`)**).
- **Hub Current case approved next-step label and status:** **`/justice`** **Current case** card shows read-only **Next step: {label}** when **`approved_next_action.label`** exists in session snapshot and **Approved next action: {Approved|Started|Handled}** when status is set — from existing **`readSessionApprovedNextAction`** + **`approvedNextActionStatusLabel`** in **`readSnapshotFromLocalSession`**; session-only (not list **`client_state`** **GET**); same label pair on **Plan** **Case readiness** when packet approved — §2 **Plan — Case readiness approved-next visibility (`/justice/plan`)**; readiness checklist, chat routing CTAs, handling queue status, workbench link, link list, Packet, Chat AI, Cases, APIs, schema unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Justice hub — approved next-step label and status on Current case (`/justice`)**).
- **Chat-ai active case prepared packet reviewed checklist row:** **`/justice/chat-ai`** **Active case** checklist tracks whether prepared case packet is approved via **`justice_prepared_packet_approved_v1`** + existing case **GET** **`client_state.prepared_packet_approved`**; after draft reviewed shows **Prepared case packet reviewed: yes/not yet** with **Review prepared case packet** → **`/justice/packet`** when not approved — navigation/readiness only; same row on hub **Current case** — §2 **Justice hub — Current case prepared packet reviewed checklist row (`/justice`)**; same row on **Saved cases** **All cases** — §2 **Saved cases — All cases prepared packet reviewed checklist row (`/justice/cases`)**; same row on **Plan** **Case readiness** — §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**; proof CRUD, **Continue** handoff, **`handleSend`**, Recap tracking, **Open**/**Record handled**, routes, APIs, schema, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged (§2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**).
- **Chat-ai active case compact readiness checklist:** **`/justice/chat-ai`** **Update in chat** **Active case** banner shows compact **Basic case info** / **Evidence** / **Submission draft reviewed** from existing **`parts`**, **`savedEvidenceCount`**, session timeline; in-page **Continue in chat below** / **Add proof below** / **Review submission draft** → **`/justice/preview`**; **Prepared case packet reviewed** when draft reviewed — §2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**; same funnel row on **Plan** **Case readiness** — §2 **Plan — Case readiness checklist prepared packet reviewed row (`/justice/plan`)**; standalone draft line replaced; trimmed focus line; approved next-step label/status on banner — §2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**; **`handleSend`**, **Continue**, proof CRUD, staged flush, handoff, session-change summary unchanged; **no** new API, schema, helper, hub, plan, cases, or preview changes; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`)**).
- **Chat-ai active case banner work-link funnel routing:** **`/justice/chat-ai`** **Update in chat** **Active case** banner footer uses **`resolveActiveCaseWorkHref`** / **`resolveActiveCaseWorkLabel`** from **`activeCaseDraftReviewed`** + **`preparedPacketApproved`** when **`!chatFirstWorkLinkContinuity`**: **Submission preview** → **`/justice/preview`** when draft not reviewed; **Review prepared case packet** → **`/justice/packet`** when draft reviewed and packet not approved; **Action plan** → **`/justice/plan`** when packet approved; optional secondary when draft reviewed only — **hidden** when **`chatFirstWorkLinkContinuity`** (§2 **Chat AI intake — signed-in work-link funnel de-bias (Update in chat) (`/justice/chat-ai`)**); checklist inline CTAs, **`handleSend`**, **`handleContinueToPreview`**, handoff panel, Recap label/status/Open/Record handled, proof CRUD, page breadcrumb, routes, APIs, schema, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged — **no** new API (§2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**).
- **Chat-ai signed-in work-link funnel de-bias:** **`/justice/chat-ai`** when **`chatFirstWorkLinkContinuity`** (**signed-in** + **`isUpdatingExistingCase`**) hides banner and **Recap → Current action tracking** footer funnel links to preview/packet/plan; breadcrumb **Justice workspace** → **`/justice`**; checklist **Review submission draft** / **Review prepared case packet** deep links unchanged; **`handleContinueToPreview`**, stay-in-chat guards, optional approved-action deep links, preview/packet/plan pages, APIs, schema unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — signed-in work-link funnel de-bias (Update in chat) (`/justice/chat-ai`)**).
- **Chat-ai Recap current-action work-link funnel routing:** **`/justice/chat-ai`** **Recap → Current action tracking** footer (**Update in chat** when **`approvedNextAction`** exists) reuses **`activeCaseWorkHref`** / **`activeCaseWorkLabel`** / **`activeCaseSecondaryWorkLink`** — same funnel as **Active case** banner: **Submission preview** → **`/justice/preview`** when draft not reviewed; **Review prepared case packet** → **`/justice/packet`** when draft reviewed and packet not approved; **Action plan** → **`/justice/plan`** when packet approved; optional secondary when draft reviewed only; no duplicate **Submission preview** when primary is preview; emerald footer styling unchanged; **`handleSend`**, **`handleContinueToPreview`**, handoff panel, proof CRUD, Active case checklist CTAs, page breadcrumb, Open/Record handled, handling UI, APIs, schema, filing/submission/contact/queue/upload/storage/automatic-action behavior unchanged — **no** new API (§2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**).
- **Chat-ai active case context banner:** **`/justice/chat-ai`** when **`isUpdatingExistingCase`** shows **Active case** banner above main chat card — company/category/product, compact readiness checklist (§2 **Chat AI intake — active case compact readiness checklist (Update in chat) (`/justice/chat-ai`)**, §2 **Chat AI intake — active case prepared packet reviewed checklist row (Update in chat) (`/justice/chat-ai`)**), read-only **Next step** / **Approved next action** when **`approvedNextAction`** exists (§2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**), trimmed focus line (**Still needed before preview…** / draft-review nudge / describe changes), funnel-aware footer work links — §2 **Chat AI intake — active case banner work-link funnel routing (Update in chat) (`/justice/chat-ai`)**; uses existing **`parts`**, **`getPreviewBasicsMissing`**, **`savedEvidenceCount`** from **`loadSavedEvidencePreview`** — **no** new API, prompts, **`handleSend`**, **Continue**, proof CRUD, staged proof, handoff, session-change summary, hub, cases, plan, or schema changes; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — active case context banner (Update in chat)**).
- **Chat-ai approved next-step label/status on Active case banner:** **`/justice/chat-ai`** **Update in chat** **Active case** banner shows read-only **Next step: {label}** and **Approved next action: {Approved|Started|Handled}** when hydrated **`approvedNextAction`** exists — mirrors Hub/Cases wording; **Recap** uses same **Approved next action:** label — §2 **Chat AI intake — Recap current-action label alignment (`/justice/chat-ai`)**; **Open** / **Record handled** remain in Recap **Current action tracking** only; **`handleApprovedNextActionOpen`**, **`handleMarkApprovedNextActionHandled`**, hydrate, proof, Continue handoff, routing, Plan, Packet, Hub, Cases, APIs, schema unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — approved next-step label/status on Active case banner (Update in chat) (`/justice/chat-ai`)**).
- **Chat-ai Recap current-action label alignment:** **`/justice/chat-ai`** **Recap → Current action tracking** shows **Approved next action: {status}** via **`approvedNextActionStatusLabel`** instead of **Status:** — copy/label-only; matches Active case banner, Hub, and Cases **All cases** / **Needs attention** (§2 **Saved cases — Needs attention approved-next-action label alignment (`/justice/cases`)**, §2 **Saved cases — Needs attention approved-next step label visibility (`/justice/cases`)**); footer work links — §2 **Chat AI intake — Recap current-action work-link funnel routing (Update in chat) (`/justice/chat-ai`)**; **Next step:**, **Open {label}**, **Record action handled**, handlers, hydrate, proof, handoff, Plan, Packet, Hub, Cases, APIs, schema unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — Recap current-action label alignment (`/justice/chat-ai`)**).
- **Chat-ai session change summary:** **`/justice/chat-ai`** on **Update in chat** captures hydrated baseline **`parts`** in **`sessionBaselinePartsRef`** (not reset per message) and first **`loadSavedEvidencePreview`** count in **`sessionBaselineEvidenceCountRef`**; **`summarizeBuildJusticeIntakePartsSessionChanges`** diffs baseline vs current **`parts`**; **Updated in this chat** standalone panel when changes exist and **What happens next** is not shown; when handoff visible, same lines fold into **`getContinueHandoffSteps`** and standalone panel hidden (§2 **Chat AI intake — Continue handoff session-change integration (Update in chat)**); hidden on fresh intake / no changes — **no** API, schema, timeline, prompts, **`handleSend`**, **Continue**, commit, proof CRUD, staged proof, active-case banner, hub, cases, plan changes; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — session change summary (Update in chat)**).
- **Prepared next action on plan / packet:** Plan **`pickPreparedNextAction`** routes primary CTA to **`/justice/packet`**; packet **`showPreparedActionPacketFraming`** shows prepared-review + **pre-approval explainer** / **readiness bridge** (§2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**) + **post-approval approved-step explainer** after **`packetApproved`** (§2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**) + approval UI only when the same post-review gates apply. Packet approval persists in **`client_state`** (**`prepared_packet_approved`** + **`approved_next_action`** with stored **label/href**) for signed-in **UUID** cases (plus session fallbacks). On **`/justice/plan`**, **`/justice/packet`**, and **`/justice/chat-ai`**, approved **Open {label}** marks **`approved_next_action.status: "started"`** before navigation (§2 **Approved next action started**, **Packet — Open approved step marks started tracking (`/justice/packet`)**, **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**); **`/justice/packet`** hydrates and displays **started** in the approval area (§2 **Packet — approved next action started surfaced**). Hydrate on both surfaces merges server + session without downgrading **started** (§2 **Approved next action started**). **Not** written to **`timeline`**. Users still **manually** prep, copy, and file externally — **no** auto-submit, queue, filing workers, or implication that Surrenderless filed/submitted/sent/contacted anyone (§2).
- **Packet Open approved step started tracking:** **`/justice/packet`** **`handleApprovedNextActionOpen`** on **Open {label}** when **`status === "approved"`** — **`started`** + **`started_at`** to session + signed-in **UUID** **`client_state`**, then navigate; mirrors plan; already **`started`**/**`completed`** uses plain **`Link`** without downgrade; approval checkpoint, **`handleApprovePreparedPacket`**, explainer copy, gates, packet body, prep routes, APIs, schema, plan, preview, chat-ai, hub, cases unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — Open approved step marks started tracking (`/justice/packet`)**).
- **Chat-ai Open approved step in Current action tracking:** **`/justice/chat-ai`** **Update in chat** **Recap → Current action tracking** when **`status === "approved"`** shows **Open {label}**; **`handleApprovedNextActionOpen`** marks **`started`** + **`started_at`**, updates session **`justice_approved_next_action_v1`**, signed-in **UUID** **`GET` + merge + `PATCH`** **`client_state`**, then navigates to **`href`** — completes approved→started loop in chat without Plan/Packet; **`started`**/**`completed`** branches unchanged; proof, handling, follow-up, banner, outcome form unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — Open approved step marks started in Current action tracking (`/justice/chat-ai`)**).
- **Chat-ai record action handled in Current action tracking:** **`/justice/chat-ai`** **Update in chat** **Current action tracking** when **`status === "started"`** shows **Opened for next step.** + **Record action handled for now**; **`handleMarkApprovedNextActionHandled`** persists **`completed`** + **`completed_at`** to session + signed-in **UUID** **`client_state`** — user can mark approved step handled without leaving chat; handling request, follow-up clear, proof CRUD, Continue handoff, active-case banner, Plan, Packet, Hub, Cases, APIs, schema unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — record action handled in Current action tracking (`/justice/chat-ai`)**).
- **Chat-ai completed-step clarity in Current action tracking:** **`/justice/chat-ai`** when **`status === "completed"`** shows **Next action recorded as handled for now** (+ **label** when present) and **Tracking only — not automatic filing or submission.** after **Approved next action: Handled** (§2 **Chat AI intake — Recap current-action label alignment (`/justice/chat-ai`)**) — clarifies **Handled** status without leaving chat; **`handleMarkApprovedNextActionHandled`**, handling/follow-up/proof/handoff/active-case banner, Plan, Packet, Hub, Cases, APIs, schema unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Chat AI intake — completed-step clarity in Current action tracking (`/justice/chat-ai`)**).
- **Packet prepared review explainer and readiness bridge:** **`/justice/packet`** **`showPreparedActionPacketFraming && !packetApproved`** emerald **Your prepared action review** card includes **`getPreparedPacketReviewExplainer`** naming **`pickPreparedNextAction`** **`stepLabel`** and **does not file or submit** copy; when signed in and **`!readyToEscalate`**, **Before you approve, finish readiness:** with **Update in chat** / **Add proof in chat** → **`/justice/chat-ai`** and **Review submission draft** → **`/justice/preview`**; **Need to fix details or add proof notes?** **Update in chat** correction line; **`handleApprovePreparedPacket`**, approval persistence, framing gates, packet body, copy/download/print, filing/tasks, APIs, schema, rules, plan, preview, chat-ai, hub, cases unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — prepared review explainer and readiness bridge (`/justice/packet`)**).
- **Packet post-approval approved-step explainer:** **`/justice/packet`** when **`packetApproved && approvedNextAction?.label`**, top emerald **Your approved case packet** card uses **`getApprovedPacketNextStepExplainer`** with bold approved step / **nothing sent automatically**; **Update in chat** kept; inner approved card clarifies **Open {label}** is in-app prep/tracking not filing; started-state record-handled guidance — §2 **Packet — started-state record-handled copy alignment (`/justice/packet`)**; **`handleApprovePreparedPacket`**, approval/**`started`**/**`completed`** persistence, framing gates, packet body, copy/download/print, filing/tasks, APIs, schema, rules, plan, preview, chat-ai, hub, cases unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — post-approval approved-step explainer and emerald card copy refresh (`/justice/packet`)**).
- **Packet started-state record-handled copy alignment:** **`/justice/packet`** **started** explainer copy (**`getApprovedPacketNextStepExplainer`** + inline post-approval duplicates) directs users to record handled from **action plan or chat intake**, not plan alone — copy-only; hydrate, Open/Record handled behavior, Plan, Chat AI, Hub, Cases, APIs, schema unchanged; **no** filing/submission/contact/queue/upload/storage/automatic-action claims (§2 **Packet — started-state record-handled copy alignment (`/justice/packet`)**).
- **RLS vs Clerk:** Child-table RLS policies target Supabase Auth `uid`; app uses **Clerk id + service role** — fine for current server-only access, confusing if client-side Supabase is introduced without redesign.
- **Analytics / profile tables** not versioned in this repo’s migrations.

---

## 7. Development workflow

1. **Branch:** Do feature work on **`cursor-dev`** (or team-agreed equivalent), not directly on `main` for risky changes.
2. **Typecheck:** Run **`npx tsc --noEmit`** before commit/PR.
3. **Test:** Exercise flows locally (intake → preview → plan → cases → evidence/tasks/filings as relevant).
4. **Commit:** Clear, scoped commits.
5. **Push** `cursor-dev` and open a **PR to `main`**.
6. **Merge** after review.
7. **Sync:** Merge or rebase **`main` back into `cursor-dev`** so the long-lived branch stays current.

Adjust branch names if the team renames them; the pattern is **integration branch → PR → main → back-merge**.

---

## Document maintenance

Update this file when:

- Product north star changes.
- A new persistent subsystem is added (new tables, major routes).
- Known limitations are fixed (e.g. pagination for cases).

**Do not** duplicate every file path here forever — link to `docs/` or code search for exhaustive inventories when needed.
